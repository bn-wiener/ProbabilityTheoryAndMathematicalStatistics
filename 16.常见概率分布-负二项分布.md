# 第十六课：负二项分布——为了等到第 $r$ 次成功，需要试多少次？

上一课的几何分布研究：

> **第一次成功出现在第几次？**

今天只做一个非常自然的推广：

> **如果我不满足于第一次成功，而是一直做到第 $r$ 次成功，那么总共需要进行多少次试验？**

这就产生了：

$$
\boxed{\text{负二项分布 Negative Binomial Distribution}}
$$

---

# 一、先看它在知识体系中的位置

现在常见离散分布这条线已经变成：

```text
一次是否成功
    ↓
Bernoulli 伯努利分布

固定做 n 次，成功多少次
    ↓
Binomial 二项分布

一直做到第一次成功，需要几次
    ↓
Geometric 几何分布

一直做到第 r 次成功，需要几次
    ↓
Negative Binomial 负二项分布 ← 今天
```

所以今天的新思想其实非常少。

可以先记住：

$$
\boxed{
\text{几何分布是负二项分布在 }r=1\text{ 时的特殊情况}
}
$$

---

# 二、问题背景：为什么几何分布还不够？

假设你不断向服务器发送请求。

每一次请求成功的概率都是：

$$
p=0.4
$$

并假设各次请求相互独立。

上一课我们可以研究：

> 第一次成功需要发多少次？

例如：

```text
失败 失败 成功
```

这时：

$$
X=3
$$

几何分布完全可以处理。

---

但是现在业务需求变了：

> 我需要成功获得 **3 个有效响应** 才能继续，问一共需要发送多少次请求？

一种可能是：

```text
成 成 成
```

总共3次。

另一种可能：

```text
失 成 失 成 成
```

总共5次。

也可能：

```text
失 失 成 失 失 成 失 成
```

总共8次。

因此随机变量可能取：

$$
3,4,5,6,\ldots
$$

我们研究的已经不是“第一次成功”。

而是：

> **第3次成功什么时候到来？**

几何分布不够用了。

---

# 三、最重要的脑中画面

假设：

* $S$：成功；
* $F$：失败。

现在要求：

> 第 $r$ 次成功恰好发生在第 $n$ 次试验。

比如：

$$
r=3,\qquad n=7
$$

是什么意思？

第7次试验必须让我们获得**第3次成功**。

所以第7次一定是：

```text
S
```

而前6次之中必须：

> 恰好已经出现2次成功。

例如：

```text
F S F F S F | S
```

或者：

```text
S F S F F F | S
```

或者：

```text
F F S S F F | S
```

共同结构是：

```text
前 n-1 次：
恰好 r-1 次成功

第 n 次：
一定成功
```

这句话就是今天整个公式的来源。

---

# 四、定义随机变量

进行一系列相互独立、成功概率均为 $p$ 的伯努利试验。

定义：

$$
X=\text{得到第 }r\text{ 次成功时所需的总试验次数}
$$

那么：

$$
X\in\{r,r+1,r+2,\ldots\}
$$

为什么最小是 $r$？

因为想得到 $r$ 次成功，再幸运也至少需要进行 $r$ 次试验。

例如：

> 想得到3次成功。

最理想：

```text
S S S
```

所以：

$$
X_{\min}=3
$$

不可能：

$$
X=2
$$

因为只做两次试验，无论如何都产生不了3次成功。

---

# 五、先用最简单的具体例子推公式

假设：

$$
r=2
$$

也就是：

> 等到第2次成功。

每次成功概率：

$$
p
$$

现在问：

$$
P(X=4)
$$

也就是：

> 第2次成功恰好出现在第4次试验。

第4次必须成功。

而前3次必须恰好有：

$$
1
$$

次成功。

于是可能的结果有：

```text
S F F S
F S F S
F F S S
```

注意：

```text
最后一个 S 是固定的
```

真正可以变化的是前3个位置中：

> 哪一个位置放第一次成功？

有：

$$
\binom31=3
$$

种方法。

---

# 六、某一种排列的概率是多少？

例如：

```text
S F F S
```

里面有：

* 2次成功；
* 2次失败。

由于各次独立：

$$
P(SFFS)
=
p(1-p)(1-p)p
$$

所以：

$$
=p^2(1-p)^2
$$

另外两种排列概率完全一样。

因此：

$$
P(X=4)
=
3p^2(1-p)^2
$$

也就是：

$$
P(X=4)
=
\binom31p^2(1-p)^2
$$

一般公式的结构已经出现了。

---

# 七、推广到一般情况

现在研究：

$$
P(X=n)
$$

表示：

> 第 $r$ 次成功恰好发生在第 $n$ 次。

我们一步一步拆。

---

## 第一步：第 $n$ 次一定成功

因为它恰好是第 $r$ 次成功。

所以最后一次贡献：

$$
p
$$

---

## 第二步：前 $n-1$ 次中必须已经成功 $r-1$ 次

如果前面少于：

$$
r-1
$$

次成功，那么第 $n$ 次还不能成为第 $r$ 次。

如果前面已经有：

$$
r
$$

次成功，那么第 $r$ 次成功早就发生了。

所以必须恰好：

$$
r-1
$$

次。

---

## 第三步：前 $n-1$ 次里有多少种安排？

从：

$$
n-1
$$

个位置里，选择：

$$
r-1
$$

个位置作为成功。

因此有：

$$
\binom{n-1}{r-1}
$$

种。

---

## 第四步：一共有多少次成功和失败？

整个序列中：

成功次数：

$$
r
$$

失败次数：

$$
n-r
$$

所以某一种具体排列的概率为：

$$
p^r(1-p)^{n-r}
$$

---

## 第五步：排列数 × 每种排列的概率

得到：

$$
\boxed{
P(X=n)
=
\binom{n-1}{r-1}
p^r(1-p)^{n-r}
}
$$

其中：

$$
n=r,r+1,r+2,\ldots
$$

这就是我们今天采用的负二项分布 PMF。

---

# 八、公式的每一部分到底在说什么？

不要整体背。

拆开：

$$
P(X=n)
=
\underbrace{\binom{n-1}{r-1}}_{\text{前 }n-1\text{ 次中安排 }r-1\text{ 次成功}}
\underbrace{p^r}_{\text{总共 }r\text{ 次成功}}
\underbrace{(1-p)^{n-r}}_{\text{总共 }n-r\text{ 次失败}}
$$

最核心逻辑只有：

> **最后一次成功位置被固定，所以只在前 $n-1$ 次里面安排前 $r-1$ 次成功。**

这也是它和二项分布最容易混淆的地方。

---

# 九、为什么不是 $\binom nr$？

这是今天非常值得想清楚的问题。

假设：

$$
X=7,\qquad r=3
$$

表示：

> 第3次成功发生在第7次。

如果我们写：

$$
\binom73
$$

意味着：

> 3次成功可以任意放在7个位置里。

但这是错误的。

因为第7个位置必须是成功。

否则第3次成功怎么会“恰好在第7次”？

所以最后一个成功位置根本不能自由选择。

只有前：

$$
6
$$

个位置需要安排前：

$$
2
$$

次成功。

因此是：

$$
\boxed{\binom62}
$$

一般就是：

$$
\boxed{\binom{n-1}{r-1}}
$$

---

# 十、完整例子：第3次成功恰好发生在第6次

假设每次成功概率：

$$
p=0.4
$$

各次独立。

定义：

$$
X=\text{获得第3次成功所需的试验总数}
$$

所以：

$$
r=3
$$

我们求：

$$
P(X=6)
$$

---

## 第一步：理解事件

第3次成功恰好出现在第6次。

所以：

```text
前5次：恰好2次成功
第6次：成功
```

---

## 第二步：选择前两次成功的位置

从前5个位置里选2个：

$$
\binom52=10
$$

---

## 第三步：计算每种排列概率

整个6次里面：

成功：

$$
3
$$

次。

失败：

$$
6-3=3
$$

次。

所以：

$$
0.4^3(0.6)^3
$$

---

## 第四步：乘起来

$$
P(X=6)
=
\binom52(0.4)^3(0.6)^3
$$

所以：

$$
\boxed{
P(X=6)=10(0.4)^3(0.6)^3
}
$$

---

# 十一、几何分布为什么是它的特殊情况？

让：

$$
r=1
$$

也就是：

> 等到第一次成功。

负二项公式：

$$
P(X=n)
=
\binom{n-1}{r-1}
p^r(1-p)^{n-r}
$$

代入：

$$
r=1
$$

得到：

$$
P(X=n)
=
\binom{n-1}{0}
p(1-p)^{n-1}
$$

而：

$$
\binom{n-1}{0}=1
$$

所以：

$$
P(X=n)
=
(1-p)^{n-1}p
$$

这不就是上一课的几何分布吗？

$$
\boxed{
\operatorname{Geometric}(p)
=
\operatorname{NegativeBinomial}(1,p)
}
$$

所以负二项分布不是和几何分布毫无关系的新东西。

它就是：

> 从等第一次成功，推广到等第 $r$ 次成功。

---

# 十二、换一个角度：负二项分布其实是多个几何等待时间之和

这个视角很重要。

假设要等待：

$$
r=3
$$

次成功。

我们可以把整个过程拆开：

```text
开始
 ↓
等到第1次成功
 ↓
再等到第2次成功
 ↓
再等到第3次成功
```

设：

$$
G_1=\text{从开始到第1次成功所需次数}
$$

$$
G_2=\text{从第1次成功后到第2次成功所需次数}
$$

$$
G_3=\text{从第2次成功后到第3次成功所需次数}
$$

那么：

$$
X=G_1+G_2+G_3
$$

而每一个：

$$
G_i\sim\operatorname{Geometric}(p)
$$

在标准独立伯努利模型下，它们也是相互独立的。

因此：

$$
\boxed{
\text{负二项变量}
=
r\text{ 个几何等待时间之和}
}
$$

这个视角以后学习期望时会非常有用。

---

# 十三、它和二项分布到底有什么区别？

这是今天最容易混淆的一对。

假设仍然是相同的独立伯努利试验，每次成功概率为 $p$。

---

## 二项分布问

> 固定做 $n$ 次，成功多少次？

固定的是：

$$
n
$$

随机的是：

$$
\text{成功次数}
$$

例如：

> 投篮10次，中了几球？

---

## 负二项分布问

> 固定要得到 $r$ 次成功，需要做多少次？

固定的是：

$$
r
$$

随机的是：

$$
\text{试验次数}
$$

例如：

> 一直投到命中3球，一共需要投多少次？

---

可以这样对照：

|       | 二项分布           | 负二项分布             |
| ----- | -------------- | ----------------- |
| 固定什么？ | 总试验次数 $n$    | 目标成功次数 $r$      |
| 随机什么？ | 成功次数           | 总试验次数             |
| 问题    | 做 $n$ 次成功几次？ | 得到 $r$ 次成功要做几次？ |
| 取值    | $0,\ldots,n$ | $r,r+1,\ldots$  |

一句话：

$$
\boxed{\text{Binomial：固定次数，数成功}}
$$

$$
\boxed{\text{Negative Binomial：固定成功数，数次数}}
$$

---

# 十四、为什么两个公式看起来这么像？

二项分布：

$$
P(Y=r)
=
\binom nr
p^r(1-p)^{n-r}
$$

负二项分布：

$$
P(X=n)
=
\binom{n-1}{r-1}
p^r(1-p)^{n-r}
$$

它们都有：

$$
p^r(1-p)^{n-r}
$$

因为在两种问题中，都包含：

* $r$ 次成功；
* $n-r$ 次失败。

区别完全来自：

> **允许这些成功和失败怎样排列。**

二项分布里：

```text
所有 n 个位置都可以自由安排
```

所以：

$$
\binom nr
$$

负二项分布里：

```text
最后一个位置必须成功
```

所以只安排前 $n-1$ 个位置：

$$
\binom{n-1}{r-1}
$$

这就是两个公式差异的底层原因。

---

# 十五、一个很漂亮的二项—负二项联系

假设：

$$
X=\text{得到第 }r\text{ 次成功所需的总试验数}
$$

考虑事件：

$$
X\le n
$$

它是什么意思？

> 到第 $n$ 次试验为止，第 $r$ 次成功已经出现。

换句话说：

> 前 $n$ 次试验中至少已经成功 $r$ 次。

如果定义：

$$
Y=\text{前 }n\text{ 次的成功总数}
$$

那么：

$$
Y\sim\operatorname{Binomial}(n,p)
$$

并且：

$$
\boxed{
\{X\le n\}
=
\{Y\ge r\}
}
$$

所以：

$$
\boxed{
P(X\le n)
=
P(Y\ge r)
}
$$

这非常值得理解。

它说明：

> 二项分布和负二项分布不是两个彼此孤立的模型，而是从两个方向观察同一个伯努利试验过程。

---

# 十六、用时间轴理解这个关系

假设目标是：

$$
r=3
$$

看前6次：

```text
第1次 第2次 第3次 第4次 第5次 第6次
  ?     ?     ?     ?     ?     ?
```

问：

> 第3次成功是否在第6次以前已经发生？

等价于问：

> 前6次里是不是至少有3次成功？

所以：

```text
等待视角
第3次成功多久到？
       ↕
计数视角
前 n 次已经成功多少次？
```

前者产生负二项分布。

后者产生二项分布。

---

# 十七、负二项分布也有两种常见参数化方式

和几何分布一样，这里必须提前防止以后看资料时混乱。

今天我们定义：

$$
X=\text{得到第 }r\text{ 次成功时的总试验次数}
$$

所以：

$$
X\in\{r,r+1,\ldots\}
$$

并且：

$$
P(X=n)
=
\binom{n-1}{r-1}
p^r(1-p)^{n-r}
$$

---

但很多教材和软件定义另一个随机变量：

$$
Y=\text{得到第 }r\text{ 次成功之前的失败总次数}
$$

这时：

$$
Y\in\{0,1,2,\ldots\}
$$

因为：

$$
\text{总试验数}
=
\text{成功数}
+
\text{失败数}
$$

所以：

$$
X=r+Y
$$

即：

$$
\boxed{Y=X-r}
$$

---

如果 $Y=k$，意味着：

* 成功 $r$ 次；
* 失败 $k$ 次；
* 最后一次仍然必须成功。

对应 PMF：

$$
\boxed{
P(Y=k)
=
\binom{k+r-1}{r-1}
p^r(1-p)^k
}
$$

所以以后看到负二项公式不一样，不要先怀疑公式。

第一件事永远应该问：

> **随机变量究竟定义成“总试验次数”，还是“失败次数”？**

---

# 十八、名字为什么叫“负二项”？

这个名字第一次看到确实很奇怪：

> 难道随机变量可以是负的吗？

不是。

“负二项”中的“负”并不是说：

$$
X<0
$$

实际上在我们的定义中：

$$
X\ge r
$$

它的名称来自更深层的**负指数二项式展开**，而不是随机变量取负值。

所以目前不要被名字误导。

只需要知道：

$$
\boxed{
\text{负二项分布的随机变量完全可以只取正整数}
}
$$

---

# 十九、反例一：成功概率每次改变

假设不断投篮，但命中概率因为疲劳变化：

$$
p_1=0.8,\qquad
p_2=0.7,\qquad
p_3=0.6,\ldots
$$

此时即使仍然问：

> 第3次命中需要多少次投篮？

也不能直接使用标准负二项分布。

因为负二项分布要求每一次试验有相同的：

$$
p
$$

---

# 二十、反例二：各次试验不独立

例如从有限袋子中不放回抽球。

每抽一次，袋子里的组成发生变化。

于是下一次成功概率也会受到前面结果影响。

这不再符合经典独立同概率伯努利结构。

因此不能机械套负二项分布。

---

# 二十一、反例三：把“成功了 $r$ 次”和“第 $r$ 次成功”混淆

假设：

$$
r=3
$$

观察结果：

```text
S S S F F
```

这5次里确实：

> 有3次成功。

但第3次成功其实在：

$$
X=3
$$

而不是：

$$
X=5
$$

因为负二项随机变量研究的是：

> **第 $r$ 次成功首次到达的时刻。**

后面的试验已经和这个 $X$ 无关了。

---

# 二十二、实际应用：网络重试

假设每次请求成功概率为：

$$
p
$$

需要获得5个成功响应以后才能完成任务。

定义：

$$
X=\text{获得第5个成功响应所需的总请求数}
$$

在独立同成功率模型下：

$$
X
$$

就具有负二项结构。

例如可以研究：

> 恰好第8次请求获得第5个成功响应的概率。

也就是：

$$
P(X=8)
=
\binom74
p^5(1-p)^3
$$

---

# 二十三、机器学习中的应用直觉

负二项分布尤其适合一种场景：

> **不是固定采多少样本，而是持续采样，直到获得一定数量的目标样本。**

例如某稀有类别出现概率为：

$$
p
$$

你需要收集：

$$
r=100
$$

个该类别样本。

那么：

$$
X=\text{收集到第100个目标样本时，总共需要观察多少样本}
$$

在理想独立同分布假设下，就具有负二项结构。

它回答的是一个很实际的工程问题：

> 为了收集足够多的稀有正样本，需要准备多少采样成本？

---

# 二十四、四种分布现在终于可以放在一起

假设每次用户购买商品的概率都是：

$$
p
$$

并假设各用户独立。

---

## 伯努利分布

观察一个用户：

> 买还是没买？

$$
X\in\{0,1\}
$$

---

## 二项分布

固定观察100个用户：

> 一共有多少人购买？

$$
X\in\{0,\ldots,100\}
$$

---

## 几何分布

不断观察：

> 第几个用户第一次购买？

$$
X\in\{1,2,\ldots\}
$$

---

## 负二项分布

不断观察：

> 第10个购买用户出现在第几个用户？

$$
X\in\{10,11,\ldots\}
$$

现在你应该能明显感受到：

> **这些分布不是四个孤立公式，而是对同一个伯努利过程提出四种不同问题。**

---

# 二十五、整个关系图

可以把它整理成：

```text
底层机制：
每次试验 → 成功 / 失败
成功概率 p
各次独立
        │
        ├──────────────┐
        │              │
        ↓              ↓
只观察一次         重复观察
        │              │
        ↓              ├─────────────────────┐
Bernoulli              │                     │
                       ↓                     ↓
                固定试验次数           固定成功目标
                       │                     │
                       ↓              ┌──────┴──────┐
                数成功次数             │             │
                       ↓              ↓             ↓
                   Binomial      第1次成功      第r次成功
                                      ↓             ↓
                                  Geometric    Negative Binomial
```

这张图比四个 PMF 更值得记住。

---

# 二十六、理解检查

请先凭直觉思考。

## 1

每次试验成功概率为：

$$
p=0.3
$$

定义：

$$
X=\text{第2次成功所需的总试验次数}
$$

那么：

$$
P(X=4)
$$

为什么应该对应：

```text
前三次恰好1次成功
+
第4次必须成功
```

？

---

## 2

为什么负二项 PMF 使用：

$$
\binom{n-1}{r-1}
$$

而不是：

$$
\binom nr
$$

？

---

## 3

如果：

$$
r=1
$$

负二项分布会退化为什么分布？

你能不能直接从公式推出？

---

## 4

下面两个问题分别对应什么分布？

> 100次投篮中命中10次的概率是多少？

与：

> 第10次命中恰好发生在第100次投篮的概率是多少？

它们虽然都有：

$$
n=100,\qquad r=10
$$

为什么不是同一个事件？

---

## 5

假设：

$$
X=\text{第5次成功所需总试验次数}
$$

那么：

$$
X=3
$$

可能发生吗？

为什么？

---

## 6 —— 最重要

你能不能自己解释：

$$
\boxed{
P(X=n)
=
\binom{n-1}{r-1}
p^r(1-p)^{n-r}
}
$$

中的每一个部分究竟从哪里来的？

如果你已经可以不靠背诵从：

> “前 $n-1$ 次恰好 $r-1$ 次成功，第 $n$ 次成功”

重新推出它，那么这一课已经真正掌握。

---

# 这一节真正需要记住的核心

1. **负二项分布研究：重复独立同概率的伯努利试验，为了得到第 $r$ 次成功，一共需要进行多少次试验。**

2. 若 $X$ 表示总试验次数，则：

$$
\boxed{
P(X=n)
=
\binom{n-1}{r-1}
p^r(1-p)^{n-r}
}
$$

其中：

$$
n=r,r+1,\ldots
$$

3. 公式中使用 $\binom{n-1}{r-1}$ 的根本原因是：**第 $n$ 次已经被固定为第 $r$ 次成功，只需要安排前面的 $r-1$ 次成功。**

4. 几何分布就是 $r=1$ 的负二项分布：

$$
\boxed{\text{Geometric}=\text{Negative Binomial with }r=1}
$$

5. 二项分布与负二项分布研究同一个伯努利过程，但视角相反：

$$
\boxed{\text{固定次数 → 数成功：Binomial}}
$$

$$
\boxed{\text{固定成功数 → 数次数：Negative Binomial}}
$$

下一步会出现另一种完全不同的问题：

> 到目前为止，我们一直在做一次、两次、三次……伯努利试验；但现实中还有很多问题是“**固定一段时间或空间内，某种事件会发生多少次？**”

例如：

* 一小时收到多少个请求；
* 一公里道路发生多少起事故；
* 一页文本出现多少个错字；
* 一段时间内服务器发生多少次故障。

这会自然把我们带到下一个非常重要的离散分布：

$$
\boxed{\text{泊松分布 Poisson Distribution}}
$$
