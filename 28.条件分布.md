# 第二十八课：条件分布——已知一个变量后，怎样重新描述另一个变量的分布？

上一课，我们研究的是：

> **不看右边数字时，左边数字怎样分布？**

这一课只改变信息状态：

> **已经看到了右边数字，左边数字的整张概率分布应该怎样更新？**

这就是**条件分布**。

先把最近几课放在一起：

```text
联合分布
两个变量怎样共同取值？
        ↓
边缘分布
不限制另一个变量，只看其中一个怎样取值。
        ↓
条件分布 ← 第二十八课
已经知道另一个变量的信息，再看其中一个怎样取值。
```

今天最核心的动作是：

$$
\boxed{\text{根据条件筛选结果，再把保留下来的概率重新归一化。}}
$$

下面先用卡片把这个动作讲清楚，再解释连续情况下为什么要使用条件密度。

---

## 一、问题背景：右边已经看到了，左边还能沿用原来的分布吗？

### 1. 继续使用同一个袋子，不改变试验设置

袋子里事先放着十张卡片，每张卡片上写着左、右两个数字。

| 卡片编号   | 左边数字 $X$ | 右边数字 $Y$ | 卡片数量 |
| ------ | ---------: | ---------: | ---: |
| 1      |          0 |          0 |    1 |
| 2、3    |          0 |          1 |    2 |
| 4、5    |          1 |          0 |    2 |
| 6      |          1 |          1 |    1 |
| 7      |          2 |          0 |    1 |
| 8、9、10 |          2 |          1 |    3 |

一次完整试验是：

> **从袋子里等可能地随机抽出一张卡片，记录这张卡片上的左、右两个数字。**

因此，每张具体卡片被抽到的概率都是：

$$
\frac1{10}.
$$

定义：

$$
X=\text{抽到的卡片上，左边的数字},
$$

$$
Y=\text{抽到的卡片上，右边的数字}.
$$

仍然要记住：

**一次抽取、一张卡片、两个读数。**

### 2. 还没有看到任何数字时

根据上一课的边缘分布：

$$
P(X=0)=0.3,\qquad
P(X=1)=0.3,\qquad
P(X=2)=0.4.
$$

这描述的是：

> 在没有额外知道右边数字的情况下，左边数字怎样分布。

### 3. 现在，只揭开右边

抽取已经完成，但卡片编号和左边数字仍然遮住。

我们先看右边，发现：

$$
Y=1.
$$

现在问：

> **已知右边为 1，左边取 0、1、2 的概率分别是多少？**

还能直接沿用：

$$
0.3,\quad0.3,\quad0.4
$$

吗？

不能不加检查地沿用。

因为原来这三个概率使用的是全部十张卡片；现在已经知道右边为 1，右边为 0 的那些卡片就不再符合我们掌握的信息。

**卡片没有重新抽取，数字也没有改变；改变的是我们知道了什么。**

这正是第六课条件概率的出发点：新信息改变了需要考虑的结果范围，而不是让原来的试验重新发生。

---

## 二、直觉：先筛选，再重新计算内部比例

### 1. 哪些卡片还符合条件？

原样本空间可以用卡片编号表示：

$$
\Omega=\{1,2,\ldots,10\}.
$$

条件“右边为 1”对应事件：

$$
B=\{Y=1\}=\{2,3,6,8,9,10\}.
$$

现在，我们只需要考虑这六张卡片：

```text
卡片编号：  2    3    6    8    9    10
左边 X：   0    0    1    2    2     2
右边 Y：   1    1    1    1    1     1
```

原来每张卡片等可能，而观察到的条件只是“右边为 1”，因此在这六张候选卡片之间，没有进一步区分它们的信息。

### 2. 在这六张里面，重新看左边数字

左边为 0 的有两张，所以：

$$
P(X=0\mid Y=1)=\frac26=\frac13.
$$

左边为 1 的有一张，所以：

$$
P(X=1\mid Y=1)=\frac16.
$$

左边为 2 的有三张，所以：

$$
P(X=2\mid Y=1)=\frac36=\frac12.
$$

于是得到：

| 左边数字 $x$ | 已知右边为 1 后的概率 |
| ---------- | -----------: |
| 0          |  $\frac13$ |
| 1          |  $\frac16$ |
| 2          |  $\frac12$ |

检查总和：

$$
\frac13+\frac16+\frac12
=
\frac26+\frac16+\frac36
=
1.
$$

**这整张表，就是已知 $Y=1$ 时，$X$ 的条件分布。**

这里还有一个容易忽略的区别：

> 剩下六张等可能的卡片，不代表三个不同的 $X$ 取值等可能。

因为三个取值分别对应两张、一张、三张卡片。

### 3. 为什么叫“重新归一化”？

右边为 1 的六张卡片，在原来全部十张卡片中，只占：

$$
P(Y=1)=\frac6{10}=0.6.
$$

但既然已经知道右边为 1，那么在这个条件下，这六张卡片就是我们需要考虑的全部结果。

因此，要让它们的总概率从原来的：

$$
0.6
$$

变成条件下的：

$$
1.
$$

做法就是：

> **把保留下来的每一部分概率，统一除以 $0.6$。**

例如，左边为 2 且右边为 1 的原始概率是：

$$
\frac3{10}=0.3.
$$

在条件范围内部，它所占的比例是：

$$
\frac{0.3}{0.6}
=
\frac{3/10}{6/10}
=
\frac36
=
\frac12.
$$

所以“归一化”不是把每种结果改成等可能，而是：

$$
\boxed{\text{保留原有的相对比例，让新的总概率等于 1。}}
$$

这就是第六课“把条件区域作为新的全部，再统一调整概率尺度”的思想。

---

## 三、数学定义：从一个条件概率，扩展到整张条件分布

### 1. 条件概率与条件分布有什么区别？

例如：

$$
P(X=2\mid Y=1)=\frac12
$$

只回答一个问题：

> 已知右边为 1，左边恰好为 2 的概率是多少？

它是**一个条件概率**。

而把：

$$
P(X=0\mid Y=1),\quad
P(X=1\mid Y=1),\quad
P(X=2\mid Y=1)
$$

全部整理出来，就描述了：

> 已知右边为 1 后，左边的整个概率规律。

它是**一个条件分布**。

这延续了第九课的区别：一个概率描述一个事件，而一个分布描述整个随机变量的概率配置。

### 2. 离散情况下的正式定义

设 $X,Y$ 是离散随机变量。

对于一个满足：

$$
P(Y=y)>0
$$

的条件值 $y$，定义：

$$
\boxed{
p_{X\mid Y}(x\mid y)
=
P(X=x\mid Y=y).
}
$$

它叫作 **$X$ 在给定 $Y=y$ 时的条件概率质量函数**。

逐个解释：

| 符号                       | 含义                       |
| ------------------------ | ------------------------ |
| $X$                    | 我们想研究其分布的随机变量            |
| $Y$                    | 提供条件信息的随机变量              |
| $x$                    | 正在询问的 $X$ 的具体候选值       |
| $y$                    | 已经给定的 $Y$ 的具体值         |
| $\mid$                 | “在……条件下”，不是除号            |
| $p_{X\mid Y}(x\mid y)$ | 已知 $Y=y$ 后，$X=x$ 的概率 |

最重要的阅读顺序是：

> **先看竖线右边，确定已知什么；再看左边，确定要问什么。**

对于固定的 $y$，让 $x$ 遍历所有可能取值，就得到一整张关于 $X$ 的条件分布。

### 3. 公式怎样从条件概率定义中得到？

第六课已经得到：

$$
P(A\mid B)=\frac{P(A\cap B)}{P(B)},
\qquad P(B)>0.
$$

现在只是把事件具体化：

$$
A=\{X=x\},\qquad B=\{Y=y\}.
$$

它们的交集是：

$$
A\cap B=\{X=x,Y=y\}.
$$

代入：

$$
P(X=x\mid Y=y)
=
\frac{P(X=x,Y=y)}{P(Y=y)}.
$$

再使用联合 PMF 和边缘 PMF 的记号：

$$
p_{X,Y}(x,y)=P(X=x,Y=y),
$$

$$
p_Y(y)=P(Y=y),
$$

得到：

$$
\boxed{
p_{X\mid Y}(x\mid y)
=
\frac{p_{X,Y}(x,y)}{p_Y(y)},
\qquad p_Y(y)>0.
}
$$

这条公式不是新的概率规则，而是：

$$
\boxed{
\text{条件概率定义}
+
\text{随机变量的取值事件}
=
\text{条件 PMF 公式}
}
$$

---

## 四、怎样从联合概率表，直接得到条件分布？

回到前两课的联合表：

| $X\backslash Y$ | $Y=0$ | $Y=1$ | $p_X(x)$ |
| ----------------- | ------: | ------: | ---------: |
| $X=0$           | $0.1$ | $0.2$ |    $0.3$ |
| $X=1$           | $0.2$ | $0.1$ |    $0.3$ |
| $X=2$           | $0.1$ | $0.3$ |    $0.4$ |
| $p_Y(y)$        | $0.4$ | $0.6$ |      $1$ |

### 1. 已知 $Y=1$：选择对应列，再除以列总和

取出 $Y=1$ 这一列：

$$
0.2,\quad0.1,\quad0.3.
$$

它们的总和是：

$$
0.6.
$$

因此统一除以 $0.6$：

$$
p_{X\mid Y}(0\mid1)=\frac{0.2}{0.6}=\frac13,
$$

$$
p_{X\mid Y}(1\mid1)=\frac{0.1}{0.6}=\frac16,
$$

$$
p_{X\mid Y}(2\mid1)=\frac{0.3}{0.6}=\frac12.
$$

**只把这一列抄下来，还不是条件分布；必须完成归一化。**

### 2. 已知 $Y=0$：另一列对应另一张条件分布

这次使用：

$$
P(Y=0)=0.4.
$$

所以：

$$
p_{X\mid Y}(0\mid0)=\frac{0.1}{0.4}=\frac14,
$$

$$
p_{X\mid Y}(1\mid0)=\frac{0.2}{0.4}=\frac12,
$$

$$
p_{X\mid Y}(2\mid0)=\frac{0.1}{0.4}=\frac14.
$$

把三个信息状态放在一起：

| $x$ | 不限制 $Y$：$p_X(x)$ |  已知 $Y=0$ |  已知 $Y=1$ |
| ----- | -------------------: | ----------: | ----------: |
| 0     |              $0.3$ | $\frac14$ | $\frac13$ |
| 1     |              $0.3$ | $\frac12$ | $\frac16$ |
| 2     |              $0.4$ | $\frac14$ | $\frac12$ |
| 合计    |                $1$ |       $1$ |       $1$ |

这里有一个重要认识：

> **条件分布通常不是一张固定的分布，而是一族随着条件值变化的分布。**

在本例中：

```text
没有揭开右边：
使用 X 的边缘分布。

揭开后看到0：
使用 X 在 Y=0 下的条件分布。

揭开后看到1：
使用 X 在 Y=1 下的条件分布。
```

### 3. 为什么每个固定条件下，总概率都是 1？

固定一个满足 $p_Y(y)>0$ 的 $y$。

对 $X$ 的所有可能取值 $x_i$ 求和：

$$
\begin{aligned}
\sum_i p_{X\mid Y}(x_i\mid y)
&=\sum_i\frac{p_{X,Y}(x_i,y)}{p_Y(y)}\\
&=\frac{\sum_i p_{X,Y}(x_i,y)}{p_Y(y)}\\
&=\frac{p_Y(y)}{p_Y(y)}\\
&=1.
\end{aligned}
$$

第二行能把分母提出，是因为 $y$ 已经固定，$p_Y(y)$ 不随 $i$ 改变。

第三行使用了上一课的边缘化公式：

> 固定 $y$，对所有 $x_i$ 的联合概率求和，得到 $p_Y(y)$。

因此，归一化的分母正好就是“保留部分的总重量”。

---

## 五、把联合、边缘、条件三个问题彻底分开

仍然看左边为 2、右边为 1。

### 联合概率：两个条件同时满足

$$
P(X=2,Y=1)=0.3.
$$

意思是：

> 在原来全部十张卡片中，左边为 2 且右边为 1 的卡片占多少？

### 边缘概率：右边不限，只要求左边为 2

$$
P(X=2)=0.4.
$$

意思是：

> 在原来全部十张卡片中，左边为 2 的卡片占多少？

### 条件概率：已知右边为 1，再问左边为 2

$$
P(X=2\mid Y=1)=0.5.
$$

意思是：

> 在右边为 1 的六张卡片中，左边为 2 的卡片占多少？

所以：

$$
\boxed{
\begin{aligned}
\text{联合：}&\quad \text{同时要求什么？}\\
\text{边缘：}&\quad \text{只关心什么，其他不限？}\\
\text{条件：}&\quad \text{已经知道什么，再问什么？}
\end{aligned}
}
$$

### 条件方向不能反过来读

我们已经算出：

$$
P(X=2\mid Y=1)=\frac36=\frac12.
$$

但是：

$$
P(Y=1\mid X=2)
$$

问的是另一个问题：

> 已知左边为 2，右边为 1 的概率是多少？

左边为 2 的卡片有四张，其中右边为 1 的有三张，因此：

$$
P(Y=1\mid X=2)=\frac34.
$$

所以：

$$
\boxed{
P(X=2\mid Y=1)
\neq
P(Y=1\mid X=2).
}
$$

它们的共同部分都是那三张卡片，但分母分别是六张和四张。

这也是为什么**条件分布不等于贝叶斯公式**：条件分布是我们要描述的对象；贝叶斯公式则提供了利用反方向条件信息进行换算的方法。本课先把条件分布本身建立清楚。

---

## 六、条件分布的几个重要边界

### 1. 已知条件，不等于知道全部结果

知道：

$$
Y=1
$$

以后，$X$ 仍然可以是：

$$
0,\quad1,\quad2.
$$

所以条件分布不是：

> 一加条件，就能唯一确定另一个变量。

条件信息可能只排除一部分情况，剩下的不确定性仍然需要用分布描述。

### 2. 增加信息，不会让每个取值的概率都增大

本例中：

$$
P(X=2)=0.4
\quad\longrightarrow\quad
P(X=2\mid Y=1)=0.5,
$$

但：

$$
P(X=1)=0.3
\quad\longrightarrow\quad
P(X=1\mid Y=1)=\frac16.
$$

一个变大，另一个变小。

**更新分布是在重新分配概率，不是给所有概率统一增加一些。**

### 3. 独立时，条件分布与边缘分布相同

第七课的独立性直觉是：

> 知道一个对象的信息，不改变对另一个对象的概率判断。

对于离散随机变量，如果 $X,Y$ 独立，则：

$$
p_{X,Y}(x,y)=p_X(x)p_Y(y).
$$

代入条件 PMF，在 $p_Y(y)>0$ 时：

$$
\begin{aligned}
p_{X\mid Y}(x\mid y)
&=\frac{p_X(x)p_Y(y)}{p_Y(y)}\\
&=p_X(x).
\end{aligned}
$$

因此：

$$
\boxed{
X,Y\text{ 独立}
\quad\Longrightarrow\quad
\text{知道 }Y\text{ 后，}X\text{ 的整张分布不变。}
}
$$

反过来，在离散情况下，如果对**所有具有正概率的条件值 $y$**、所有候选值 $x$，都有：

$$
p_{X\mid Y}(x\mid y)=p_X(x),
$$

就能推出独立。

**只检查某一个取值的概率碰巧没变，还不够。**

### 4. 条件概率为零时，不能直接使用这个比值

本例中：

$$
P(Y=2)=0.
$$

因此不能写：

$$
P(X=1\mid Y=2)=\frac00=0.
$$

$\frac00$ 没有定义。

准确结论是：

> **这个初等比值公式不能给出答案，而不是答案等于零。**

第六课已经明确要求分母 $P(B)>0$，也预告了连续变量的精确条件值需要另作处理。后面我们就来解决这个问题。

---

## 七、条件 CDF：在给定条件下，怎样累计概率？

条件分布建立以后，CDF 的思想完全不变。

以前：

$$
F_X(t)=P(X\le t).
$$

现在增加已知条件：

$$
\boxed{
F_{X\mid Y}(t\mid y)
=
P(X\le t\mid Y=y).
}
$$

这里特意用 $t$ 表示筛选 $X$ 的阈值，避免与已知的 $y$ 混淆。

这两个数的角色不同：

> $y$ 决定使用哪一张条件分布；$t$ 决定在这张分布上累计到哪里。

### 1. 用本例计算

已知 $Y=1$ 时：

$$
X=0,1,2
$$

的概率分别是：

$$
\frac13,\quad\frac16,\quad\frac12.
$$

所以：

$$
\begin{aligned}
F_{X\mid Y}(1\mid1)
&=P(X\le1\mid Y=1)\\
&=P(X=0\mid Y=1)+P(X=1\mid Y=1)\\
&=\frac13+\frac16\\
&=\frac12.
\end{aligned}
$$

完整的条件 CDF 是：

$$
F_{X\mid Y}(t\mid1)=
\begin{cases}
0,&t<0,\\[3pt]
\frac13,&0\le t<1,\\[3pt]
\frac12,&1\le t<2,\\[3pt]
1,&t\ge2.
\end{cases}
$$

仍然是一条楼梯，只不过台阶高度来自**条件 PMF**。

### 2. 一个容易写错的公式

一般不能直接写：

$$
F_{X\mid Y}(t\mid y)
=
\frac{F_{X,Y}(t,y)}{F_Y(y)}.
$$

为什么？

因为右边实际是：

$$
\frac{P(X\le t,Y\le y)}{P(Y\le y)}
=
P(X\le t\mid Y\le y),
$$

前提是分母大于零。

它给定的是：

$$
Y\le y,
$$

而不是：

$$
Y=y.
$$

**“已知右边恰好是某个值”与“已知右边不超过某个值”，不是同一个条件。**

---

## 八、三个分布怎样形成一个完整闭环？

我们已经知道：

$$
\text{联合分布}
\longrightarrow
\text{边缘分布}
$$

以及：

$$
\text{联合分布与条件变量的边缘分布}
\longrightarrow
\text{条件分布}.
$$

现在反过来看。

### 1. 条件分布乘条件的概率，恢复联合分布

从：

$$
p_{X\mid Y}(x\mid y)
=
\frac{p_{X,Y}(x,y)}{p_Y(y)}
$$

出发，两边乘以 $p_Y(y)$：

$$
\boxed{
p_{X,Y}(x,y)
=
p_{X\mid Y}(x\mid y)\,p_Y(y).
}
$$

直觉是：

```text
Y=y 在整体中占多少？
          ×
在 Y=y 的内部，X=x 占多少？
          ↓
X=x 且 Y=y 在整体中占多少？
```

例如：

$$
P(X=2,Y=1)
=
\frac12\times0.6
=
0.3.
$$

**这个乘法不要求独立，因为使用的是条件概率。**

### 2. 再把各个条件分组汇总，恢复边缘分布

上一课有：

$$
p_X(x)=\sum_j p_{X,Y}(x,y_j).
$$

把刚才的乘法表达式代入：

$$
\boxed{
p_X(x)
=
\sum_j
p_{X\mid Y}(x\mid y_j)\,p_Y(y_j).
}
$$

这里对具有正概率的条件值 $y_j$ 求和；零概率分组不会贡献概率。

它表示：

> **边缘分布，是各个条件分布按照条件出现的概率进行加权汇总的结果。**

例如恢复 $P(X=2)$：

$$
\begin{aligned}
P(X=2)
&=P(X=2\mid Y=0)P(Y=0)\\
&\quad+P(X=2\mid Y=1)P(Y=1)\\
&=\frac14\times0.4+\frac12\times0.6\\
&=0.1+0.3\\
&=0.4.
\end{aligned}
$$

为什么不能直接取两个条件概率的算术平均？

因为两个条件出现的概率不是各占一半，而是：

$$
P(Y=0)=0.4,\qquad P(Y=1)=0.6.
$$

**不仅要知道每组内部怎样分布，还要知道各组在整体中占多大比例。**

---

## 九、连续情况下：单点概率为零，怎样理解“已知 $V=v$”？

现在从离散卡片转到连续随机变量。

设：

$$
(U,V)
$$

存在联合概率密度：

$$
f_{U,V}(u,v).
$$

我们想研究：

> 已知第二个坐标为 $v$，第一个坐标怎样分布？

### 1. 为什么不能照搬单点概率之比？

对于具有密度的连续变量：

$$
P(V=v)=0.
$$

因此不能直接计算：

$$
\frac{P(U=u,V=v)}{P(V=v)}.
$$

它会变成没有意义的零除以零。

但这不表示我们不能讨论连续条件分布。

关键是：

> **先研究有宽度的小区域，再通过极限理解精确条件值，而不是直接拿两个零相除。**

### 2. 先把“精确值”换成一个很窄的范围

暂时只知道：

$$
v<V\le v+h,
$$

其中 $h>0$ 是一个很小的宽度。

在平面上，这是一条很薄的水平带。

然后问：

$$
u<U\le u+\Delta u,
$$

其中 $\Delta u>0$ 是第一个坐标方向的小宽度。

两个要求一起构成一个小矩形。

在密度局部变化足够平缓的地方：

$$
P(u<U\le u+\Delta u,\ v<V\le v+h)
\approx
f_{U,V}(u,v)\,\Delta u\,h.
$$

而整条水平带的概率近似为：

$$
P(v<V\le v+h)
\approx f_V(v)\,h.
$$

这里：

$$
f_V(v)=\int_{-\infty}^{+\infty}f_{U,V}(s,v)\,ds
$$

是上一课学过的边缘密度；$s$ 是沿第一个坐标积分时使用的临时变量。

这些近似沿用第十二课的思想：小区间的概率约等于密度乘以宽度，二维小矩形则再多乘一个方向的宽度。

### 3. 此时可以使用普通条件概率

因为薄带有正的宽度，在相应密度为正时，它有正概率。

所以：

$$
\begin{aligned}
&P(u<U\le u+\Delta u\mid v<V\le v+h)\\
&\approx
\frac{f_{U,V}(u,v)\,\Delta u\,h}{f_V(v)\,h}\\
&=
\frac{f_{U,V}(u,v)}{f_V(v)}\,\Delta u.
\end{aligned}
$$

观察最后一行：

$$
\text{条件下的小区间概率}
\approx
\text{某个密度}\times\Delta u.
$$

因此，当条件带逐渐变窄时，自然得到条件密度的表达式：

$$
\boxed{
f_{U\mid V}(u\mid v)
=
\frac{f_{U,V}(u,v)}{f_V(v)},
\qquad 0<f_V(v)<\infty.
}
$$

**这里除的是密度，不是单点概率。**

上面的窄带推导提供了局部规则情形下的直觉。更一般地，这个比值给出条件密度的一种标准表示；条件分布的唯一性允许在一组总概率为零的条件值上存在例外。本课不展开这部分测度论细节。

### 4. 为什么除以边缘密度，恰好能归一化？

固定 $v$，对 $u$ 积分：

$$
\begin{aligned}
\int_{-\infty}^{+\infty}f_{U\mid V}(u\mid v)\,du
&=
\int_{-\infty}^{+\infty}
\frac{f_{U,V}(u,v)}{f_V(v)}\,du\\
&=
\frac{\int_{-\infty}^{+\infty}f_{U,V}(u,v)\,du}
{f_V(v)}\\
&=\frac{f_V(v)}{f_V(v)}\\
&=1.
\end{aligned}
$$

所以它确实是一条关于 $u$ 的概率密度。

对应的条件区间概率为：

$$
\boxed{
P(a<U\le b\mid V=v)
=
\int_a^b f_{U\mid V}(u\mid v)\,du.
}
$$

这里 $a,b$ 是第一个坐标的区间端点。

整体思想与离散情况一致：

```text
离散：
取出满足条件的概率，再除以其总和。

连续：
取出给定条件值对应的密度截面，
再除以这个截面对剩余坐标的积分。
```

---

## 十、连续例子：在上一课的三角形里，知道纵坐标以后会怎样？

### 1. 明确一次试验

继续使用上一课的连续例子：

> 在三角形区域 $0\le v\le u\le1$ 内，按面积均匀地随机选取一个点，记录横坐标 $U$ 和纵坐标 $V$。

不是先独立选择横坐标、再独立选择纵坐标。

**一次试验是在指定三角形内选一个点，两个坐标共同来自这个点。**

三角形面积为：

$$
\frac12.
$$

为了让总概率为 1，区域内的联合密度为 2：

$$
f_{U,V}(u,v)=
\begin{cases}
2,&0\le v\le u\le1,\\
0,&\text{其他位置}.
\end{cases}
$$

### 2. 固定纵坐标，先确定横坐标范围

假设给定：

$$
V=v,\qquad 0<v<1.
$$

由三角形的约束：

$$
v\le u\le1.
$$

也就是说：

> 知道纵坐标为 $v$ 后，横坐标只能落在从 $v$ 到 1 的范围内。

例如给定：

$$
V=\frac14,
$$

横坐标只能在：

$$
\frac14\le U\le1
$$

之间。

脑中的画面是：

```text
V
↑
1                       ●
                      / │
                    /   │
                  /     │
1/4       ●═════════════│
         /  已知纵坐标后 │
        /  横坐标的范围  │
0      ●────────────────● → U
       0   1/4          1
```

### 3. 先求分母：$V$ 的边缘密度

固定 $v$，允许的横坐标范围是 $v$ 到 1。

所以：

$$
\begin{aligned}
f_V(v)
&=\int_v^1 2\,du\\
&=2(1-v).
\end{aligned}
$$

### 4. 再求条件密度

在允许的范围 $v\le u\le1$ 内：

$$
\begin{aligned}
f_{U\mid V}(u\mid v)
&=\frac{f_{U,V}(u,v)}{f_V(v)}\\
&=\frac{2}{2(1-v)}\\
&=\frac1{1-v}.
\end{aligned}
$$

因此，对于 $0<v<1$：

$$
\boxed{
f_{U\mid V}(u\mid v)=
\begin{cases}
\frac1{1-v},&v\le u\le1,\\
0,&\text{其他}.
\end{cases}
}
$$

这是区间 $[v,1]$ 上的均匀分布。

为什么？

因为在固定纵坐标的截面上，原联合密度处处相同；统一归一化以后，仍然处处相同。

### 5. 给定 $V=\frac14$，具体算一次

此时：

$$
f_V\left(\frac14\right)
=
2\left(1-\frac14\right)
=
\frac32.
$$

所以：

$$
f_{U\mid V}\left(u\mid\frac14\right)
=
\frac{2}{3/2}
=
\frac43,
\qquad \frac14\le u\le1.
$$

检查面积：

$$
\text{宽度}\times\text{高度}
=
\left(1-\frac14\right)\times\frac43
=
\frac34\times\frac43
=
1.
$$

这里：

$$
f_V\left(\frac14\right)=\frac32>1,
$$

以及：

$$
f_{U\mid V}\left(u\mid\frac14\right)=\frac43>1
$$

都没有问题，因为它们是密度，不是概率。

现在求：

$$
P\left(U\le\frac12\mid V=\frac14\right).
$$

给定条件下，$U$ 从 $\frac14$ 开始，因此：

$$
\begin{aligned}
P\left(U\le\frac12\mid V=\frac14\right)
&=\int_{1/4}^{1/2}\frac43\,du\\
&=\frac43\left(\frac12-\frac14\right)\\
&=\frac43\times\frac14\\
&=\frac13.
\end{aligned}
$$

整个计算的来源只有：

```text
固定纵坐标
      ↓
找出允许的横坐标范围
      ↓
取联合密度截面并归一化
      ↓
对目标横坐标区间积分
```

**条件密度的高度仍不是概率；它下面的区间面积才是条件概率。**

---

## 十一、条件分布建立以后，原来的概率工具仍然可以使用

固定一个条件以后，我们得到的仍然是一张合法分布。

因此，我们仍然可以计算区间概率、累计概率，也可以提取均值等特征。

例如，回到卡片模型，已知 $Y=1$ 后：

$$
X=0,1,2
$$

的概率分别为：

$$
\frac13,\quad\frac16,\quad\frac12.
$$

第二十一课的期望思想是“取值乘概率，再求和”。现在换成条件下的概率权重即可。

于是：

$$
\begin{aligned}
E[X\mid Y=1]
&=0\times\frac13
+1\times\frac16
+2\times\frac12\\
&=\frac16+1\\
&=\frac76.
\end{aligned}
$$

这个记号表示：

> 已知 $Y=1$ 时，$X$ 的条件分布的平均位置。

它不是说本次左边会出现 $\frac76$。

**本次左边仍然只能是 0、1、2；$\frac76$ 是这张条件分布的重心。**

这里先展示与旧知识的连接，不展开条件期望的系统理论。

整条链现在是：

```text
随机抽取一张卡片
        ↓
样本点：具体抽到哪张卡片
        ↓
随机向量：(X,Y)，读取两个数字
        ↓
联合分布：数字组合怎样分配概率
        ↓
观察到 Y=1
        ↓
条件分布：在这个信息下，X 怎样分配概率
        ↓
条件下的区间概率、均值等特征
```

---

## 十二、实际应用：我们往往需要的是“给定输入后的分布”

### 1. 机器学习、深度学习与计算机视觉

第六课已经提到：分类问题中，我们通常不只关心“某个类别总体有多常见”，而关心“看到输入以后，这个样本属于各个类别的概率”。

把输入图像记为 $I$，类别编码记为 $C$。

那么：

$$
P(C=c)
$$

描述某个类别在总体中的比例。

而：

$$
P(C=c\mid I=i)
$$

描述已经给定具体图像 $i$ 后，类别为 $c$ 的概率。

对于固定输入，遍历全部类别，就得到一张条件分布。

**模型输出的类别概率是在尝试描述这张条件分布，但不能仅因为输出非负、总和为 1，就认定它等于真实条件分布。**

### 2. 数据分析：总体错误率与分组错误率

沿用上一课人为设定的测试集：100 张图像中，30 张是弱光图像，其中 12 张预测错误、18 张预测正确。

定义一次试验：

> 从这 100 张图像中等可能地抽一张。

如果已经知道抽到的是弱光图像，那么“预测是否错误”的条件分布是：

$$
P(\text{错误}\mid\text{弱光})=\frac{12}{30}=0.4,
$$

$$
P(\text{正确}\mid\text{弱光})=\frac{18}{30}=0.6.
$$

而：

$$
\frac{12}{100}=0.12
$$

是“弱光并且错误”的联合概率，不能当作弱光条件下的错误率。

### 3. 强化学习与工程问题

在强化学习中，可以先给定当前状态和动作，再研究下一状态的分布。

在工程中，可以先给定某种已观测工况，再研究任务耗时或设备误差的分布。

两者都在问：

> **已经知道一部分信息以后，剩下的不确定量怎样分布？**

但“观察到某个工况”不等于“人为改变工况”：条件分布首先描述已有联合模型中的概率关系，不能仅凭它就断定干预某个变量会产生同样的效果。

---

## 十三、理解检查

先思考两个问题，再对照解释。

### 问题一：已知右边为 0，左边至少为 1 的概率是多少？

在 $Y=0$ 的条件下，左边的分布是：

$$
P(X=0\mid Y=0)=\frac14,
$$

$$
P(X=1\mid Y=0)=\frac12,
$$

$$
P(X=2\mid Y=0)=\frac14.
$$

因此：

$$
\begin{aligned}
P(X\ge1\mid Y=0)
&=P(X=1\mid Y=0)+P(X=2\mid Y=0)\\
&=\frac12+\frac14\\
&=\frac34.
\end{aligned}
$$

不是把联合表中的 $0.2+0.1=0.3$ 直接作为答案，因为还需要除以条件概率 $0.4$。

### 问题二：固定条件以后，每种取值的概率是不是都一样？

不是。

条件化保留符合条件结果之间原来的相对概率关系，只做统一归一化。

本例中，已知 $Y=1$ 后：

$$
\frac13,\quad\frac16,\quad\frac12
$$

并不相同。

**归一化保证总和为 1，不保证各项相等。**

---

## 这一节真正需要记住的核心

**条件分布描述：已经知道一部分信息以后，随机变量的整个概率规律；随机变量的含义没有改变，改变的是用于判断的概率权重。**

**离散情况下，先筛选条件对应的联合概率，再除以条件的边缘概率：$p_{X\mid Y}(x\mid y)=p_{X,Y}(x,y)/p_Y(y)$，前提是分母大于零。**

**连续情况下，不能直接除以单点概率；存在联合密度时，通常通过“联合密度除以条件变量的边缘密度”得到条件密度，再通过积分计算条件概率。**

**联合分布研究共同取值，边缘分布不限制其他变量，条件分布利用其他变量的已知信息；独立时，条件分布与边缘分布相同。**

**条件分布乘以条件出现的概率，可以恢复联合概率；再对不同条件分组汇总，就恢复边缘分布。**

下一课进入**第二十九课：随机变量函数的分布**。我们已经学习了怎样共同观察、忽略或利用某个变量的信息，接下来就自然要问：**如果把已有数值再加工成一个新数值，例如 $Z=X^2$，概率应该怎样随这次变换重新分配？**
