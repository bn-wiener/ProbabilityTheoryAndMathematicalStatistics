# 第二十五课：多维随机变量——从一个随机数字，到一组共同出现的数字

第二十四课最后留下了一个问题：

> **我们已经会用协方差、相关系数概括两个随机变量的关系，但怎样完整描述它们到底会如何“一起取值”？**

这正是上一课指向的下一阶段：**多维随机变量与联合分布**。

今天先把这一步的基础建立牢固：

> **同一次随机试验产生多个数值时，我们为什么必须把这些数值作为一个整体研究？**

我们会用一张简单的联合概率表说明它的意义，但暂时不展开二维概率密度、二重积分等内容。

---

## 一、问题背景：只知道两个量“各自怎样”，为什么还不够？

先考虑一个具体问题：

> **如果我们已经知道两个随机变量各自的概率分布，是不是就能知道它们怎样一起取值？**

我们用一个抽卡片的例子，把这个问题说清楚。

### 1. 先明确：一次试验到底做什么？

准备甲、乙两个袋子，每个袋子里都有两张卡片。每张卡片上都写着两个数字：一个在左边，一个在右边。

我们规定：

> **一次完整试验，就是从事先指定的一个袋子里随机抽出一张卡片，读取这张卡片上左、右两个数字，然后结束。**

假设同一个袋子里的两张卡片等可能被抽到，因此每张被抽到的概率都是 $\frac12$。

定义两个随机变量：

$$
X=\text{抽到的那张卡片上，左边的数字},
$$

$$
Y=\text{抽到的那张卡片上，右边的数字}.
$$

例如，抽到一张左边写着 0、右边写着 1 的卡片，那么本次就得到：

$$
X=0,\qquad Y=1.
$$

合起来记为：

$$
(X,Y)=(0,1).
$$

**注意：只抽了一张卡片，却记录了两个数字。$X$ 不是第一次抽取，$Y$ 也不是第二次抽取；它们来自同一次抽取中的同一张卡片。**

接下来，我们分别研究“从甲袋抽”和“从乙袋抽”这两种设置，**不是从两个袋子各抽一张，也不需要随机选择袋子。**

### 2. 两个袋子里的卡片有什么不同？

**甲袋：两张卡片上的左右数字都相同。**

| 卡片 | 左边数字 $X$ | 右边数字 $Y$ |   抽到这张卡片的概率 |
| -- | ---------: | ---------: | ----------: |
| 甲1 |          0 |          0 | $\frac12$ |
| 甲2 |          1 |          1 | $\frac12$ |

因此，从甲袋抽一张，只可能得到：

$$
(0,0)\quad\text{或}\quad(1,1),
$$

每种组合出现的概率都是 $\frac12$。

**乙袋：两张卡片上的左右数字都不同。**

| 卡片 | 左边数字 $X$ | 右边数字 $Y$ |   抽到这张卡片的概率 |
| -- | ---------: | ---------: | ----------: |
| 乙1 |          0 |          1 | $\frac12$ |
| 乙2 |          1 |          0 | $\frac12$ |

因此，从乙袋抽一张，只可能得到：

$$
(0,1)\quad\text{或}\quad(1,0),
$$

每种组合出现的概率也都是 $\frac12$。

这里的“概率为 $\frac12$”来自明确的抽取规则：**袋子里有两张等可能被抽到的卡片，其中一张对应这个组合。**

它不是说要抽两次，更不是说重复抽取两次，就必定每种结果各出现一次。

### 3. 如果只看左边，或者只看右边，会发现什么？

先只看左边数字 $X$，暂时不管右边。

甲袋中，一张卡片左边是 0，另一张左边是 1。

乙袋中，也是一张卡片左边是 0，另一张左边是 1。

因此，在两种设置下都有：

$$
P(X=0)=\frac12,\qquad P(X=1)=\frac12.
$$

也就是说：

> **两个袋子中，左边数字 $X$ 的概率分布完全相同。**

再只看右边数字 $Y$。

甲袋中，一张卡片右边是 0，另一张右边是 1。

乙袋中，虽然两个数字的配对方式不同，但仍然是一张卡片右边是 0，另一张右边是 1。

因此，在两种设置下也都有：

$$
P(Y=0)=\frac12,\qquad P(Y=1)=\frac12.
$$

所以：

> **两个袋子中，右边数字 $Y$ 的概率分布也完全相同。**

到这里，我们得到：

**单独看 $X$，两种设置一样；单独看 $Y$，两种设置也一样。**

### 4. 但是，问“两个数字同时为 1”，答案就不同了

现在提出一个新问题：

> **抽出一张卡片，这张卡片的左边和右边都是 1，概率是多少？**

**从甲袋抽时：**

甲袋有一张 $(0,0)$，一张 $(1,1)$。

两张等可能的卡片中，有一张满足“左右都是 1”，所以：

$$
P(X=1,Y=1)=\frac12
\qquad\text{（甲袋设置）}.
$$

这里的逗号表示“并且”：**左边等于 1，并且同一张卡片的右边也等于 1。**

**从乙袋抽时：**

乙袋只有 $(0,1)$ 和 $(1,0)$，没有任何一张卡片的左右都是 1。

所以：

$$
P(X=1,Y=1)=0
\qquad\text{（乙袋设置）}.
$$

现在，关键区别出现了：

> **虽然 $X$ 各自的分布一样，$Y$ 各自的分布也一样，但它们同时等于 1 的概率却不一样。**

### 5. 我们究竟缺少了什么信息？

缺少的是：

> **哪个左边数字，和哪个右边数字，写在同一张卡片上。**

甲袋中，0 与 0 配对，1 与 1 配对。

乙袋中，0 与 1 配对，1 与 0 配对。

**每个位置上 0、1 各自的出现概率没有变，但两个位置之间的配对关系变了。**

因此，如果只告诉我们：

$$
P(X=0)=P(X=1)=\frac12,
$$

$$
P(Y=0)=P(Y=1)=\frac12,
$$

仍然不足以确定：

$$
P(X=1,Y=1).
$$

因为这些已知信息既符合甲袋，也符合乙袋，而它们给出的共同取值概率不同。

这就是本课要解决的问题：

$$
\boxed{
\text{知道各自怎样取值}
\neq
\text{知道它们怎样共同取值}
}
$$

为了保留这种配对信息，我们需要把同一次试验中的两个读数 $(X,Y)$ 作为一个整体研究，这就是**二维随机变量**的出发点；再描述每一种数值组合获得多少概率，就进入了**联合分布**。

---

## 二、先把今天放回知识地图

前几课主要研究的是分布的摘要：

```text
第二十一课：期望
            中心在哪里？
                ↓
第二十二课：方差
            自己散得多开？
                ↓
第二十三课：协方差
            两个变量是否倾向一起偏？
                ↓
第二十四课：相关系数
            去掉尺度后，线性共同变化有多强？
```

从今天开始，我们要回到这些摘要背后的完整对象：

```text
第二十五课：多维随机变量
            把共同出现的多个数值作为一个整体
                ↓
联合分布
这些数值组合分别获得多少概率？
                ↓
边缘分布
只看其中一个变量，怎样恢复它的分布？
                ↓
条件分布
已知其中一些变量，其他变量的分布怎样改变？
```

这里有一个很重要的认识：

> 协方差和相关系数不是比联合分布“更完整”的信息；它们是从共同概率规律中提取出来的少量摘要。

第一课的知识地图也明确区分了“提取分布特征”和“研究多个随机变量的完整关系”这两个阶段。

---

## 三、直觉：以前一次结果变成一个数字，现在变成一组数字

第八课已经建立：

> 随机变量是一条确定的映射规则：把样本点转换成一个数值；随机的是最终出现哪个样本点。

例如：

```text
一个样本点 ω
      ↓
随机变量 X
      ↓
一个数字 X(ω)
```

但是，同一个样本点往往包含不止一种我们关心的信息。

于是，可以让同一个结果同时进入两个“读数器”：

```text
                     ┌──→ X(ω)
一个样本点 ω ─────────┤
                     └──→ Y(ω)
```

然后把两个输出放在一起：

$$
\bigl(X(\omega),Y(\omega)\bigr).
$$

这就是今天最重要的画面：

```text
以前：
一次结果 → 一个数字

现在：
一次结果 → 一组有顺序的数字
```

注意，新增的思想不是“同时存在两个变量”这么简单，而是：

> **保留它们在同一次试验中的对应关系。**

下面用一个完整例子把这句话讲透。

---

## 四、最简单的例子：同样是抛两次硬币，同时记录两件事情

### 1. 先明确随机试验

连续、独立地抛两次公平硬币，记录先后顺序。

样本空间是：

$$
\Omega=
\{\text{正正},\text{正反},\text{反正},\text{反反}\}.
$$

四个样本点各自的概率都是：

$$
\frac14.
$$

这里“一次完整试验”包含两次抛掷。

例如：

$$
\omega=\text{正反}
$$

是这次完整试验的一个基本结果。

### 2. 第一个随机变量：正面出现次数

定义：

$$
X=\text{两次抛掷中正面出现的次数}.
$$

所以：

```text
正正 → 2
正反 → 1
反正 → 1
反反 → 0
```

### 3. 第二个随机变量：两次结果是否相同

定义：

$$
Y=
\begin{cases}
1,&\text{两次结果相同},\\
0,&\text{两次结果不同}.
\end{cases}
$$

所以：

```text
正正 → 1
正反 → 0
反正 → 0
反反 → 1
```

这两个观察规则，其实已经在第八课“同一个随机试验可以定义很多随机变量”中出现过。今天我们把它们正式组合起来研究。

### 4. 把两个输出放在同一行

| 样本点 $\omega$ | $X(\omega)$：正面次数 | $Y(\omega)$：是否相同 | 成对输出      |
| -------------- | -----------------: | -----------------: | --------- |
| 正正             |                  2 |                  1 | $(2,1)$ |
| 正反             |                  1 |                  0 | $(1,0)$ |
| 反正             |                  1 |                  0 | $(1,0)$ |
| 反反             |                  0 |                  1 | $(0,1)$ |

例如：

$$
\omega=\text{正正}
$$

时：

$$
X(\omega)=2,\qquad Y(\omega)=1.
$$

把这两条信息合在一起：

$$
\bigl(X(\omega),Y(\omega)\bigr)=(2,1).
$$

这不是两个毫无联系的数字。

它们共同描述的是：

> **同一个样本点的两个数值特征。**

---

## 五、数学定义：什么是二维随机变量？

现在有了直觉，再正式定义。

设 $X$ 和 $Y$ 是定义在**同一个概率空间**上的两个随机变量。

这里“同一个概率空间”，可以先理解成：

> 使用同一套样本结果、事件和概率规则，能够明确判断两个数值怎样共同出现。

将它们组成有序的一对：

$$
\boxed{\mathbf Z=(X,Y)}
$$

就得到一个**二维随机变量**，也叫**二维随机向量**。

它的映射形式是：

$$
\boxed{
\mathbf Z:\Omega\longrightarrow\mathbb R^2
}
$$

具体到一个样本点：

$$
\boxed{
\mathbf Z(\omega)
=
\bigl(X(\omega),Y(\omega)\bigr)
}
$$

逐个解释这几个符号：

| 符号                    | 含义                     |
| --------------------- | ---------------------- |
| $\Omega$            | 原始样本空间                 |
| $\omega$            | 其中一个样本点                |
| $X,Y$               | 两条数值映射规则，也叫这个随机向量的两个分量 |
| $\mathbf Z$         | 把两条规则组合起来的整体           |
| $\mathbb R^2$       | 所有有序实数对组成的空间，可以画成一个平面  |
| $\mathbf Z(\omega)$ | 本次样本点对应的具体数值对          |

因此，定义背后的逻辑只是：

```text
一个数描述的信息不够
          ↓
从同一个结果中提取两个数
          ↓
保留它们的对应关系
          ↓
把有序数对作为整体
          ↓
二维随机变量
```

### 为什么强调“有序”？

因为第一、第二个位置代表不同含义。

本例中：

$$
(2,1)
$$

表示：

```text
正面次数是 2；
两次结果相同。
```

而：

$$
(1,2)
$$

会被解释成：

```text
正面次数是 1；
“是否相同”的编码是 2。
```

后者甚至不符合 $Y$ 的取值规定。

所以：

$$
\boxed{(X,Y)\text{ 中的位置有含义，不能随便交换。}}
$$

---

## 六、这里最容易混淆的四个层次

我们仍然使用：

$$
\omega=\text{正正}.
$$

那么：

| 对象        | 本例中的表达                         | 它是什么                |
| --------- | ------------------------------ | ------------------- |
| 样本点       | $\text{正正}$                  | 一个原始试验结果            |
| 二维随机变量    | $\mathbf Z=(X,Y)$            | 一整套映射规则             |
| 随机向量的具体取值 | $\mathbf Z(\text{正正})=(2,1)$ | 一个确定的数值对            |
| 关于取值的事件   | $\{X=2,Y=1\}$                | 所有能产生这一数值对的样本点组成的集合 |

其中：

$$
\{X=2,Y=1\}=\{\text{正正}\}.
$$

所以：

$$
P(X=2,Y=1)=\frac14.
$$

这时的 $\frac14$，才是概率。

请把这条链记清楚：

```text
正正
原始结果
   ↓
(X,Y)
提取两个数值的规则
   ↓
(2,1)
得到的数值对
   ↓
{X=2，Y=1}
所有会产生这个数值对的原始结果
   ↓
P(X=2，Y=1)
这个事件的概率
```

**数值对、事件、概率，仍然是三个不同层次。**

---

## 七、二维随机变量的取值，为什么不能随便“排列组合”？

在本例中，单看 $X$，它可能取：

$$
0,1,2.
$$

单看 $Y$，它可能取：

$$
0,1.
$$

于是很容易误以为，它们可以组成六种结果：

$$
(0,0),(0,1),(1,0),(1,1),(2,0),(2,1).
$$

但实际映射表告诉我们，只有：

$$
\boxed{
(0,1),\quad(1,0),\quad(2,1)
}
$$

会出现。

为什么？

当 $X=0$，没有正面，只能是“反反”，两次必然相同，所以 $Y=1$。

当 $X=1$，恰好一个正面，只能是“正反”或“反正”，两次必然不同，所以 $Y=0$。

当 $X=2$，只能是“正正”，两次必然相同，所以 $Y=1$。

因此：

$$
P(X=1,Y=1)=0.
$$

虽然：

$$
X=1
$$

单独有可能，

$$
Y=1
$$

单独也有可能，

但：

> **它们没有办法在本例的同一次试验中一起出现。**

这正说明：只知道各个分量的取值范围，还不知道整个随机向量的取值结构。

---

## 八、几何直觉：概率从数轴上的位置，搬到了平面上的位置

一维随机变量的一次取值，是数轴上的一个位置：

```text
──────────●────────────────→ x
          1
```

二维随机变量的一次取值，是平面上的一个位置：

```text
y
↑
│                  ● (x,y)
│
└──────────────────────────→ x
```

本例中：

$$
(X,Y)\in\{(0,1),(1,0),(2,1)\}.
$$

可以画成：

```text
y：Y 的取值

1   │  ●                       ●
    │ (0,1)                   (2,1)
    │
0   │              ●
    │             (1,0)
    └──────────────────────────────→ x：X 的取值
       0            1           2
```

这里要特别校准坐标轴的含义：

> **横轴是 $X$ 的取值，纵轴是 $Y$ 的取值；纵轴不是概率，也不是概率密度。**

这和以前的一维概率质量图不同。

以前的一维概率质量图：

```text
横轴：变量的取值
纵轴：该取值的概率
```

现在这张二维取值图：

```text
横轴：第一个变量的取值
纵轴：第二个变量的取值
```

那么概率放在哪里？

可以标在每个点旁边，也可以用点的大小表示。

于是，本例的图会变成：

```text
y：Y 的取值

1   │  ●  概率1/4              ●  概率1/4
    │
0   │              ●  概率1/2
    └──────────────────────────────→ x：X 的取值
       0            1           2
```

**坐标告诉我们“是什么组合”，概率告诉我们“这个组合有多容易出现”。**

---

## 九、联合分布的起点：每一组数字获得多少概率？

有了随机向量，还需要描述它的概率规律。

这与第八课到第九课的过渡完全一样：

```text
随机变量：
样本点怎样变成数字？

概率分布：
这些数字分别获得多少概率？
```

现在变成：

```text
随机向量：
样本点怎样变成一组数字？

联合分布：
这些数字组合分别获得多少概率？
```

第九课把分布解释成“按映射结果重新汇总概率重量”；今天只是把汇总位置从一个数，换成一个数值对。

### 1. 概率怎样汇总到数值对上？

回到映射表：

```text
反反，概率1/4 ─────────────→ (0,1)

正反，概率1/4 ──────┐
                    ├─────→ (1,0)
反正，概率1/4 ──────┘

正正，概率1/4 ─────────────→ (2,1)
```

因为“正反”和“反正”都变成：

$$
(1,0),
$$

所以它们的概率需要相加：

$$
P(X=1,Y=0)
=
\frac14+\frac14
=
\frac12.
$$

于是：

$$
P(X=0,Y=1)=\frac14,
$$

$$
P(X=1,Y=0)=\frac12,
$$

$$
P(X=2,Y=1)=\frac14.
$$

其他数值对的概率为 0。

### 2. 用一张表表达

| $X\backslash Y$ |     $Y=0$ |     $Y=1$ |
| ----------------- | ----------: | ----------: |
| $X=0$           |       $0$ | $\frac14$ |
| $X=1$           | $\frac12$ |       $0$ |
| $X=2$           |       $0$ | $\frac14$ |

这张表就是本例的**联合概率分布表**。

“联合”不是某种新的概率运算。

它只是强调：

> **我们给共同取值的组合分配概率，而不是分别给两个变量列两张互不关联的表。**

### 3. 对应的数学记号

对于这样的离散型随机向量，定义：

$$
\boxed{
p_{X,Y}(x,y)=P(X=x,Y=y)
}
$$

称为**联合概率质量函数**。

其中，$X,Y$ 是随机变量，$x,y$ 是我们拿来询问的两个具体数值；下标 $X,Y$ 表明我们研究的是哪一对变量。

例如：

$$
p_{X,Y}(1,0)=\frac12
$$

表示：

> 正面恰好出现一次，并且两次结果不同，这件事的概率是二分之一。

### 4. 逗号究竟是什么意思？

这里的：

$$
P(X=x,Y=y)
$$

是：

$$
\boxed{
P\bigl(\{X=x\}\cap\{Y=y\}\bigr)
}
$$

的简写。

也就是说：

$$
\boxed{
\text{逗号表示“并且”，不是“相乘”。}
}
$$

更完整地，事件本身是：

$$
\{X=x,Y=y\}
=
\{\omega\in\Omega:
X(\omega)=x,\ Y(\omega)=y\}.
$$

这句话只是说：

> 到原来的样本空间里，寻找同时让两个数值条件成立的样本点。

---

## 十、怎样从联合概率表计算一个事件？

仍然只用已经学过的规则：**找到满足条件的结果，把互斥结果的概率相加。**

### 问题一：正面次数至少为 1，而且两次相同

我们要求：

$$
P(X\ge1,Y=1).
$$

先找表中满足条件的格子：

$$
(X,Y)=(1,1)
$$

或者：

$$
(X,Y)=(2,1).
$$

这两种取值组合互斥，因此：

$$
\begin{aligned}
P(X\ge1,Y=1)
&=P(X=1,Y=1)+P(X=2,Y=1)\\
&=0+\frac14\\
&=\frac14.
\end{aligned}
$$

用原始结果检查：

> 至少一个正面，而且两次相同，只能是“正正”。

完全一致。

### 问题二：只关心 $X=1$，不再限制 $Y$

这时，$Y$ 取 0 或 1 都可以，所以：

$$
\begin{aligned}
P(X=1)
&=P(X=1,Y=0)+P(X=1,Y=1)\\
&=\frac12+0\\
&=\frac12.
\end{aligned}
$$

你已经看到了以后“边缘分布”的核心动作：

> **不关心哪个变量，就把它的各种取值情况汇总掉。**

今天先理解这个动作，不展开一般公式。

### 为什么整张表的概率总和必须为 1？

因为一次试验结束后，随机向量必然落到它的某个取值组合上，而且不同的精确取值组合不能同时实现。

所以：

$$
0+\frac14+\frac12+0+0+\frac14=1.
$$

这仍然来自旧知识：

```text
所有可能情况覆盖整个试验
+
不同取值组合彼此互斥
+
总概率为1
```

并不是二维世界另外发明了一套概率规则。

---

## 十一、最重要的反例：两个变量放在一起，不代表可以把概率相乘

本例中：

$$
P(X=1)=\frac12.
$$

而“两次相同”包含“正正”和“反反”，所以：

$$
P(Y=1)=\frac14+\frac14=\frac12.
$$

如果直接相乘，会得到：

$$
P(X=1)P(Y=1)=\frac14.
$$

但我们已经知道：

$$
P(X=1,Y=1)=0.
$$

所以：

$$
\boxed{
P(X=1,Y=1)\neq P(X=1)P(Y=1)
}
$$

原因不是“二维概率不能相乘”，而是：

> **本例中的 $X,Y$ 不独立。**

用条件概率看得更清楚。

一旦知道：

$$
X=1,
$$

我们就知道两次一定不同，因此：

$$
P(Y=1\mid X=1)=0.
$$

可在没有这个条件时：

$$
P(Y=1)=\frac12.
$$

信息改变了概率判断，所以不独立。

### 一个特别值得注意的区别

本例中：

> 两次硬币抛掷是独立的。

但：

> 从这两次抛掷中提取出的 $X$ 和 $Y$，却不是独立的。

这不矛盾。

因为“第一次抛掷”和“第二次抛掷”，与“正面总次数”和“两次是否相同”，不是同一对研究对象。

$$
\boxed{
\text{底层试验独立}
\not\Rightarrow
\text{从试验中构造的任意两个随机变量独立}
}
$$

判断独立性之前，必须先明确：

> **到底在判断哪两个对象？**

---

## 十二、把第二十四课接回来：这个例子的相关系数竟然是 0

这个例子还有一个很好的作用：它能把“完整结构”和“摘要”之间的区别展示出来。

我们已经知道，$X$ 一旦确定，$Y$ 就完全确定：

```text
X=0 → Y=1
X=1 → Y=0
X=2 → Y=1
```

所以两者显然有依赖关系。

但它们的协方差是多少？

### 1. 先求两个均值

$$
\begin{aligned}
E[X]
&=0\times\frac14
+1\times\frac12
+2\times\frac14\\
&=1.
\end{aligned}
$$

而：

$$
\begin{aligned}
E[Y]
&=1\times\frac14
+0\times\frac12
+1\times\frac14\\
&=\frac12.
\end{aligned}
$$

### 2. 看三个取值组合的偏差乘积

协方差研究的是：

$$
(X-E[X])(Y-E[Y]).
$$

代入两个均值：

$$
(X-1)\left(Y-\frac12\right).
$$

逐个看：

| 取值组合      | $X-1$ | $Y-\frac12$ |         偏差乘积 |          概率 |
| --------- | ------: | ------------: | -----------: | ----------: |
| $(0,1)$ |  $-1$ |   $\frac12$ | $-\frac12$ | $\frac14$ |
| $(1,0)$ |   $0$ |  $-\frac12$ |        $0$ | $\frac12$ |
| $(2,1)$ |   $1$ |   $\frac12$ |  $\frac12$ | $\frac14$ |

取概率加权平均：

$$
\begin{aligned}
\operatorname{Cov}(X,Y)
&=
-\frac12\times\frac14
+0\times\frac12
+\frac12\times\frac14\\
&=-\frac18+\frac18\\
&=0.
\end{aligned}
$$

由于两个变量都有有限且非零的方差，相关系数的分母是正数，因此：

$$
\boxed{\operatorname{Corr}(X,Y)=0.}
$$

### 3. 为什么会这样？

观察取值图：

```text
Y
↑
1   ●               ●
     \             /
      \           /
0          ●
    └────────────────→ X
    0      1        2
```

$X$ 从 0 增加到 1 时，$Y$ 下降；$X$ 从 1 增加到 2 时，$Y$ 上升。

它不是统一的“越大越大”或“越大越小”。

因此，正负的共同偏离在平均时抵消了。

但依赖关系并没有消失。

这就再次验证了：

$$
\boxed{
\text{不相关}
\neq
\text{独立}
}
$$

### 4. 再进一步：连这些摘要都相同，完整结构仍然可能不同

现在保持 $X$ 不变，另外独立抛一枚公平硬币，用它定义：

$$
Y'=
\begin{cases}
1,&\text{这枚新硬币为正面},\\
0,&\text{这枚新硬币为反面}.
\end{cases}
$$

那么，$Y'$ 与原来的 $Y$ 具有相同的单变量分布：各以一半概率取 0 和 1。

但 $Y'$ 与 $X$ 独立，所以它们的协方差、相关系数也都是 0。

于是，两个系统：

$$
(X,Y)
\qquad\text{和}\qquad
(X,Y')
$$

有相同的各自分布，也有相同的协方差和相关系数。

可是：

$$
P(X=1,Y=1)=0,
$$

而：

$$
P(X=1,Y'=1)
=
\frac12\times\frac12
=
\frac14.
$$

**共同概率规律仍然不同。**

这就是为什么：

> **期望、方差、协方差、相关系数这些摘要，一般不能代替完整的联合分布。**

---

## 十三、“多维”到底是什么意思？几个边界要分清

### 1. 维数不是可能结果的个数

本例的随机向量是：

$$
(X,Y).
$$

它有两个分量，所以是二维。

虽然它只有三个可能取值：

$$
(0,1),(1,0),(2,1),
$$

也不会因此叫“三维随机变量”。

同样，骰子点数有六个可能值，但点数本身仍是一维随机变量。

$$
\boxed{
\text{维数看记录几个坐标，不看有几个可能结果。}
}
$$

### 2. 维数不是独立随机性的数量

本例中，知道 $X$ 就知道 $Y$，但 $(X,Y)$ 仍然是一个二维随机向量。

“二维”只说明我们选择了两个分量来描述结果，不保证这两个分量提供彼此独立的信息。

所以：

$$
\boxed{
\text{二维}
\neq
\text{两个独立的随机来源}
}
$$

### 3. 多维不等于连续型

本例是二维的，但它的取值可以逐个列出来，所以是离散型随机向量。

维数和离散、连续是在回答不同问题：

```text
维数：
一次记录几个数值分量？

离散或连续：
这些取值及其概率以什么形式组织？
```

### 4. $(X,Y)$ 不是 $X+Y$

$$
(X,Y)
$$

保留两个位置。

而：

$$
X+Y
$$

把两个数字压成一个数字。

例如：

$$
(0,1)\neq(1,0),
$$

但它们相加后都等于：

$$
1.
$$

因此：

> 组成向量是在保留多个分量；求和则是在构造一个新的单变量，并可能丢掉配对细节。

### 5. “同一次试验”不等于“同一时刻”

比如：

$$
X=\text{第一次抛掷是否为正面},
$$

$$
Y=\text{第二次抛掷是否为正面}.
$$

它们发生在不同时间，仍然可以作为一次“两次抛掷试验”中的两个分量。

真正需要的是：

> **明确什么样的观测属于同一条记录，以及这些记录怎样产生。**

---

## 十四、从二维推广到多维，其实没有增加新的核心思想

如果同一个概率模型中有：

$$
X_1,X_2,\ldots,X_d,
$$

这 $d$ 个随机变量，就可以组成：

$$
\boxed{
\mathbf X=(X_1,X_2,\ldots,X_d).
}
$$

它是一个 $d$ 维随机向量。

对于一个样本点 $\omega$：

$$
\mathbf X(\omega)
=
\bigl(X_1(\omega),X_2(\omega),\ldots,X_d(\omega)\bigr).
$$

这里，$d$ 表示分量个数；下标 $1,2,\ldots,d$ 用来区分不同的观察规则。

所以：

```text
一维：一次结果 → 一个数
二维：一次结果 → 两个数
多维：一次结果 → 多个数
```

而随机向量的一次具体实现，可以记作：

$$
\mathbf x=(x_1,\ldots,x_d).
$$

仍然是：

$$
\boxed{
\mathbf X\text{ 是随机对象，}\quad
\mathbf x\text{ 是一次具体观测值。}
}
$$

如果观察很多次，得到：

$$
\mathbf x^{(1)},\mathbf x^{(2)},\ldots,\mathbf x^{(n)},
$$

上标括号中的编号表示第几次观测；每一条记录内部的 $d$ 个分量，才决定向量的维数。

因此：

> 100 次观测，每次记录 3 个特征，是 100 个三维观测，不是一个“100 维随机变量”。

---

## 十五、实际应用：为什么算法中总要保留“同一条样本”？

### 机器学习、深度学习与数据分析

假设我们从一张随机抽取的图像中记录：

$$
X_1=\text{平均亮度},\qquad
X_2=\text{目标面积},\qquad
Y=\text{模型是否预测错误}.
$$

那么：

$$
(X_1,X_2,Y)
$$

就是对同一张图像的共同数值描述。

如果把亮度、面积、是否出错这三列分别打乱，每一列自己的数值分布不会改变，但：

> 哪一种亮度和面积对应哪一种错误情况，已经被破坏了。

这正是“分别知道各自分布”与“知道共同分布”的区别。

### 计算机视觉

假设目标中心的位置用：

$$
(U,V)
$$

表示，其中 $U$ 是横坐标，$V$ 是纵坐标。

只知道横坐标经常出现在哪里，以及纵坐标经常出现在哪里，还不足以知道目标中心在平面上怎样分布。

比如，点可能集中在一条对角线上，也可能分散在整个区域里。

这两种空间结构，需要通过坐标的共同取值来区分。

### 强化学习与工程记录

在强化学习中，一次环境交互可能同时产生“下一状态”和“奖励”；在工程系统中，一次任务可能同时产生“耗时”和“能耗”。

这些量都应该按同一次交互、同一次任务配对记录。

否则，我们只能知道它们各自怎样波动，却无法研究：

> 哪一种状态变化对应哪一种奖励，或者哪一种耗时对应哪一种能耗。

---

## 十六、理解检查：把今天的思想迁移到骰子

掷一次公平六面骰子。

定义：

$$
D=\text{骰子点数},
$$

$$
I=
\begin{cases}
1,&\text{点数是偶数},\\
0,&\text{点数是奇数}.
\end{cases}
$$

先想一想：

> $(D,I)$ 有几个分量、几个可能取值？$(3,1)$ 能不能出现？两个分量是否独立？

先从原始样本点逐个映射：

```text
1 → (1,0)
2 → (2,1)
3 → (3,0)
4 → (4,1)
5 → (5,0)
6 → (6,1)
```

因此，它是一个**二维随机向量**，共有六个可能取值，每个取值的概率都是 $\frac16$。

$(3,1)$ 不可能出现，因为点数为 3 时，“是否偶数”的编码必须是 0。

两个分量也不独立：一旦知道骰子点数，奇偶性就已经确定。

这一个例子同时检查了：

```text
维数 ≠ 可能结果个数
单独可能 ≠ 共同可能
多个分量 ≠ 相互独立
```

---

## 这一节真正需要记住的核心

**多维随机变量是在同一个概率模型中，把多个随机变量组成一个有序整体；一次试验得到的是一组相互对应的数值。**

**维数表示分量个数，不表示可能结果个数，也不表示独立随机来源的个数。**

**联合分布描述这些数值组合怎样分配概率；$P(X=x,Y=y)$ 表示两个条件同时成立的概率，不能未经独立性判断就直接相乘。**

**各自的分布，以及期望、方差、协方差、相关系数这些摘要，一般都不足以确定完整的共同概率规律。**

下一课自然进入**联合分布的系统学习**：我们已经知道研究对象是平面上的随机取值，接下来就要学习怎样完整表达这张概率地图，以及怎样计算其中一个区域的概率。
