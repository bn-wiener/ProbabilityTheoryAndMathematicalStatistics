# 第六课：条件概率——得到新信息后，概率为什么会改变？

今天只研究一个核心问题：

> **当我们已经知道某件事情发生了以后，应该怎样重新判断另一件事情发生的可能性？**

我们现在位于：

```text
随机现象
   ↓
样本空间
   ↓
事件
   ↓
概率
   ↓
条件概率  ← 今天
   ↓
独立性
   ↓
随机变量
```

---

# 1. 为什么普通概率还不够？

先考虑一个非常简单的问题。

掷一次公平骰子：

$$
\Omega=\{1,2,3,4,5,6\}
$$

定义事件：

$$
A=\{4,5,6\}
$$

表示：

> 点数大于 3。

因为一共有 6 个等可能结果，其中 3 个大于 3，所以：

$$
P(A)=\frac{3}{6}=\frac12
$$

到这里完全是上一课的内容。

但现在我突然告诉你一个新信息：

> **这次掷出的点数是偶数。**

定义：

$$
B=\{2,4,6\}
$$

现在再问：

> 已经知道它是偶数，那么它大于 3 的可能性是多少？

还能继续回答：

$$
\frac12
$$

吗？

不能直接这么做。

因为新信息已经排除了：

$$
1,3,5
$$

现在你真正需要考虑的可能结果只剩：

$$
\{2,4,6\}
$$

其中大于 3 的是：

$$
\{4,6\}
$$

所以新的可能性变成：

$$
\frac23
$$

注意发生了什么：

```text
原来不知道任何额外信息：

Ω = {1,2,3,4,5,6}

“大于3”
→ {4,5,6}
→ 概率 3/6 = 1/2


后来知道“是偶数”：

有效范围变成
{2,4,6}

其中“大于3”
→ {4,6}
→ 新概率 2/3
```

这就是条件概率产生的根本原因：

> **信息改变了我们还需要考虑的可能世界。**

---

# 2. 条件概率不是创造了新的随机性

这里要先建立一个很重要的直觉。

骰子其实没有因为你“知道它是偶数”而重新掷一次。

真实结果没有改变。

改变的是：

> **你掌握的信息。**

例如实际结果可能早就是：

$$
6
$$

只是最开始你不知道。

最开始：

```text
你认为：
1 2 3 4 5 6
都有可能
```

知道“偶数”以后：

```text
你排除：
1 3 5

只保留：
2 4 6
```

所以条件概率描述的是：

> 在已经掌握某个条件的前提下，对其他事件重新进行概率判断。

---

# 3. 条件概率的核心画面：缩小样本空间

这是本节最重要的直觉。

原样本空间：

```text
Ω
┌──────────────────────────────┐
│                              │
│  1   2   3   4   5   6       │
│                              │
└──────────────────────────────┘
```

我们知道事件：

$$
B=\{2,4,6\}
$$

已经发生。

于是从我们的信息角度看：

```text
原来的 Ω
┌──────────────────────────────┐
│  1   [2]  3  [4]  5  [6]    │
└──────────────────────────────┘
       ↑       ↑       ↑
       现在只剩这些需要考虑
```

所以可以暂时把 \(B\) 想成新的“小世界”：

```text
条件 B 已发生后

新的有效范围
┌──────────────────┐
│  2     4     6    │
└──────────────────┘
```

现在我们再问：

> 在这个小世界里面，有多少部分同时属于 \(A\)？

这就是条件概率。

---

# 4. 条件概率的数学记号

“在 \(B\) 已经发生的条件下，\(A\) 发生的概率”记作：

$$
P(A\mid B)
$$

读作：

> 在 \(B\) 条件下 \(A\) 的概率。

或者：

> 已知 \(B\) 发生时，\(A\) 的条件概率。

这里一定要逐个理解符号：

* \(P\)：概率规则；
* \(A\)：我们真正想研究的事件；
* \(B\)：已经知道发生的条件；
* $(\mid)$：读作“在……条件下”；
* $(P(A\mid B)$：已经知道 \(B\) 发生后，对 \(A\) 的重新概率判断。

注意：

$$
P(A\mid B)
$$

绝对不是：

$$
\frac{P(A)}{P(B)}
$$

也不是“\(A\) 除以 \(B\)”。

竖线 \(\mid\) 是条件符号。

---

# 5. 条件概率公式是怎么“长出来”的？

现在不要直接背公式。

我们从刚才的直觉一步步推。

假设：

> 已知 \(B\) 已经发生。

那么原样本空间 \(\Omega\) 中，不属于 \(B\) 的部分已经被排除了。

所以我们现在只在：

$$
B
$$

里面讨论概率。

---

## 第一步：新的“总范围”是谁？

原来全部可能性是：

$$
\Omega
$$

现在知道 \(B\) 发生以后，新的有效范围是：

$$
B
$$

因此，在这个新的世界里，我们要把 \(B\) 当作“全部”。

原来：

$$
P(\Omega)=1
$$

现在条件在 \(B\) 下，也应该把 \(B\) 重新归一化成：

$$
1
$$

这叫做**重新归一化**。

---

## 第二步：我们真正关心 \(B\) 里面哪一部分？

我们想知道 \(A\) 是否发生。

但既然已经知道结果在 \(B\) 中，那么真正有用的是：

> 既属于 \(A\)，又属于 \(B\) 的那些结果。

这正是：

$$
A\cap B
$$

所以：

```text
新的总区域：
B

其中满足 A 的区域：
A ∩ B
```

于是直觉上：

$$
P(A\mid B)
=
\frac{\text{\(A\) 与 \(B\) 共同区域的概率重量}}
{\text{\(B\) 区域的概率重量}}
$$

也就是：

$$
\boxed{
P(A\mid B)
=
\frac{P(A\cap B)}{P(B)}
}
$$

前提是：

$$
P(B)>0
$$

---

# 6. 为什么一定要除以 \(P(B)\)？

这是整个公式最值得理解的地方。

很多人会疑惑：

> 为什么不是直接用 \(P(A\cap B)\)？

因为：

$$
P(A\cap B)
$$

是在**原来的整个样本空间 \(\Omega\)** 上测量出来的概率。

但现在我们的世界已经缩小到了 \(B\)。

我们需要重新把：

$$
B
$$

的概率总量从原来的 \(P(B)\)，拉伸成新的 1。

因此要除以：

$$
P(B)
$$

你可以把它理解成“放大”。

例如：

$$
P(B)=0.5
$$

而：

$$
P(A\cap B)=\frac13
$$

在原世界中，共同区域占：

$$
\frac13
$$

但现在我们只看原来那一半大小的 \(B\)。

因此共同区域在这个新世界里占：

$$
\frac{\frac13}{\frac12}
=
\frac23
$$

---

# 7. 用骰子完整推一次

仍然是公平六面骰子。

$$
\Omega=\{1,2,3,4,5,6\}
$$

定义：

$$
A=\{4,5,6\}
$$

表示“大于3”。

定义：

$$
B=\{2,4,6\}
$$

表示“偶数”。

---

## 普通概率

$$
P(A)=\frac36=\frac12
$$

---

## 现在知道 \(B\) 已发生

也就是说：

> 已知点数是偶数。

于是：

$$
A\cap B
=
\{4,6\}
$$

因此：

$$
P(A\cap B)=\frac26=\frac13
$$

而：

$$
P(B)=\frac36=\frac12
$$

代入条件概率公式：

$$
P(A\mid B)
=
\frac{P(A\cap B)}{P(B)}
$$

得到：

$$
P(A\mid B)
=
\frac{\frac13}{\frac12}
$$

除以一个分数等于乘以它的倒数：

$$
=
\frac13\times2
$$

所以：

$$
\boxed{
P(A\mid B)=\frac23
}
$$

这和我们直接从：

$$
\{2,4,6\}
$$

中数出：

$$
\{4,6\}
$$

完全一致。

---

# 8. 用“面积”理解条件概率

把整个样本空间想象成面积为 1 的区域。

```text
Ω
┌────────────────────────────────┐
│                                │
│           B                    │
│      ┌────────────────┐        │
│      │                │        │
│      │   ┌──────┐     │        │
│      │   │ A∩B  │     │        │
│      │   └──────┘     │        │
│      │                │        │
│      └────────────────┘        │
│                                │
└────────────────────────────────┘
```

如果不知道 \(B\)，我们用整个大框作为分母。

但知道 \(B\) 发生后：

```text
只看这里：

      ┌────────────────┐
      │       B        │
      │                │
      │   ┌──────┐     │
      │   │ A∩B  │     │
      │   └──────┘     │
      │                │
      └────────────────┘
```

于是：

$$
P(A\mid B)
=
\frac{A\cap B\text{ 的概率面积}}
{B\text{ 的概率面积}}
$$

所以条件概率本质上就是：

> **在 \(B\) 这块区域内部，\(A\) 占多大比例。**

---

# 9. 一个非常关键的比较

现在比较：

$$
P(A)
$$

和：

$$
P(A\mid B)
$$

它们描述的是完全不同的信息状态。

### \(P(A)\)

表示：

> 在当前没有额外加入 \(B\) 这个信息时，\(A\) 有多可能。

### \(P(A\mid B)\)

表示：

> 已知 \(B\) 已经发生以后，\(A\) 有多可能。

所以：

```text
P(A)
= 原信息状态下对 A 的判断

P(A|B)
= 加入 B 这个信息之后，对 A 的新判断
```

条件概率最核心的新思想就是：

> **概率判断必须依赖我们已经掌握的信息。**

---

# 10. \(P(A\mid B)\) 和 \(P(B\mid A)\) 完全不是一回事

这是以后学习贝叶斯公式时最重要的伏笔。

还是骰子的例子。

定义：

$$
A=\{4,5,6\}
$$

大于3。

$$
B=\{2,4,6\}
$$

偶数。

我们刚才得到：

$$
P(A\mid B)=\frac23
$$

意思是：

> 已知偶数，问大于3的概率。

现在反过来：

$$
P(B\mid A)
$$

是什么意思？

> 已知大于3，问偶数的概率。

已知 \(A\) 后，可能结果只剩：

$$
\{4,5,6\}
$$

其中偶数有：

$$
\{4,6\}
$$

所以：

$$
P(B\mid A)=\frac23
$$

这一次碰巧相等。

但这只是这个例子碰巧如此。

一般来说：

$$
\boxed{
P(A\mid B)\neq P(B\mid A)
}
$$

---

# 11. 用一个更明显的例子看“方向”

假设某公司有：

```text
100 名员工
```

其中：

```text
20 人是算法工程师
80 人不是
```

再假设：

```text
10 人会 Python
其中 8 人是算法工程师
```

定义：

* \(A\)：是算法工程师；
* \(B\)：会 Python。

那么：

### 已知会 Python，问是不是算法工程师

会 Python 的一共 10 人。

其中算法工程师 8 人。

所以：

$$
P(A\mid B)=\frac8{10}=0.8
$$

---

### 已知是算法工程师，问会不会 Python

算法工程师一共 20 人。

其中会 Python 的有 8 人。

所以：

$$
P(B\mid A)=\frac8{20}=0.4
$$

因此：

$$
0.8\neq0.4
$$

为什么？

因为虽然两者的共同部分都是那 8 个人，但**分母不同**。

```text
P(A|B)

分母：
所有 B
= 所有会 Python 的人


P(B|A)

分母：
所有 A
= 所有算法工程师
```

这句话以后一定要牢牢记住：

> **条件放在竖线右边，它决定了“我们现在在哪个世界里计算”。**

---

# 12. 条件概率公式的两个视角

条件概率公式：

$$
P(A\mid B)
=
\frac{P(A\cap B)}{P(B)}
$$

可以有两种直觉。

## 视角一：缩小样本空间

知道 \(B\) 发生：

```text
Ω
↓
缩小到 B
↓
看其中多少属于 A
```

这是最适合初学的直觉。

---

## 视角二：重新归一化

原来 \(B\) 的总概率是：

$$
P(B)
$$

但进入条件世界后，我们规定：

$$
P(B\mid B)=1
$$

因此所有位于 \(B\) 中的概率重量，都需要统一除以：

$$
P(B)
$$

从而重新让总重量变成 1。

这就是“归一化”。

这个思想以后会反复出现：

* 贝叶斯公式；
* 条件分布；
* 概率密度归一化；
* softmax；
* posterior distribution。

---

# 13. 为什么要求 \(P(B)>0\)？

公式中：

$$
P(A\mid B)
=
\frac{P(A\cap B)}{P(B)}
$$

分母是：

$$
P(B)
$$

所以如果：

$$
P(B)=0
$$

在我们目前的初等定义里就不能直接这样除。

因为除以 0 没有定义。

更直觉地说：

> 如果条件 \(B\) 在当前模型下根本没有概率重量，那么“已知 \(B\) 发生”的普通条件概率操作就失去了这个简单公式的基础。

这里暂时只记住：

$$
\boxed{P(B)>0}
$$

以后学连续型随机变量时，会遇到一个更有意思的问题：

> 单点概率可以为 0，但我们仍然经常说“已知 \(X=x\)”。

那需要更高级的条件分布理论，现在先不展开。

---

# 14. 条件概率并不一定会让概率变大

容易产生一个错误直觉：

> “增加信息以后，概率是不是应该变大？”

不是。

新信息可能让 \(A\)：

* 更可能；
* 更不可能；
* 完全不变。

---

## 情况一：变大

刚才骰子：

$$
P(A)=\frac12
$$

但：

$$
P(A\mid B)=\frac23
$$

知道是偶数以后，“大于3”变得更可能。

---

## 情况二：变小

还是：

$$
A=\{\text{大于3}\}
$$

如果告诉你：

$$
C=\{1,2,3,4\}
$$

即“点数不超过4”。

那么在：

$$
\{1,2,3,4\}
$$

中，只有 4 大于3。

所以：

$$
P(A\mid C)=\frac14
$$

比原来的：

$$
\frac12
$$

更小。

---

## 情况三：不变

如果某个条件 \(B\) 的出现完全没有改变 \(A\) 的概率：

$$
P(A\mid B)=P(A)
$$

这正是我们下一课“独立性”的入口。

所以独立性不是凭空出现的概念。

它实际上在问：

> **知道 \(B\) 以后，关于 \(A\) 的概率判断有没有改变？**

如果没改变，就意味着 \(B\) 没有给 \(A\) 提供有效概率信息。

---

# 15. 条件概率和事件交集是什么关系？

从定义：

$$
P(A\mid B)
=
\frac{P(A\cap B)}{P(B)}
$$

两边同时乘：

$$
P(B)
$$

得到：

$$
\boxed{
P(A\cap B)
=
P(B)P(A\mid B)
}
$$

这就是概率的**乘法公式**。

先不要单独背。

它的直觉非常自然：

> “\(A\) 和 \(B\) 同时发生”

可以拆成：

```text
先进入 B
概率 P(B)

然后在 B 已经发生的情况下
A 也发生
概率 P(A|B)
```

所以：

$$
P(A\cap B)
=
P(B)\times P(A\mid B)
$$

同样，也可以先考虑 \(A\)：

$$
P(A\cap B)
=
P(A)P(B\mid A)
$$

因此：

$$
\boxed{
P(A)P(B\mid A)
=
P(B)P(A\mid B)
}
$$

先把它放在这里。

下一阶段继续变形，就会长出著名的**贝叶斯公式**。

但这一课暂时不深入贝叶斯公式。

---

# 16. 树状图为什么特别适合条件概率？

考虑这样一个简单模型。

一家公司：

```text
男性：60%
女性：40%
```

假设：

```text
男性中有 30% 会购买
女性中有 50% 会购买
```

我们可以画成：

```text
                    ┌── 购买     0.30
          ┌── 男性 ─┤
          │  0.60   └── 不购买   0.70
开始 ─────┤
          │         ┌── 购买     0.50
          └── 女性 ─┤
             0.40   └── 不购买   0.50
```

这里：

$$
0.30
$$

不是：

> 男性且购买的总体概率。

而是：

> **已知男性以后，购买的条件概率。**

即：

$$
P(\text{购买}\mid\text{男性})=0.30
$$

如果要求：

> 又是男性，又购买。

则要沿着路径相乘：

$$
0.60\times0.30=0.18
$$

因为：

$$
P(\text{男性}\cap\text{购买})
=
P(\text{男性})
P(\text{购买}\mid\text{男性})
$$

这就是乘法公式的直观来源。

---

# 17. 条件概率 vs 普通概率

这是本节最重要的一组概念区别。

| 概念             | 问的问题             |
| -------------- | ---------------- |
| \(P(A)\)       | 在当前总体信息下，A 有多可能？ |
| \(P(A\mid B)\) | 已知 B 发生后，A 有多可能？ |
| \(P(A\cap B)\) | A 和 B 同时发生有多可能？  |

特别容易混淆的是后两个。

### \(P(A\mid B)\)

条件世界里面的概率。

### \(P(A\cap B)\)

在原样本空间中，“A 和 B 同时发生”这件事的概率。

二者关系：

$$
P(A\mid B)
=
\frac{P(A\cap B)}{P(B)}
$$

所以一般：

$$
P(A\mid B)\neq P(A\cap B)
$$

---

# 18. 一个反例：条件概率可能比原概率大很多

假设一个学校：

```text
1000 人
```

其中：

```text
10 人是校篮球队成员
```

所以随机抽一个人：

$$
P(\text{篮球队})=\frac{10}{1000}=0.01
$$

只有 1%。

现在告诉你：

> 已知这个人的身高超过 2 米。

假设全校只有 12 个人超过 2 米，其中 8 个是篮球队员。

那么：

$$
P(\text{篮球队}\mid\text{身高}>2m)
=
\frac8{12}
=
\frac23
$$

从：

$$
1\%
$$

一下变成：

$$
66.7\%
$$

为什么？

不是篮球队突然变多了，而是：

> “身高超过2米”这个新信息大幅缩小了候选范围，而且新的候选人中篮球队员占比很高。

这就是信息的力量。

---

# 19. 现实世界中条件概率到底是什么？

实际上，我们每天都在使用条件概率，只是没有写公式。

比如：

> “这个人平时很守时，他今天迟到的可能性不高。”

这里其实是在使用：

$$
P(\text{迟到}\mid\text{已知其历史表现})
$$

再例如：

> “已经检测到车辆前方有行人，发生碰撞风险是多少？”

这是：

$$
P(\text{碰撞}\mid\text{检测到行人})
$$

再比如：

> “用户已经把商品加入购物车，他最终购买的概率是多少？”

这是：

$$
P(\text{购买}\mid\text{已加入购物车})
$$

所以条件概率本质上就是：

> **基于已有信息进行更新后的概率判断。**

---

# 20. 在机器学习里它尤其重要

条件概率几乎贯穿整个机器学习。

例如图像分类。

设：

* \(X\)：观察到的图像；
* \(Y\)：类别。

真正希望模型学到的，常常不是单纯：

$$
P(Y)
$$

即“某个类别总体有多常见”。

而是：

$$
P(Y\mid X)
$$

即：

> **已经看到这张图像 \(X\) 后，它属于类别 \(Y\) 的概率。**

例如：

```text
没有看图之前：

P(猫) = 0.20


看到尖耳朵、胡须、猫脸特征后：

P(猫 | 这张图像)
可能变成 0.97
```

输入数据 \(X\) 本质上就是“新信息”。

机器学习分类很大程度上就是：

> 利用输入信息不断缩小和重新分配不同标签的可能性。

---

# 21. 再连接回前五课

现在把整条主线重新拼一次。

前面已经知道：事件是样本空间中的一组结果。

普通概率是给事件分配可能性重量。

现在的新思想只有一个：

```text
原来的概率：

Ω
↓
研究 A
↓
P(A)


得到新信息 B：

Ω
↓
知道结果已经落入 B
↓
有效世界缩小为 B
↓
重新研究 A∩B 在 B 中占多少
↓
P(A|B)
```

因此：

$$
\boxed{
\text{条件概率}
=
\text{旧概率知识}
+
\text{“信息改变有效样本空间”这个新思想}
}
$$

---

# 22. 现在请特别区分四个对象

假设：

$$
A=\text{患病}
$$

$$
B=\text{检测阳性}
$$

那么：

$$
P(A)
$$

是：

> 一个人本身患病的概率。

而：

$$
P(B)
$$

是：

> 一个人检测阳性的概率。

而：

$$
P(A\cap B)
$$

是：

> 一个人既患病，同时检测又是阳性的概率。

而：

$$
P(A\mid B)
$$

则是：

> **已经知道检测阳性以后，这个人真正患病的概率。**

这四个量不是一回事。

事实上，这个区别就是以后医学检测、贝叶斯公式中最容易犯错的地方。

---

# 理解检查

先不要看公式机械计算，凭直觉判断。

### 问题 1

公平骰子。

已知：

> 点数小于 4。

现在问：

> 点数是奇数的概率是多少？

有效结果只剩：

$$
\{1,2,3\}
$$

其中奇数有几个？

---

### 问题 2

如果：

$$
P(A)=0.2
$$

而：

$$
P(A\mid B)=0.8
$$

这说明知道 \(B\) 后，关于 \(A\) 的判断发生了什么变化？

---

### 问题 3

下面两个问题是否相同？

> 已知一个人患病，他检测阳性的概率是多少？

和：

> 已知一个人检测阳性，他患病的概率是多少？

也就是：

$$
P(\text{阳性}\mid\text{患病})
$$

与：

$$
P(\text{患病}\mid\text{阳性})
$$

是否一定相等？

---

### 问题 4

为什么条件概率公式中分母是：

$$
P(B)
$$

而不是：

$$
P(A)
$$

可以只用一句话回答：

> 因为……

---

# 这一节真正需要记住的核心

只需要先抓住下面 5 句话：

1. **条件概率研究的是：获得新信息以后，概率应该怎样重新计算。**
2. 已知 \(B\) 发生，可以直观理解为把有效样本空间从 \(\Omega\) 缩小到 \(B\)。
3. 在 \(B\) 中真正属于 \(A\) 的部分是 \(A\cap B\)，因此

   $$
   \boxed{P(A\mid B)=\frac{P(A\cap B)}{P(B)}}
   $$
4. **竖线右边决定条件世界**，所以 \(P(A\mid B)\) 与 \(P(B\mid A)\) 一般完全不同。
5. 条件信息可能让一个事件的概率变大、变小，也可能完全不变。

而最后一种情况：

$$
P(A\mid B)=P(A)
$$

非常特殊。

它意味着：

> **知道 \(B\) 以后，关于 \(A\) 的概率判断完全没有变化。**

这正是下一课为什么会自然进入：

# **独立性**

因为“独立”本质上就是在问：

> **一个事件是否给另一个事件提供了概率信息？**
