# 第十八课：指数分布——等待时间为什么会呈现“指数规律”？

假设你正在等待服务器收到下一条请求。

你已经等了 10 分钟，但请求还没有来。

现在考虑一个问题：

> **因为已经等了很久，接下来一分钟内收到请求的可能性，会不会变大？**

这不能只凭“等待”两个字判断。

如果你在等一个定时任务，距离预定时间越近，任务确实越接近触发。但如果请求按照一种**不受已经等待多久影响的随机机制**到来，那么过去的等待不会让下一分钟自动变得更特殊。

**指数分布描述的，正是后一种机制下的等待时间。**

本课最重要的，不是记住一条密度公式，而是理解这条逻辑：

```text
已经等待多久，不改变接下来的发生机制
                    ↓
每过相同长度的时间，“仍未发生”的概率按相同比例缩小
                    ↓
等待时间形成指数分布
```

---

## 一、先明确：我们到底在研究哪个随机变量？

### 1. 同一个随机现象，可以提取不同的数字

假设从观察开始，服务器收到请求的时刻是：

```text
单位：分钟

0 ─────●──────────●───●────────────●────→ 时间
      2.4        7.1  8.6         15.0
```

这是某一次观测得到的结果。下一次观测，请求到来的时刻可能完全不同。

我们可以从这条时间记录中提取两个数字。

**第一种：统计前 10 分钟来了多少条请求。**

这次结果是：

$$
N(10)=3.
$$

其中，$N(10)$ 是“前 10 分钟请求总数”这个随机变量。

**第二种：记录第一条请求到来之前，等待了多久。**

这次结果是：

$$
T=2.4\text{ 分钟}.
$$

其中，$T$ 是“从观察起点到第一条请求的等待时间”这个随机变量。

它们的区别是：

| 观察方式      | 随机变量记录什么     | 数值类型             |
| --------- | ------------ | ---------------- |
| 固定时间，数请求  | 这段时间里发生了几次   | $0,1,2,\ldots$ |
| 从起点等第一条请求 | 第一次发生之前经过了多久 | 非负的连续时间          |

上一课的泊松分布研究第一种；今天的指数分布研究第二种。

### 2. 和几何分布的联系

第十五课的几何分布研究：

> 一次一次地试，第一次成功出现在第几次？

今天研究：

> 时间连续流逝，第一次事件出现在什么时候？

所以我们现在的位置是：

```text
几何分布：离散的“等几次”
              │
              │ 从试验次数转向连续时间
              ↓
指数分布：连续的“等多久”
```

它们共享一个思想：**第一次成功之前，前面必须一直没有成功。**几何分布的尾概率，正是通过“前面所有试验都失败”得到的。

---

## 二、什么样的等待机制，才会形成指数分布？

先不急着写分布公式。我们必须先把模型说清楚。

假设请求到达满足这样的理想条件：

**相同长度的时间段，具有相同的到达规律；不重叠时间段内的请求数相互独立；时间段足够短时，发生两次或更多次到达的概率，相比发生一次要小得多。**

这就是上一课恒定速率泊松到达模型背后的结构。

在这个模型中，请求不会因为“已经很久没有来了”就自动加速到来，也不会因为刚刚来过一条，就必须休息一段时间。

### 1. 用 $\lambda$ 描述“来得多快”

本课用：

$$
\lambda>0
$$

表示**单位时间的到达率**。

例如，假设：

$$
\lambda=0.1/\text{分钟}.
$$

它描述事件发生的快慢。对于很短的时间段 $h$，有：

$$
P(\text{这段时间内至少来一条请求})
\approx \lambda h.
$$

这里，$h$ 是时间段的长度。

例如 $h=0.01$ 分钟时：

$$
\lambda h=0.1\times0.01=0.001.
$$

因此，这么短的一段时间里，发生到达的概率大约为 $0.001$。

**注意这是小时间段近似，不是任意时长都成立的等式。**不能把它直接用于很长的时间，算出一个超过 1 的“概率”。

### 2. 区分“发生率”和“整个区间的泊松参数”

若单位时间发生率为 $\lambda$，观察时间长度为 $t$，那么这一整段时间内的请求数满足：

$$
N(t)\sim\operatorname{Poisson}(\lambda t).
$$

这里：

$$
\underbrace{\lambda}_{\text{每单位时间的发生率}}
\quad\neq\quad
\underbrace{\lambda t}_{\text{整个观察区间的泊松参数}}.
$$

例如：

$$
\lambda=0.1/\text{分钟},\qquad t=20\text{ 分钟},
$$

则：

$$
\lambda t=0.1\times20=2.
$$

所以前 20 分钟的计数模型是：

$$
N(20)\sim\operatorname{Poisson}(2).
$$

**时间单位必须一致。**发生率用“每分钟”，时间就用“分钟”；两者相乘后，$\lambda t$ 没有单位。

---

## 三、先不求“什么时候来”，而求“到现在还没来”

这是推导指数分布最关键的一步。

我们定义：

$$
T=\text{从观察起点到第一条请求的等待时间}.
$$

现在想知道：

$$
P(T>t).
$$

其中，$t$ 是人为选定的一个时间阈值。

这句话的意思是：

> 等待超过 $t$ 时间，第一条请求仍然没有来。

### 1. 把等待问题，翻译成计数问题

请看这条时间轴：

```text
0 ───────────────── t ─────────●────→ 时间
       没有任何请求              第一条请求
```

“第一条请求在 $t$ 之后才来”，意味着什么？

意味着：

> 前 $t$ 时间内，请求数为 0。

所以：

$$
\boxed{\{T>t\}=\{N(t)=0\}}.
$$

这不是近似，也不是一个需要背的新定理。

它只是**同一个事件的两种说法**：

```text
等待语言：第一条请求还没来
                  ↕
计数语言：目前一共来了0条
```

因此：

$$
P(T>t)=P(N(t)=0).
$$

### 2. 使用上一课的泊松公式

因为：

$$
N(t)\sim\operatorname{Poisson}(\lambda t),
$$

对于 $t>0$，前 $t$ 时间没有请求的概率为：

$$
P(N(t)=0)
=
e^{-\lambda t}\frac{(\lambda t)^0}{0!}.
$$

这里，正数的零次方为 1，而且 $0!=1$，所以：

$$
P(N(t)=0)=e^{-\lambda t}.
$$

于是得到：

$$
\boxed{P(T>t)=e^{-\lambda t},\qquad t\geq0}.
$$

在 $t=0$ 时，这个式子给出 $e^0=1$：模型中第一条请求的等待时间以概率 1 大于零，与连续时间到达的设定一致。

这里的 $e\approx2.71828$ 是自然常数，上一课从二项分布的极限推导泊松分布时已经出现过。

### 3. 为什么它叫“指数”分布？

因为时间 $t$ 出现在了指数的位置：

$$
e^{-\lambda t}.
$$

但更重要的是它背后的变化方式：

> **每经过相同长度的时间，仍然没等到的概率，会再乘上同一个比例。**

例如，假设每过 10 分钟，没有请求到达的概率都是 $q$。

由于不同时间段的到达情况独立：

$$
\begin{aligned}
P(T>10)&=q,\\
P(T>20)&=q\times q=q^2,\\
P(T>30)&=q\times q\times q=q^3.
\end{aligned}
$$

这是**按比例衰减**，而不是每过 10 分钟就减去同一个数。

几何分布中的 $(1-p)^k$，和这里的 $e^{-\lambda t}$，都在表达这种“连续乘上未发生比例”的思想。

---

## 四、从“还没来”，得到整个等待时间分布

现在我们已经抓住了最重要的公式：

$$
P(T>t)=e^{-\lambda t}.
$$

接下来，CDF 和 PDF 都能从它长出来。

### 1. CDF：截至 $t$，已经来了的概率

CDF 的定义是：

$$
F_T(t)=P(T\leq t).
$$

“已经来了”和“还没来”互为补事件，所以：

$$
P(T\leq t)=1-P(T>t).
$$

因此：

$$
\boxed{
F_T(t)=
\begin{cases}
0,&t<0,\\[3pt]
1-e^{-\lambda t},&t\geq0.
\end{cases}
}
$$

这里，$t<0$ 时概率为零，是因为等待时间不可能为负数。

所以：

$$
e^{-\lambda t}
$$

是“**还没来**”的概率，而：

$$
1-e^{-\lambda t}
$$

是“**已经来过至少一次**”的概率。

不要把两者记反。

### 2. PDF：第一次到达，为什么偏偏落在 $t$ 附近？

这一部分不能只停留在“对 CDF 求导”。

我们先问：

> 第一条请求落在很短的区间 $(t,t+h]$ 内，需要满足什么？

需要两件事同时成立：

```text
0 ───────────────── t ───── t+h
       前面一直没来      这里第一次来
```

**第一件事：前 $t$ 时间一直没有请求。**

概率为：

$$
e^{-\lambda t}.
$$

**第二件事：在接下来的短时间 $h$ 内出现请求。**

由于未来这段时间的到达机制不受前面影响，其概率近似为：

$$
\lambda h.
$$

因此：

$$
P(t<T\leq t+h)
\approx
e^{-\lambda t}\lambda h.
$$

另一方面，第十二课已经建立了：

$$
\text{很短区间的概率}
\approx
\text{该位置的密度}\times\text{区间宽度}.
$$

因此：

$$
P(t<T\leq t+h)\approx f_T(t)h.
$$

这里 $f_T(t)$ 是等待时间在 $t$ 附近的概率密度。

把这两个表达式对照，并让 $h$ 趋于零，就得到：

$$
\boxed{f_T(t)=\lambda e^{-\lambda t}}.
$$

也可以通过 CDF 验证。对于 $t>0$：

$$
\begin{aligned}
f_T(t)
&=\frac{d}{dt}\left(1-e^{-\lambda t}\right)\\
&=0-\left(-\lambda e^{-\lambda t}\right)\\
&=\lambda e^{-\lambda t}.
\end{aligned}
$$

第二行的 $-\lambda$，来自指数 $-\lambda t$ 对时间的变化率。

**但真正应该记住的，是密度的两个组成部分：**

$$
\boxed{
f_T(t)
=
\underbrace{e^{-\lambda t}}_{\text{前面一直没有发生}}
\;\underbrace{\lambda}_{\text{接下来发生的速率}}
}.
$$

这和几何分布的结构非常接近：

$$
\underbrace{(1-p)^{k-1}}_{\text{前面全部失败}}
\;\underbrace{p}_{\text{这一次成功}}.
$$

区别在于：连续时间中的 $\lambda$ 是**速率**，还需要乘上一个时间宽度，才近似得到概率。

---

## 五、现在正式给出指数分布的定义

若随机变量 $T$ 的概率密度为：

$$
\boxed{
f_T(t)=
\begin{cases}
\lambda e^{-\lambda t},&t\geq0,\\[3pt]
0,&t<0,
\end{cases}
\qquad \lambda>0,
}
$$

则称 $T$ 服从参数为 $\lambda$ 的指数分布，记作：

$$
T\sim\operatorname{Exp}(\lambda).
$$

这里采用的是**发生率参数**的写法：$\lambda$ 越大，事件来得越快，等待通常越短。

它确实是一条合法的密度：函数非负，而且总面积为 1。具体地：

$$
\begin{aligned}
\int_0^b\lambda e^{-\lambda t}\,dt
&=\left[-e^{-\lambda t}\right]_0^b\\
&=-e^{-\lambda b}-(-1)\\
&=1-e^{-\lambda b}.
\end{aligned}
$$

当 $b$ 不断增大时，$e^{-\lambda b}$ 趋于零，所以总面积趋于 1。

### 三个表达式，分别回答三个问题

| 表达式，$t\geq0$             | 数学含义           | 问的是什么           |
| -------------------------- | -------------- | --------------- |
| $e^{-\lambda t}$         | $P(T>t)$     | 到现在还没来吗？        |
| $1-e^{-\lambda t}$       | $P(T\leq t)$ | 到现在已经来了吗？       |
| $\lambda e^{-\lambda t}$ | $f_T(t)$     | 第一次到达在这里附近有多密集？ |

**前两个是概率，第三个是密度。**

特别地：

$$
P(T=t)=0
$$

并不等于：

$$
f_T(t)=0.
$$

一个精确时间点没有宽度；只有取一个时间区间，密度才能通过面积形成概率。

---

## 六、只用一个例子，把三类概率算清楚

仍然假设：

$$
\lambda=0.1/\text{分钟},
\qquad
T\sim\operatorname{Exp}(0.1).
$$

### 问题一：10 分钟后还没来，概率是多少？

这是：

$$
P(T>10).
$$

代入尾概率：

$$
P(T>10)=e^{-0.1\times10}=e^{-1}\approx0.3679.
$$

因此，等待超过 10 分钟的概率约为 **36.79%**。

### 问题二：10 分钟以内已经来了，概率是多少？

它是上一事件的补事件：

$$
P(T\leq10)=1-e^{-1}\approx0.6321.
$$

因此，10 分钟以内等到第一条请求的概率约为 **63.21%**。

### 问题三：第一次请求在第 10 到第 20 分钟之间到来，概率是多少？

注意，这个问题是**站在最初的观察起点问的**。

我们要计算：

$$
P(10<T\leq20).
$$

“超过 10 分钟还没来”的情况，可以分成两类：

```text
10 到 20 分钟之间终于来了
或
超过 20 分钟仍然没来
```

所以：

$$
P(T>10)
=
P(10<T\leq20)+P(T>20).
$$

移项得到：

$$
\begin{aligned}
P(10<T\leq20)
&=P(T>10)-P(T>20)\\
&=e^{-1}-e^{-2}\\
&\approx0.2325.
\end{aligned}
$$

因此，这个概率约为 **23.25%**。

更一般地，对于 $0\leq a<b$：

$$
\boxed{
P(a<T\leq b)=e^{-\lambda a}-e^{-\lambda b}.
}
$$

它也是密度曲线在 $a$ 到 $b$ 之间的面积。

---

## 七、最重要的理解关口：已经等了 10 分钟之后，应该怎样重新判断？

现在换一个信息状态。

不是站在起点，而是你已经实际等了 10 分钟，并且知道：

> 请求确实还没有来。

此时你问：

> 接下来的 10 分钟内，收到第一条请求的概率是多少？

还能直接回答刚才的 **23.25%** 吗？

不能。

因为刚才计算的是：

$$
P(10<T\leq20),
$$

而现在计算的是：

$$
P(10<T\leq20\mid T>10).
$$

你已经知道 $T>10$，所以必须在“已经等过 10 分钟仍没来”的那些可能结果中，重新计算比例。

按照条件概率：

$$
P(10<T\leq20\mid T>10)
=
\frac{P(10<T\leq20)}{P(T>10)}.
$$

这里分子不需要再额外求交集，因为 $10<T\leq20$ 本来就意味着 $T>10$。

代入：

$$
\begin{aligned}
P(10<T\leq20\mid T>10)
&=\frac{e^{-1}-e^{-2}}{e^{-1}}\\
&=\frac{e^{-1}(1-e^{-1})}{e^{-1}}\\
&=1-e^{-1}\\
&\approx0.6321.
\end{aligned}
$$

结果又回到了 **63.21%**。

也就是说：

> **已经等了 10 分钟但没等到，从现在重新往后看 10 分钟，等到的概率与刚开始时完全相同。**

这就是指数分布的**无记忆性**。

### 把它推广到任意时长

设已经等了 $s$ 时间，事件仍未发生。

“还要再等超过 $u$ 时间”，对应总等待时间超过 $s+u$。所以：

$$
P(T>s+u\mid T>s).
$$

由于 $T>s+u$ 一定意味着 $T>s$，根据条件概率：

$$
\begin{aligned}
P(T>s+u\mid T>s)
&=\frac{P(T>s+u)}{P(T>s)}\\
&=\frac{e^{-\lambda(s+u)}}{e^{-\lambda s}}\\
&=\frac{e^{-\lambda s}e^{-\lambda u}}{e^{-\lambda s}}\\
&=e^{-\lambda u}\\
&=P(T>u).
\end{aligned}
$$

因此：

$$
\boxed{
P(T>s+u\mid T>s)=P(T>u),
\qquad s,u\geq0.
}
$$

这里“不变”的是**剩余等待时间的分布**，不是已经花掉的时间，也不是总等待时间。

---

## 八、为什么密度一直下降，未来的发生机会却没有下降？

这是本课最值得彻底想明白的地方。

指数密度：

$$
f_T(t)=\lambda e^{-\lambda t}
$$

随 $t$ 增大而下降。

你可能会问：

> 既然密度越来越低，怎么还能说“接下来发生的机会不变”？

因为它们的比较基准不同。

### 1. 密度站在最初的起点看

“第一条请求在第 20 分钟附近出现”，必须先满足：

> 前面将近 20 分钟一直没有请求。

“第一条请求在第 1 分钟附近出现”，只需要满足：

> 前面将近 1 分钟没有请求。

前面的空白时间越长，这个前提越难满足，所以**从起点看**，较晚才首次发生的密度会降低。

### 2. 无记忆性站在“已经等到现在”的位置看

现在你已经知道：

> 前面确实一直没有请求。

这个前提不再需要猜测。我们只在满足它的结果中，研究接下来会发生什么。

于是，过去那段空白时间被作为条件接受下来，未来仍然按同样的发生率运行。

用刚才的模型对照：

| 时间窗口     | 从起点看，第一次请求落在这个窗口的概率 | 已知窗口开始前一直没来，本窗口内到来的条件概率 |
| -------- | ------------------: | ----------------------: |
| 0～10 分钟  |              63.21% |                  63.21% |
| 10～20 分钟 |              23.25% |                  63.21% |
| 20～30 分钟 |               8.55% |                  63.21% |

左列概率不断下降，因为能一直等到更晚的情况越来越少。

右列不变，因为每次都是在“已经等到窗口起点还没来”的前提下，重新看未来 10 分钟。

所以：

$$
\boxed{
\text{较晚才首次发生更罕见}
\quad\text{与}\quad
\text{剩余等待分布不变}
\quad\text{并不矛盾}.
}
$$

再看一次密度的结构：

$$
f_T(t)
=
\underbrace{P(T>t)}_{\text{越来越小}}
\underbrace{\lambda}_{\text{保持不变}}.
$$

**下降的是“还没等到”的概率，不是给定已经等到这里之后的发生率。**

---

## 九、一个反例：随机等待，并不一定服从指数分布

假设某个任务的完成时间，在 0 到 10 分钟之间均匀随机分布。

这也是连续的、随机的等待时间。

但刚开始时，等待超过 2 分钟的概率为：

$$
\frac{10-2}{10}=0.8.
$$

现在已经等了 9 分钟，任务还没完成。

从现在起，还会再等超过 2 分钟吗？

不会。因为任务最迟在第 10 分钟完成，而“再等超过 2 分钟”意味着总时间超过第 11 分钟。

所以：

$$
P(T>11\mid T>9)=0,
$$

但：

$$
P(T>2)=0.8.
$$

两者不同，说明这个等待时间没有无记忆性。

这个反例告诉我们：

> **指数分布不是“所有正的随机等待时间”的统一模型。它要求等待历史不会改变剩余等待的规律。**

如果设备越用越容易坏、系统到某个截止时间一定触发、某次事件会诱发后续事件，或者到达率随时间明显变化，都不能不加检查地套用这一模型。

另外，无记忆性的结论是在**给定固定 $\lambda$** 的模型内成立的。实际不知道发生率时，历史观测仍然可以用于估计它；“无记忆”不等于“历史数据没有价值”。

---

## 十、把它放回实际建模中

假设你正在设计一个事件驱动系统。系统等待下一条输入，超过 $L$ 时间仍未等到就触发超时处理。

若经过分析，输入到达可以采用发生率为 $\lambda$ 的泊松模型，那么：

$$
P(\text{等待超时})=P(T>L)=e^{-\lambda L}.
$$

这时，指数分布把一个工程问题：

> “设置这个等待阈值，超时会有多常见？”

转换成了可以计算的概率问题。

同样，在机器学习的事件时间预测或强化学习的事件驱动仿真中，也可以把指数分布作为一种候选等待模型。但选择它的理由应该是**发生机制适合恒定速率假设**，而不是“输出是一个正数”。

从现实到数学的完整链条仍然是：

```text
请求随机到达
      ↓
一次结果：整条到达时间记录
      ↓
随机变量 T：提取到第一条请求的等待时间
      ↓
在恒定速率泊松到达模型下，T 服从指数分布
      ↓
利用分布计算“多久内到来”“超过多久”“落在哪段时间”
```

---

## 这一节真正需要记住的核心

**指数分布描述的是恒定发生率模型下，到第一次事件的连续等待时间，而不是任意等待时间。**

**所有公式的起点都是：等待超过 $t$，等价于前 $t$ 时间内一次事件都没有发生，因此 $P(T>t)=e^{-\lambda t}$。**

**由补事件得到 CDF $1-e^{-\lambda t}$；由“前面一直没发生 × 接下来发生的速率”理解 PDF $\lambda e^{-\lambda t}$。**

**无记忆性表示：已知一直等到现在仍没发生，剩余等待时间的分布不变；它不表示较晚才首次发生的无条件概率不变。**

理解了“一段这样的等待”，后面才自然进入“等到第 $r$ 次事件，需要把多少段等待加起来”的问题。

最后只检查一个理解点：**已经等了 10 分钟仍无请求，现在要判断“接下来 10 分钟内会不会来”，为什么不能直接使用 $P(10<T\leq20)$？你需要把哪些结果排除，再在哪些结果中重新计算比例？**


# 第十八课：指数分布——“等下一次事件”究竟在研究什么？

假设你打开一个服务器监控程序，从现在开始计时。

下一条请求什么时候到来？

可能等了 2 分钟就来了，也可能等了 15 分钟，还可能等了 40 分钟仍然没来。

我们今天研究的，就是这个随机的**等待时长**。

但真正需要弄清楚的不只是一个公式，而是：

> **什么样的随机机制，会让等待时间服从指数分布？为什么它的概率密度不断下降，却又说“已经等了多久，不影响接下来还要等多久”？**

下面始终使用同一个例子：**等待服务器的下一条请求**。

---

## 一、先弄清楚：我们究竟在研究哪个随机变量？

### 1. 同一个随机现象，可以提取不同的数字

假设某次观察中，请求分别在以下时刻到来：

```text
时间单位：分钟

开始计时
0────●────────●───────────●────────→ 时间
     4        12          25
```

这条时间轴包含很多信息：第一个请求什么时候来，第二个什么时候来，两个请求之间隔了多久……

但是，我们可以只提取其中一个数字。

**上一课的提问是：**

> 前 20 分钟一共来了多少条请求？

定义：

$$
N(20)=\text{前20分钟的请求数量}
$$

这次观察中：

$$
N(20)=2
$$

因为第 4 分钟和第 12 分钟各来了一条请求。

**本课的提问是：**

> 从开始计时，到第一条请求到来，等了多久？

定义：

$$
T=\text{从开始计时到下一条请求到来的等待时间}
$$

这次观察中：

$$
T=4
$$

注意，这两个数字来自**同一个现实结果**：

```text
同一串到达时刻：4，12，25，……

             ├─ 数前20分钟有几个请求 → N(20)=2
             │
             └─ 量到第一个请求的距离 → T=4
```

所以：

> **泊松模型研究的计数，与指数模型研究的等待时间，不是两个互不相干的现实世界，而是同一到达机制下的两种观察方式。** 在恒定速率的泊松到达模型中，这两种分布正是这样联系起来的。([概率课程][1])

### 2. 不要混淆这四个层次

| 层次                 | 本课中的例子                   | 它是什么      |
| ------------------ | ------------------------ | --------- |
| 现实随机现象             | 请求陆续到达                   | 现实过程      |
| 一个样本结果 $\omega$  | 到达时刻为 $4,12,25,\ldots$ | 一次完整的到达记录 |
| 随机变量 $T$         | 提取第一条请求的等待时长             | 映射规则      |
| 具体取值 $T(\omega)$ | $4$ 分钟                 | 本次得到的数字   |

再问：

$$
P(T>20)
$$

这时讨论的已经不是某次实际观察的数字，而是一个事件的概率：

> **哪些到达记录，会使我们等了 20 分钟还没有等到第一条请求？这些记录一共占多少概率？**

这条主线始终没有改变：

```text
随机到达过程
    ↓
可能的到达记录
    ↓
随机变量 T 提取等待时长
    ↓
研究这些等待时长的概率分布
```

---

## 二、为什么不能说“只要是等待时间，就服从指数分布”？

先比较两个假设系统。

### 系统 A：严格定时到达

每隔 20 分钟，系统一定收到一条请求。

如果我们刚收到一条请求就开始计时，那么下一条一定在 20 分钟之后到来：

```text
0────────────20────────────40────────────60
             ●             ●             ●
```

这里没有随机等待：

$$
T=20
$$

如果已经等了 19 分钟，我们知道再等 1 分钟就到了。

### 系统 B：随机到达

请求没有固定时间表。

它可能很快到来，也可能很久不来。更重要的是：

> **已经等了多久，并不会让系统自动进入“更该到达”或者“更不该到达”的状态。**

今天的指数模型对应的是这种机制，而不是系统 A。

两个系统都可以有“每小时平均 3 条请求”的数量水平，但等待规律完全不同。

因此：

$$
\boxed{
\text{知道平均到达数量}
\quad\text{不等于}\quad
\text{知道完整的等待时间分布}
}
$$

### 今天具体假设什么？

我们使用一个理想的、恒定速率的泊松到达模型。它的关键结构是：

1. **机制不随时间位置改变。** 同样长的观察区间，具有相同的计数分布。
2. **不重叠区间的计数相互独立。** 前一段发生了什么，不改变后一段的到达规律。
3. **事件不是成批瞬间到来的。** 在足够短的时间段里，一次到达的概率与时长近似成正比，两次及以上到达的概率相对于时长可以忽略。([概率课程][1])

先把这三个条件合成一个画面：

> 请求随机到来，没有固定班次，没有因等待而产生的“补偿”，也没有一个请求必然引发一批请求的连锁机制。

**后面的结论，都建立在这个模型上。**

---

## 三、先认识参数：$\lambda$ 是“发生率”，不是“发生概率”

假设这个模型的请求到达速率为：

$$
\lambda=3\text{ 条/小时}
$$

换成分钟：

$$
\lambda=\frac{3}{60}=0.05\text{ 条/分钟}
$$

**下面统一用分钟表示时间，所以使用 $\lambda=0.05$。**

### 1. 三个量分别负责什么？

| 符号            | 含义            | 例子                   |
| ------------- | ------------- | -------------------- |
| $\lambda$   | 单位时间的到达速率     | $0.05$ 条/分钟        |
| $t$         | 我们选定的观察时长     | $20$ 分钟            |
| $\lambda t$ | 整个观察区间对应的平均计数 | $0.05\times20=1$ 条 |

因此，前 $t$ 分钟的请求数量满足：

$$
N(t)\sim\operatorname{Poisson}(\lambda t)
$$

这里要校准上一课与本课的记号：

> 上一课可以用一个参数表示“整个区间的平均次数”；本课把单位时间的速率写成 $\lambda$，所以整个区间的泊松参数应写成 **$\lambda t$**。

### 2. 为什么 $\lambda t$ 不是概率？

因为它表示平均计数，而不是“有没有发生”的概率。

例如观察 60 分钟：

$$
\lambda t=0.05\times60=3
$$

平均计数可以是 3。

但概率不可能是 3。

所以，下面两句话不能混为一谈：

```text
这一小时平均来3条请求。

这一小时至少来一条请求的概率是多少？
```

前者是数量水平，后者才是一个事件的概率。

同样，$\lambda=0.05$ 也不能直接当成“未来任意一分钟至少到达一次的精确概率”。足够短的区间可以用“速率乘时长”近似概率，但一般区间需要完整的概率公式。

---

## 四、不要先求密度：先问“为什么还没等到？”

我们现在想知道 $T$ 的分布。

可以问很多问题：

$$
P(T\leq20),\qquad P(10<T\leq30),\qquad P(T>20)
$$

其中，**最容易从上一课出发计算的是最后一个：**

> 等待超过 20 分钟，意味着什么？

### 1. 把时间语言翻译成计数语言

如果：

$$
T>20
$$

那么前 20 分钟里，第一条请求还没出现。

也就是说：

$$
N(20)=0
$$

反过来也成立：如果前 20 分钟一条请求都没有，那么第一条请求的等待时间一定超过 20 分钟。

因此：

$$
\boxed{
\{T>20\}=\{N(20)=0\}
}
$$

推广到任意 $t\geq0$：

$$
\boxed{
\{T>t\}=\{N(t)=0\}
}
$$

这个等式不是两个公式之间的巧合，而是**同一个事件的两种说法**：

```text
时间语言：
第一条请求的等待时间超过 t。

计数语言：
前 t 时间里一条请求都没来。
```

于是：

$$
P(T>t)=P(N(t)=0)
$$

**这一句，是泊松计数与指数等待之间最重要的桥梁。**

### 2. 现在才使用泊松公式

对于 $t>0$，我们已假设：

$$
N(t)\sim\operatorname{Poisson}(\lambda t)
$$

所以：

$$
P(N(t)=k)
=
e^{-\lambda t}\frac{(\lambda t)^k}{k!}
$$

我们关心“一条都没来”，因此令 $k=0$：

$$
\begin{aligned}
P(N(t)=0)
&=e^{-\lambda t}\frac{(\lambda t)^0}{0!}\\
&=e^{-\lambda t}\frac{1}{1}\\
&=e^{-\lambda t}.
\end{aligned}
$$

这里用到了：正数的零次方等于 1，且 $0!=1$。

所以：

$$
\boxed{
P(T>t)=e^{-\lambda t}
}
$$

在 $t=0$ 时，尚未经历任何等待，模型下 $P(T>0)=1$，也与 $e^0=1$ 一致。

这个函数描述的是：

> **到时间 $t$ 为止，仍然没等到第一条请求的概率。**

它称为**生存函数**，也可以直接叫**尾概率函数**：

$$
\boxed{
S_T(t)=P(T>t)=e^{-\lambda t},\qquad t\geq0
}
$$

这里“生存”只是传统名称，不一定与生命或故障有关。在本例中，它表示“等待还没结束”。([NIST][2])

---

## 五、为什么是“指数下降”，而不是每分钟减去同样的概率？

这是理解指数分布的第一处关键。

假设我们每隔 10 分钟看一次。

设：

$$
q=P(\text{一个10分钟区间内没有请求})
$$

在我们的模型中，每个 10 分钟区间的这个概率都相同。

### 等待超过 10 分钟

需要第一个区间没有请求：

$$
P(T>10)=q
$$

### 等待超过 20 分钟

需要：

```text
前10分钟没有请求
并且
后10分钟也没有请求
```

由于两个区间的计数独立：

$$
P(T>20)=q\times q=q^2
$$

### 等待超过 30 分钟

需要三个区间全部没有请求：

$$
P(T>30)=q^3
$$

因此：

```text
时间：         0       10       20       30
仍未等到概率：  1   →    q   →    q²  →    q³
                   ×q       ×q       ×q
```

请注意：

> **每经过同样长的一段时间，剩下的概率乘以同一个比例，而不是减去同一个数。**

这就是指数变化的核心结构。

在本例中：

$$
q=e^{-0.05\times10}=e^{-0.5}\approx0.6065
$$

所以：

$$
P(T>20)=q^2=e^{-1}\approx0.3679
$$

$$
P(T>30)=q^3=e^{-1.5}\approx0.2231
$$

每个 10 分钟，都保留原来尚未等到概率的约 $60.65\%$。

这里不是：

```text
每10分钟，从最初的总概率中减掉固定的39.35%。
```

而是：

```text
每10分钟，从“目前仍未等到的那部分”中，
结束约39.35%的等待。
```

这句话后面会解释为什么“密度下降”和“无记忆性”不矛盾。

---

## 六、从“还没等到”，自然得到“已经等到”

我们已经知道：

$$
S_T(t)=P(T>t)
$$

但有时更关心：

> 在 $t$ 分钟以内，请求有没有到来？

这对应：

$$
P(T\leq t)
$$

### 1. 两个事件互为补事件

一次等待，不外乎两种情况：

```text
到 t 时已经结束：T ≤ t

到 t 时仍未结束：T > t
```

它们互斥，并且覆盖全部情况。

所以：

$$
P(T\leq t)+P(T>t)=1
$$

移项：

$$
P(T\leq t)=1-P(T>t)
$$

代入尾概率：

$$
\boxed{
F_T(t)=P(T\leq t)=1-e^{-\lambda t}
}
$$

由于等待时间不能为负数，完整的分布函数是：

$$
\boxed{
F_T(t)=
\begin{cases}
0,&t<0,\\[4pt]
1-e^{-\lambda t},&t\geq0.
\end{cases}
}
$$

具有这个分布函数的随机变量，称为服从参数为 $\lambda>0$ 的**指数分布**，记为：

$$
\boxed{
T\sim\operatorname{Exp}(\lambda)
}
$$

这里采用的是**速率参数**的写法。([NIST][3])

### 2. 用同一个时间点区分两个函数

例如 $t=20$：

$$
S_T(20)=e^{-1}\approx0.3679
$$

表示：

> 等了 20 分钟，仍没有请求到来的概率约为 $36.79\%$。

而：

$$
F_T(20)=1-e^{-1}\approx0.6321
$$

表示：

> 20 分钟以内，第一条请求已经到来的概率约为 $63.21\%$。

它们没有描述不同的随机变量，只是站在事件的两面看：

$$
\boxed{F_T(t)+S_T(t)=1}
$$

---

## 七、最需要慢下来的一步：概率密度到底从哪里来？

到这里，我们知道了“超过某个时间”和“不超过某个时间”的概率。

但还没有解释：

> 等待时间在 $t$ 附近，概率究竟有多密集？

这才是 PDF 要回答的问题。

### 1. 先不要问一个点，先问一个很短的区间

例如：

> 第一条请求是否在第 20 分钟到第 20.1 分钟之间到来？

这是一个有宽度的区间：

```text
时间轴

─────────────│════│──────────────────→
            20  20.1
              ↑
          一段很短的时间
```

一般写成：

$$
P(t<T\leq t+h)
$$

其中：

* $t$：区间起点；
* $h>0$：区间宽度；
* $t+h$：区间终点。

为了让第一条请求落在这段区间，必须同时满足两件事：

```text
第一件事：前 t 分钟，一条请求都没来。

第二件事：接下来的 h 分钟，至少来了一条。
```

注意第二件事是“至少一条”，因为我们只需要知道第一条已经出现，不要求这段时间恰好只来一条。

### 2. 先写概率乘法，而不是凭直觉直接相乘

设：

$$
A=\{T>t\}
$$

表示前 $t$ 分钟没有请求。

再设 $B$ 为：

> 接下来长度为 $h$ 的区间里，至少到达一条请求。

那么：

$$
\{t<T\leq t+h\}=A\cap B
$$

根据条件概率的乘法公式：

$$
P(A\cap B)=P(A)P(B\mid A)
$$

所以：

$$
P(t<T\leq t+h)
=
P(T>t)\,P(B\mid T>t)
$$

因为不重叠区间的计数独立，知道前面没有到达，不改变接下来这段的到达规律。

于是：

$$
P(B\mid T>t)=P(B)
$$

而长度为 $h$ 的区间里，至少到达一次的概率是：

$$
P(B)=1-e^{-\lambda h}
$$

因此得到精确公式：

$$
\boxed{
P(t<T\leq t+h)
=
e^{-\lambda t}\left(1-e^{-\lambda h}\right)
}
$$

先理解它的两个因子：

$$
\underbrace{e^{-\lambda t}}_{\text{前面一直没等到}}
\quad\times\quad
\underbrace{\left(1-e^{-\lambda h}\right)}_{\text{接下来这段至少到达一次}}
$$

### 3. 区间足够短时，发生率开始发挥作用

发生率 $\lambda$ 的一个重要含义是：

> 对足够短的区间 $h$，其中至少发生一次事件的概率，近似为 $\lambda h$。

即：

$$
1-e^{-\lambda h}\approx\lambda h
$$

这里是**近似号**，不是对任意 $h$ 都成立的等号。

代入刚才的公式：

$$
P(t<T\leq t+h)
\approx
e^{-\lambda t}\lambda h
$$

重新排列：

$$
\boxed{
P(t<T\leq t+h)
\approx
\underbrace{\lambda e^{-\lambda t}}_{\text{每单位时长的概率密度}}
\underbrace{h}_{\text{区间宽度}}
}
$$

现在 PDF 自然出现了：

$$
\boxed{
f_T(t)=\lambda e^{-\lambda t},\qquad t>0
}
$$

它不是凭空增加的公式，而是：

> **在一个很短的时间区间里，概率除以区间宽度，得到这个位置附近的概率密度。**

更严格地写：

$$
f_T(t)
=
\lim_{h\downarrow0}
\frac{P(t<T\leq t+h)}{h}
$$

这里 $h\downarrow0$ 表示区间宽度从正数不断缩小到 0。

因为：

$$
\frac{P(t<T\leq t+h)}{h}
=
e^{-\lambda t}
\frac{1-e^{-\lambda h}}{h}
$$

而后面这个比值在 $h\to0$ 时趋于发生率 $\lambda$，所以：

$$
f_T(t)=e^{-\lambda t}\lambda
$$

这与指数分布的标准密度表达式一致。([NIST][2])

### 4. 它为什么也等于 CDF 的导数？

因为：

$$
P(t<T\leq t+h)=F_T(t+h)-F_T(t)
$$

因此：

$$
f_T(t)
=
\lim_{h\downarrow0}
\frac{F_T(t+h)-F_T(t)}{h}
$$

这就是 $F_T$ 在 $t>0$ 处的导数。

所以：

$$
\boxed{f_T(t)=F_T'(t)}
$$

这里的导数不是额外的概率规则。

它只是：

> **累计概率增加了多少，除以我们向右移动了多远。**

这正是第十二课中“CDF 是累计概率，PDF 是局部密度”的关系。

---

## 八、现在重新看 PDF：为什么曲线高，不等于这个点概率大？

指数分布的 PDF 通常写成：

$$
f_T(t)=
\begin{cases}
\lambda e^{-\lambda t},&t\geq0,\\
0,&t<0.
\end{cases}
$$

它在起点附近最高，之后不断下降：

```text
密度 fT(t)
  ↑
λ │\
  │ \
  │  \
  │   \__
  │      \____
0 └────────────────────────→ 等待时间 t
  0
```

### 1. 曲线高度表达的是“附近的密集程度”

例如：

$$
f_T(20)=0.05e^{-1}\approx0.01839
$$

单位是：

$$
1/\text{分钟}
$$

它不是说：

> 恰好在第 20 分钟到达的概率是 $1.839\%$。

它说的是：

> 第 20 分钟附近，每单位时间宽度对应的概率密度大约是多少。

要得到概率，还必须乘一个足够短的宽度，或对一个区间积分。

### 2. 单点概率为什么仍为零？

用刚才的小区间公式：

$$
P(t<T\leq t+h)\approx f_T(t)h
$$

当宽度 $h$ 越来越接近 0 时，这段概率也趋近于 0。

所以对于指数分布：

$$
\boxed{P(T=t)=0}
$$

一个精确时刻没有宽度，也就没有概率面积。

但这不表示最终不会出现一个具体等待时长。最终一定会观测到某个时长，只是**事先指定的任意一个无限精确的时刻，都没有单独的正概率质量**。

### 3. 为什么密度可以大于 1？

同一个到达模型：

$$
\lambda=0.05/\text{分钟}=3/\text{小时}
$$

如果横轴使用分钟，密度在起点的常用取值是 $0.05$。

如果横轴使用小时，密度在起点的常用取值是 $3$。

**概率没有改变，只是横轴单位改变了，密度的数值也跟着改变。**

因此，密度值不能直接拿来与“概率必须小于等于 1”比较。

真正的概率是：

$$
P(a<T\leq b)=\int_a^b f_T(t)\,dt
$$

其中：

```text
密度 × 时间宽度
    ↓
没有单位的概率
```

### 4. 整条曲线下的面积为什么是 1？

我们已经从 CDF 知道：

$$
F_T(0)=0
$$

并且：

$$
\lim_{t\to\infty}F_T(t)=1
$$

而密度就是累计概率的增长速度，所以把整个非负时间轴上的密度累加起来：

$$
\int_0^\infty f_T(t)\,dt
=
F_T(\infty)-F_T(0)
=
1-0
=
1
$$

这里 $F_T(\infty)$ 表示上述极限。

因此，这不是一条随便画出来的下降曲线，而是一条**总概率面积为 1 的密度曲线**。

---

## 九、最关键的理解：密度下降，为什么不代表“越等越难等到”？

我们现在已经看到：

$$
f_T(t)=\lambda e^{-\lambda t}
$$

会随 $t$ 增大而下降。

但模型又说，到达机制没有随着等待时间改变。

看起来是不是矛盾？

**不矛盾。因为两种说法使用的参照人群不同。**

### 1. 想象同时开始大量独立的等待实验

每个实验都使用相同的到达模型。

所有实验一起开始计时，每个实验在收到第一条请求时结束。

用模型概率计算，可以得到：

| 时间区间       | 区间开始时仍未结束的比例，占最初全部实验 | 在这个区间结束的比例，占最初全部实验 |
| ---------- | -------------------: | -----------------: |
| 第 0～10 分钟  |            $100\%$ |        $39.35\%$ |
| 第 10～20 分钟 |          $60.65\%$ |        $23.87\%$ |
| 第 20～30 分钟 |          $36.79\%$ |        $14.47\%$ |

这些数值是模型计算结果，不是实际模拟数据。

观察最后一列：

```text
第一个10分钟结束的实验多。
第二个10分钟结束的实验少一些。
第三个10分钟结束的实验更少。
```

为什么？

因为很多实验早就结束了，不可能再在后面的区间“第一次结束”。

所以：

> **越到后面，仍有资格在那个时段第一次等到请求的实验，已经越来越少。**

这解释了无条件密度为什么下降。

### 2. 现在换一个分母

不再以最初全部实验为分母，而只看：

> 到当前时刻仍然没等到请求的实验。

那么：

$$
\frac{23.87\%}{60.65\%}\approx39.35\%
$$

$$
\frac{14.47\%}{36.79\%}\approx39.35\%
$$

也就是说：

> 在每个 10 分钟区间开始时，仍未结束的实验中，都有约 $39.35\%$ 会在接下来的 10 分钟结束。

结束的**绝对比例相对于最初全部实验在下降**，但是结束的**条件比例相对于目前仍未结束的实验保持不变**。

这就是核心区别：

```text
概率密度：
相对于最初全部等待实验，
谁会在这个时刻附近第一次结束？

条件发生概率：
只看现在仍未结束的实验，
谁会在接下来一段时间结束？
```

### 3. 数学上，两个分母怎样体现？

我们已经推导：

$$
P(t<T\leq t+h)
=
e^{-\lambda t}\left(1-e^{-\lambda h}\right)
$$

现在增加条件：

> 已知已经等到 $t$，仍未发生。

由于事件 $\{t<T\leq t+h\}$ 包含在 $\{T>t\}$ 中：

$$
\begin{aligned}
P(t<T\leq t+h\mid T>t)
&=\frac{P(t<T\leq t+h)}{P(T>t)}\\[4pt]
&=\frac{e^{-\lambda t}(1-e^{-\lambda h})}
{e^{-\lambda t}}\\[4pt]
&=1-e^{-\lambda h}.
\end{aligned}
$$

于是：

$$
\boxed{
P(t<T\leq t+h\mid T>t)=1-e^{-\lambda h}
}
$$

右边没有 $t$。

它只与“未来再观察多长时间 $h$”有关，不与“已经等了多久 $t$”有关。

当 $h$ 很小时：

$$
P(t<T\leq t+h\mid T>t)\approx\lambda h
$$

因此，指数分布的**瞬时条件发生率**为常数 $\lambda$。这与密度 $f_T(t)$ 下降并不冲突。([NIST][2])

**请特别记住：恒定的是条件发生率，不是概率密度。**

---

## 十、无记忆性：不变的是“剩余等待”，不是“总等待”

现在再来正式学习无记忆性，就不会只剩下一个需要背诵的公式。

### 1. 先把三种时间分开

假设已经等了 $s$ 分钟，第一条请求仍没来。

我们还想继续等 $u$ 分钟：

```text
最初起点                  现在                  未来
0────────────────────────s────────────────────s+u
          已等 s                     再等 u
```

三个量分别是：

* $T$：从最初起点算起的总等待时间；
* $s$：已经过去的时长；
* $T-s$：在已知 $T>s$ 的前提下，剩余的等待时间。

现在的问题是：

> 已经等了 $s$ 分钟还没来，那么还要再等超过 $u$ 分钟的概率是多少？

写成：

$$
P(T-s>u\mid T>s)
$$

由于：

$$
T-s>u
\iff
T>s+u
$$

所以它等于：

$$
P(T>s+u\mid T>s)
$$

### 2. 一步一步推导

根据条件概率：

$$
P(T>s+u\mid T>s)
=
\frac{P(\{T>s+u\}\cap\{T>s\})}
{P(T>s)}
$$

因为 $u\geq0$，如果总等待超过 $s+u$，当然也超过 $s$。

因此：

$$
\{T>s+u\}\subseteq\{T>s\}
$$

交集就等于较小的那个事件：

$$
\{T>s+u\}\cap\{T>s\}
=
\{T>s+u\}
$$

于是：

$$
P(T>s+u\mid T>s)
=
\frac{P(T>s+u)}{P(T>s)}
$$

代入尾概率：

$$
=
\frac{e^{-\lambda(s+u)}}{e^{-\lambda s}}
$$

将分子拆开：

$$
e^{-\lambda(s+u)}
=
e^{-\lambda s}e^{-\lambda u}
$$

约去共同因子：

$$
=
\frac{e^{-\lambda s}e^{-\lambda u}}
{e^{-\lambda s}}
=
e^{-\lambda u}
$$

而：

$$
e^{-\lambda u}=P(T>u)
$$

因此：

$$
\boxed{
P(T-s>u\mid T>s)
=
P(T>s+u\mid T>s)
=
P(T>u)
}
$$

这就是指数分布的无记忆性：**已知一直等到现在仍未发生后，剩余等待时间的分布，与刚开始时的等待时间分布相同。**([概率课程][4])

### 3. 用本例把容易混淆的概率分开

仍然使用：

$$
\lambda=0.05/\text{分钟}
$$

从一开始问：

> 总共等超过 30 分钟的概率是多少？

答案是：

$$
P(T>30)=e^{-0.05\times30}=e^{-1.5}\approx0.2231
$$

但是，现在已经知道等了 20 分钟还没来。

再问：

> 从现在开始，还要再等超过 10 分钟的概率是多少？

答案是：

$$
\begin{aligned}
P(T>30\mid T>20)
&=P(T>10)\\
&=e^{-0.05\times10}\\
&=e^{-0.5}\\
&\approx0.6065.
\end{aligned}
$$

所以：

$$
P(T>30)\neq P(T>30\mid T>20)
$$

它们的区别不是公式技巧，而是信息状态不同：

```text
前一个：
从所有实验里，找总等待超过30分钟的。

后一个：
先筛出已经超过20分钟的实验，
再问其中多少会超过30分钟。
```

### 4. “无记忆”不等于过去的信息完全没用

已知 $T>20$，当然已经知道总等待时间至少超过 20 分钟。

因此，过去的信息确实改变了对**总等待时间**的认识。

不变的是：

> 从现在重新计时，还要等待多久的分布。

所以不要把无记忆性误读成：

> “知道已经等了20分钟，对 $T$ 什么信息都没有提供。”

它提供了信息，只是没有改变**剩余等待**的分布。

---

## 十一、把三个常用概率问题真正算完整

现在不增加新理论，只把已经得到的工具用起来。

以下仍取：

$$
T\sim\operatorname{Exp}(0.05)
$$

时间单位统一为分钟。

### 问题一：20 分钟内已经收到请求

“20 分钟内已经收到”对应：

$$
T\leq20
$$

所以使用 CDF：

$$
\begin{aligned}
P(T\leq20)
&=1-e^{-0.05\times20}\\
&=1-e^{-1}\\
&\approx0.6321.
\end{aligned}
$$

### 问题二：第一条请求在第 10 到第 30 分钟之间到来

对应：

$$
10<T\leq30
$$

先不要代入公式，先理解集合关系：

```text
等待超过10分钟的所有实验
        ↓
减去其中等待超过30分钟的实验
        ↓
剩下的就是等待在10到30分钟之间的实验
```

因此：

$$
P(10<T\leq30)
=
P(T>10)-P(T>30)
$$

代入：

$$
\begin{aligned}
P(10<T\leq30)
&=e^{-0.05\times10}-e^{-0.05\times30}\\
&=e^{-0.5}-e^{-1.5}\\
&\approx0.6065-0.2231\\
&\approx0.3834.
\end{aligned}
$$

这也等于：

$$
F_T(30)-F_T(10)
$$

两种方法是在计算同一块概率。

### 问题三：已经等了 20 分钟，未来 10 分钟内会不会到来？

对应：

$$
P(20<T\leq30\mid T>20)
$$

这是条件概率，不是普通区间概率。

根据前面的推导：

$$
\begin{aligned}
P(20<T\leq30\mid T>20)
&=1-e^{-0.05\times10}\\
&=1-e^{-0.5}\\
&\approx0.3935.
\end{aligned}
$$

而无条件的：

$$
P(20<T\leq30)
=
e^{-1}-e^{-1.5}
\approx0.1447
$$

二者相差很大。

因为：

> $0.1447$ 相对于最初全部实验；$0.3935$ 只相对于已经等过 20 分钟的那部分实验。

这里是本课最值得真正理解的一组数。

---

## 十二、它与几何分布的关系，不只是“一个离散、一个连续”

第十五课中，几何分布研究：

> 重复进行独立、同成功概率的试验，第一次成功出现在第几次？

其随机变量是一个次数：

$$
G\in\{1,2,3,\ldots\}
$$

并具有：

$$
P(G>k)=(1-p)^k
$$

因为“第一次成功还没出现”，意味着前 $k$ 次全部失败。

现在，我们可以把连续时间人为切成等长小片。

设每片长度为：

$$
\Delta
$$

例如 $\Delta=1$ 分钟：

```text
0────1────2────3────4────5────→ 时间
 片1  片2  片3  片4  片5
```

对每一片只记录：

```text
这一片至少来过一条请求 → 成功

这一片一条请求都没来   → 失败
```

每片成功概率为：

$$
p_\Delta=1-e^{-\lambda\Delta}
$$

因为不同时间片的计数独立，所以这些“成功/失败”记录构成独立、同成功概率的伯努利试验。

定义：

$$
G_\Delta=\text{第一个含有请求的时间片编号}
$$

那么：

$$
G_\Delta\sim\operatorname{Geometric}(p_\Delta)
$$

但要注意：

$$
G_\Delta
$$

是**第几片**，不是等待了多长时间。

要转换成时间，需要乘以每片的长度：

$$
\Delta G_\Delta
$$

如果第一条请求出现在第 $G_\Delta$ 片，那么：

$$
(G_\Delta-1)\Delta<T\leq G_\Delta\Delta
$$

所以用时间片右端点近似真正到达时刻时，误差小于一片：

$$
0\leq\Delta G_\Delta-T<\Delta
$$

当时间片越来越细：

$$
\Delta\to0
$$

这个误差也越来越小。

因此，“指数分布是几何分布的连续时间对应”可以具体理解为：

> **先用几何分布数要等多少个时间片，再乘上每片的时长；当时间片越来越细，就逐渐还原连续的指数等待时间。**

它不是把整数编号直接当成连续时间。

---

## 十三、什么时候这个模型会失效？实际使用时该检查什么？

### 1. 有固定时间表，就不能套无记忆性

回到每隔 20 分钟准时到达的系统。

刚开始等时，还要等 20 分钟。

已经等了 19 分钟时，只剩 1 分钟。

剩余等待明显改变，因此不是今天的指数等待机制。

### 2. 发生率随“年龄”变化，也不能套

假设某种设备越用越容易坏。

那么：

> 已经使用很久还没坏，与刚开始使用时，未来短时间内的故障风险不一样。

这就不符合指数模型的恒定条件发生率。

指数寿命模型适用于故障率近似恒定的阶段，不能因为寿命是一个正数，就自动采用指数分布。([NIST][2])

### 3. “等下一次到达”与“排队等到被服务”，不是同一个变量

服务器场景中特别容易出现这个混淆：

```text
等下一条新请求到达
```

与：

```text
一个已经到达的请求，等多久才轮到它处理
```

不是同一件事。

后者还受到队列长度、服务速度、调度规则等因素影响。

因此，即使你为“请求到达”采用了泊松模型，也不能据此直接宣布：

> 每个请求的排队时长或响应时长都服从指数分布。

**先定义随机变量，再选择分布。不能只看到“等待”两个字就套模型。**

### 4. 一个具体工程应用：监听程序多久没收到事件才提醒？

假设一个监听程序等待下一条事件，而到达机制确实符合本课模型。

如果规定：

> 等了 20 分钟还没有事件，就发出提醒。

那么正常模型下，提醒被触发的概率是：

$$
P(T>20)=e^{-1}\approx36.79\%
$$

这个比例并不小。

所以，“20 分钟没等到”未必是异常，更不能因为平均每小时来 3 次，就认为每隔 20 分钟一定会来一次。

在这里，概率模型帮助我们做的是：

> **区分“随机机制本来就会产生的长等待”，与“需要怀疑机制已经变化的现象”。**

对机器学习中的事件时间预测，也应该保留同样的建模意识：即使模型为不同输入预测不同的速率，是否采用指数等待规律，仍然是一项需要检验的分布假设，而不是网络输出一个正数之后就自动成立。

---

## 十四、把整节课重新装回知识地图

现在我们可以用一条链概括全部推导：

```text
请求按照恒定速率的泊松机制到达
              ↓
定义 T：到下一条请求的等待时长
              ↓
T > t 等价于前 t 时间里零次到达
              ↓
P(T > t) = e^(-λt)
              ↓
用补事件得到 CDF
F_T(t) = 1 - e^(-λt)
              ↓
看一个很短区间里的概率
P(t < T ≤ t+h) ≈ λe^(-λt)h
              ↓
得到 PDF
f_T(t) = λe^(-λt)
              ↓
只在“尚未等到”的实验中重新计算概率
              ↓
剩余等待分布不变
              ↓
无记忆性
```

三个函数也可以放在同一张表里：

| 对象              | 它回答的问题                  | 表达式，$t\geq0$             |
| --------------- | ----------------------- | -------------------------- |
| 生存函数 $S_T(t)$ | 到 $t$ 为止，仍没等到的概率是多少？  | $e^{-\lambda t}$         |
| 分布函数 $F_T(t)$ | 到 $t$ 为止，已经等到的概率是多少？  | $1-e^{-\lambda t}$       |
| 密度函数 $f_T(t)$ | 在 $t$ 附近，等待时长的概率有多密集？ | $\lambda e^{-\lambda t}$ |

**前两个是概率，第三个是密度。**

再把两个“率”分开：

$$
\underbrace{f_T(t)}_{\text{相对于最初全部实验的局部概率密度}}
=
\underbrace{P(T>t)}_{\text{到现在仍未结束的比例}}
\underbrace{\lambda}_{\text{尚未结束条件下的瞬时发生率}}
$$

也就是：

$$
\boxed{
\lambda e^{-\lambda t}
=
e^{-\lambda t}\times\lambda
}
$$

这个等式的含义，比单独记住 PDF 公式更重要：

> **后期第一次到达的实验少，不是因为每个尚未结束的实验越来越难等到，而是因为尚未结束的实验本身越来越少。**

---

## 这一节真正需要记住的核心

1. **指数分布刻画的是一种特定机制下的连续等待时间，不是所有等待时间的通用分布。**

2. 它最根本的推导是：**等待超过 $t$，等价于前 $t$ 时间里零次到达**，因此得到 $P(T>t)=e^{-\lambda t}$。

3. CDF 是“已经等到”的累计概率，PDF 是“在某个时刻附近第一次等到”的概率密度；**密度必须结合时间宽度，才能变成概率**。

4. **密度下降与发生率恒定并不矛盾：前者相对于最初全部实验，后者以目前仍未结束的实验为条件。**

5. **无记忆性说的是剩余等待分布不变，不是总等待时间没有获得新信息，也不是现实中的等待都不会受到历史影响。**

下一步自然会问：既然已经理解了“到下一次事件的一段等待”，那么为了等到第 $r$ 次事件，需要把多少段等待加起来？这将成为下一种等待时间分布的起点。

[1]: https://www.probabilitycourse.com/chapter11/11_1_2_basic_concepts_of_the_poisson_process.php "Basic Concepts of the Poisson Process"
[2]: https://www.itl.nist.gov/div898/handbook/apr/section1/apr161.htm "8.1.6.1. Exponential"
[3]: https://www.itl.nist.gov/div898/handbook/eda/section3/eda3667.htm "1.3.6.6.7. Exponential Distribution"
[4]: https://www.probabilitycourse.com/chapter4/4_2_2_exponential.php "Exponential Distribution | Definition | Memoryless Random Variable"
