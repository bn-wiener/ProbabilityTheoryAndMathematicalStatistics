# 第二十九课：随机变量函数的分布——数值经过变换以后，概率怎样重新分配？

上一课，我们研究了：

> 已经知道一个变量的信息以后，另一个变量怎样分布？

今天换一个问题：

> **已经知道随机变量 $X$ 的分布。如果把它的每个取值再加工一下，例如平方、取绝对值、乘以常数，得到的新变量怎样分布？**

例如：

$$
Z=X^2.
$$

我们想知道的，不只是“某一次 $X=2$，所以 $Z=4$”，而是：

> **在整个随机模型中，$Z$ 可能取哪些值？这些值或区间分别获得多少概率？**

今天先研究**一个随机变量经过函数变换**的情形。两个变量相加形成的新分布，留到下一课。

```text
随机变量 X 的分布
        ↓
通过确定的规则 g 加工取值
        ↓
新随机变量 Z=g(X)
        ↓
求 Z 的分布
```

本课最重要的思想是：

$$
\boxed{\text{变换改变的是数值的位置；概率要按这些数值的新去向重新汇总。}}
$$

---

## 一、问题背景：知道原始读数的分布，怎样得到平方后读数的分布？

### 1. 先明确一次试验做什么

**本课换一个新袋子，不再使用前几课左右各写一个数字的卡片。**

这个袋子里有五张卡片，每张只写一个数：

| 卡片编号 | 卡片上的数字 |
| ---- | -----: |
| 1    | $-2$ |
| 2    | $-1$ |
| 3    |  $0$ |
| 4    |  $1$ |
| 5    |  $2$ |

规定一次完整试验：

> **从袋子里等可能地随机抽出一张卡片，读取卡片上的数字，再把这个数字平方，记录平方后的结果。**

每张卡片被抽到的概率都是：

$$
\frac15.
$$

定义：

$$
X=\text{抽到的卡片上的原始数字},
$$

$$
Z=\text{把这个原始数字平方后得到的数字}.
$$

因此：

$$
Z=X^2.
$$

例如，抽到编号 2 的卡片：

$$
X=-1,
$$

电脑进行一次确定的计算：

$$
Z=(-1)^2=1.
$$

**这里仍然只抽了一次、只抽了一张卡片。平方不是第二次随机试验。**

### 2. 为什么会关心平方后的数字？

可以把卡片上的数看成一个简化的误差模型：

```text
负数：偏小
正数：偏大
0：没有偏差
```

如果只关注误差大小，并希望较大的误差受到更强的惩罚，就可能记录平方误差：

$$
Z=X^2.
$$

注意：

> $|X|$ 是到 0 的距离，$X^2$ 是这个距离的平方；两者不能当作同一个量。

第二十二课选择平方偏差时，已经讨论过平方能够消除正负抵消，并更突出较大偏差的作用。

现在我们进一步问：

> 平方后的指标 $Z$，究竟怎样分布？

仅知道原始数字有五种，还没有回答这个问题。因为平方可能把不同的输入合并成相同的输出。

---

## 二、直觉与定义：随机变量后面，再接一条数值处理规则

第八课已经建立：

$$
X:\Omega\longrightarrow\mathbb R.
$$

它表示：随机变量 $X$ 把一个样本点转换成一个实数。随机的是最终出现哪个样本点，而不是这条映射规则每次任意改变。

今天只是在后面再接一条规则：

$$
g(x)=x^2.
$$

于是：

```text
样本点 ω
抽到了哪张卡片？
        ↓ X
原始数字 X(ω)
        ↓ g
加工后的数字 g(X(ω))
```

把这条完整的新规则命名为 $Z$：

$$
\boxed{
Z(\omega)=g\bigl(X(\omega)\bigr).
}
$$

通常简写成：

$$
\boxed{Z=g(X).}
$$

这里几个对象要分开：

| 对象         | 它是什么？                   |
| ---------- | ----------------------- |
| $\omega$ | 某一个原始样本点，例如“抽到编号 2 的卡片” |
| $X$      | 从样本点读取原始数字的规则           |
| $g$      | 对数字进行加工的确定规则，例如平方       |
| $Z$      | 先读取、再加工所组成的新随机变量        |
| $x$      | 一次可能的原始读数               |
| $z=g(x)$ | 对应的加工后读数                |

严格地说，变换 $g$ 要满足使输出取值条件仍然对应事件的“可测性”要求；本课使用的平方、绝对值、线性变换以及后面的阈值规则都满足这个要求。

### 为什么 $g$ 是确定的，$Z$ 却仍然是随机变量？

因为计算规则虽然确定，输入值还不确定。

```text
输入 -2 → 一定输出 4
输入 -1 → 一定输出 1
输入  0 → 一定输出 0
输入  1 → 一定输出 1
输入  2 → 一定输出 4
```

在抽取之前，我们不知道输入会是哪一个，所以也不知道输出会是哪一个。

另外：

$$
X^2=X\times X
$$

表示**同一个读数与它自己相乘**，不是再抽一次卡片，获得另一个独立数字后相乘。

---

## 三、离散情形：不同输入得到相同输出时，概率怎样处理？

### 1. 先列出完整的变换表

| 卡片编号 $\omega$ | 原始读数 $X(\omega)$ | 平方后 $Z(\omega)$ |   抽到这张卡片的概率 |
| --------------- | -----------------: | ----------------: | ----------: |
| 1               |             $-2$ |             $4$ | $\frac15$ |
| 2               |             $-1$ |             $1$ | $\frac15$ |
| 3               |              $0$ |             $0$ | $\frac15$ |
| 4               |              $1$ |             $1$ | $\frac15$ |
| 5               |              $2$ |             $4$ | $\frac15$ |

所以，$Z$ 的可能取值不是五个，而是：

$$
0,\quad1,\quad4.
$$

原因是：

$$
(-1)^2=1^2=1,
$$

$$
(-2)^2=2^2=4.
$$

### 2. 求 $P(Z=1)$

先不计算，先问：

> 哪些原始读数经过平方，会得到 1？

解方程：

$$
x^2=1.
$$

得到两个输入：

$$
x=-1,\qquad x=1.
$$

因此：

$$
\{Z=1\}
=
\{X=-1\}\cup\{X=1\}.
$$

这两个事件互斥：同一次抽取中，原始数字不可能既为 $-1$ 又为 $1$。

所以：

$$
\begin{aligned}
P(Z=1)
&=P(X=-1)+P(X=1)\\
&=\frac15+\frac15\\
&=\frac25.
\end{aligned}
$$

**这里相加，不是相乘。我们不是要求两个原始数字同时出现，而是其中任意一个出现，都能产生同一个输出。**

### 3. 求其他输出的概率

只有 $X=0$ 会得到 $Z=0$，因此：

$$
P(Z=0)=P(X=0)=\frac15.
$$

而 $X=-2$ 或 $X=2$ 都会得到 $Z=4$，所以：

$$
P(Z=4)=\frac15+\frac15=\frac25.
$$

最终：

| $z$      |       $0$ |       $1$ |       $4$ |
| ---------- | ----------: | ----------: | ----------: |
| $P(Z=z)$ | $\frac15$ | $\frac25$ | $\frac25$ |

检查：

$$
\frac15+\frac25+\frac25=1.
$$

这就是 $Z=X^2$ 的完整分布，其他数值的概率为 0。

### 4. 几何直觉：相同终点的概率要合并

```text
原始位置及概率                  新位置及概率

-2，概率1/5 ───────┐
                   ├────────→ 4，概率2/5
 2，概率1/5 ───────┘

-1，概率1/5 ───────┐
                   ├────────→ 1，概率2/5
 1，概率1/5 ───────┘

 0，概率1/5 ─────────────────→ 0，概率1/5
```

第九课已经把分布解释成“按照映射结果重新组织概率重量”。今天只是把这件事再做一遍：第一次从样本点汇总到 $X$，第二次从 $X$ 汇总到 $Z$。

---

## 四、离散变换公式：把所有能够产生这个输出的输入找齐

设 $X$ 的可能取值为：

$$
x_1,x_2,\ldots
$$

其中 $i$ 是取值列表中的编号。

记：

$$
p_X(x_i)=P(X=x_i).
$$

现在定义：

$$
Z=g(X).
$$

如果要计算：

$$
P(Z=z),
$$

就先找出所有满足：

$$
g(x_i)=z
$$

的原始取值。

这些不同的 $X$ 取值事件彼此互斥，因此：

$$
\boxed{
p_Z(z)
=
\sum_{\{i:\,g(x_i)=z\}}p_X(x_i).
}
$$

这条公式读作：

> **遍历 $X$ 的可能取值，只把那些经过 $g$ 后等于 $z$ 的输入挑出来，再把它们的概率相加。**

它的关键不是求和符号，而是下面这个问题：

$$
\boxed{\text{哪些输入会产生我正在查询的输出？}}
$$

### 两个必须避免的误解

**第一，不是把概率也进行同样的变换。**

虽然：

$$
Z=X^2,
$$

但不能因此把原来的概率平方。

本例中：

$$
P(Z=1)=\frac25,
$$

不是：

$$
\left(\frac15\right)^2.
$$

平方作用在**取值**上，不是作用在**概率**上。

**第二，不需要像条件分布那样重新除以某个条件概率。**

条件分布筛选掉一部分结果后，需要重新归一化。

这里没有排除任何原始结果，只是把它们搬到新位置。因此总概率天然仍然是 1。

---

## 五、最通用的方法：先求新变量的 CDF

离散情况下可以逐个列输入。

如果输入有连续无穷多个值，就不能靠列一张有限表完成计算。这时，第十课的 CDF 提供了统一入口：

$$
F_X(x)=P(X\le x).
$$

CDF 描述的是阈值左侧的累计概率，而且能够完整确定分布。

对于：

$$
Z=g(X),
$$

直接从定义出发：

$$
\boxed{
F_Z(z)
=
P(Z\le z)
=
P(g(X)\le z).
}
$$

这条公式对离散、连续等情形都成立。

三个符号的角色是：

$Z$ 是新随机变量；$z$ 是人为指定的输出阈值；$F_Z(z)$ 是输出不超过该阈值的概率。

真正需要做的，是把：

$$
g(X)\le z
$$

翻译成关于原始变量 $X$ 的条件。

### 以平方为例

因为：

$$
Z=X^2,
$$

所以：

$$
F_Z(z)=P(X^2\le z).
$$

当 $z<0$ 时，平方不可能为负，因此：

$$
F_Z(z)=0.
$$

当 $z\ge0$ 时：

$$
X^2\le z
$$

等价于：

$$
|X|\le\sqrt z,
$$

又等价于：

$$
-\sqrt z\le X\le\sqrt z.
$$

所以：

$$
\boxed{
F_Z(z)=P(-\sqrt z\le X\le\sqrt z),
\qquad z\ge0.
}
$$

这一步就是整个平方变换的核心。

### 为什么不能只写 $X\le\sqrt z$？

因为这样漏掉了对负数那一侧的限制。

例如：

$$
X=-2,\qquad z=1.
$$

虽然：

$$
-2\le\sqrt1=1,
$$

但：

$$
(-2)^2=4>1.
$$

所以 $X=-2$ 不应该被计入。

正确条件必须同时包含：

$$
X\ge-\sqrt z
$$

和：

$$
X\le\sqrt z.
$$

**把输出条件翻译回输入条件时，不能遗漏任何一侧，也不能额外包含不符合要求的输入。**

---

## 六、连续例子：均匀随机数平方以后，还均匀吗？

现在换一个明确的连续试验，与刚才的抽卡片模型区分。

> **在区间 $[-1,1]$ 内，按长度均匀地随机选取一个实数 $U$，再计算 $W=U^2$。**

这里仍然只有一次随机选择；平方是对同一个数的确定加工。

“按长度均匀”表示：相同长度的输入区间具有相同概率。

输入区间总长度为：

$$
1-(-1)=2.
$$

因此：

$$
f_U(u)=
\begin{cases}
\frac12,&-1\le u\le1,\\
0,&\text{其他位置}.
\end{cases}
$$

### 1. 先确定输出范围

由于：

$$
-1\le U\le1,
$$

所以：

$$
0\le W=U^2\le1.
$$

但这并不意味着 $W$ 在 $[0,1]$ 上均匀。

**知道输出范围，只知道数值会落在哪里；还不知道概率怎样分配。**

### 2. 从 CDF 开始

我们要求：

$$
F_W(w)=P(W\le w).
$$

当 $0\le w\le1$ 时：

$$
\begin{aligned}
F_W(w)
&=P(U^2\le w)\\
&=P(-\sqrt w\le U\le\sqrt w).
\end{aligned}
$$

输入区间：

$$
[-\sqrt w,\sqrt w]
$$

的长度是：

$$
\sqrt w-(-\sqrt w)=2\sqrt w.
$$

输入是均匀分布，所以这个区间的概率为：

$$
\frac{\text{目标区间长度}}{\text{整个区间长度}}
=
\frac{2\sqrt w}{2}
=
\sqrt w.
$$

因此：

$$
\boxed{
F_W(w)=
\begin{cases}
0,&w<0,\\[3pt]
\sqrt w,&0\le w<1,\\[3pt]
1,&w\ge1.
\end{cases}
}
$$

我们已经得到了 $W$ 的完整分布。

### 3. 用一个数值看出它不均匀

求：

$$
P\left(W\le\frac14\right).
$$

由上式：

$$
P\left(W\le\frac14\right)
=
\sqrt{\frac14}
=
\frac12.
$$

也就是说：

> 输出区间 $[0,\frac14]$ 只占整个输出范围长度的四分之一，却获得了一半的概率。

为什么？

因为：

$$
0\le U^2\le\frac14
$$

对应：

$$
-\frac12\le U\le\frac12.
$$

这段输入区间已经占了原始区间长度的一半。

因此：

$$
\boxed{\text{输入均匀，经过非线性变换后，输出不一定均匀。}}
$$

### 4. 再从 CDF 得到密度

在 $0<w<1$ 时：

$$
F_W(w)=\sqrt w=w^{1/2}.
$$

求导：

$$
\frac{d}{dw}w^{1/2}
=
\frac12w^{-1/2}
=
\frac1{2\sqrt w}.
$$

因此可以取：

$$
\boxed{
f_W(w)=
\begin{cases}
\frac1{2\sqrt w},&0<w<1,\\[3pt]
0,&\text{其他位置}.
\end{cases}
}
$$

端点处密度具体取什么值，不影响区间概率。这里把端点密度也约定为 0。

检查总面积：

$$
\int_0^1\frac1{2\sqrt w}\,dw
=
\left[\sqrt w\right]_0^1
=
1.
$$

所以这是一条合法密度。

### 5. 密度靠近 0 时很高，是否意味着 $P(W=0)>0$？

不是。

因为：

$$
W=0
\iff
U=0,
$$

而原始连续变量满足：

$$
P(U=0)=0.
$$

所以：

$$
P(W=0)=0.
$$

密度：

$$
\frac1{2\sqrt w}
$$

在 $w$ 接近 0 时很高，描述的是**靠近 0 的小区间内，概率相对于区间宽度很密集**，不是说 0 这个点堆积了正概率。

这仍然遵循第十二课的区别：PDF 的高度不是概率，区间下面积才是概率。

---

## 七、为什么连续变换不能只“替换横坐标”，还要调整密度高度？

这是本课第二个重要理解关口。

继续使用：

$$
U\text{ 在 }[-1,1]\text{ 上均匀分布}.
$$

但这一次不平方，而是另定义：

$$
T=2U.
$$

它把原数轴上的位置向两边拉开两倍：

```text
U 的取值范围：
-1 ├──────────────────┤ 1
          长度2

T 的取值范围：
-2 ├────────────────────────────────────┤ 2
                    长度4
```

### 1. 概率没有增加，范围却变宽了

原来密度是：

$$
\frac12.
$$

如果变换后仍然保持高度 $\frac12$，新范围长度是 4，那么总面积会变成：

$$
4\times\frac12=2.
$$

这不可能是概率密度。

要保持总面积为 1，密度必须变成：

$$
\frac14.
$$

于是：

$$
f_T(t)=
\begin{cases}
\frac14,&-2\le t\le2,\\
0,&\text{其他}.
\end{cases}
$$

### 2. 用同一事件验证“概率守恒”

输出落在：

$$
0\le T\le1
$$

等价于输入落在：

$$
0\le U\le\frac12.
$$

原始区间的概率：

$$
\frac12\times\frac12=\frac14.
$$

变换后区间的概率：

$$
1\times\frac14=\frac14.
$$

完全相同。

变化的是：

```text
区间宽度：扩大2倍
密度高度：缩小为原来的一半
概率面积：保持不变
```

所以：

$$
\boxed{
\text{数值被拉伸，密度需要相应降低；数值被压缩，密度需要相应提高。}
}
$$

这里说的是对应区域内的概率守恒，**不是说变换前后同一个数字附近的概率相同**。

---

## 八、单调变换的密度公式，为什么会出现导数？

前面的两倍拉伸，可以推广到局部拉伸程度不一样的函数。

设：

$$
Z=g(X).
$$

假设 $X$ 有密度，并且 $g$ 在承载 $X$ 概率的区间上连续可微、严格单调，导数不为 0。

“严格单调”意味着这段范围内一直严格增加，或者一直严格减少，因此每个输出只有一个对应输入。

### 1. 先比较输入、输出的小区间宽度

在某个输入位置 $x$，对应输出为：

$$
z=g(x).
$$

如果输入取一小段正的宽度：

$$
\Delta x,
$$

输出对应区间的宽度近似为：

$$
\Delta z\approx |g'(x)|\Delta x.
$$

其中：

$g'(x)$ 是导数，描述输出相对于输入的局部变化快慢。

绝对值是因为我们计算的是区间长度；函数递减时方向会翻转，但长度不能为负。

### 2. 同一部分结果的概率必须相同

第十二课已经建立，在密度局部变化不明显的小区间内：

$$
\text{概率}\approx\text{密度}\times\text{宽度}.
$$

这是理解下面推导的旧知识。

输入小区间的概率约为：

$$
f_X(x)\Delta x.
$$

对应输出小区间的概率约为：

$$
f_Z(z)\Delta z.
$$

由于在这里变换一一对应，这两部分描述同一个事件，因此：

$$
f_Z(z)\Delta z
\approx
f_X(x)\Delta x.
$$

代入宽度关系：

$$
f_Z(z)|g'(x)|\Delta x
\approx
f_X(x)\Delta x.
$$

约去 $\Delta x$，再通过小区间极限得到密度变换公式：

$$
\boxed{
f_Z(z)=\frac{f_X(x)}{|g'(x)|},
\qquad x=g^{-1}(z).
}
$$

这里：

$$
g^{-1}(z)
$$

表示**反函数**：从输出 $z$ 找回对应输入 $x$，不是 $1/g(z)$。

公式在相应输出范围内使用，输出范围之外密度为 0。

整条公式的意义是：

$$
\boxed{
\text{新密度}
=
\frac{\text{对应位置的原密度}}{\text{局部长度拉伸倍数}}.
}
$$

### 3. 用刚才的两倍变换验证

对于：

$$
T=2U,
$$

变换规则为：

$$
g(u)=2u.
$$

导数是：

$$
g'(u)=2.
$$

于是：

$$
f_T(t)
=
\frac{f_U(t/2)}2
=
\frac{1/2}{2}
=
\frac14,
\qquad -2<t<2.
$$

这与直接用面积得到的结果一致。

### 4. 为什么不能把这个单分支公式直接用于整个平方变换？

因为：

$$
g(x)=x^2
$$

在包含正负数的区间上不是一一对应的。

对 $z>0$，有两个候选输入：

$$
x=\sqrt z,\qquad x=-\sqrt z.
$$

因此要分别计算两条输入分支的贡献，再相加。

如果 $X$ 有密度，那么对 $z>0$：

$$
\boxed{
f_Z(z)
=
\frac{f_X(\sqrt z)}{2\sqrt z}
+
\frac{f_X(-\sqrt z)}{2\sqrt z}.
}
$$

两个分母都来自：

$$
|g'(x)|=|2x|=2\sqrt z.
$$

两个分子则分别来自正、负两个输入位置。原取值范围之外的密度按 0 处理。

这与离散情况完全呼应：

> **所有能产生同一个输出的来源，都不能漏掉；每个来源先考虑局部伸缩，再把贡献合起来。**

不过，在初学阶段，遇到平方这样的非单调变换，优先从：

$$
F_Z(z)=P(X^2\le z)
$$

入手，通常更容易避免遗漏。

---

## 九、一个重要反例：连续输入，经过函数以后一定仍然连续吗？

不一定。

继续使用：

$$
U\text{ 在 }[-1,1]\text{ 上均匀分布}.
$$

这次定义一个阈值输出：

$$
B=
\begin{cases}
1,&U>0,\\
0,&U\le0.
\end{cases}
$$

这也是：

$$
B=g(U),
$$

只不过函数 $g$ 不再是平方，而是“判断是否大于 0”。

由于正半段、负半段各占一半概率：

$$
P(B=1)=P(U>0)=\frac12,
$$

$$
P(B=0)=P(U\le0)=\frac12.
$$

所以 $B$ 是一个伯努利随机变量，而不是连续变量。

第十三课的示性随机变量，正是在把一个事件是否发生转换成 0/1。现在我们看到，它也是函数变换的一个例子。

为什么类型变了？

因为函数把一整段输入都压到了同一个输出点：

```text
所有 U≤0 的输入 ─────→ 0
所有 U>0 的输入 ─────→ 1
```

于是输出点可以获得正概率。

因此：

$$
\boxed{\text{输入有密度，不代表任意函数变换后的输出也一定有密度。}}
$$

这也是为什么最稳妥的入口仍然是：

$$
F_Z(z)=P(g(X)\le z).
$$

先弄清楚输出分布是什么类型，再决定用 PMF 还是 PDF 描述。

---

## 十、与期望、方差连接：其实你以前已经在使用随机变量的函数

### 1. $X^2$ 本身是随机变量，$E[X^2]$ 才是一个平均值

回到本课开始的五张卡片模型：

$$
P(X=-2)=P(X=-1)=P(X=0)=P(X=1)=P(X=2)=\frac15.
$$

我们已经得到：

$$
Z=X^2,
$$

并且：

$$
P(Z=0)=\frac15,\qquad
P(Z=1)=\frac25,\qquad
P(Z=4)=\frac25.
$$

因此：

$$
\begin{aligned}
E[Z]
&=0\times\frac15
+1\times\frac25
+4\times\frac25\\
&=2.
\end{aligned}
$$

也就是：

$$
E[X^2]=2.
$$

但：

$$
E[X]
=
\frac{-2-1+0+1+2}{5}
=
0.
$$

所以：

$$
(E[X])^2=0.
$$

两者不同：

$$
\boxed{
E[X^2]=2
\neq
(E[X])^2=0.
}
$$

原因是操作顺序不同：

```text
E[X²]：
每个结果先平方
        ↓
形成新的数值与概率配置
        ↓
再求平均

(E[X])²：
先把原分布压缩成一个平均值
        ↓
再把这个平均值平方
```

这正是第二十二课区分“先平方再平均”与“先平均再平方”的内容。

### 2. 只想求期望时，不一定要先求完整的新分布

在这个有限离散模型中，还可以直接计算：

$$
E[X^2]
=
(-2)^2\frac15
+(-1)^2\frac15
+0^2\frac15
+1^2\frac15
+2^2\frac15
=
2.
$$

为什么这样也对？

因为可以先把相同输出合并，再计算加权平均；也可以先计算每个输入对应输出的加权贡献，最后一起相加。

两种做法只改变了分组顺序。

一般地，在相应期望有限的条件下，离散情形有：

$$
\begin{aligned}
E[g(X)]
&=\sum_z z\,P(g(X)=z)\\
&=\sum_z z
\sum_{\{i:g(x_i)=z\}}p_X(x_i)\\
&=\sum_i g(x_i)p_X(x_i).
\end{aligned}
$$

最后一步只是把同一个输出组重新拆回各个输入，每个原始取值恰好被计算一次。

因此要分清：

> **求新变量的完整分布**，与**只求新变量的期望**，不是同一个任务。

### 3. 方差也可以放进这条链

设：

$$
\mu=E[X].
$$

定义新随机变量：

$$
Q=(X-\mu)^2.
$$

那么：

$$
\operatorname{Var}(X)=E[Q].
$$

这里：

$X-\mu$ 是带方向的偏差；$Q$ 是平方偏差这个随机变量；$\operatorname{Var}(X)$ 是它的平均值。

**平方偏差本身会随着一次次结果变化，方差则是整个模型的一个固定统计特征。**

---

## 十一、实际应用：把原始不确定性传递到我们真正关心的指标上

### 机器学习、深度学习与计算机视觉：从预测误差到损失

考虑一个固定模型，从某个图像分布中随机抽取一张图像。

定义：

$$
R=\text{这张图像上某个位置预测的带符号误差},
$$

并把平方误差作为损失：

$$
L=R^2.
$$

那么，$R$ 的分布与 $L$ 的分布描述的是不同对象。

前者区分偏大还是偏小；后者合并正负误差，并突出大误差。

例如，对于 $c\ge0$：

$$
\begin{aligned}
P(L>c)
&=P(R^2>c)\\
&=P(|R|>\sqrt c).
\end{aligned}
$$

因此“损失超过阈值”的问题，可以翻译回“原始误差超出容许范围”的问题。

### 数据分析与工程：换单位后，密度也要换尺度

假设某段随机耗时用小时记录为 $H$，改用分钟记录为：

$$
M=60H.
$$

同一次任务没有变，耗时对应的事件概率也没有变。

但横轴数值扩大 60 倍，因此在 $H$ 有密度时：

$$
f_M(m)
=
\frac1{60}f_H\left(\frac m{60}\right).
$$

这里的 $\frac1{60}$，正是为了补偿区间宽度扩大，保持概率面积不变。

### 强化学习：对同一次交互的奖励进行确定加工

固定当前状态和动作，一次交互产生原始奖励 $R$。

如果记录的是：

$$
Z=g(R),
$$

那么 $Z$ 的分布来自 $R$ 的分布经过这条规则的重新组织，不是又独立生成了一份奖励。

不同变换可能合并、放大或压缩原始奖励之间的差别。因此研究加工后的指标，首先要明确函数 $g$ 到底做了什么。

---

## 十二、理解检查

### 问题一：回到五张卡片，令 $A=|X|$，它怎样分布？

先找输出：

$$
|-2|=2,\quad|-1|=1,\quad|0|=0,\quad|1|=1,\quad|2|=2.
$$

所以：

$$
P(A=0)=\frac15,
$$

$$
P(A=1)=\frac25,
$$

$$
P(A=2)=\frac25.
$$

它与 $Z=X^2$ 的概率分组相同，但输出位置不同：

```text
绝对值：0、1、2
平方：  0、1、4
```

所以不能因为概率权重相同，就说它们具有同一个分布。

### 问题二：如果知道 $Z=X^2=1$，能否恢复 $X$？

在五张卡片模型中，不能唯一恢复。

因为可能是：

$$
X=-1,
$$

也可能是：

$$
X=1.
$$

平方丢掉了符号信息。

因此：

> 已知 $X$ 可以确定 $Z$，不代表已知 $Z$ 一定可以确定 $X$。

### 问题三：求函数变换后的分布，第一步应该做什么？

不是直接找一条密度公式代入。

应该先明确：

$$
Z=g(X)
$$

究竟把哪些输入送到了哪些输出，尤其要检查：

> **输出范围是什么？同一个输出是否有多个输入来源？**

之后再决定使用离散求和、CDF，还是满足条件时的密度变换公式。

---

## 这一节真正需要记住的核心

**$Z=g(X)$ 是对同一次随机结果得到的数值进行确定加工；没有额外引入一次随机抽取。**

**求新分布的本质，是找出哪些原始取值会产生目标输出，再把这些来源的概率正确汇总。**

**最通用的入口是 $F_Z(z)=P(g(X)\le z)$：先把输出条件翻译回输入条件，再用原分布计算。**

**连续情形中，密度还要补偿数值变换造成的区间伸缩；多个输入分支对应同一输出时，它们的贡献需要相加。**

**函数作用在随机变量的取值上，不是直接作用在概率或密度高度上；变换后的分布类型也不一定与输入相同。**

下一课进入**第三十课：随机变量之和的分布与卷积**。今天我们把一个输入加工成新数值；接下来要研究：**同一次试验得到两个读数 $X,Y$，再计算 $S=X+Y$，各个数值组合的概率怎样汇总到“和”上？**
