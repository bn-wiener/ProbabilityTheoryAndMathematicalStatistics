# 第四课：事件——把“我们关心的问题”翻译成结果集合

今天研究概率论中的第二个数学对象：

> 我们通常不关心某一个精确结果，而关心“某类结果是否发生”。如何表达这类问题？

当前位置：

```text
随机现象
   ↓
样本空间
   ↓
事件  ← 今天
   ↓
概率
   ↓
条件概率
```

---

# 1. 问题背景：为什么有了样本空间，还需要事件？

考虑一次掷骰子试验：

\[
\Omega=\{1,2,3,4,5,6\}
\]

样本空间已经列出了所有可能结果。

但现实中，我们关心的问题通常不是：

> 是否恰好掷出4点？

还可能是：

- 是否掷出偶数？
- 点数是否大于3？
- 是否掷出了1点或6点？
- 是否没有掷出6点？

其中“掷出偶数”对应：

\[
\{2,4,6\}
\]

它不是一个基本结果，而是一组符合要求的结果。

这就产生了“事件”：

> 事件把一个现实问题，翻译成样本空间中的一组结果。

如果没有事件概念，我们只能分别讨论1点、2点、3点等基本结果，很难直接表达“偶数”“不合格”“发生故障”“识别正确”等真正关心的问题。

---

# 2. 直觉解释：事件是样本空间中的一个区域

把样本空间想象成一张地图：

```text
样本空间 Ω
┌────────────────────────────┐
│                            │
│   1        3        5       │
│                            │
│       ┌──────────────┐     │
│       │  2    4    6  │     │
│       │   偶数事件 A  │     │
│       └──────────────┘     │
│                            │
└────────────────────────────┘
```

整个大框是样本空间，包含所有可能结果。

里面圈出的区域是事件：

> 只要最终结果落进这个区域，我们就说事件发生了。

假设事件 \(A\) 表示“掷出偶数”：

- 掷出2：结果落在 \(A\) 中，事件发生；
- 掷出4：结果落在 \(A\) 中，事件发生；
- 掷出6：结果落在 \(A\) 中，事件发生；
- 掷出3：结果不在 \(A\) 中，事件没有发生。

因此，事件的本质不是一句话，而是：

> 让这句话成立的所有基本结果组成的集合。

---

# 3. 数学定义

设随机试验的样本空间为 \(\Omega\)。

样本空间 \(\Omega\) 的一个子集 \(A\)，称为一个随机事件，简称事件。

写成：

\[
A\subseteq\Omega
\]

符号含义如下：

- \(\Omega\)：所有可能基本结果组成的样本空间；
- \(A\)：我们关心的某个事件；
- \(\subseteq\)：是……的子集；
- \(A\subseteq\Omega\)：事件 \(A\) 中的所有结果都来自样本空间 \(\Omega\)。

例如：

\[
\Omega=\{1,2,3,4,5,6\}
\]

定义事件：

\[
A=\{\text{掷出偶数}\}
\]

把文字条件展开，就得到：

\[
A=\{2,4,6\}
\]

所以事件不是样本空间之外增加的新结果，而是：

> 从样本空间已有的结果中，挑出满足某个条件的那些结果。

---

# 4. 事件什么时候算“发生”？

假设一次试验最终实现的样本点是 \(\omega\)。

如果：

\[
\omega\in A
\]

那么称事件 \(A\) 发生。

其中：

- \(\omega\)：本次实际出现的基本结果；
- \(A\)：一个事件，也就是一组基本结果；
- \(\in\)：属于；
- \(\omega\in A\)：本次结果属于事件 \(A\) 所包含的结果。

例如：

\[
A=\{2,4,6\}
\]

本次掷出：

\[
\omega=4
\]

因为：

\[
4\in A
\]

所以“掷出偶数”事件发生了。

如果本次掷出：

\[
\omega=3
\]

因为3不属于 \(A\)，所以事件 \(A\) 没有发生。

可以压缩为：

```text
实际结果落入事件集合
           ↓
       事件发生
```

---

# 5. 一个样本点发生，但多个事件可以同时发生

这是一个非常重要的区别。

掷一次骰子，假设实际得到：

\[
\omega=4
\]

定义三个事件：

\[
A=\{2,4,6\}
\]

表示“点数是偶数”。

\[
B=\{4,5,6\}
\]

表示“点数大于3”。

\[
C=\{1,2,3,4,5\}
\]

表示“没有掷出6”。

由于4同时属于 \(A\)、\(B\)、\(C\)，所以本次试验中：

- “点数是偶数”发生了；
- “点数大于3”发生了；
- “没有掷出6”也发生了。

一次试验只实现一个样本点，但这个样本点可能同时满足多个条件。

```text
本次唯一结果：4
       │
       ├── 属于偶数事件
       ├── 属于大于3事件
       └── 属于没有掷出6事件
```

因此：

```text
一个试验实现一个基本结果
≠
一个试验只能发生一个事件
```

---

# 6. 样本点 vs 事件

以掷骰子为例：

| 概念 | 例子 | 描述什么 |
|---|---|---|
| 随机试验 | 掷一次骰子 | 现实中的操作 |
| 样本空间 | \(\{1,2,3,4,5,6\}\) | 全部可能结果 |
| 样本点 | \(4\) | 一个基本结果 |
| 事件 | \(\{2,4,6\}\) | 一组符合条件的结果 |
| 实际结果 | 本次掷出4 | 本次真正实现的样本点 |

最简单的判断方法是：

```text
只有一个最基本结果 → 样本点
包含一组符合条件的结果 → 事件
包含所有可能结果 → 样本空间
```

不过，一个只包含单个样本点的集合，也可以构成事件。这会引出下面的分类。

---

# 7. 基本事件与复合事件

## 基本事件

只包含一个样本点的事件，称为基本事件。

例如：

\[
A=\{4\}
\]

表示“恰好掷出4点”。

注意：

- \(4\) 是一个样本点；
- \(\{4\}\) 是包含该样本点的集合，因此是一个事件。

这两个对象层次不同：

```text
4      → 一个结果
{4}    → 由这个结果构成的事件
```

初学时可以把它们理解为：

- \(4\)：本次可能得到的点数；
- \(\{4\}\)：“是否得到4点”这个问题。

---

## 复合事件

包含两个或更多样本点的事件，称为复合事件。

例如：

\[
B=\{2,4,6\}
\]

表示“掷出偶数”。

它由三个基本结果组合而成。

---

# 8. 必然事件与不可能事件

## 必然事件

整个样本空间 \(\Omega\) 本身也是一个事件，称为必然事件。

掷一次普通骰子：

\[
\Omega=\{1,2,3,4,5,6\}
\]

事件“点数在1到6之间”对应整个样本空间。

无论实际出现哪个结果，它都会发生。

```text
实际结果一定属于 Ω
         ↓
必然事件一定发生
```

“必然”是相对于当前模型而言的。

如果骰子可能碎裂、没有落地或无法读取，而模型没有考虑这些情况，那么模型中的“必然”未必覆盖现实中的全部意外。

---

## 不可能事件

不包含任何样本点的集合称为空集，记作：

\[
\varnothing
\]

它也叫不可能事件。

例如，掷一个普通六面骰子时：

> 掷出7点。

没有任何样本点能让这句话成立，因此对应：

\[
\varnothing
\]

注意：

> 不可能事件不是“概率很小的事件”，而是在当前样本空间中根本没有对应结果的事件。

例如连续十次都掷出6点非常罕见，但仍然可能发生，所以它不是不可能事件。

---

# 9. 事件之间的三种基本运算

现实问题中经常出现三个词：

```text
或
并且
不
```

事件运算就是把这些语言翻译成集合关系。

---

## 9.1 “A或B发生”：事件的并

定义：

\[
A\cup B
\]

读作“\(A\) 并 \(B\)”或“\(A\) 与 \(B\) 的并集”。

它表示：

> \(A\) 发生，或者 \(B\) 发生，或者二者同时发生。

注意，数学中的“或”通常包含“二者同时发生”。

设：

\[
A=\{2,4,6\}
\]

表示“偶数”。

\[
B=\{4,5,6\}
\]

表示“大于3”。

那么：

\[
A\cup B=\{2,4,5,6\}
\]

为什么？

- 2满足 \(A\)；
- 4和6同时满足 \(A\)、\(B\)；
- 5满足 \(B\)；
- 这些结果只需满足至少一个条件，就属于并集。

```text
A ∪ B = 至少满足一个条件
```

---

## 9.2 “A和B同时发生”：事件的交

定义：

\[
A\cap B
\]

读作“\(A\) 交 \(B\)”或“\(A\) 与 \(B\) 的交集”。

它表示：

> \(A\) 和 \(B\) 同时发生。

仍然使用：

\[
A=\{2,4,6\},\qquad B=\{4,5,6\}
\]

那么：

\[
A\cap B=\{4,6\}
\]

因为4和6既是偶数，又大于3。

```text
A ∩ B = 同时满足两个条件
```

---

## 9.3 “A没有发生”：事件的补

事件 \(A\) 的补事件常记作：

\[
A^c
\]

读作“\(A\) 的补事件”。

它表示：

> 样本空间中所有不属于 \(A\) 的结果。

如果：

\[
\Omega=\{1,2,3,4,5,6\}
\]

且：

\[
A=\{2,4,6\}
\]

那么：

\[
A^c=\{1,3,5\}
\]

因为不属于偶数事件的结果恰好是奇数。

```text
A：发生偶数
Aᶜ：没有发生偶数，也就是发生奇数
```

补事件必须相对于一个明确的样本空间来定义。样本空间不同，“不属于 \(A\)”所包含的结果也可能不同。

---

# 10. 用区域理解事件运算

```text
样本空间 Ω
┌──────────────────────────────┐
│                              │
│       ______      ______      │
│      /      \____/      \     │
│     /   A   /共同\   B    \    │
│     \      \区域/        /    │
│      \______\__/________/     │
│                              │
└──────────────────────────────┘
```

- \(A\cup B\)：两个圆覆盖的全部区域；
- \(A\cap B\)：两个圆中间重叠的区域；
- \(A^c\)：样本空间内、但圆 \(A\) 外面的区域。

这三个操作以后会直接进入概率计算：

- “至少一个发生”通常对应并集；
- “同时发生”通常对应交集；
- “没有发生”通常对应补集。

---

# 11. 互斥事件

如果两个事件不能在一次试验中同时发生，它们称为互斥事件。

数学上表示：

\[
A\cap B=\varnothing
\]

这里：

- \(A\cap B\)：同时满足 \(A\) 和 \(B\) 的结果；
- \(\varnothing\)：没有任何结果；
- 整个式子表示：不存在让 \(A\) 和 \(B\) 同时发生的样本点。

例如掷一次骰子：

\[
A=\{1,3,5\}
\]

表示“奇数”。

\[
B=\{2,4,6\}
\]

表示“偶数”。

一个点数不可能既是奇数又是偶数，所以：

\[
A\cap B=\varnothing
\]

于是 \(A\) 与 \(B\) 互斥。

---

# 12. 互斥不等于独立

现在只建立边界，独立性将在后面详细学习。

- **互斥**讨论：两个事件能不能同时发生；
- **独立**讨论：一个事件发生后，是否改变我们对另一个事件可能性的判断。

例如掷一次骰子：

- “掷出奇数”和“掷出偶数”互斥；
- 如果已经知道掷出了奇数，那么“掷出偶数”就不可能发生。

它们显然会相互影响，因此通常不是独立的。

```text
互斥：不能一起发生
独立：一个是否发生不影响另一个
```

除特殊的不可能事件外，两个有可能发生的互斥事件通常不会独立。

---

# 13. 一个完整例子：连续抛两次硬币

随机试验：

> 连续抛两次硬币，记录先后顺序。

样本空间：

\[
\Omega=
\{\text{正正},\text{正反},\text{反正},\text{反反}\}
\]

定义事件 \(A\)：

> 至少出现一次正面。

逐个检查：

- 正正：满足；
- 正反：满足；
- 反正：满足；
- 反反：不满足。

所以：

\[
A=
\{\text{正正},\text{正反},\text{反正}\}
\]

定义事件 \(B\)：

> 两次结果相同。

所以：

\[
B=\{\text{正正},\text{反反}\}
\]

那么：

### \(A\cap B\)

表示“至少出现一次正面，并且两次结果相同”。

只有：

\[
A\cap B=\{\text{正正}\}
\]

### \(A\cup B\)

表示“至少出现一次正面，或者两次结果相同”。

检查四个结果后发现，每一个至少满足一个条件：

\[
A\cup B=\Omega
\]

### \(A^c\)

表示“没有出现正面”。

所以：

\[
A^c=\{\text{反反}\}
\]

这里仍然没有计算概率。我们只是先把自然语言准确翻译成事件集合。

---

# 14. 反例与常见错误

## 错误一：把事件和事件发生混为一谈

事件 \(A=\{2,4,6\}\) 在掷骰子前就已经定义好了。

实验结束后，我们才判断它是否发生。

```text
实验前：事件已经存在
实验后：确定事件是否发生
```

事件不是只有发生以后才存在。

---

## 错误二：认为一次试验只能发生一个事件

一次试验只有一个实际样本点，但这个样本点可以属于多个事件。

掷出4点时，“偶数”“大于3”“不是6”可以同时发生。

---

## 错误三：把“或”误解成只能二选一

数学中的：

\[
A\cup B
\]

通常允许 \(A\) 与 \(B\) 同时发生。

只有明确说“恰好一个发生”时，才需要排除二者共同发生的情况。

---

## 错误四：把低概率事件称为不可能事件

“不可能事件”表示没有任何样本点能让它发生。

低概率只表示不常发生，并不表示绝对不会发生。

---

## 错误五：把互斥理解成独立

互斥意味着一个发生时另一个不能发生，这恰恰说明二者存在强烈关系，而不是“互不影响”。

---

# 15. 与已有知识连接

今天的知识可以概括为：

```text
样本空间
+
一个我们关心的条件
=
事件
```

完整关系是：

```text
现实问题：
“骰子是否出现偶数？”
          ↓
样本空间：
{1,2,3,4,5,6}
          ↓
挑出让问题成立的结果：
{2,4,6}
          ↓
得到事件 A
```

因此：

- 样本空间回答“什么结果可能出现”；
- 事件回答“哪些结果属于我们关心的情况”；
- 下一课的概率将回答“这个事件发生的可能性有多大”。

---

# 16. 实际应用

## 机器学习

在分类问题中可以定义：

- \(A\)：模型预测正确；
- \(B\)：样本属于某个类别；
- \(A\cap B\)：样本属于该类别，并且模型预测正确；
- \(A^c\)：模型预测错误。

准确率、召回率等指标，本质上都在研究不同事件之间的关系。

---

## 计算机视觉

可以定义：

- \(A\)：图像中存在行人；
- \(B\)：检测器报告存在行人。

于是：

- \(A\cap B\)：确实有行人，并且成功检测；
- \(A\cap B^c\)：确实有行人，但检测器漏检；
- \(A^c\cap B\)：没有行人，但检测器误报。

---

## 工程可靠性

设：

- \(A\)：电源系统故障；
- \(B\)：冷却系统故障。

那么：

- \(A\cup B\)：至少一个系统故障；
- \(A\cap B\)：两个系统同时故障；
- \(A^c\cap B^c\)：两个系统都没有故障。

---

# 理解检查

设掷一次骰子的样本空间为：

\[
\Omega=\{1,2,3,4,5,6\}
\]

定义：

\[
A=\{2,4,6\},\qquad B=\{4,5,6\}
\]

请先自己判断：

1. 如果本次掷出4，哪些事件发生了？
2. \(A\cap B\) 包含哪些结果？
3. \(A\cup B\) 包含哪些结果？
4. \(A^c\) 包含哪些结果？
5. \(A\) 与 \(B\) 是否互斥？为什么？
6. “掷出4点”作为样本点应写成 \(4\)，作为事件应写成什么？

# 这一节真正需要记住的核心

1. 事件是样本空间的一个子集，也就是让某个问题成立的一组基本结果。  
2. 一次试验只实现一个样本点，但这个样本点可以使多个事件同时发生。  
3. \(A\cup B\) 表示至少一个发生，\(A\cap B\) 表示同时发生，\(A^c\) 表示 \(A\) 没有发生。  
4. 必然事件是整个样本空间，不可能事件是空集；低概率事件不等于不可能事件。  
5. 互斥表示不能同时发生，独立表示互不影响，这两个概念不能混淆。

下一步自然进入“概率”：现在我们已经能够准确表达事件，接下来要解决的是——如何给事件分配一个数字，表示它发生的可能性大小？