# 第二十三课：协方差——两个随机变量会不会“偏向同一个方向一起变化”？

上一课我们学习了方差：

$$
\operatorname{Var}(X)
=
E[(X-E[X])^2]
$$

它回答：

> **一个随机变量 $X$ 自己围绕均值波动得有多厉害？**

但现实问题通常不止一个随机变量。

例如：

$$
X=\text{身高}
$$

$$
Y=\text{体重}
$$

我们不再只想知道：

> 身高自己的波动有多大？

或者：

> 体重自己的波动有多大？

而会进一步问：

> **一个人的身高比平均身高高时，他的体重是否也倾向于比平均体重高？**

这就从“一个变量自己的波动”，进入：

$$
\boxed{\text{两个变量是否一起波动}}
$$

今天的核心就是：

$$
\boxed{\text{协方差 Covariance}}
$$

---

# 一、先把它放回整条主线

目前我们已经走到：

```text
概率分布
   ↓
期望
“中心在哪里？”
   ↓
方差
“一个变量自己散得多开？”
   ↓
协方差  ← 今天
“两个变量是否一起偏？”
   ↓
相关系数
“这种共同变化到底有多强？”
```

所以今天的新思想可以先压缩成：

$$
\boxed{
\text{协方差}
=
\text{方差思想从一个随机变量推广到两个随机变量}
}
$$

这一点非常重要。

---

# 二、问题背景：只知道两个变量各自的方差，为什么还不够？

假设我们观察两组随机变量：

$$
X=\text{每天气温}
$$

$$
Y=\text{每天冰淇淋销量}
$$

可能出现：

```text
天气较冷 → 销量较低
天气一般 → 销量一般
天气较热 → 销量较高
```

看起来：

$$
X\uparrow
\quad\Rightarrow\quad
Y\uparrow
$$

也就是：

> 两个变量倾向于同方向变化。

---

再换一个例子：

$$
X=\text{商品价格}
$$

$$
Y=\text{销量}
$$

可能出现：

```text
价格较高 → 销量较低
价格较低 → 销量较高
```

也就是：

$$
X\uparrow
\quad\Rightarrow\quad
Y\downarrow
$$

它们倾向于反方向变化。

---

再看第三种：

$$
X=\text{今天骰子的点数}
$$

$$
Y=\text{另一枚独立骰子的点数}
$$

第一枚大还是小，并没有明显告诉我们第二枚会大还是小。

那么：

> 两者可能没有明显的线性共同变化趋势。

---

所以我们需要一个量区分：

```text
一起大、一起小

一个大、另一个小

没有明显共同方向
```

这就是协方差产生的原因。

---

# 三、先不要公式：什么叫“两个变量一起偏”？

假设：

$$
E[X]=170
$$

表示平均身高170cm。

以及：

$$
E[Y]=65
$$

表示平均体重65kg。

现在观察一个人：

$$
X=180,\qquad Y=75
$$

那么：

$$
X-E[X]=180-170=10
$$

而：

$$
Y-E[Y]=75-65=10
$$

两个都是正数。

意思是：

```text
身高比平均高
+
体重也比平均高
```

它们在**同方向偏离各自中心**。

---

再观察另一个人：

$$
X=160,\qquad Y=55
$$

则：

$$
X-E[X]=-10
$$

$$
Y-E[Y]=-10
$$

两个都是负数。

也就是说：

```text
身高比平均低
+
体重也比平均低
```

虽然现在都向“左”偏，但仍然是：

> **同方向偏离。**

---

# 四、一个非常自然的问题：怎样把“同方向”变成数字？

我们已经有：

$$
X-E[X]
$$

以及：

$$
Y-E[Y]
$$

现在想判断：

> 两个偏差符号是不是一样？

最自然的方法是什么？

**相乘。**

因为：

### 情况 1：两个都高于平均

$$
(+)\times(+)=+
$$

---

### 情况 2：两个都低于平均

$$
(-)\times(-)=+
$$

---

### 情况 3：一个高，一个低

$$
(+)\times(-)=-
$$

或者：

$$
(-)\times(+) = -
$$

所以乘积：

$$
\boxed{
(X-E[X])(Y-E[Y])
}
$$

天然就能检测：

> 两个变量偏离均值的方向是否一致。

这就是协方差公式真正的来源。

---

# 五、先看一张最重要的符号表

设：

$$
X-\mu_X
$$

和：

$$
Y-\mu_Y
$$

分别表示两个变量相对各自均值的偏差。

| $X$ 的偏差 | $Y$ 的偏差 | 乘积 | 含义      |
| --------- | --------- | -: | ------- |
| 正         | 正         |  正 | 一起偏大    |
| 负         | 负         |  正 | 一起偏小    |
| 正         | 负         |  负 | 一个大，一个小 |
| 负         | 正         |  负 | 一个小，一个大 |

于是：

$$
\boxed{
\text{正乘积}
\Rightarrow
\text{同方向偏离}
}
$$

$$
\boxed{
\text{负乘积}
\Rightarrow
\text{反方向偏离}
}
$$

这就是整节课最核心的直觉。

---

# 六、但是一次观测不够

假设某一天：

$$
X-\mu_X>0
$$

同时：

$$
Y-\mu_Y>0
$$

只能说明：

> 这一次两个变量一起偏大。

但我们真正想知道的是整个随机机制：

> **长期来看，它们是不是经常一起偏？**

所以我们必须对这个乘积取平均。

也就是：

$$
E[
(X-E[X])(Y-E[Y])
]
$$

于是协方差自然定义出来。

---

# 七、正式数学定义

两个随机变量 $X,Y$ 的协方差定义为：

$$
\boxed{
\operatorname{Cov}(X,Y)
=
E[
(X-E[X])(Y-E[Y])
]
}
$$

我们逐层读。

---

## 第一步

$$
E[X]
$$

找到 $X$ 的中心。

---

## 第二步

$$
X-E[X]
$$

表示 $X$ 本次离自己的中心偏多少。

---

## 第三步

$$
Y-E[Y]
$$

表示 $Y$ 本次离自己的中心偏多少。

---

## 第四步

$$
(X-E[X])(Y-E[Y])
$$

判断：

> 这一次两者是同方向偏，还是反方向偏？

同时也记录偏离有多大。

---

## 第五步

对所有可能情况取期望：

$$
E[
(X-E[X])(Y-E[Y])
]
$$

得到整体平均共同偏离趋势。

所以：

$$
\boxed{
\text{协方差}
=
\text{两个变量相对各自中心的偏差乘积的平均}
}
$$

---

# 八、为什么不是直接研究 $XY$？

这是一个非常重要的问题。

你可能会想：

> 既然两个变量一起大时 $XY$ 也大，为什么不直接用 $E[XY]$？

因为：

$$
XY
$$

会被变量自身的绝对位置严重影响。

---

例如：

### 系统 A

$$
X\approx1000
$$

$$
Y\approx2000
$$

哪怕它们一点共同变化关系都没有，

$$
XY
$$

本身也会很大。

---

协方差先做：

$$
X-E[X]
$$

和：

$$
Y-E[Y]
$$

相当于：

> 把两个变量的中心都搬到0。

变成：

```text
X：
原来中心 μX
↓
平移到0

Y：
原来中心 μY
↓
平移到0
```

然后才研究：

> 它们是怎样围绕中心一起偏离的。

所以：

$$
\boxed{
\text{协方差研究的是共同波动，而不是两个变量绝对值的乘积大小}
}
$$

---

# 九、从图像看协方差：正协方差

假设数据大致是：

```text
Y
↑
|
|                 ●
|             ●
|          ●
|       ●
|    ●
| ●
+----------------------------→ X
```

大体趋势：

$$
X\text{ 大}
\Rightarrow
Y\text{ 也大}
$$

并且：

$$
X\text{ 小}
\Rightarrow
Y\text{ 也小}
$$

因此，大量点会出现：

```text
X偏差正，Y偏差正
或
X偏差负，Y偏差负
```

乘积大多为正。

所以：

$$
\boxed{
\operatorname{Cov}(X,Y)>0
}
$$

叫：

> **正协方差。**

直觉：

$$
\boxed{\text{同涨同跌}}
$$

这里“涨跌”只是方向直觉，不一定真的对应时间变化。

---

# 十、负协方差

例如：

```text
Y
↑
|
| ●
|    ●
|       ●
|          ●
|             ●
|                 ●
+----------------------------→ X
```

大体：

$$
X\text{ 越大}
\Rightarrow
Y\text{ 越小}
$$

于是常见的是：

```text
X偏正，Y偏负
或
X偏负，Y偏正
```

乘积主要是负数。

所以：

$$
\boxed{
\operatorname{Cov}(X,Y)<0
}
$$

叫负协方差。

直觉：

$$
\boxed{\text{一个偏大时，另一个倾向偏小}}
$$

---

# 十一、协方差接近0是什么画面？

例如：

```text
Y
↑
|       ●      ●
|  ●                 ●
|           ●
|      ●          ●
| ●               ●
|           ●
+----------------------------→ X
```

没有清晰的向上或向下线性方向。

可能：

$$
\operatorname{Cov}(X,Y)\approx0
$$

意思是：

> 平均来看，同向偏离和反向偏离大致抵消。

但这里马上有一个重要边界：

$$
\boxed{
\operatorname{Cov}(X,Y)=0
}
$$

**不等于**

$$
\boxed{
X,Y\text{ 没有任何关系}
}
$$

后面会专门讲这个反例。

---

# 十二、最简单的离散例子：完全同向

假设：

$$
X=
\begin{cases}
-1,&0.5\\
1,&0.5
\end{cases}
$$

并定义：

$$
Y=X
$$

所以可能情况只有：

| $X$ | $Y$ |  概率 |
| ----: | ----: | --: |
|    -1 |    -1 | 0.5 |
|     1 |     1 | 0.5 |

先算期望：

$$
E[X]=0
$$

$$
E[Y]=0
$$

因此：

$$
\operatorname{Cov}(X,Y)
=
E[XY]
$$

因为中心就是0。

---

当：

$$
X=-1,Y=-1
$$

乘积：

$$
XY=1
$$

当：

$$
X=1,Y=1
$$

乘积也是：

$$
1
$$

所以：

$$
E[XY]
=
1\times0.5+1\times0.5
=
1
$$

因此：

$$
\boxed{
\operatorname{Cov}(X,Y)=1
}
$$

符合直觉：

> 两者完全同方向变化。

---

# 十三、完全反向的例子

仍然：

$$
X=
\begin{cases}
-1,&0.5\\
1,&0.5
\end{cases}
$$

但定义：

$$
Y=-X
$$

于是：

| $X$ | $Y$ |  概率 |
| ----: | ----: | --: |
|    -1 |     1 | 0.5 |
|     1 |    -1 | 0.5 |

仍然：

$$
E[X]=E[Y]=0
$$

但：

$$
XY=-1
$$

永远成立。

所以：

$$
\operatorname{Cov}(X,Y)
=
E[XY]
=-1
$$

因此：

$$
\boxed{
\operatorname{Cov}(X,Y)=-1
}
$$

完全符合：

> 一个向上，另一个就向下。

---

# 十四、方差其实就是“自己和自己的协方差”

现在看：

$$
\operatorname{Cov}(X,X)
$$

根据定义：

$$
=
E[
(X-E[X])(X-E[X])
]
$$

也就是：

$$
=
E[
(X-E[X])^2
]
$$

这不就是：

$$
\operatorname{Var}(X)
$$

吗？

所以：

$$
\boxed{
\operatorname{Cov}(X,X)
=
\operatorname{Var}(X)
}
$$

这是一个非常漂亮的关系。

也就是说：

> **方差其实是协方差的特殊情况。**

---

可以这样理解：

```text
方差：
X 和自己怎么一起波动？

协方差：
X 和另一个变量 Y 怎么一起波动？
```

因此：

$$
\boxed{
\text{Covariance 是 Variance 的二维推广}
}
$$

---

# 十五、另一个非常重要的公式

教材经常写：

$$
\boxed{
\operatorname{Cov}(X,Y)
=
E[XY]-E[X]E[Y]
}
$$

和方差一样，不要背。

我们从定义推出来。

---

# 十六、从定义一步一步展开

从：

$$
\operatorname{Cov}(X,Y)
=
E[(X-E[X])(Y-E[Y])]
$$

开始。

为了简化，设：

$$
\mu_X=E[X]
$$

$$
\mu_Y=E[Y]
$$

那么：

$$
\operatorname{Cov}(X,Y)
=
E[(X-\mu_X)(Y-\mu_Y)]
$$

---

先展开括号：

$$
(X-\mu_X)(Y-\mu_Y)
$$

得到：

$$
XY-\mu_XY-\mu_YX+\mu_X\mu_Y
$$

所以：

$$
\operatorname{Cov}(X,Y)
=
E[
XY-\mu_XY-\mu_YX+\mu_X\mu_Y
]
$$

利用期望线性性：

$$
=
E[XY]
-\mu_XE[Y]
-\mu_YE[X]
+\mu_X\mu_Y
$$

因为：

$$
E[X]=\mu_X
$$

$$
E[Y]=\mu_Y
$$

所以：

$$
=
E[XY]
-\mu_X\mu_Y
-\mu_Y\mu_X
+\mu_X\mu_Y
$$

中间整理：

$$
-\mu_X\mu_Y
-\mu_X\mu_Y
+\mu_X\mu_Y
=
-\mu_X\mu_Y
$$

得到：

$$
\boxed{
\operatorname{Cov}(X,Y)
=
E[XY]-E[X]E[Y]
}
$$

---

# 十七、这个公式在直觉上说什么？

$$
E[XY]
$$

描述：

> 两个变量乘积的平均。

而：

$$
E[X]E[Y]
$$

表示：

> 如果只把两个变量各自的中心拿出来相乘，会是多少。

二者之差：

$$
E[XY]-E[X]E[Y]
$$

捕捉：

> 实际共同变化有没有超出“只看各自平均水平”能解释的部分。

不过真正建立直觉时，还是建议优先记：

$$
\boxed{
E[(X-E[X])(Y-E[Y])]
}
$$

因为“共同偏离均值”的画面更清晰。

---

# 十八、为什么协方差是对称的？

显然：

$$
(X-E[X])(Y-E[Y])
$$

和：

$$
(Y-E[Y])(X-E[X])
$$

完全相同。

所以：

$$
\boxed{
\operatorname{Cov}(X,Y)
=
\operatorname{Cov}(Y,X)
}
$$

协方差没有方向顺序。

它只描述：

> 两个变量如何共同变化。

---

# 十九、如果 $X,Y$ 独立，协方差是多少？

假设：

$$
X,Y
$$

独立。

独立的重要性质之一是，在适当可积条件下：

$$
E[XY]
=
E[X]E[Y]
$$

因此：

$$
\operatorname{Cov}(X,Y)
=
E[XY]-E[X]E[Y]
$$

变成：

$$
=0
$$

所以：

$$
\boxed{
X,Y\text{ 独立}
\Rightarrow
\operatorname{Cov}(X,Y)=0
}
$$

也就是：

$$
\boxed{
\text{独立} \Rightarrow \text{不相关（零协方差）}
}
$$

但注意方向！

---

# 二十、反过来成立吗？

如果：

$$
\operatorname{Cov}(X,Y)=0
$$

能不能推出：

$$
X,Y\text{ 独立}
$$

？

一般：

$$
\boxed{\text{不能}}
$$

这是概率论中非常重要的区别。

---

# 二十一、经典反例：$Y=X^2$

设：

$$
X
$$

在：

$$
[-1,1]
$$

上对称分布。

定义：

$$
Y=X^2
$$

那么 $X$ 和 $Y$ 有没有关系？

当然有！

因为只要知道：

$$
X
$$

就完全知道：

$$
Y
$$

例如：

$$
X=0.5
$$

则：

$$
Y=0.25
$$

所以它们甚至具有确定性关系：

$$
Y=X^2
$$

显然绝不独立。

---

但它们的协方差可能为0。

为什么？

因为：

$$
\operatorname{Cov}(X,Y)
=
\operatorname{Cov}(X,X^2)
$$

若 $X$ 关于0对称：

$$
E[X]=0
$$

并且：

$$
E[X^3]=0
$$

因为 $x^3$ 是奇函数，正负两侧抵消。

于是：

$$
\operatorname{Cov}(X,X^2)
=
E[X^3]-E[X]E[X^2]
$$

得到：

$$
0-0=0
$$

所以：

$$
\boxed{
\operatorname{Cov}(X,Y)=0
}
$$

但：

$$
Y=X^2
$$

说明二者关系极强。

因此：

$$
\boxed{
\text{零协方差只能说明没有线性的共同偏离趋势}
}
$$

不能说明：

> 完全没有依赖关系。

---

# 二十二、用图像看这个反例

如果：

$$
Y=X^2
$$

散点图：

```text
Y
↑
| ●                 ●
|   ●             ●
|     ●         ●
|       ●     ●
|         ● ●
|          ●
+------------------------→ X
            0
```

这是一条非常明显的抛物线。

关系很强。

但从“向左/向右线性趋势”看：

* 左边 $X$ 增大时 $Y$ 反而下降；
* 右边 $X$ 增大时 $Y$ 上升。

两部分正负贡献抵消。

所以协方差可以为0。

这说明：

$$
\boxed{
\text{协方差主要捕捉线性方向关系}
}
$$

---

# 二十三、独立 vs 不相关

这是今天最重要的概念辨析。

### 独立

问的是：

> 知道 $X$ 的信息，会不会改变对 $Y$ 的概率判断？

这是一个非常强的条件。

---

### 不相关

通常指：

$$
\operatorname{Cov}(X,Y)=0
$$

它只说明：

> 没有平均意义上的线性共同偏离。

因此：

$$
\boxed{
\text{独立}
\Rightarrow
\text{不相关}
}
$$

但一般：

$$
\boxed{
\text{不相关}
\not\Rightarrow
\text{独立}
}
$$

这和第七课的独立性一定要连起来。

---

# 二十四、为什么上一课的方差加法里会出现协方差？

上一课我们提前见过：

$$
\operatorname{Var}(X+Y)
=
\operatorname{Var}(X)
+
\operatorname{Var}(Y)
+
2\operatorname{Cov}(X,Y)
$$

现在终于可以理解它为什么存在。

---

定义：

$$
Z=X+Y
$$

那么：

$$
Z-E[Z]
=
(X-E[X])+(Y-E[Y])
$$

方差：

$$
\operatorname{Var}(X+Y)
=
E[
((X-E[X])+(Y-E[Y]))^2
]
$$

平方展开：

$$
(a+b)^2=a^2+2ab+b^2
$$

所以：

$$
=
E[
(X-E[X])^2
]
$$

$$
+
2E[
(X-E[X])(Y-E[Y])
]
$$

$$
+
E[
(Y-E[Y])^2
]
$$

第一项是：

$$
\operatorname{Var}(X)
$$

第三项是：

$$
\operatorname{Var}(Y)
$$

中间正好是：

$$
2\operatorname{Cov}(X,Y)
$$

所以：

$$
\boxed{
\operatorname{Var}(X+Y)
=
\operatorname{Var}(X)
+
\operatorname{Var}(Y)
+
2\operatorname{Cov}(X,Y)
}
$$

---

# 二十五、为什么协方差会影响“总波动”？

这其实非常直观。

## 如果两个变量同方向变化

例如：

```text
X 高时 Y 也高
X 低时 Y 也低
```

那么：

$$
X+Y
$$

的波动会被强化。

例如：

```text
+5 + +5 = +10
-5 + -5 = -10
```

所以：

$$
\operatorname{Cov}(X,Y)>0
$$

会增加总方差。

---

## 如果两个变量反方向变化

例如：

```text
X +5
Y -5
```

相加：

$$
0
$$

反而互相抵消。

所以负协方差：

$$
\operatorname{Cov}(X,Y)<0
$$

会降低：

$$
X+Y
$$

的波动。

---

这就是投资组合分散风险的一个最基本数学来源：

> 如果不同资产不是总朝同一个方向波动，总组合波动可能下降。

---

# 二十六、一个非常极端的例子

假设：

$$
Y=X
$$

那么：

$$
X+Y=2X
$$

所以：

$$
\operatorname{Var}(X+Y)
=
4\operatorname{Var}(X)
$$

现在用协方差公式：

$$
\operatorname{Var}(X+Y)
=
\operatorname{Var}(X)
+
\operatorname{Var}(Y)
+
2\operatorname{Cov}(X,Y)
$$

因为：

$$
Y=X
$$

所以：

$$
\operatorname{Var}(Y)=\operatorname{Var}(X)
$$

并且：

$$
\operatorname{Cov}(X,Y)
=
\operatorname{Var}(X)
$$

于是：

$$
=
\operatorname{Var}(X)
+\operatorname{Var}(X)
+2\operatorname{Var}(X)
$$

得到：

$$
4\operatorname{Var}(X)
$$

完全一致。

---

# 二十七、反过来：$Y=-X$

如果：

$$
Y=-X
$$

那么：

$$
X+Y=0
$$

所以：

$$
\operatorname{Var}(X+Y)=0
$$

此时：

$$
\operatorname{Cov}(X,Y)
=
\operatorname{Cov}(X,-X)
$$

等于：

$$
-\operatorname{Var}(X)
$$

于是：

$$
\operatorname{Var}(X+Y)
$$

$$
=
\operatorname{Var}(X)
+
\operatorname{Var}(X)
-
2\operatorname{Var}(X)
$$

得到：

$$
0
$$

这说明负协方差确实能让波动相互抵消。

---

# 二十八、协方差最大的问题：数值本身很难直接比较

现在来了一个新的问题。

假设：

$$
X=\text{身高，单位 cm}
$$

$$
Y=\text{体重，单位 kg}
$$

那么：

$$
X-E[X]
$$

单位是：

$$
cm
$$

而：

$$
Y-E[Y]
$$

单位：

$$
kg
$$

相乘之后：

$$
\operatorname{Cov}(X,Y)
$$

单位就是：

$$
\boxed{cm\cdot kg}
$$

这很奇怪。

---

更麻烦的是：

如果把身高从厘米改成米：

$$
X'=\frac{X}{100}
$$

协方差会跟着缩小100倍。

但现实中身高和体重的关系根本没有改变。

所以：

$$
\boxed{
\text{协方差的大小受单位影响}
}
$$

这意味着：

> 协方差的正负很好解释，但绝对数值不方便直接衡量“关系有多强”。

这就是为什么下一步必须进入：

$$
\boxed{\text{相关系数}}
$$

---

# 二十九、协方差的缩放性质

假设：

$$
U=aX+b
$$

$$
V=cY+d
$$

那么：

$$
\boxed{
\operatorname{Cov}(U,V)
=
ac\operatorname{Cov}(X,Y)
}
$$

为什么 $b,d$ 不影响？

因为加常数只是平移中心：

```text
全部身高 +100
```

不会改变共同波动模式。

而乘：

$$
a,c
$$

会改变偏差尺度。

所以协方差乘：

$$
ac
$$

这也再次说明：

> 协方差受到变量尺度影响。

---

# 三十、机器学习里协方差是什么意思？

假设一个数据集有两个特征：

$$
X_1=\text{图片亮度}
$$

$$
X_2=\text{对比度}
$$

如果：

$$
\operatorname{Cov}(X_1,X_2)>0
$$

可能意味着：

> 高于平均亮度的样本，往往也具有高于平均水平的对比度。

---

如果：

$$
\operatorname{Cov}(X_1,X_2)<0
$$

可能意味着：

> 一个特征偏大时，另一个倾向偏小。

---

大量特征一起研究时，就会形成：

$$
\boxed{\text{协方差矩阵}}
$$

它是：

* PCA；
* 高斯分布；
* 多元统计；
* Kalman Filter；
* 特征去相关；
* whitening；

的重要基础。

不过我们现在先不要进入矩阵。

---

# 三十一、二维正态分布为什么会需要协方差？

之前的单变量正态：

$$
X\sim\mathcal N(\mu,\sigma^2)
$$

只需要：

```text
中心 μ
+
波动 σ²
```

但如果同时研究：

$$
(X,Y)
$$

只知道：

$$
\operatorname{Var}(X)
$$

和：

$$
\operatorname{Var}(Y)
$$

仍然不够。

因为我们还不知道：

> 两个变量如何一起变化。

例如二维数据可能是：

```text
      /
     /
    /
```

或者：

```text
    \
     \
      \
```

也可能：

```text
   ○○○
 ○○○○○
   ○○
```

所以多维正态分布需要：

$$
\boxed{\text{协方差矩阵}}
$$

来描述不同方向的联合波动。

这就是协方差为什么是进入多维概率世界的重要入口。

---

# 三十二、一个容易犯的错误：正协方差不等于因果关系

假设：

$$
\operatorname{Cov}(X,Y)>0
$$

只能说明：

> $X,Y$ 具有同方向线性共同变化趋势。

不能说明：

$$
X\rightarrow Y
$$

更不能说明：

> $X$ 导致了 $Y$。

例如：

```text
气温升高
↓
冰淇淋销量升高

气温升高
↓
游泳人数升高
```

于是：

> 冰淇淋销量和游泳人数可能正相关。

但不代表：

> 吃冰淇淋导致人去游泳。

可能存在第三个变量：

$$
\text{气温}
$$

同时影响两者。

所以：

$$
\boxed{
\text{相关 / 协方差}
\neq
\text{因果}
}
$$

---

# 三十三、把方差与协方差完全统一

现在我们可以用一张表理解。

| 对象     | 方差                | 协方差                                               |
| ------ | ----------------- | ------------------------------------------------- |
| 研究几个变量 | 1个                | 2个                                                |
| 公式     | $E[(X-E[X])^2]$ | $E[(X-E[X])(Y-E[Y])]$                           |
| 问的问题   | $X$ 自己散多开？      | $X,Y$ 是否一起偏？                                    |
| 正负     | 一定 $\ge0$       | 可以正、负、0                                           |
| 特殊关系   | —                 | $\operatorname{Cov}(X,X)=\operatorname{Var}(X)$ |

所以：

$$
\boxed{
\operatorname{Var}(X)
=
\operatorname{Cov}(X,X)
}
$$

是理解二者关系最值得记的一条公式。

---

# 三十四、协方差的几何脑内画面

以后看到：

$$
\operatorname{Cov}(X,Y)
$$

脑子里不要先出现公式。

先出现一个二维平面。

中心：

$$
(E[X],E[Y])
$$

把这个中心当成原点：

```text
                Y比均值大
                    ↑
           II       │       I
                    │
       X低,Y高      │     X高,Y高
                    │
────────────────────┼────────────→ X比均值大
                    │
       X低,Y低      │     X高,Y低
                    │
          III       │       IV
                    ↓
```

注意四个象限：

### 第一象限

$$
X-\mu_X>0,\quad Y-\mu_Y>0
$$

乘积：

$$
+
$$

---

### 第三象限

$$
X-\mu_X<0,\quad Y-\mu_Y<0
$$

乘积：

$$
+
$$

所以 I、III 象限支持：

$$
\text{正协方差}
$$

---

### 第二象限

一正一负。

乘积：

$$
-
$$

### 第四象限

也是一正一负。

乘积：

$$
-
$$

所以 II、IV 象限支持：

$$
\text{负协方差}
$$

---

于是你可以把协方差理解成：

> **概率质量更多落在 I、III 象限，还是 II、IV 象限？**

这可能是协方差最强的几何直觉。

---

# 三十五、理解检查

先不要机械代公式。

### 1

如果：

$$
X>E[X]
$$

并且：

$$
Y>E[Y]
$$

那么：

$$
(X-E[X])(Y-E[Y])
$$

是正还是负？

它对协方差产生什么方向的贡献？

---

### 2

如果：

$$
X<E[X]
$$

同时：

$$
Y<E[Y]
$$

为什么仍然对协方差产生**正贡献**？

---

### 3

如果：

$$
Y=X
$$

为什么：

$$
\operatorname{Cov}(X,Y)
=
\operatorname{Var}(X)
$$

？

---

### 4

如果：

$$
Y=-X
$$

协方差应该倾向于正还是负？

为什么？

---

### 5

如果：

$$
X,Y
$$

独立，那么：

$$
\operatorname{Cov}(X,Y)
$$

是多少？

---

### 6

如果：

$$
\operatorname{Cov}(X,Y)=0
$$

能否说明 $X,Y$ 独立？

为什么：

$$
Y=X^2
$$

是一个重要反例？

---

### 7 —— 最重要

你能否不用公式解释：

> 为什么协方差一定要计算

$$
(X-E[X])(Y-E[Y])
$$

而不是直接计算：

$$
XY
$$

？

核心应该是：

> 我们不是在研究两个变量的绝对大小，而是在先消除各自中心以后，研究**二者是否倾向于同方向偏离自己的中心**。

---

# 这一节真正需要记住的核心

只需要牢牢记住下面 **5 句话**：

1. **方差研究一个随机变量自己怎样偏离均值；协方差研究两个随机变量是否倾向于一起偏离各自均值。**

2. 协方差定义为：

$$
\boxed{
\operatorname{Cov}(X,Y)
=
E[(X-E[X])(Y-E[Y])]
}
$$

之所以相乘，是因为：

$$
(+)(+),(-)(-)\rightarrow+
$$

表示同方向偏离；

$$
(+)(-),(-)(+)\rightarrow-
$$

表示反方向偏离。

3. 可以推导得到：

$$
\boxed{
\operatorname{Cov}(X,Y)
=
E[XY]-E[X]E[Y]
}
$$

而：

$$
\boxed{
\operatorname{Cov}(X,X)
=
\operatorname{Var}(X)
}
$$

4. 如果 $X,Y$ 独立，则：

$$
\boxed{
\operatorname{Cov}(X,Y)=0
}
$$

但反过来一般不成立：

$$
\boxed{
\text{零协方差}
\not\Rightarrow
\text{独立}
}
$$

因为协方差主要捕捉的是**线性共同变化趋势**。

5. 协方差的正负容易解释，但大小受单位影响。因此它还不能公平地比较不同变量之间“关系究竟有多强”。

---

所以下一步就非常自然了：

> 身高和体重的协方差是 $50$，温度和耗电量的协方差是 $3$，能不能说前者关系更强？

**不能。**

因为单位、尺度完全不同。

我们必须把协方差进一步“标准化”，消除 $X$ 和 $Y$ 各自的波动尺度：

$$
\boxed{
\rho_{X,Y}
=
\frac{\operatorname{Cov}(X,Y)}
{\sigma_X\sigma_Y}
}
$$

这就是下一课：

$$
\boxed{\text{相关系数 Correlation Coefficient}}
$$

它会把今天的“共同变化方向”，进一步变成一个统一落在：

$$
[-1,1]
$$

之间的无量纲强度指标。
