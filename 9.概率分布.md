# 第九课：概率分布——随机变量的“概率地图”

上一阶段，我们完成的是：

```text
现实随机现象
   ↓
样本空间 Ω
   ↓
事件
   ↓
概率
   ↓
条件概率
   ↓
独立性
   ↓
随机变量 X
```

从随机变量开始，我们已经进入了“数字世界”。整个课程地图下一步自然就是：

```text
随机变量
   ↓
概率分布  ← 第九课
   ↓
分布函数
   ↓
离散型 / 连续型随机变量
   ↓
常见概率分布
   ↓
期望、方差……
```

这正是最初知识地图中规划的第二阶段。

今天只解决一个核心问题：

> **随机变量已经把随机结果变成数字了，为什么还需要“概率分布”？**

---

## 一、问题背景：只有随机变量，还缺了什么？

先看一个最简单的问题。

连续抛两次公平硬币。

样本空间：

$$
\Omega=
\{\text{正正},\text{正反},\text{反正},\text{反反}\}
$$

我们定义随机变量：

$$
X=\text{两次抛掷中正面出现的次数}
$$

于是：

```text
样本点 ω          X(ω)

正正      ─────→    2
正反      ─────→    1
反正      ─────→    1
反反      ─────→    0
```

所以 $X$ 的可能取值是：

$$
0,\ 1,\ 2
$$

到这里，随机变量解决了一个非常重要的问题：

> 把原本的“正正、正反、反正、反反”转成可以计算的数字。

这也正是我们最开始知识地图里的“数字世界”：随机变量负责把样本结果映射成数字。

但是现在我问你：

> $X=0,1,2$，知道这三个数字以后，你是否已经完整了解这个随机变量？

**还没有。**

因为我们还不知道：

```text
0 有多容易出现？
1 有多容易出现？
2 有多容易出现？
```

也就是：

$$
P(X=0)=?
$$

$$
P(X=1)=?
$$

$$
P(X=2)=?
$$

这才是“概率分布”要解决的问题。

---

# 二、为什么仅仅知道“可能取值”远远不够？

假设有两个随机变量：

```text
X 可能取：0、1、2
Y 也可能取：0、1、2
```

看起来它们似乎一样。

但进一步告诉你：

### 随机变量 X

$$
P(X=0)=0.01
$$

$$
P(X=1)=0.98
$$

$$
P(X=2)=0.01
$$

### 随机变量 Y

$$
P(Y=0)=0.49
$$

$$
P(Y=1)=0.02
$$

$$
P(Y=2)=0.49
$$

虽然它们都只能取：

$$
0,1,2
$$

但它们的随机行为完全不同。

画出来：

```text
X：

0        1                           2
|        |                           |
█        ████████████████████        █
0.01     0.98      0.01
```

而 $Y$：

```text
0                  1         2
|                  |         |
██████████         █         ██████████
0.49              0.02       0.49
```

所以：

> **随机变量可能取什么值，只告诉我们“可能性候选名单”。**

而：

> **概率分布告诉我们这些值分别拥有多少概率重量。**

这就是今天最重要的直觉。

---

# 三、最核心的画面：概率从样本空间“搬到”数轴上

这节最建议你建立这个脑内模型。

以前我们已经知道：

> 概率就是分配给事件的“可能性重量”。

例如抛两次公平硬币：

```text
样本空间 Ω

正正      正反      反正      反反
 ↓         ↓         ↓         ↓
1/4       1/4       1/4       1/4
```

现在随机变量 $X$ 做了一件事情：

```text
正正 ─────→ 2
正反 ─────→ 1
反正 ─────→ 1
反反 ─────→ 0
```

注意关键地方：

**正反和反正虽然是两个不同样本点，但都被 $X$ 映射成了数字1。**

所以它们携带的概率也会汇合到1上：

```text
样本空间                         数轴

反反  1/4 ───────────────────→ 0

正反  1/4 ────────┐
                  ├──────────→ 1
反正  1/4 ────────┘

正正  1/4 ───────────────────→ 2
```

于是数轴上的概率重量变成：

$$
0\quad\longrightarrow\quad \frac14
$$

$$
1\quad\longrightarrow\quad \frac14+\frac14=\frac12
$$

$$
2\quad\longrightarrow\quad \frac14
$$

也就是：

| $X$ 的取值 |      概率 |
| --------: | ------: |
|         0 | $1/4$ |
|         1 | $1/2$ |
|         2 | $1/4$ |

这张表就是这个随机变量最基本的**概率分布**。

---

# 四、所以概率分布究竟是什么？

先不要急着记正式定义。

把三个对象分开：

```text
样本空间 Ω
告诉我们：
现实实验可能出现哪些基本结果

随机变量 X
告诉我们：
每个基本结果被转换成哪个数字

X 的概率分布
告诉我们：
这些数字分别获得多少概率重量
```

因此可以暂时把概率分布理解为：

> **概率分布描述随机变量各种可能取值，以及概率重量如何分配在这些取值上。**

第一课其实已经提前给出了这条主线：

```text
随机结果
   ↓
随机变量把结果变成数字
   ↓
研究这些数字分别有多大可能出现
   ↓
概率分布
```



---

# 五、最关键的公式是怎么“长出来”的？

现在才进入数学表达。

假设随机变量是：

$$
X:\Omega\to\mathbb R
$$

上一课如果已经建立随机变量的概念，那么这句话表示：

> 对每个样本点 $\omega$，随机变量 $X$ 都给它指定一个实数 $X(\omega)$。

现在我们想问：

> $X$ 取到某个具体数字 $x$ 的概率是多少？

写成：

$$
P(X=x)
$$

但是要注意：

**概率 $P$ 原本是给“事件”计算概率的。**

第五课中我们定义的就是：

$$
P(A)
$$

其中 $A$ 必须是样本空间中的一个事件。

问题来了：

$$
X=x
$$

看起来是一条关于数字的等式。

它怎么变成事件？

---

## 第一步：问哪些样本点会导致 $X=x$

我们去样本空间里寻找：

$$
\{\omega\in\Omega:X(\omega)=x\}
$$

先逐个解释。

### $\omega$

表示一个样本点。

### $\omega\in\Omega$

表示：

> 在样本空间所有可能结果中寻找。

### $X(\omega)$

表示：

> 样本点 $\omega$ 经过随机变量 $X$ 映射以后得到的数字。

### $X(\omega)=x$

表示：

> 这个样本点最终被映射成数字 $x$。

所以：

$$
\{\omega\in\Omega:X(\omega)=x\}
$$

整个集合表示：

> **所有能够让随机变量 $X$ 最终取值为 $x$ 的样本点。**

它是样本空间的一个子集。

也就是说：

> 它是一个事件。

而事件本来就是样本空间中满足某个条件的一组样本点。

---

# 六、于是 $P(X=x)$ 的真正含义出现了

严格一些写：

$$
P(X=x)
=
P\big(\{\omega\in\Omega:X(\omega)=x\}\big)
$$

这条公式特别重要。

千万不要把它当成要死记的新公式。

它其实只是：

```text
我想知道 X=x 的概率
        ↓
概率只能给事件称重
        ↓
所以先找：
哪些样本点会导致 X=x
        ↓
这些样本点组成一个事件
        ↓
计算这个事件的概率
```

因此：

$$
\boxed{
P(X=x)
=
P(\text{所有使 }X=x\text{ 的样本点})
}
$$

---

# 七、完整推导一次：两次抛硬币

继续使用：

$$
\Omega=
\{\text{正正},\text{正反},\text{反正},\text{反反}\}
$$

随机变量：

$$
X=\text{正面出现次数}
$$

---

## 1. 求 $P(X=0)$

先不要计算。

先问：

> 哪些样本点会使正面出现0次？

只有：

$$
\{\text{反反}\}
$$

所以：

$$
\{X=0\}
=
\{\text{反反}\}
$$

于是：

$$
P(X=0)
=
P(\{\text{反反}\})
=
\frac14
$$

---

## 2. 求 $P(X=1)$

问：

> 哪些样本点会使正面恰好出现一次？

两个：

$$
\{\text{正反},\text{反正}\}
$$

所以：

$$
\{X=1\}
=
\{\text{正反},\text{反正}\}
$$

因为公平且两次独立抛掷时，这两个基本结果各占 $1/4$，所以：

$$
P(X=1)
=
\frac14+\frac14
=
\frac12
$$

---

## 3. 求 $P(X=2)$

满足条件的样本点只有：

$$
\{\text{正正}\}
$$

于是：

$$
P(X=2)=\frac14
$$

最终：

$$
\boxed{
P(X=0)=\frac14,\quad
P(X=1)=\frac12,\quad
P(X=2)=\frac14
}
$$

这整组信息，就是 $X$ 的概率分布。

---

# 八、你现在必须区分四层东西

这里是学习随机变量以后最容易混乱的地方。

我们把它们彻底拆开。

## 第一层：随机变量 $X$

$$
X=\text{两次抛硬币中正面出现次数}
$$

它是一个**映射规则**。

不是0，也不是1，也不是2。

---

## 第二层：随机变量的取值 $x$

例如：

$$
x=1
$$

这是一个普通数字。

---

## 第三层：事件 $\{X=1\}$

表示：

> 那些能够让 $X$ 等于1的所有样本点。

在我们的例子中：

$$
\{X=1\}
=
\{\text{正反},\text{反正}\}
$$

它是一个**事件**。

---

## 第四层：概率 $P(X=1)$

这是事件 $\{X=1\}$ 的概率：

$$
P(X=1)=\frac12
$$

它是一个0～1之间的**数字**。

---

因此：

```text
X
随机变量 / 映射

↓

1
随机变量的一个可能取值

↓

{X=1}
一个事件

↓

P(X=1)
这个事件的概率
```

这四层一定不能混。

---

# 九、一个特别重要的新认识：随机变量会“合并”样本点

回头看：

```text
正反 ──→ 1
反正 ──→ 1
```

随机变量并不要求：

> 一个样本点对应一个独一无二的数字。

完全可以有多个样本点映射到同一个数。

事实上，这是随机变量非常重要的作用：

> **把原始随机世界中我们不关心的区别压缩掉。**

第三课其实已经提前出现过这个思想：如果只记录两次抛硬币中正面的总数，那么“正反”和“反正”都会被记录为1；课程当时特别强调，通常先建立足够细的样本空间，再通过随机变量合并信息。

现在我们终于可以更准确地描述它：

```text
原始样本空间
保留全部细节

        ↓ X

随机变量的取值空间
只保留研究需要的信息

        ↓

概率分布
把原来的概率重量一起搬过来
```

---

# 十、概率分布本质上就是“概率的重新组织”

这一句话非常重要。

以前概率存在于样本空间：

```text
ω₁ → 某些概率重量
ω₂ → 某些概率重量
ω₃ → 某些概率重量
……
```

随机变量建立：

$$
\omega\longmapsto X(\omega)
$$

以后，就可以把概率按照 $X$ 的取值重新汇总：

```text
样本空间                     数轴

ω₁ ───┐
ω₂ ───┼────────────→ x₁      得到一些概率
ω₃ ───┘

ω₄ ───┐
ω₅ ───┼────────────→ x₂      得到一些概率
ω₆ ───┘
```

所以从数学本质上说：

> **随机变量的分布，就是样本空间上的概率经过随机变量映射后，在数轴上形成的概率规律。**

以后如果你看到更高级教材说：

> “随机变量诱导了实数轴上的一个概率分布”

其实说的就是这张图。

不用被“诱导”这个词吓到。

它只是：

> **X 把样本点搬到了数字上，概率重量跟着一起搬过去。**

---

# 十一、为什么概率分布比一次观测重要得多？

假设我们实际做了一次实验：

$$
X=1
$$

这只是告诉我们：

> 本次实验结果恰好是1。

它完全没有告诉我们整个随机机制是什么样。

例如下面三个模型：

### 模型 A

$$
P(X=1)=0.99
$$

### 模型 B

$$
P(X=1)=0.50
$$

### 模型 C

$$
P(X=1)=0.01
$$

一次实验中，三个模型都**可能**观察到：

$$
X=1
$$

但这个观测在三个模型里的含义完全不同。

这和我们前面一直强调的思想完全一致：

> 单次结果不能代表整个随机机制。

第二课已经指出，一次实际结果只是众多可能结果中的一次实现，不能代表完整规律。

概率分布研究的正是：

> **整个随机机制，而不是某一次结果。**

---

# 十二、随机变量 vs 概率分布

这是本节最重要的一组区别。

| 概念         | 回答的问题        |
| ---------- | ------------ |
| 随机变量 $X$ | 样本结果怎样转换成数字？ |
| $X$ 的取值  | 最终可能得到哪些数字？  |
| 概率分布       | 各个数字有多大概率出现？ |
| 实际观测值      | 这一次最终得到哪个数字？ |

例如：

```text
抛两次硬币
       ↓
X = 正面次数
       ↓
可能取值：0、1、2
       ↓
概率分布：
0 → 1/4
1 → 1/2
2 → 1/4
       ↓
本次真正实验
       ↓
假设观察到 X=1
```

因此：

$$
\boxed{
\text{随机变量}\neq\text{随机变量的取值}\neq\text{概率分布}
}
$$

---

# 十三、概率 vs 概率分布

另一组特别容易混淆的概念。

## 概率

例如：

$$
P(X=1)=\frac12
$$

只描述一个事件的概率。

它是一条局部信息。

---

## 概率分布

则是：

```text
X=0 → 1/4
X=1 → 1/2
X=2 → 1/4
```

它描述所有可能取值的整体概率结构。

所以：

```text
一个概率
= 一个事件的可能性重量

一个概率分布
= 整个随机变量的概率重量配置
```

可以类比：

```text
一个人的身高
≠
整个班级的身高分布

某一天的温度
≠
全年温度分布

一个概率
≠
整个概率分布
```

---

# 十四、概率分布必须满足什么？

先研究我们现在这种可以列出取值的情形。

假设：

$$
X\in\{x_1,x_2,\ldots\}
$$

每个值对应概率：

$$
p_i=P(X=x_i)
$$

为什么这些 $p_i$ 必须满足两个条件？

---

## 条件一：概率不能为负

因为：

$$
p_i=P(X=x_i)
$$

本质上还是事件的概率。

概率公理要求概率非负。

因此：

$$
p_i\geq0
$$

---

## 条件二：所有可能取值的总概率为1

一次实验以后，$X$ 总会取到它的某个可能值。

例如：

$$
X\in\{0,1,2\}
$$

那么：

```text
X=0
或
X=1
或
X=2
```

必有一个发生。

而它们互相排斥：

> 一次实验中 $X$ 不可能既等于0又等于1。

所以根据互斥事件概率相加，以及整个样本空间概率为1：

$$
P(X=0)+P(X=1)+P(X=2)=1
$$

一般写成：

$$
\sum_i P(X=x_i)=1
$$

这里的：

$$
\sum
$$

只是“把所有这些概率加起来”。

因此：

$$
\boxed{
p_i\geq0,\qquad
\sum_i p_i=1
}
$$

这不是新的神秘规定。

它直接继承自概率本身的公理。

---

# 十五、图像直觉：概率质量放在数轴哪里？

两次抛硬币的分布：

$$
P(X=0)=\frac14
$$

$$
P(X=1)=\frac12
$$

$$
P(X=2)=\frac14
$$

可以想象成：

```text
概率
0.5 ┤          ●
    │          │
    │          │
0.25┤ ●        │        ●
    │ │        │        │
    │ │        │        │
  0 ┼─┼────────┼────────┼────→ X
      0        1        2
```

高度表示：

> 概率重量在不同数值上的分配。

现在先把它看作一张“概率质量图”。

下一些课程我们会进一步区分：

* 离散随机变量怎样描述这种质量；
* 连续随机变量为什么不能简单给每个点分配概率；
* PDF 与这里究竟有什么不同。

今天不要提前混在一起。

---

# 十六、反例一：取值一样，分布可以不同

考虑两个硬币。

## 公平硬币

定义：

$$
X=
\begin{cases}
1,&\text{正面}\\
0,&\text{反面}
\end{cases}
$$

那么：

$$
P(X=1)=0.5
$$

$$
P(X=0)=0.5
$$

---

## 严重偏向正面的硬币

定义同一个随机变量：

$$
Y=
\begin{cases}
1,&\text{正面}\\
0,&\text{反面}
\end{cases}
$$

但假设：

$$
P(Y=1)=0.9
$$

$$
P(Y=0)=0.1
$$

两者可能取值完全相同：

$$
\{0,1\}
$$

但概率分布不同。

所以：

$$
\boxed{
\text{可能取值相同}
\not\Rightarrow
\text{概率分布相同}
}
$$

---

# 十七、反例二：随机变量不同，也可能拥有相同分布

这是稍微高级一点、但非常重要的认识。

抛一枚公平硬币。

定义：

$$
X=
\begin{cases}
1,&\text{正面}\\
0,&\text{反面}
\end{cases}
$$

另一次独立抛硬币定义：

$$
Y=
\begin{cases}
1,&\text{反面}\\
0,&\text{正面}
\end{cases}
$$

从“规则”上看：

$$
X\neq Y
$$

因为它们对正反面的编码方式不同。

但是：

$$
P(X=0)=P(X=1)=\frac12
$$

同样：

$$
P(Y=0)=P(Y=1)=\frac12
$$

所以它们的**概率分布相同**。

这告诉我们：

> 概率分布不记录随机变量到底是怎样从原始样本点构造出来的，它只记录最终数字的概率规律。

也就是：

```text
不同的随机机制
       ↓
有可能产生
       ↓
相同的数值分布
```

---

# 十八、为什么概率论后面越来越少讨论原始样本空间？

现在你应该能看到一个非常大的转折。

最开始我们需要写：

$$
\Omega
$$

因为必须先把现实随机实验变成数学对象。

但是一旦：

1. 定义好了随机变量；
2. 知道了它的概率分布；

那么很多问题都可以直接在数字世界中解决。

例如以后我们会问：

$$
E[X]=?
$$

$$
Var(X)=?
$$

$$
P(X\leq 3)=?
$$

很多时候都不需要每次重新回到：

```text
ω₁、ω₂、ω₃……
```

因为概率分布已经把与 $X$ 有关的概率信息整理到了数轴上。

可以理解成：

```text
原始样本空间
可能极其复杂
      ↓
随机变量
提取我们关心的数字
      ↓
概率分布
保存与这个数字有关的随机规律
      ↓
以后主要研究这个分布
```

这就是随机变量如此重要的原因之一。

---

# 十九、机器学习里，“分布”到底在说什么？

现在终于可以理解机器学习中经常出现的说法：

> “数据来自某个分布。”

先不要把这句话想得特别神秘。

例如图像分类：

```text
现实世界
   ↓
随机产生一个样本
   ↓
(X,Y)
```

其中：

* $X$：图像；
* $Y$：类别标签。

所谓“数据分布”，本质上是在研究：

> 不同的 $X,Y$ 组合出现的可能性规律。

这也是为什么机器学习经常讨论：

```text
训练分布
测试分布
真实数据分布
分布偏移 distribution shift
```

如果训练数据与实际部署环境来自明显不同的概率分布，那么：

> 即使训练集准确率非常高，也不能保证真实环境中的表现。

---

# 二十、深度学习中的概率输出

分类模型可能给出：

```text
猫   0.80
狗   0.15
鸟   0.05
```

可以把类别编码成：

$$
Y\in\{猫,狗,鸟\}
$$

这组数字就在描述一种概率分配：

$$
P(Y=\text{猫})=0.80
$$

$$
P(Y=\text{狗})=0.15
$$

$$
P(Y=\text{鸟})=0.05
$$

它们满足：

$$
0.80+0.15+0.05=1
$$

当然，在真正机器学习中还经常是：

$$
P(Y\mid X=x)
$$

也就是：

> 在已经看到某张具体图片 $x$ 的条件下，标签 $Y$ 如何分布。

这里就重新连接到了前面学过的**条件概率**。

所以知识并不是割裂的：

```text
条件概率
+
随机变量
+
概率分布
=
条件分布
```

后面我们会正式学。

---

# 二十一、把目前所有知识串成一条完整链

还是连续抛两次公平硬币。

### 现实随机现象

```text
连续抛两次硬币
```

↓

### 样本空间

$$
\Omega=
\{\text{正正},\text{正反},\text{反正},\text{反反}\}
$$

样本空间解决“所有基本结果是什么”。

↓

### 事件

例如：

$$
A=\{\text{正正},\text{正反},\text{反正}\}
$$

表示“至少出现一次正面”。

事件是满足某条件的一组样本结果。

↓

### 概率

例如：

$$
P(A)=\frac34
$$

概率给事件分配可能性重量。

↓

### 随机变量

$$
X=\text{正面出现次数}
$$

于是：

```text
正正 → 2
正反 → 1
反正 → 1
反反 → 0
```

↓

### 概率分布

$$
P(X=0)=\frac14
$$

$$
P(X=1)=\frac12
$$

$$
P(X=2)=\frac14
$$

也就是：

> **概率重量现在被重新组织到了数字0、1、2上。**

这一步就是今天第九课的核心。

---

# 二十二、现在先不要混淆“分布”和“分布函数”

我们的课程地图是：

$$
\text{概率分布}
\rightarrow
\text{分布函数}
$$

所以今天只需要知道：

> **概率分布是随机变量整体概率规律的总称。**

至于下一阶段为什么还需要一个函数：

$$
F(x)=P(X\leq x)
$$

以及：

* 为什么不是只研究 $P(X=x)$？
* 为什么连续型随机变量尤其需要 $F(x)$？
* $F(x)$ 到底在数轴上累积什么？

这些留到下一课。

现在不要把：

```text
概率分布
概率质量函数
概率密度函数
分布函数
```

四个概念混为一谈。

它们并不完全是同一个东西。

---

# 二十三、理解检查

先不要看答案，凭现在的直觉判断。

### 1

一个随机变量：

$$
X\in\{0,1,2\}
$$

这是否已经完整说明了 $X$ 的概率分布？

---

### 2

如果：

$$
P(X=1)=0.7
$$

这里的 $X=1$ 从概率论底层看，对应的是什么？

A. 一个普通数字
B. 一个事件
C. 一个概率
D. 一个样本空间

---

### 3

两个随机变量可能取值都是：

$$
\{0,1\}
$$

它们的分布一定相同吗？

---

### 4

为什么：

$$
P(X=1)
$$

可以使用概率符号 $P$？

也就是说：

> “$X=1$”为什么能够作为概率的输入？

---

### 5

两次抛公平硬币，$X$ 表示正面次数。

为什么：

$$
P(X=1)=\frac12
$$

而不是 $\frac14$？

关键不是背结果，而是找到：

> 哪些原始样本点都被 $X$ 映射到了1。

---

# 这一节真正需要记住的核心

压缩到5句话：

1. **随机变量解决“样本结果变成什么数字”，概率分布解决“这些数字分别有多大概率”。**
2. $P(X=x)$ 的底层含义是：找到所有满足 $X(\omega)=x$ 的样本点，再计算这些样本点组成事件的概率。
3. **概率分布可以理解为：随机变量把样本点映射到数轴以后，原来的概率重量也被一起搬到了数轴上。**
4. 随机变量、随机变量的取值、事件 $\{X=x\}$、概率 $P(X=x)$ 是四个不同层次。
5. 可能取值相同不代表分布相同；概率分布描述的是整个随机变量的概率规律，而不是某一次实际观测。

你现在已经从：

```text
事件世界
```

真正进入：

```text
数字世界
      ↓
分布世界
```

下一步自然会产生一个问题：

> **如果随机变量可能有非常多、甚至连续无限多个取值，我们怎样用一个统一的函数描述它整个数轴上的概率规律？**

这就会把我们带到下一课的核心：**分布函数 CDF**。
