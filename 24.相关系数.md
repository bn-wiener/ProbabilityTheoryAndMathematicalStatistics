# 第二十四课：相关系数——两个随机变量“线性一起变化”的强度到底有多大？

上一课我们学习了协方差：

$$
\operatorname{Cov}(X,Y)
=
E[(X-E[X])(Y-E[Y])]
$$

它能告诉我们：

* $X,Y$ 倾向同方向变化，协方差通常为正；
* 倾向反方向变化，协方差通常为负；
* 没有明显线性共同变化趋势，协方差可能接近 0。

但是上一课最后留下了一个问题：

> **协方差的正负很好理解，可是它的绝对大小到底意味着什么？**

例如：

$$
\operatorname{Cov}(X,Y)=50
$$

和：

$$
\operatorname{Cov}(A,B)=3
$$

我们能不能说：

> 第一对变量的关系一定比第二对强？

**不能。**

因为协方差会受到：

* 变量单位；
* 数值尺度；
* 自身波动大小；

的影响。

因此今天要解决：

$$
\boxed{
\text{怎样消除尺度影响，只保留两个变量线性共同变化的“纯强度”？}
}
$$

这就是：

$$
\boxed{\text{相关系数 Correlation Coefficient}}
$$

---

# 一、它在整个知识体系中的位置

目前这一条主线已经非常清楚：

```text
随机变量 X
   ↓
期望 E[X]
中心在哪里？
   ↓
方差 Var(X)
X 自己波动多大？
   ↓
协方差 Cov(X,Y)
X 和 Y 是否一起波动？
   ↓
相关系数 Corr(X,Y) ← 今天
一起波动的方向和强度有多大？
```

所以今天的新思想可以先压缩成：

$$
\boxed{
\text{相关系数}
=
\text{标准化之后的协方差}
}
$$

这句话和第二十课的 Z-score 非常相似。

当时我们把：

$$
x-\mu
$$

除以：

$$
\sigma
$$

得到：

$$
z=\frac{x-\mu}{\sigma}
$$

是在消除单个变量的单位和尺度。

今天我们会对协方差做同样的事情。

---

# 二、问题背景：为什么协方差的大小不能直接比较？

先看一个非常简单的例子。

假设：

$$
X=\text{身高，单位 cm}
$$

$$
Y=\text{体重，单位 kg}
$$

假设：

$$
\operatorname{Cov}(X,Y)=40
$$

单位是什么？

因为：

$$
X-E[X]
$$

单位是：

$$
cm
$$

而：

$$
Y-E[Y]
$$

单位是：

$$
kg
$$

所以协方差单位是：

$$
cm\cdot kg
$$

---

现在只做一件事：

> 把身高从厘米改成米。

定义：

$$
X'=\frac{X}{100}
$$

现实中的人完全没有变化。

身高和体重的关系当然也没有变化。

但是：

$$
\operatorname{Cov}(X',Y)
=
\operatorname{Cov}\left(\frac{X}{100},Y\right)
$$

根据协方差缩放性质：

$$
=
\frac1{100}\operatorname{Cov}(X,Y)
$$

所以：

$$
=0.4
$$

你看：

```text
原来用 cm：
Cov = 40

换成 m：
Cov = 0.4
```

关系明明完全一样，

协方差却变了100倍。

所以：

$$
\boxed{
\text{协方差大小本身不是一个无单位的关系强度}
}
$$

---

# 三、问题到底出在哪里？

协方差：

$$
\operatorname{Cov}(X,Y)
=
E[(X-\mu_X)(Y-\mu_Y)]
$$

实际上同时混合了两种信息：

### 信息一

两个变量是否倾向同方向变化。

### 信息二

两个变量自己本身波动得有多大。

例如，如果 $X$ 的数值尺度扩大100倍：

$$
X\to100X
$$

那么：

$$
X-\mu_X
$$

也扩大100倍。

于是协方差自然也扩大100倍。

所以协方差不仅在测：

> “一起变化得有多紧”。

还受到：

> “每个变量自身尺子有多大”

的影响。

---

# 四、怎样消除这种尺度影响？

回想第二十课的标准化：

$$
Z_X=\frac{X-\mu_X}{\sigma_X}
$$

它表示：

> $X$ 偏离自己的均值多少个标准差。

同理：

$$
Z_Y=\frac{Y-\mu_Y}{\sigma_Y}
$$

表示：

> $Y$ 偏离自己的均值多少个标准差。

现在我们不再比较：

$$
X-\mu_X
$$

和：

$$
Y-\mu_Y
$$

的原始单位偏差。

而比较：

$$
\boxed{
\frac{X-\mu_X}{\sigma_X}
}
$$

与：

$$
\boxed{
\frac{Y-\mu_Y}{\sigma_Y}
}
$$

也就是：

> **两个变量分别偏离自己中心多少个“自身标准波动单位”。**

这样厘米、公斤、元、秒等原始单位就被消掉了。

---

# 五、相关系数的公式是怎么长出来的？

协方差：

$$
\operatorname{Cov}(X,Y)
=
E[(X-\mu_X)(Y-\mu_Y)]
$$

现在把两个偏差分别标准化。

得到：

$$
E\left[
\frac{X-\mu_X}{\sigma_X}
\frac{Y-\mu_Y}{\sigma_Y}
\right]
$$

因为：

$$
\sigma_X,\sigma_Y
$$

都是常数，可以提出期望：

$$
=
\frac{
E[(X-\mu_X)(Y-\mu_Y)]
}{
\sigma_X\sigma_Y
}
$$

而分子就是协方差：

$$
\operatorname{Cov}(X,Y)
$$

因此得到：

$$
\boxed{
\rho_{X,Y}
=
\frac{
\operatorname{Cov}(X,Y)
}{
\sigma_X\sigma_Y
}
}
$$

这就是 Pearson 相关系数。

也常写成：

$$
\boxed{
\operatorname{Corr}(X,Y)
}
$$

---

# 六、每个符号到底是什么意思？

$$
\rho_{X,Y}
=
\frac{
\operatorname{Cov}(X,Y)
}{
\sigma_X\sigma_Y
}
$$

逐个解释。

## $\rho$

希腊字母 rho，读作“柔”。

通常用来表示总体相关系数。

---

## $\operatorname{Cov}(X,Y)$

描述：

> 两个变量是否倾向同方向或反方向偏离各自均值。

---

## $\sigma_X$

$X$ 的标准差：

$$
\sigma_X=\sqrt{\operatorname{Var}(X)}
$$

表示 $X$ 自己的波动尺度。

---

## $\sigma_Y$

同理，是 $Y$ 的波动尺度。

---

所以整个式子可以翻译成：

$$
\boxed{
\text{共同波动}
\div
\text{两个变量自身波动尺度的乘积}
}
$$

也就是：

> **把共同波动放到各自的标准尺度下衡量。**

---

# 七、相关系数还有一个更重要的理解

因为：

$$
Z_X=\frac{X-E[X]}{\sigma_X}
$$

和：

$$
Z_Y=\frac{Y-E[Y]}{\sigma_Y}
$$

都是标准化变量，

所以：

$$
\rho_{X,Y}
=
E[Z_XZ_Y]
$$

为什么？

因为标准化以后：

$$
E[Z_X]=E[Z_Y]=0
$$

而且：

$$
\operatorname{Var}(Z_X)=
\operatorname{Var}(Z_Y)=1
$$

因此：

$$
\boxed{
\rho_{X,Y}
=
\operatorname{Cov}(Z_X,Z_Y)
}
$$

也就是说：

> **相关系数就是两个标准化随机变量之间的协方差。**

这个理解非常重要。

---

# 八、为什么相关系数没有单位？

假设：

$$
X=\text{身高}
$$

单位：

$$
cm
$$

那么：

$$
\sigma_X
$$

也是：

$$
cm
$$

所以：

$$
\frac{X-\mu_X}{\sigma_X}
$$

单位：

$$
\frac{cm}{cm}=1
$$

没有单位。

同理：

$$
\frac{Y-\mu_Y}{\sigma_Y}
$$

也没有单位。

所以：

$$
\rho
$$

最终没有单位。

这意味着：

> 无论你把身高用米还是厘米，相关系数都不变。

这正是我们想要的性质。

---

# 九、用换单位验证一次

假设：

$$
X' =100X
$$

那么：

$$
\operatorname{Cov}(X',Y)
=
100\operatorname{Cov}(X,Y)
$$

同时：

$$
\sigma_{X'}
=
100\sigma_X
$$

所以：

$$
\rho_{X',Y}
=
\frac{
100\operatorname{Cov}(X,Y)
}{
100\sigma_X\sigma_Y
}
$$

100约掉：

$$
=
\frac{
\operatorname{Cov}(X,Y)
}{
\sigma_X\sigma_Y
}
$$

因此：

$$
\boxed{
\rho_{X',Y}=\rho_{X,Y}
}
$$

所以换单位不会改变相关强度。

---

# 十、相关系数为什么一定在 $[-1,1]$ 之间？

这是相关系数最重要的数学性质：

$$
\boxed{
-1\le\rho_{X,Y}\le1
}
$$

为什么？

这个结论来自 Cauchy–Schwarz（柯西—施瓦茨）不等式。

为了看懂推导，我们先回顾标准化变量：

$$
Z_X
=
\frac{X-\mu_X}{\sigma_X}
$$

和：

$$
Z_Y
=
\frac{Y-\mu_Y}{\sigma_Y}
$$

这里假设 $X,Y$ 的方差有限且不为 $0$，这样标准化才有意义。

标准化以后，它们的均值都是 $0$：

$$
E[Z_X]=E[Z_Y]=0
$$

它们的方差都是 $1$：

$$
\operatorname{Var}(Z_X)
=
\operatorname{Var}(Z_Y)
=
1
$$

接下来有一个非常关键、也很容易让人疑惑的结论：

$$
E[Z_X^2]=E[Z_Y^2]=1
$$

下面分几步解释这个结论。

---

## 1. 为什么 $E[Z_X^2]=1$？

根据定义：

$$
Z_X
=
\frac{X-\mu_X}{\sigma_X}
$$

平方以后：

$$
Z_X^2
=
\frac{(X-\mu_X)^2}{\sigma_X^2}
$$

两边取期望：

$$
E[Z_X^2]
=
E\left[
\frac{(X-\mu_X)^2}{\sigma_X^2}
\right]
$$

因为 $\sigma_X^2$ 是常数，所以可以提出：

$$
E[Z_X^2]
=
\frac{E[(X-\mu_X)^2]}{\sigma_X^2}
$$

而方差的定义就是：

$$
E[(X-\mu_X)^2]
=
\operatorname{Var}(X)
=
\sigma_X^2
$$

因此：

$$
E[Z_X^2]
=
\frac{\sigma_X^2}{\sigma_X^2}
=
1
$$

同理：

$$
E[Z_Y^2]=1
$$

所以：

$$
\boxed{
E[Z_X^2]=E[Z_Y^2]=1
}
$$

这其实就是“标准化以后方差为 $1$”的另一种表达。

因为：

$$
\operatorname{Var}(Z_X)
=
E[Z_X^2]-(E[Z_X])^2
$$

又因为 $E[Z_X]=0$，所以：

$$
\operatorname{Var}(Z_X)=E[Z_X^2]=1
$$

---

## 2. 为什么 $E[Z_X]=0$，但 $E[Z_X^2]=1$？

这两个结论并不矛盾。

$E[Z_X]=0$ 是因为正偏差和负偏差可以相互抵消；平方以后，正数和负数都变成非负数，因此不会再相互抵消。

例如，假设随机变量 $Z$ 有一半概率等于 $1$，一半概率等于 $-1$，那么：

$$
E[Z]
=
\frac12\times1
+
\frac12\times(-1)
=
0
$$

但是：

$$
E[Z^2]
=
\frac12\times1^2
+
\frac12\times(-1)^2
=
1
$$

因此：

```text
E[Z] = 0
表示 Z 以 0 为中心

E[Z²] = 1
表示 Z 到中心的平均平方距离为 1
```

需要注意：

> $E[Z_X^2]=1$ 不表示 $Z_X^2$ 每次都等于 $1$，只表示 $Z_X^2$ 的平均值等于 $1$。

---

## 3. 用 Cauchy–Schwarz 不等式限制相关系数

前面已经得到：

$$
\rho_{X,Y}=E[Z_XZ_Y]
$$

Cauchy–Schwarz 不等式告诉我们：

$$
\left|E[Z_XZ_Y]\right|
\le
\sqrt{
E[Z_X^2]E[Z_Y^2]
}
$$

而标准化以后：

$$
E[Z_X^2]=E[Z_Y^2]=1
$$

所以：

$$
|\rho_{X,Y}|
=
\left|E[Z_XZ_Y]\right|
\le
\sqrt{1\times1}
=
1
$$

也就是：

$$
\boxed{
-1\le\rho_{X,Y}\le1
}
$$

---

## 4. 什么时候等于 $1$ 或 $-1$？

如果两个标准化变量每次都完全相同：

$$
Z_Y=Z_X
$$

那么：

$$
Z_XZ_Y=Z_X^2
$$

因此：

$$
\rho
=
E[Z_XZ_Y]
=
E[Z_X^2]
=
1
$$

此时两个变量完全同方向变化，相关系数达到最大值。

如果两个标准化变量每次都完全相反：

$$
Z_Y=-Z_X
$$

那么：

$$
Z_XZ_Y=-Z_X^2
$$

因此：

$$
\rho
=
E[Z_XZ_Y]
=
-E[Z_X^2]
=
-1
$$

此时两个变量完全反方向变化，相关系数达到最小值。

任何不那么完美的同步关系都会落在中间：

$$
-1<\rho<1
$$

所以可以把整个推导概括为：

```text
先把 X、Y 标准化
        ↓
两个变量的平均平方大小都变成 1
        ↓
Cauchy–Schwarz 不等式限制二者乘积的期望
        ↓
相关系数只能位于 [-1,1]
```

---

# 十一、$\rho=1$ 到底意味着什么？

如果：

$$
\rho_{X,Y}=1
$$

不是只表示：

> “正相关很强”。

而是在方差有限且非零的条件下表示：

> 两个变量具有**完全正线性关系**。

为什么能从 $\rho=1$ 推出完全的线性关系？

关键是考察两个标准化变量之差的平方：

$$
(Z_X-Z_Y)^2
$$

它的期望为：

$$
\begin{aligned}
E[(Z_X-Z_Y)^2]
&=E[Z_X^2-2Z_XZ_Y+Z_Y^2]\\
&=E[Z_X^2]-2E[Z_XZ_Y]+E[Z_Y^2]
\end{aligned}
$$

因为标准化以后：

$$
E[Z_X^2]=E[Z_Y^2]=1
$$

并且：

$$
E[Z_XZ_Y]=\rho_{X,Y}
$$

所以：

$$
E[(Z_X-Z_Y)^2]
=
1-2\rho_{X,Y}+1
$$

当 $\rho_{X,Y}=1$ 时：

$$
E[(Z_X-Z_Y)^2]
=
1-2+1
=
0
$$

由于平方不可能是负数：

$$
(Z_X-Z_Y)^2\ge0
$$

一个非负随机变量的期望等于 $0$，意味着它只能以概率 $1$ 等于 $0$。否则，只要它有正概率大于 $0$，其平均值就不可能为 $0$。

因此：

$$
(Z_X-Z_Y)^2=0
$$

也就是：

$$
\boxed{Z_X=Z_Y}
$$

这表示每一次观测中，$X$ 高于或低于自己均值多少个标准差，$Y$ 就恰好高于或低于自己均值同样多个标准差。

把标准化变量换回原来的定义：

$$
\frac{X-\mu_X}{\sigma_X}
=
\frac{Y-\mu_Y}{\sigma_Y}
$$

移项得到：

$$
Y
=
\frac{\sigma_Y}{\sigma_X}X
+
\left(
\mu_Y-
\frac{\sigma_Y}{\sigma_X}\mu_X
\right)
$$

令：

$$
a=\frac{\sigma_Y}{\sigma_X}>0
$$

以及：

$$
b
=
\mu_Y-a\mu_X
$$

便得到：

$$
\boxed{
Y=aX+b,
\qquad a>0
}
$$

所以完整的逻辑是：

$$
\boxed{
\rho_{X,Y}=1
\quad\Longrightarrow\quad
Z_X=Z_Y
\quad\Longrightarrow\quad
Y=aX+b,\ a>0
}
$$

这就是为什么相关系数等于 $1$ 意味着完全正线性关系。严格地说，上述等式都是“几乎必然”成立，也就是允许在概率为 $0$ 的情形上出现例外。

例如：

$$
Y=2X+10
$$

散点图：

```text
Y
↑
|                 ●
|             ●
|         ●
|     ●
| ●
+----------------------→ X
```

所有点严格落在一条向上的直线上。

于是：

$$
\boxed{\rho=1}
$$

---

# 十二、为什么 $Y=2X+10$ 仍然是相关系数1？

你可能注意：

> 两个变量并不相等。

但相关系数不是在问：

> 数值是否完全一样。

而是在问：

> **标准化之后，它们是否完全同步。**

如果：

$$
Y=2X+10
$$

那么：

$$
E[Y]=2E[X]+10
$$

而：

$$
\sigma_Y=2\sigma_X
$$

所以：

$$
\frac{Y-E[Y]}{\sigma_Y}
$$

变成：

$$
\frac{
2X+10-(2E[X]+10)
}{
2\sigma_X
}
$$

分子：

$$
=2(X-E[X])
$$

因此：

$$
=
\frac{2(X-E[X])}{2\sigma_X}
$$

得到：

$$
=
\frac{X-E[X]}{\sigma_X}
$$

所以：

$$
Z_Y=Z_X
$$

完全同步。

因此：

$$
\rho=1
$$

---

# 十三、$\rho=-1$ 意味着什么？

如果：

$$
\rho=-1
$$

表示存在：

$$
a<0
$$

和常数 $b$，使：

$$
Y=aX+b
$$

也就是完全反向线性关系。

例如：

$$
Y=-3X+10
$$

散点图：

```text
Y
↑
| ●
|    ●
|       ●
|          ●
|             ●
+----------------------→ X
```

每当 $X$ 增大，

$Y$ 按固定比例减少。

因此：

$$
\boxed{\rho=-1}
$$

---

# 十四、$\rho=0$ 呢？

如果：

$$
\rho=0
$$

意味着：

$$
\operatorname{Cov}(X,Y)=0
$$

也就是：

> 没有平均意义上的线性共同变化趋势。

但上一课已经强调：

$$
\boxed{
\rho=0
\not\Rightarrow
X,Y\text{ 没有关系}
}
$$

这一点今天要再次强调。

---

# 十五、经典反例：完美关系但相关系数为0

设：

$$
Y=X^2
$$

而 $X$ 关于0对称。

图像：

```text
Y
↑
| ●                 ●
|   ●             ●
|     ●         ●
|       ●     ●
|         ● ●
|          ●
+------------------------→ X
            0
```

$X,Y$ 显然存在极强关系：

$$
Y
$$

完全由：

$$
X
$$

决定。

但由于左右两边线性趋势相反：

```text
左半边：
X增加，Y下降

右半边：
X增加，Y上升
```

两边贡献可能正好抵消。

于是：

$$
\boxed{\rho=0}
$$

所以 Pearson 相关系数测的是：

$$
\boxed{\text{线性关系}}
$$

不是：

$$
\boxed{\text{任意关系}}
$$

---

# 十六、看散点图理解不同的 $\rho$

## $\rho\approx1$

```text
Y
↑
|              ●
|           ●
|        ●
|     ●
|  ●
+----------------→ X
```

非常强的正线性关系。

---

## $\rho\approx0.7$

```text
Y
↑
|              ●
|        ●  ●
|      ●
|  ●       ●
| ●
+----------------→ X
```

总体向上，但不完全贴直线。

---

## $\rho\approx0$

```text
Y
↑
| ●       ●
|     ●
|  ●           ●
|        ●
| ●         ●
+----------------→ X
```

没有明显线性方向。

---

## $\rho\approx-0.8$

```text
Y
↑
| ●
|   ●   ●
|      ●
|          ●
|             ●
+----------------→ X
```

明显负线性趋势。

---

# 十七、相关系数大小该怎么理解？

通常：

$$
|\rho|
$$

越接近1，

说明：

> 点越接近某条直线。

越接近0：

> 线性关系越弱。

但不要机械规定：

```text
0.3一定叫弱
0.7一定叫强
```

因为不同领域尺度不同。

例如：

* 医学；
* 金融；
* 社会科学；
* 物理实验；
* 推荐系统；

对“多大算重要”的标准可能差异很大。

所以相关系数的数值必须结合实际背景解释。

---

# 十八、正相关不意味着“一起增加”

这里有一个语言细节。

相关系数研究的是随机变量之间的统计关系，

并不一定涉及时间。

例如：

$$
X=\text{身高}
$$

$$
Y=\text{体重}
$$

我们说正相关时，不是在说：

> 同一个人随着时间身高增加，体重就增加。

而是在说：

> 在总体中，身高高于平均的人，体重也更倾向高于平均。

所以“同涨同跌”只是帮助建立方向直觉。

更准确的语言是：

$$
\boxed{
\text{同方向偏离各自均值}
}
$$

---

# 十九、相关系数为什么对平移不敏感？

假设：

$$
X'=X+1000
$$

你只是整体加一个常数。

那么：

$$
X'-E[X']
$$

等于：

$$
X+1000-(E[X]+1000)
$$

所以：

$$
=X-E[X]
$$

中心化偏差完全没变。

因此相关系数不会变化。

所以：

$$
\boxed{
\operatorname{Corr}(X+b,Y+d)
=
\operatorname{Corr}(X,Y)
}
$$

只要标准差非零。

---

# 二十、正比例缩放时也不变

如果：

$$
X'=aX+b
$$

$$
Y'=cY+d
$$

其中：

$$
a>0,\quad c>0
$$

那么：

$$
\boxed{
\operatorname{Corr}(X',Y')
=
\operatorname{Corr}(X,Y)
}
$$

意思是：

> 正向改变单位不会改变相关系数。

例如：

```text
厘米 → 米
摄氏度的线性正比例缩放
元 → 万元
```

相关强度保持不变。

---

# 二十一、如果乘了负数会怎样？

假设：

$$
Y'=-Y
$$

这相当于把 $Y$ 数轴方向整个反过来。

原来：

```text
X大 → Y大
```

现在：

```text
X大 → -Y小
```

所以相关系数符号会反转。

一般：

$$
\operatorname{Corr}(aX+b,cY+d)
=
\operatorname{sign}(ac)
\operatorname{Corr}(X,Y)
$$

其中 $a,c\neq0$。

所以：

* 正比例缩放：相关符号不变；
* 负比例翻转：相关符号翻转。

---

# 二十二、为什么相关系数可以理解成“方向夹角”？

这一部分稍微高级，但非常值得建立直觉。

假设我们有很多观测：

$$
x_1,\ldots,x_n
$$

以及：

$$
y_1,\ldots,y_n
$$

先把它们中心化：

$$
x_i-\bar x
$$

$$
y_i-\bar y
$$

可以把两组数看成两个向量：

$$
\mathbf x_c
$$

和：

$$
\mathbf y_c
$$

样本相关系数本质上和：

$$
\frac{
\mathbf x_c^\top\mathbf y_c
}{
\|\mathbf x_c\|
\|\mathbf y_c\|
}
$$

结构相同。

这不就是向量余弦吗？

$$
\cos\theta
$$

所以：

$$
\boxed{
\text{相关系数可以理解成中心化数据向量之间的余弦相似度}
}
$$

---

# 二十三、这个几何视角为什么解释了 $[-1,1]$？

因为：

$$
-1\le\cos\theta\le1
$$

当两个向量同方向：

$$
\theta=0
$$

所以：

$$
\cos\theta=1
$$

对应：

$$
\rho=1
$$

---

反方向：

$$
\theta=\pi
$$

所以：

$$
\cos\theta=-1
$$

对应：

$$
\rho=-1
$$

---

垂直：

$$
\theta=\frac\pi2
$$

所以：

$$
\cos\theta=0
$$

对应没有线性投影关系。

这个几何解释以后学习：

* PCA；
* cosine similarity；
* covariance matrix；

时会再次出现。

---

# 二十四、总体相关系数 vs 样本相关系数

这是进入数据分析前必须区分的一组概念。

目前我们讲的：

$$
\rho_{X,Y}
$$

是理论概率分布上的：

> **总体相关系数。**

定义：

$$
\rho_{X,Y}
=
\frac{
\operatorname{Cov}(X,Y)
}{
\sigma_X\sigma_Y
}
$$

---

但现实中我们通常不知道真正的概率分布。

我们只有数据：

$$
(x_1,y_1),\ldots,(x_n,y_n)
$$

这时会计算样本相关系数：

$$
r
$$

用有限数据估计总体中的：

$$
\rho
$$

所以：

```text
理论世界：
ρ

实际样本：
r
```

和之前：

```text
理论期望 E[X]
vs
样本平均 x̄
```

是非常类似的关系。

---

# 二十五、相关系数不等于因果关系

这是最必须强调的边界之一。

假设：

$$
\rho_{X,Y}
$$

很高，

只能说明：

> $X,Y$ 存在较强线性统计关系。

不能直接得出：

$$
X\rightarrow Y
$$

---

例如：

$$
X=\text{冰淇淋销量}
$$

$$
Y=\text{溺水事故数量}
$$

夏季二者可能同时升高。

所以：

$$
X,Y
$$

可能正相关。

但显然不能得出：

> 多卖冰淇淋导致溺水。

真正的共同原因可能是：

$$
Z=\text{气温}
$$

气温升高同时导致：

```text
冰淇淋销量 ↑
游泳人数 ↑
溺水风险 ↑
```

所以：

$$
\boxed{
\text{相关性}
\neq
\text{因果性}
}
$$

---

# 二十六、还有一种情况：反向因果

即使：

$$
X,Y
$$

确实存在因果关系，

只看到相关性也不知道方向。

例如观察到：

$$
X\text{ 与 }Y\text{ 高度相关}
$$

可能是：

$$
X\rightarrow Y
$$

也可能：

$$
Y\rightarrow X
$$

还可能：

$$
Z\rightarrow X,\quad Z\rightarrow Y
$$

甚至可能多种机制同时存在。

所以：

> 相关系数只描述关联结构，不给出因果方向。

---

# 二十七、一个重要反例：相关很高也可能没有实际价值

假设两个变量：

$$
X
$$

和：

$$
Y=1000000X
$$

那么：

$$
\rho=1
$$

说明完全线性相关。

但是否有实际意义取决于：

> $X,Y$ 分别是什么。

如果：

$$
Y
$$

只是 $X$ 换了一个单位，

那么虽然相关系数完美，

却根本没有提供新的信息。

所以：

$$
\boxed{
\text{统计关系强}
\neq
\text{业务价值大}
}
$$

---

# 二十八、相关系数还有一个重要局限：只看线性

例如：

$$
Y=X^2
$$

关系是确定的，

但是 Pearson 相关系数可能为0。

再例如：

$$
Y=\sin X
$$

也可能存在强烈规律，

但整个范围上的线性相关却很弱。

所以如果散点图出现：

* U形；
* 圆形；
* 波浪形；
* 分段关系；

只看 Pearson 相关系数可能严重误导。

因此数据分析中：

$$
\boxed{\text{先看散点图，再看相关系数}}
$$

通常是很好的原则。

---

# 二十九、机器学习里为什么相关性很重要？

## 1. 特征冗余

假设：

$$
X_1=\text{身高 cm}
$$

$$
X_2=\text{身高 m}
$$

显然：

$$
|\rho|=1
$$

这两个特征几乎携带完全相同的信息。

大量高度相关的特征可能造成：

* 冗余；
* 多重共线性；
* 参数解释困难。

---

## 2. 特征分析

研究某个特征 $X$ 与目标 $Y$ 的相关系数，可以初步了解：

> 是否存在明显线性关系。

但不能仅凭相关系数判断：

> 这个特征对模型一定有用。

因为模型可能利用非线性关系。

---

## 3. PCA

PCA 的一个重要背景就是：

> 数据的不同方向具有怎样的方差和协方差结构？

最终会构造：

$$
\boxed{\text{协方差矩阵}}
$$

分析其特征向量与特征值。

所以今天的相关与协方差是 PCA 的地基之一。

---

# 三十、相关系数和标准化的关系尤其重要

假设把两个变量都标准化：

$$
Z_X=\frac{X-\mu_X}{\sigma_X}
$$

$$
Z_Y=\frac{Y-\mu_Y}{\sigma_Y}
$$

则：

$$
E[Z_X]=E[Z_Y]=0
$$

$$
\operatorname{Var}(Z_X)
=
\operatorname{Var}(Z_Y)=1
$$

然后：

$$
\operatorname{Cov}(Z_X,Z_Y)
$$

就是：

$$
\rho_{X,Y}
$$

所以：

$$
\boxed{
\text{相关系数}
=
\text{标准化变量之间的协方差}
}
$$

这把第二十课、第二十二课、第二十三课全部串起来了：

```text
标准化
(X-μ)/σ
       ↓
去掉单位和尺度
       ↓
两个标准化变量求协方差
       ↓
相关系数
```

---

# 三十一、一个具体数值例子

假设：

$$
\operatorname{Cov}(X,Y)=12
$$

并且：

$$
\sigma_X=3
$$

$$
\sigma_Y=4
$$

那么：

$$
\rho
=
\frac{12}{3\times4}
$$

所以：

$$
\boxed{\rho=1}
$$

意味着完全正线性关系。

---

如果协方差变成：

$$
6
$$

那么：

$$
\rho
=
\frac6{12}
=
0.5
$$

说明存在中等程度的正线性共同变化。

---

如果：

$$
\operatorname{Cov}(X,Y)=-9
$$

那么：

$$
\rho
=
\frac{-9}{12}
=
-0.75
$$

说明比较明显的负线性关系。

---

# 三十二、为什么不能出现 $\rho=2$？

假设你计算得到：

$$
\rho=2
$$

那么一定哪里出错了。

因为相关系数理论上：

$$
\boxed{
|\rho|\le1
}
$$

原因可以从标准化理解。

两个变量标准差都已经被调整成1。

它们最极端的同步情况也不过：

$$
Z_Y=Z_X
$$

得到：

$$
\rho=1
$$

或者：

$$
Z_Y=-Z_X
$$

得到：

$$
\rho=-1
$$

不可能比“完全同步”还同步。

---

# 三十三、为什么相关系数要求标准差不能为0？

公式：

$$
\rho
=
\frac{\operatorname{Cov}(X,Y)}
{\sigma_X\sigma_Y}
$$

如果：

$$
\sigma_X=0
$$

意味着：

$$
X
$$

是常数，完全不波动。

例如：

$$
X=5
$$

永远成立。

那么分母为0，相关系数不能这样定义。

这也符合直觉：

> 一个根本不会变化的变量，谈“它和另一个变量是否共同变化”没有普通 Pearson 相关意义。

所以一般要求：

$$
\boxed{
\sigma_X>0,\quad\sigma_Y>0
}
$$

---

# 三十四、相关系数 vs 协方差

现在把它们彻底分开。

|            | 协方差                         | 相关系数                                                 |
| ---------- | --------------------------- | ---------------------------------------------------- |
| 公式         | $\operatorname{Cov}(X,Y)$ | $\frac{\operatorname{Cov}(X,Y)}{\sigma_X\sigma_Y}$ |
| 描述方向       | 可以                          | 可以                                                   |
| 描述线性强度     | 受尺度影响                       | 可统一比较                                                |
| 有单位吗       | 有                           | 无                                                    |
| 范围固定吗      | 没有                          | $[-1,1]$                                           |
| 换正比例单位是否变化 | 会                           | 不会                                                   |

所以：

$$
\boxed{
\text{协方差是原始共同波动}
}
$$

而：

$$
\boxed{
\text{相关系数是标准化共同波动}
}
$$

---

# 三十五、独立、不相关、相关系数为0的关系

这一组一定要整理清楚。

如果：

$$
X,Y\text{ 独立}
$$

并且相关矩存在，那么：

$$
\operatorname{Cov}(X,Y)=0
$$

所以：

$$
\rho=0
$$

因此：

$$
\boxed{
\text{独立}
\Rightarrow
\text{不相关}
}
$$

但是：

$$
\boxed{
\rho=0
\not\Rightarrow
\text{独立}
}
$$

例如：

$$
Y=X^2
$$

就是经典反例。

---

有一个重要特殊情况：

> 如果 $X,Y$ **联合正态**，那么零协方差可以推出独立。

即在联合高斯情形中：

$$
\operatorname{Cov}(X,Y)=0
$$

会进一步得到：

$$
X\perp Y
$$

这是正态分布非常特殊而强大的性质。

现在先记为以后多维正态的伏笔，不深入证明。

---

# 三十六、相关系数 vs 回归斜率也不是一回事

假设：

$$
Y\approx aX+b
$$

回归中的：

$$
a
$$

表示：

> $X$ 增加1个原始单位时，$Y$ 平均改变多少原始单位。

它受到单位影响。

而：

$$
\rho
$$

表示：

> 标准化以后，两者线性共同变化的紧密程度。

所以：

$$
\boxed{
\text{相关系数}\neq\text{回归斜率}
}
$$

例如：

$$
Y=1000X
$$

虽然斜率：

$$
1000
$$

非常大，

但相关系数只是：

$$
1
$$

---

# 三十七、从整个概率论主线看今天增加了什么？

我们现在已经从单变量走到了双变量关系。

完整链条：

```text
随机变量 X
   ↓
E[X]
中心

   ↓

Var(X)
X 自己围绕中心怎么波动

   ↓

随机变量 Y

   ↓

Cov(X,Y)
X、Y 是否一起偏离中心

   ↓

Corr(X,Y)
去掉二者自身尺度后，
这种线性共同偏离到底有多强
```

所以相关系数并不是突然出现的新指标。

它完全是：

$$
\boxed{
\text{协方差}
+
\text{标准化思想}
}
$$

---

# 理解检查

先不要急着往下看。

## 1

为什么：

$$
\operatorname{Cov}(X,Y)=50
$$

不能直接说明比：

$$
\operatorname{Cov}(A,B)=5
$$

关系更强？

---

## 2

为什么相关系数要除以：

$$
\sigma_X\sigma_Y
$$

？

这两个标准差在这里究竟起什么作用？

---

## 3

如果：

$$
Y=2X+10
$$

为什么相关系数是：

$$
1
$$

而不是2？

---

## 4

如果：

$$
Y=-X
$$

相关系数是多少？

为什么？

---

## 5

若：

$$
\rho=0
$$

能否说明：

> $X,Y$ 没有关系？

请用：

$$
Y=X^2
$$

解释。

---

## 6

为什么把 $X$ 从“米”改成“厘米”以后，相关系数不应该改变？

---

## 7 —— 最重要

请尝试自己解释：

$$
\boxed{
\rho
=
\frac{\operatorname{Cov}(X,Y)}
{\sigma_X\sigma_Y}
}
$$

为什么本质上就是：

> **先分别把 $X,Y$ 转换成“偏离自己均值多少个标准差”，然后研究这两个无单位偏差是否倾向一起变化。**

如果这一句话真正理解了，相关系数就不再是一条需要背的公式。

---

# 这一节真正需要记住的核心

只需要牢牢记住下面 **5 句话**：

1. **协方差虽然能描述两个变量共同变化的方向，但大小受单位和尺度影响，所以不能直接比较关系强弱。**

2. 相关系数通过标准化消除尺度：

$$
\boxed{
\rho_{X,Y}
=
\frac{\operatorname{Cov}(X,Y)}
{\sigma_X\sigma_Y}
}
$$

它可以理解为：

$$
\boxed{
\text{标准化后的 }X,Y\text{ 的协方差}
}
$$

3. Pearson 相关系数满足：

$$
\boxed{-1\le\rho\le1}
$$

其中：

* $1$：完全正线性关系；
* $-1$：完全负线性关系；
* $0$：没有线性相关，但不一定没有其他关系。

4. **相关不等于因果。**一个高相关可能来自共同原因、反向因果或其他结构。

5. 从概念层次上：

$$
\boxed{
\text{方差}
\rightarrow
\text{协方差}
\rightarrow
\text{相关系数}
}
$$

分别是在回答：

> 自己怎么波动 → 两个变量是否一起波动 → 去掉尺度后，一起波动有多强。

---

下一步为什么自然进入**多维随机变量和联合分布**？

因为现在我们已经开始同时研究 $X,Y$，但目前只是用：

$$
E[X],\quad E[Y],\quad
\operatorname{Var}(X),\quad
\operatorname{Var}(Y),\quad
\operatorname{Cov}(X,Y)
$$

这些摘要描述它们。

可问题是：

> **如果我想完整描述 $X$ 和 $Y$ 到底会怎样“一起取值”，仅靠协方差和相关系数够吗？**

当然不够。

我们真正需要研究：

$$
\boxed{
P(X=x,Y=y)
}
$$

或者连续情况下的二维概率结构。

这将自然进入下一阶段：

$$
\boxed{
\text{多维随机变量与联合分布}
}
$$

也就是从：

> “两个变量有没有关系”

进一步进入：

> **“两个变量完整的共同概率规律到底是什么？”**
