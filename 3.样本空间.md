# 第三课：样本空间——给所有可能结果建一个“容器”

今天只研究一个核心概念：

> 如何把现实中的随机试验，转换成一个可以进行数学研究的对象？

我们现在走到这里：

```text
随机现象
   ↓
随机试验
   ↓
样本空间  ← 今天
   ↓
事件
   ↓
概率
```

上一课还在现实世界中讨论“抛硬币”“掷骰子”。从这一课开始，我们第一次把现实问题翻译成数学语言。

---

# 1. 问题背景：为什么需要样本空间？

假设我们进行一次试验：

> 连续抛两次硬币，记录每次的正反面。

可能结果有哪些？

很多初学者会先回答：

```text
0次正面、1次正面、2次正面
```

但另一位同学可能回答：

```text
正正、正反、反正、反反
```

这两种回答看起来都合理，但记录的信息不同。

例如：

```text
正反 → 1次正面
反正 → 1次正面
```

如果只记录正面次数，“正反”和“反正”会被合并；如果记录每次的顺序，它们就是两个不同结果。

于是，在计算概率之前，我们必须先回答：

- 这次试验究竟记录什么？
- 一个最基本的结果是什么？
- 哪些结果被认为不同？
- 是否已经包含所有可能结果？
- 有没有把同一个实际结果重复记录？

样本空间就是为了解决这些问题而产生的。

---

# 2. 直觉解释：样本空间是一张完整的“可能性地图”

想象一次随机试验是一条岔路。

抛一次硬币：

```text
                 ┌── 正面
抛一次硬币 ──────┤
                 └── 反面
```

样本空间就是把所有岔路终点收集在一起：

```text
┌───────────────────┐
│       正面   反面   │
└───────────────────┘
```

它像一个盒子：

```text
样本空间
┌────────────────────┐
│ 所有可能的基本结果   │
│ 一个不能少           │
│ 一个不能重复实现     │
└────────────────────┘
```

也可以把它理解为一张地图：

- 地图之外：当前模型认为不会出现的结果；
- 地图之内：这次试验可能出现的所有结果；
- 实验完成后：地图上恰好有一个基本结果成为现实。

---

# 3. 数学定义

对于一个随机试验：

> 所有可能的基本结果组成的集合，称为这个随机试验的样本空间。

通常用希腊字母大写 Omega 表示：$$\Omega$$
读作“欧米伽”。

如果样本空间中的一个具体基本结果用小写 Omega 表示：

$$
\omega
$$

那么可以写成：

$$
\omega\in\Omega
$$

这个表达式中的每个符号分别表示：

- $(\Omega)$：所有可能基本结果组成的集合；
- $(\omega)$：其中某一个基本结果；
- $(\in)$：属于；
- $(\omega\in\Omega)$：这个具体结果属于样本空间。

例如抛一枚硬币：

$$
\Omega=\{\text{正面},\text{反面}\}
$$

花括号表示“一个集合”。

其中：

$$
\omega=\text{正面}
$$

表示把“正面”选作一个具体的基本结果。

---

# 4. 什么叫“基本结果”？

基本结果也叫样本点。

它表示：

> 按照我们事先规定的观察方式，一次试验结束后得到的最完整、不可再区分的单个结果。

例如掷一次骰子：

$$
\Omega=\{1,2,3,4,5,6\}
$$

其中“掷出4点”就是一个样本点。

需要注意：

```text
样本点不是空间中的几何点
```

“点”在这里只表示样本空间里的一个单独成员。它可以是：

- 一个数字；
- 一个文字标签；
- 一个有顺序的组合；
- 一条曲线；
- 一张图像；
- 一段时间序列。

因此，样本点不一定是数字。

---

# 5. 一个合格的样本空间需要满足什么？

## 条件一：完整性

样本空间必须包含当前模型中所有可能的基本结果。

例如抛一枚普通硬币，如果只写：

$$
\Omega=\{\text{正面}\}
$$

那么反面没有被包含，样本空间不完整。

完整性表示：

> 无论试验实际出现什么结果，都能在样本空间中找到它。

---

## 条件二：基本结果彼此排斥

一次试验结束后，不能同时实现两个不同的基本结果。

掷一次骰子，不可能既得到2点又得到5点。

```text
本次是2点 → 就不是5点
本次是5点 → 就不是2点
```

这里说的“彼此排斥”不是说它们不能作为候选项同时写在样本空间中，而是说：

> 一次具体试验中，只会实现其中一个基本结果。

---

## 条件三：结果的区分标准必须明确

例如抛一枚硬币，如果硬币可能竖着立住，我们可以建立两种模型。

### 简化模型

忽略极少发生的立边情况：

$$
\Omega=\{\text{正面},\text{反面}\}
$$

### 更细致的模型

把立边也纳入考虑：

$$
\Omega=\{\text{正面},\text{反面},\text{立边}\}
$$

两者不一定谁“绝对正确”。

关键在于：

- 研究目的是什么；
- 哪些结果值得区分；
- 简化假设是否合理；
- 是否清楚说明模型边界。

样本空间不是机械地从现实中抄出来的，它是我们对现实问题进行抽象后建立的模型。

---

# 6. 样本空间是怎样从现实问题中“长出来”的？

建立样本空间可以分成四步。

## 第一步：明确随机试验

先说清楚我们在做什么。

例如：

> 连续抛两次硬币，并按照先后顺序记录结果。

“按照先后顺序”非常重要，因为它决定“正反”和“反正”是否要区分。

---

## 第二步：明确一次记录的单位

每次硬币结果可能是：

```text
正
或
反
```

两次记录组合在一起，才构成整个试验的一个基本结果。

---

## 第三步：系统列出所有可能组合

用树状图表示：

```text
第一次             第二次             最终结果

                 ┌── 正 ─────────→ 正正
        ┌── 正 ──┤
        │        └── 反 ─────────→ 正反
开始 ───┤
        │        ┌── 正 ─────────→ 反正
        └── 反 ──┤
                 └── 反 ─────────→ 反反
```

所以：

$$
\Omega=
\{\text{正正},\text{正反},\text{反正},\text{反反}\}
$$

---

## 第四步：检查是否完整且互不重叠

检查两个问题：

1. 是否存在某种实际结果没有写进去？
2. 一次试验是否可能同时对应两个样本点？

这里四个结果覆盖了全部可能，并且一次试验只会对应其中一个，所以它是合适的样本空间。

---

# 7. 最简单例子

## 例一：抛一枚硬币

随机试验：

> 抛一枚硬币，观察朝上的一面。

样本空间：

$$
\Omega=\{\text{正面},\text{反面}\}
$$

如果本次得到正面：

```text
随机试验：抛硬币
可能结果：正面、反面
本次样本点：正面
```

注意：

- 整个集合是样本空间；
- “正面”只是其中一个样本点。

---

## 例二：掷一个六面骰子

随机试验：

> 掷一次骰子，记录朝上面的点数。

样本空间：

$$
\Omega=\{1,2,3,4,5,6\}
$$

如果本次掷出4点：

$$
\omega=4
$$

这里的4是结果标签，不是概率。

---

## 例三：检查一个零件

随机试验：

> 随机抽取一个零件，检查是否合格。

样本空间可以写为：

$$
\Omega=\{\text{合格},\text{不合格}\}
$$

但如果研究的是具体缺陷类型，也可以建立更细的空间：

$$
\Omega=
\{\text{合格},\text{划痕},\text{裂纹},\text{尺寸异常},\text{其他缺陷}\}
$$

不同研究问题可能需要不同粒度的样本空间。

---

## 例四：记录灯泡寿命

随机试验：

> 点亮一个灯泡，记录它从开始工作到损坏的时间。

结果可能是许多不同的非负时间：

$$
\Omega=\{t\mid t\geq 0\}
$$

这里：

- \(t\) 表示灯泡寿命；
- \(t\geq 0\) 表示寿命不能是负数；
- 竖线可以读成“满足”；
- 整个表达式表示“所有大于或等于0的时间”。

这个样本空间通常不能一个个列完，因为结果可能是：

```text
100小时
100.5小时
100.51小时
……
```

这说明样本空间既可能是有限集合，也可能包含无限多个结果。

---

# 8. 三种常见样本空间

现在只建立直觉，暂时不深入分类理论。

## 有限样本空间

结果数量有限。

例如掷一次骰子：

$$
\{1,2,3,4,5,6\}
$$

---

## 可数无限样本空间

结果有无限多个，但可以按照顺序编号。

例如不断抛硬币，直到第一次出现正面。记录抛掷次数：

$$
\Omega=\{1,2,3,4,\ldots\}
$$

含义是：

- 第1次就出现正面；
- 第2次才第一次出现正面；
- 第3次才第一次出现正面；
- 如此继续。

省略号表示这个过程没有预设的最大次数。

---

## 连续样本空间

结果可能落在某段连续范围内。

例如灯泡寿命、身高、温度、测量误差。

在数学模型中，两个可选结果之间通常还能找到更多结果：

```text
23℃
23.1℃
23.01℃
23.001℃
……
```

这种样本空间以后会连接到连续型随机变量。

---

# 9. 样本空间不是唯一的

这是本节最需要建立的建模意识。

同一个现实过程，可以根据研究目标建立不同的样本空间。

仍以抛两次硬币为例。

## 如果记录具体顺序

$$
\Omega_1=
\{\text{正正},\text{正反},\text{反正},\text{反反}\}
$$

## 如果只记录正面出现次数

$$
\Omega_2=\{0,1,2\}
$$

为什么不同？

因为观察问题不同：

- \(\Omega_1\) 保留了每一次抛掷的顺序；
- \(\Omega_2\) 只保留正面总数，丢掉了顺序。

例如“正反”和“反正”在第一个空间中是不同结果，但在第二个空间中都会被记录为1。

这说明：

> 建立样本空间，本质上是在决定我们保留哪些信息、忽略哪些信息。

通常应当先建立足够细致的样本空间，再通过事件或随机变量把结果合并。这样更不容易过早丢失信息。

---

# 10. 样本与样本点：注意语境

在概率论的初始阶段：

- 样本空间：全部可能结果的集合；
- 样本点：一个可能的基本结果。

但进入数理统计以后，“样本”一词通常表示：

> 从总体中实际观察到的一组数据。

例如调查100个人的身高，这100个身高值组成一个样本。

所以以后要根据语境区分：

```text
概率论中的样本点
= 一次随机试验的基本结果

统计学中的样本
= 从总体中获得的一组观测数据
```

目前我们重点使用“样本点”，避免混淆。

---

# 11. 样本空间 vs 事件

这两个概念下一节会详细比较，今天先看边界。

假设掷一次骰子：

$$
\Omega=\{1,2,3,4,5,6\}
$$

整个样本空间回答：

> 所有可能结果是什么？

如果我们关心“是否掷出偶数”，对应的结果是：

$$
\{2,4,6\}
$$

它不是一个基本结果，而是由多个基本结果组成的集合。

这样的“我们关心的一组结果”，就叫事件。

因此：

```text
样本点：一个基本结果，例如4
事件：一组符合条件的结果，例如{2,4,6}
样本空间：全部基本结果，例如{1,2,3,4,5,6}
```

可以形成一个包含关系：

```text
样本空间 Ω
┌─────────────────────────┐
│  1        3        5     │
│                         │
│      ┌────────────┐     │
│      │  2   4   6  │     │
│      │ “偶数”事件  │     │
│      └────────────┘     │
└─────────────────────────┘
```

---

# 12. 反例与边界

## 反例一：遗漏可能结果

掷一次六面骰子却写成：

$$
\{1,2,3,4,5\}
$$

遗漏了6，所以不完整。

---

## 反例二：结果相互重叠

抽查一个人的年龄，写成：

```text
青年
成年人
老年人
```

如果“青年”定义为18～35岁，“成年人”定义为18岁及以上，那么25岁同时属于“青年”和“成年人”。

这不能直接作为互不重叠的基本结果。

可以改成：

```text
0～17岁
18～35岁
36～59岁
60岁及以上
```

前提是边界定义清楚。

---

## 反例三：把事件误当成基本结果

掷一次骰子时写：

```text
1点、2点、奇数、偶数
```

这是混合了两种层次：

- “1点”和“2点”像基本结果；
- “奇数”和“偶数”各自包含多个点数。

而且它们相互重叠，例如1既属于“1点”，又属于“奇数”。

建模时应保持同一层次。

---

## 反例四：认为样本空间中的结果一定等可能

样本空间只说明：

> 哪些结果可能出现。

它没有自动说明：

> 每个结果出现的可能性是否相同。

例如：

$$
\Omega=\{\text{晴},\text{雨}\}
$$

虽然只有两个结果，但不能因此判断晴天和雨天各占一半。

```text
结果有两个
≠
两个结果等可能
```

概率要到后面的课程中单独定义。

---

# 13. 与已有知识连接

今天的新知识可以概括为：

```text
随机现象
+
一次试验的观察规则
+
集合思想
=
样本空间
```

上一课解决的是：

> 现实中什么过程适合用概率论研究？

这一课解决的是：

> 怎样把这个过程的全部可能结果装进一个数学容器？

我们完成了第一次关键转换：

```text
现实过程                  数学模型

抛两次硬币      ─────→    {正正, 正反, 反正, 反反}
检查零件        ─────→    {合格, 不合格}
记录灯泡寿命    ─────→    所有非负时间
```

---

# 14. 实际应用

## 机器学习分类

图像分类问题的样本空间可能是所有可能输入图像。

如果只考虑模型输出，结果空间可能是：

```text
猫、狗、鸟、其他
```

类别定义不清或类别相互重叠，会直接导致训练标签混乱。

---

## 自动驾驶

研究车辆前方状态时，可以建立：

```text
无障碍物、行人、机动车、非机动车、未知障碍物
```

如果遗漏“未知障碍物”，模型在现实中遇到未见目标时就可能没有合适的表达方式。

---

## 工程可靠性

研究设备状态时，样本空间可能是：

```text
正常、轻微故障、严重故障、完全失效
```

不同粒度会影响后面的风险评估和维护决策。

---

## 强化学习

智能体执行动作后，环境可能转移到不同状态。

定义清楚所有可能状态，相当于建立环境的可能性空间。状态定义过粗会丢失信息；过细则会增加学习难度。

---

# 理解检查

请凭直觉思考：

1. 连续抛两次硬币，只记录正面总数时，为什么“正反”和“反正”不再是两个不同结果？
2. 掷一次骰子时，\(\{2,4,6\}\) 是一个样本点，还是一组样本点？
3. 天气只有“晴”和“雨”两个结果，是否意味着两者各有一半的可能性？
4. 同一个随机试验为什么可以建立不同的样本空间？
5. 一个合格的样本空间为什么需要“完整且基本结果互不重叠”？

# 这一节真正需要记住的核心

1. 样本空间是一次随机试验所有可能基本结果组成的集合，通常记作 \(\Omega\)。  
2. 样本点是样本空间中的一个基本结果，通常记作 \(\omega\)；一次试验最终实现一个样本点。  
3. 样本空间要覆盖全部可能结果，并使不同基本结果在一次试验中不能同时实现。  
4. 同一个现实过程可以因观察目的不同而采用不同的样本空间；建立样本空间就是决定保留哪些信息。  
5. 样本空间只说明“什么可能发生”，尚未说明“发生的可能性有多大”。

下一步自然进入“事件”：现实中我们通常不只关心某一个精确结果，而会关心“一组结果是否发生”，例如“骰子点数是否为偶数”或“产品是否出现任何缺陷”。