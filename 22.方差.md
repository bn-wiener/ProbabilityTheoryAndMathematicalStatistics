# 第二十二课：方差——有了期望，为什么还必须知道“离中心有多散”？

上一课我们学习了数学期望：

$$
E[X]
$$

它回答的是：

> **随机变量长期来看，平均落在什么位置？**

但上一课最后留下了一个非常关键的问题。

考虑两个随机变量：

$$
A=
\begin{cases}
0,&P=0.5\\
10,&P=0.5
\end{cases}
$$

以及：

$$
B=5
$$

它们都有：

$$
E[A]=E[B]=5
$$

可是它们的随机行为显然完全不同：

```text
A：
0 -------- 5 -------- 10
●                     ●
一半在0，一半在10

B：
0 -------- 5 -------- 10
           ●
永远等于5
```

虽然中心一样，但：

* $A$ 每次都离中心很远；
* $B$ 每次都正好在中心。

所以：

$$
\boxed{\text{只知道期望，不足以描述随机变量}}
$$

我们还需要回答第二个问题：

> **随机结果通常离自己的中心有多远？**

这就是今天的核心：

$$
\boxed{\text{方差 Variance}}
$$

---

# 一、它在整个概率论知识体系中的位置

现在我们已经完成：

```text
随机变量
   ↓
概率分布
   ↓
常见概率分布
   ↓
数学期望
   ↓
方差 ← 今天
   ↓
标准差
   ↓
协方差
   ↓
相关系数
```

可以把概率分布的摘要理解成：

```text
完整概率分布
      ↓
先问中心在哪
      ↓
E[X]

还不够
      ↓
再问围绕中心散得多开
      ↓
Var(X)
```

所以：

$$
\boxed{
\text{期望描述位置，方差描述分散程度}
}
$$

---

# 二、问题背景：为什么不能只看期望？

假设有两台机器生产长度目标为：

$$
100\text{ mm}
$$

的零件。

### 机器 A

实际产品：

```text
99.9
100.0
100.1
99.8
100.2
```

整体都紧紧围绕100。

---

### 机器 B

实际产品：

```text
90
110
92
108
100
```

平均下来也可能接近：

$$
100
$$

所以两台机器都可能有：

$$
E[X]=100
$$

但是工程质量显然完全不同。

机器 A：

> 稳定。

机器 B：

> 波动很大。

因此，仅仅知道：

$$
E[X]=100
$$

无法判断机器是否稳定。

我们需要第二个数：

> **这些结果离100通常有多远？**

---

# 三、最自然的第一个想法：直接计算“离中心的距离”

假设随机变量中心为：

$$
\mu=E[X]
$$

那么一次结果：

$$
X
$$

相对中心的偏离就是：

$$
X-\mu
$$

例如：

$$
\mu=5
$$

如果：

$$
X=8
$$

那么：

$$
X-\mu=3
$$

表示：

> 向右偏了3。

如果：

$$
X=2
$$

那么：

$$
X-\mu=-3
$$

表示：

> 向左偏了3。

所以：

$$
\boxed{
X-E[X]
}
$$

叫作：

> 随机变量相对自身均值的偏差。

---

# 四、那为什么不直接计算 $E[X-E[X]]$？

这是方差诞生的真正起点。

我们不是直接接受平方，而是先尝试最自然的方法。

既然：

$$
X-E[X]
$$

描述离中心的偏差，

那么我们是不是可以计算：

$$
E[X-E[X]]
$$

来衡量平均偏离？

试试看。

利用期望线性性：

$$
E[X-E[X]]
$$

因为：

$$
E[X]
$$

本身是一个常数，所以：

$$
=
E[X]-E[X]
$$

因此：

$$
\boxed{
E[X-E[X]]=0
}
$$

永远是0。

---

# 五、为什么平均偏差永远是0？

这不是巧合。

因为均值本来就是分布的“平衡中心”。

例如：

$$
X=
\begin{cases}
0,&0.5\\
10,&0.5
\end{cases}
$$

均值：

$$
E[X]=5
$$

偏差：

```text
X=0  →  偏差 -5
X=10 →  偏差 +5
```

加权平均：

$$
(-5)\times0.5
+
(+5)\times0.5
=0
$$

于是：

$$
E[X-E[X]]=0
$$

问题在于：

> 左边偏差是负的，右边偏差是正的，它们互相抵消了。

但我们真正想测的是：

> **离中心有多远。**

我们并不希望“向左5”和“向右5”互相抵消。

---

# 六、那怎么办？需要“去掉方向，只保留距离大小”

我们需要把：

$$
-5
$$

和：

$$
+5
$$

都变成：

$$
5
$$

最直接的方法之一是：

$$
|X-E[X]|
$$

绝对值。

确实可以定义：

$$
E[|X-E[X]|]
$$

这叫平均绝对偏差一类的量。

它完全可以描述离中心的距离。

那么问题来了：

> 为什么概率论最经典的分散指标最后没有选绝对值，而选了平方？

也就是：

$$
(X-E[X])^2
$$

---

# 七、为什么使用平方？

平方有几个非常重要的性质。

## 第一：去掉正负方向

如果：

$$
X-E[X]=5
$$

那么：

$$
5^2=25
$$

如果：

$$
X-E[X]=-5
$$

那么：

$$
(-5)^2=25
$$

于是：

$$
\boxed{
\text{左右同样远}
\Rightarrow
\text{贡献相同}
}
$$

这正是我们想要的。

---

## 第二：偏得越远，惩罚增长得越快

例如：

```text
偏差    平方偏差

1   →   1
2   →   4
3   →   9
10  →   100
```

所以特别远的异常值会受到更大权重。

这让方差对“大偏离”非常敏感。

---

## 第三：平方在数学上非常容易处理

这是一个非常深的原因。

例如：

$$
(a+b)^2
$$

可以展开：

$$
a^2+2ab+b^2
$$

这种代数结构会自然产生：

* 方差加法；
* 协方差；
* 最小二乘；
* 欧氏距离；
* 正态分布；
* $L_2$ 损失。

而绝对值：

$$
|x|
$$

在0处不可导，而且很多代数展开不如平方方便。

所以平方不仅有直觉意义，还有非常强的数学结构。

---

# 八、于是方差自然定义出来

我们真正想计算的是：

> 随机变量相对均值的**平方偏差的平均值**。

因此定义：

$$
\boxed{
\operatorname{Var}(X)
=
E[(X-E[X])^2]
}
$$

这就是方差。

有时也写作：

$$
\boxed{
\sigma_X^2
=
\operatorname{Var}(X)
}
$$

---

# 九、逐个拆解方差公式

公式：

$$
\operatorname{Var}(X)
=
E[(X-E[X])^2]
$$

不要整体背。

我们一步一步读。

---

## 第一步：$E[X]$

找到分布中心。

---

## 第二步：$X-E[X]$

每一次结果离中心偏了多少。

---

## 第三步：

$$
(X-E[X])^2
$$

去掉偏差方向，同时放大较大的偏差。

---

## 第四步：

$$
E[(X-E[X])^2]
$$

对这些平方偏差取概率加权平均。

所以整句话就是：

$$
\boxed{
\text{方差}
=
\text{随机变量到均值的平方距离的平均值}
}
$$

---

# 十、用两个极端例子建立直觉

## 随机变量 A：完全不波动

$$
A=5
$$

概率1。

那么：

$$
E[A]=5
$$

每一次：

$$
A-E[A]=5-5=0
$$

平方：

$$
0^2=0
$$

所以：

$$
\boxed{
\operatorname{Var}(A)=0
}
$$

这非常符合直觉：

> 永远不离开中心，波动为0。

---

## 随机变量 B：一半0，一半10

$$
B=
\begin{cases}
0,&0.5\\
10,&0.5
\end{cases}
$$

先求期望：

$$
E[B]
=
0\times0.5+10\times0.5
=
5
$$

然后偏差：

### 当 $B=0$

$$
B-E[B]=-5
$$

平方：

$$
25
$$

### 当 $B=10$

$$
B-E[B]=5
$$

平方：

$$
25
$$

因此：

$$
\operatorname{Var}(B)
=
25\times0.5+25\times0.5
$$

所以：

$$
\boxed{
\operatorname{Var}(B)=25
}
$$

对比：

$$
E[A]=E[B]=5
$$

但：

$$
\operatorname{Var}(A)=0
$$

$$
\operatorname{Var}(B)=25
$$

于是差别被准确描述出来了。

---

# 十一、离散随机变量的方差怎样计算？

如果：

$$
X
$$

可能取：

$$
x_1,x_2,\ldots
$$

对应概率：

$$
p_i=P(X=x_i)
$$

先设：

$$
\mu=E[X]
$$

那么：

$$
\boxed{
\operatorname{Var}(X)
=
\sum_i
(x_i-\mu)^2p_i
}
$$

这不是新公式。

只是把：

$$
E[(X-\mu)^2]
$$

按照期望定义展开。

整个过程：

```text
每个可能值 xi
      ↓
减均值 μ
      ↓
平方
      ↓
乘出现概率 pi
      ↓
全部相加
```

---

# 十二、完整例子：公平骰子的方差

设：

$$
X=\text{公平六面骰子的点数}
$$

上一课已经知道：

$$
E[X]=3.5
$$

现在求：

$$
\operatorname{Var}(X)
$$

---

## 第一步：列出离中心的距离

| $x$ | $x-3.5$ |
| ----: | --------: |
|     1 |      -2.5 |
|     2 |      -1.5 |
|     3 |      -0.5 |
|     4 |       0.5 |
|     5 |       1.5 |
|     6 |       2.5 |

---

## 第二步：平方

| $x$ | $(x-3.5)^2$ |
| ----: | ------------: |
|     1 |          6.25 |
|     2 |          2.25 |
|     3 |          0.25 |
|     4 |          0.25 |
|     5 |          2.25 |
|     6 |          6.25 |

每个概率：

$$
\frac16
$$

所以：

$$
\operatorname{Var}(X)
=
\frac16
(
6.25+2.25+0.25+0.25+2.25+6.25
)
$$

括号里：

$$
17.5
$$

所以：

$$
\operatorname{Var}(X)
=
\frac{17.5}{6}
$$

即：

$$
\boxed{
\operatorname{Var}(X)=\frac{35}{12}\approx2.9167
}
$$

---

# 十三、可是“方差 = 2.9167”到底是什么单位？

这是一个非常重要的问题。

如果：

$$
X
$$

单位是：

$$
\text{cm}
$$

那么：

$$
X-E[X]
$$

单位仍然：

$$
\text{cm}
$$

但平方之后：

$$
(X-E[X])^2
$$

单位变成：

$$
\text{cm}^2
$$

所以：

$$
\operatorname{Var}(X)
$$

单位也是：

$$
\boxed{\text{cm}^2}
$$

这就产生一个实际解释问题。

例如身高方差：

$$
25\text{ cm}^2
$$

我们不太自然地说：

> 身高典型波动是25平方厘米。

这不是原数据的单位。

所以需要下一个概念。

---

# 十四、标准差为什么自然出现？

既然方差的单位被平方了，

我们就把它开平方：

$$
\boxed{
\sigma_X
=
\sqrt{\operatorname{Var}(X)}
}
$$

这叫：

> 标准差 Standard Deviation。

如果：

$$
X
$$

单位是：

$$
\text{cm}
$$

那么：

$$
\operatorname{Var}(X)
$$

单位：

$$
\text{cm}^2
$$

再开平方：

$$
\sqrt{\text{cm}^2}
=
\text{cm}
$$

于是标准差重新回到了原始单位。

因此：

$$
\boxed{
\text{标准差是“用原始单位表达的波动尺度”}
}
$$

这正好连接你第二十课补充中问过的：

> 为什么 $\sigma$ 能和 $x-\mu$ 相除？

因为它们拥有相同的单位。

---

# 十五、方差 vs 标准差

这两个概念一定要主动分开。

| 概念   | 方差                        | 标准差                              |
| ---- | ------------------------- | -------------------------------- |
| 记号   | $\operatorname{Var}(X)$ | $\sigma_X$                     |
| 定义   | $E[(X-E[X])^2]$         | $\sqrt{\operatorname{Var}(X)}$ |
| 单位   | 原单位平方                     | 原单位                              |
| 主要作用 | 数学推导特别方便                  | 实际解释更加直观                         |

关系：

$$
\boxed{
\operatorname{Var}(X)=\sigma_X^2
}
$$

所以正态分布：

$$
X\sim\mathcal N(\mu,\sigma^2)
$$

里面第二个参数：

$$
\sigma^2
$$

就是方差。

而：

$$
\sigma
$$

才是标准差。

---

# 十六、现在重新理解正态分布中的 $\sigma$

第二十课：

$$
X\sim\mathcal N(\mu,\sigma^2)
$$

当时我们说：

> $\sigma$ 控制分布横向铺开的尺度。

现在终于可以给出正式定义：

$$
\boxed{
\sigma^2=\operatorname{Var}(X)
}
$$

以及：

$$
\boxed{
\sigma=\sqrt{\operatorname{Var}(X)}
}
$$

所以：

$$
\frac{x-\mu}{\sigma}
$$

真正是在做：

$$
\frac{
\text{实际离均值的距离}
}{
\text{该分布的标准波动尺度}
}
$$

也就是 Z-score。

至此，第二十课那个问题从“直觉解释”正式进入了数学定义。

---

# 十七、方差最重要的第二个公式从哪里来？

教材经常直接给：

$$
\boxed{
\operatorname{Var}(X)
=
E[X^2]-(E[X])^2
}
$$

这条公式非常重要。

但绝对不要死记。

我们从定义一步一步展开。

起点：

$$
\operatorname{Var}(X)
=
E[(X-E[X])^2]
$$

为了简化符号，设：

$$
\mu=E[X]
$$

于是：

$$
\operatorname{Var}(X)
=
E[(X-\mu)^2]
$$

---

# 十八、第一步：平方展开

使用：

$$
(a-b)^2=a^2-2ab+b^2
$$

所以：

$$
(X-\mu)^2
=
X^2-2\mu X+\mu^2
$$

于是：

$$
\operatorname{Var}(X)
=
E[X^2-2\mu X+\mu^2]
$$

---

# 十九、第二步：利用期望线性性

因为：

$$
E[A+B+C]
=
E[A]+E[B]+E[C]
$$

所以：

$$
=
E[X^2]
-
E[2\mu X]
+
E[\mu^2]
$$

因为：

$$
\mu
$$

是常数，可以提出：

$$
E[2\mu X]
=
2\mu E[X]
$$

而：

$$
E[\mu^2]
=
\mu^2
$$

于是：

$$
\operatorname{Var}(X)
=
E[X^2]
-
2\mu E[X]
+
\mu^2
$$

---

# 二十、第三步：代入 $\mu=E[X]$

因为：

$$
E[X]=\mu
$$

所以：

$$
-2\mu E[X]
=
-2\mu^2
$$

于是：

$$
\operatorname{Var}(X)
=
E[X^2]-2\mu^2+\mu^2
$$

合并：

$$
-2\mu^2+\mu^2=-\mu^2
$$

因此：

$$
\boxed{
\operatorname{Var}(X)
=
E[X^2]-\mu^2
}
$$

而：

$$
\mu=E[X]
$$

所以最终：

$$
\boxed{
\operatorname{Var}(X)
=
E[X^2]-(E[X])^2
}
$$

---

# 二十一、一定要区分 $E[X^2]$ 和 $(E[X])^2$

这是概率论中非常容易混淆的一组。

## $E[X^2]$

先：

$$
X\to X^2
$$

再求期望。

即：

```text
每个结果先平方
↓
再做概率加权平均
```

---

## $(E[X])^2$

先求：

$$
E[X]
$$

得到一个数，

再平方。

即：

```text
先算平均位置
↓
再把平均位置平方
```

一般：

$$
\boxed{
E[X^2]\neq(E[X])^2
}
$$

它们的差：

$$
E[X^2]-(E[X])^2
$$

恰好就是：

$$
\operatorname{Var}(X)
$$

---

# 二十二、一个例子彻底区分它们

设：

$$
X=
\begin{cases}
0,&0.5\\
10,&0.5
\end{cases}
$$

先求：

$$
E[X]=5
$$

所以：

$$
(E[X])^2=25
$$

再求：

$$
E[X^2]
$$

因为：

$$
X^2=
\begin{cases}
0,&0.5\\
100,&0.5
\end{cases}
$$

所以：

$$
E[X^2]
=
0\times0.5
+
100\times0.5
=
50
$$

于是：

$$
\operatorname{Var}(X)
=
50-25
=
25
$$

和之前完全一致。

---

# 二十三、为什么方差一定非负？

从定义：

$$
\operatorname{Var}(X)
=
E[(X-E[X])^2]
$$

对于任何结果：

$$
(X-E[X])^2\ge0
$$

一个永远非负的随机变量，其期望也不会为负。

所以：

$$
\boxed{
\operatorname{Var}(X)\ge0
}
$$

这不是额外规定。

它直接来自：

$$
\text{平方不可能为负}
$$

---

# 二十四、什么时候方差等于0？

如果：

$$
\operatorname{Var}(X)=0
$$

意味着：

$$
E[(X-E[X])^2]=0
$$

而平方偏差永远非负。

要让平均等于0，在通常概率意义下只能是：

$$
(X-E[X])^2=0
$$

几乎必然成立。

所以：

$$
X=E[X]
$$

几乎必然。

直觉上：

$$
\boxed{
\text{方差为0}
\Rightarrow
\text{随机变量没有真正的波动}
}
$$

---

# 二十五、常数平移为什么不改变方差？

假设：

$$
Y=X+b
$$

也就是所有结果都整体加：

$$
b
$$

例如：

```text
原来：
1 2 3

全部 +100：

101 102 103
```

整个分布只是左右移动。

它的分散程度根本没变。

所以应该有：

$$
\boxed{
\operatorname{Var}(X+b)
=
\operatorname{Var}(X)
}
$$

我们推一下。

因为：

$$
E[X+b]=E[X]+b
$$

所以：

$$
Y-E[Y]
=
(X+b)-(E[X]+b)
$$

中间的：

$$
+b-b
$$

抵消。

得到：

$$
Y-E[Y]
=
X-E[X]
$$

因此平方偏差完全不变。

于是：

$$
\boxed{
\operatorname{Var}(X+b)
=
\operatorname{Var}(X)
}
$$

---

# 二十六、乘一个常数为什么方差乘平方？

假设：

$$
Y=aX
$$

比如把所有长度从“米”转换成“厘米”：

$$
Y=100X
$$

那么偏差也会扩大：

$$
100
$$

倍。

所以平方偏差扩大：

$$
100^2
$$

倍。

一般：

$$
\boxed{
\operatorname{Var}(aX)
=
a^2\operatorname{Var}(X)
}
$$

进一步：

$$
\boxed{
\operatorname{Var}(aX+b)
=
a^2\operatorname{Var}(X)
}
$$

注意：

$$
b
$$

不影响方差。

$$
a
$$

以平方形式影响方差。

---

# 二十七、为什么标准差只乘 $|a|$？

因为：

$$
\sigma_X
=
\sqrt{\operatorname{Var}(X)}
$$

若：

$$
Y=aX+b
$$

则：

$$
\operatorname{Var}(Y)
=
a^2\operatorname{Var}(X)
$$

开平方：

$$
\sigma_Y
=
\sqrt{a^2\operatorname{Var}(X)}
$$

所以：

$$
\boxed{
\sigma_Y
=
|a|\sigma_X
}
$$

为什么是绝对值？

因为：

$$
\sqrt{a^2}=|a|
$$

例如：

$$
Y=-X
$$

只是把数轴左右翻转。

波动尺度不应该变成负数。

---

# 二十八、方差的线性性和期望完全不同

上一课：

$$
E[X+Y]
=
E[X]+E[Y]
$$

永远成立。

很多人于是会误以为：

$$
\operatorname{Var}(X+Y)
=
\operatorname{Var}(X)+\operatorname{Var}(Y)
$$

也永远成立。

**这是错的。**

一般情况下：

$$
\boxed{
\operatorname{Var}(X+Y)
=
\operatorname{Var}(X)
+
\operatorname{Var}(Y)
+
2\operatorname{Cov}(X,Y)
}
$$

这里突然出现：

$$
\operatorname{Cov}(X,Y)
$$

也就是：

> 协方差。

我们下一阶段会正式学习它。

现在只先记边界：

$$
\boxed{
\text{期望求和不要求独立}
}
$$

但是：

$$
\boxed{
\text{方差求和需要考虑两个随机变量如何一起变化}
}
$$

---

# 二十九、如果 $X,Y$ 独立会怎样？

如果：

$$
X,Y
$$

独立，并且方差存在，那么：

$$
\operatorname{Cov}(X,Y)=0
$$

于是：

$$
\boxed{
\operatorname{Var}(X+Y)
=
\operatorname{Var}(X)
+
\operatorname{Var}(Y)
}
$$

这就是为什么独立随机误差累加时，方差可以相加。

注意：

> 不是标准差直接相加。

而是：

$$
\boxed{\text{方差相加}}
$$

---

# 三十、独立噪声为什么会“部分抵消”？

假设我们对同一个量进行多次测量。

第 $i$ 次测量可以写成：

$$
X_i=\mu+\varepsilon_i
$$

其中：

- $\mu$ 是真实值；
- $\varepsilon_i$ 是第 $i$ 次测量的随机误差。

假设这些随机误差相互独立，并且：

$$
E[\varepsilon_i]=0,
\qquad
\operatorname{Var}(\varepsilon_i)=\sigma^2
$$

$E[\varepsilon_i]=0$ 的意思不是每次误差都等于 $0$，而是误差有时为正、有时为负，长期来看没有固定的偏向。

现在把 $n$ 次测量相加：

$$
S_n=X_1+\cdots+X_n
$$

代入 $X_i=\mu+\varepsilon_i$：

$$
S_n
=
n\mu
+
(\varepsilon_1+\cdots+\varepsilon_n)
$$

所以总和由两部分组成：

$$
\underbrace{n\mu}_{\text{确定的中心}}
+
\underbrace{(\varepsilon_1+\cdots+\varepsilon_n)}_{\text{随机误差}}
$$

确定的部分每次都增加 $\mu$，因此总共增长为 $n\mu$。

但是，随机误差有正有负。例如：

$$
+1,-1,+1,-1
$$

相加后可能接近 $0$。这就是所谓的“部分抵消”。

这种抵消不是说每一次都一定恰好抵消，而是说独立误差不会总是朝同一个方向累积。

从方差来看，因为误差相互独立：

$$
\operatorname{Var}
(\varepsilon_1+\cdots+\varepsilon_n)
=
n\sigma^2
$$

因此，误差总和的标准差是：

$$
\sqrt{n\sigma^2}
=
\sqrt n\,\sigma
$$

也就是说：

```text
确定的中心：增长 n 倍
随机波动：  只增长 √n 倍
```

例如，每次误差都是 $+1$ 或 $-1$，两种情况的概率相同。

测量 $100$ 次时，如果所有误差都朝同一个方向，总误差可以达到 $100$；但是由于正负误差随机出现，典型的总误差尺度只有：

$$
\sqrt{100}=10
$$

需要特别注意：

> “部分抵消”不表示总和的波动变小。

总和的标准差从 $\sigma$ 增长到了 $\sqrt n\,\sigma$，仍然在变大；只不过它增长得比总量的中心 $n\mu$ 慢。

真正会随着测量次数增加而变得更稳定的，是下一节中的**样本平均**。

---

# 三十一、样本平均为什么越来越稳定？

仍然考虑：

$$
X_i=\mu+\varepsilon_i
$$

把 $n$ 次测量相加以后再除以 $n$，得到样本平均：

$$
\bar X
=
\frac{X_1+\cdots+X_n}{n}
$$

代入 $X_i=\mu+\varepsilon_i$：

$$
\bar X
=
\mu
+
\frac{\varepsilon_1+\cdots+\varepsilon_n}{n}
$$

这说明样本平均也由两部分组成：

$$
\underbrace{\mu}_{\text{真实值}}
+
\underbrace{
\frac{\varepsilon_1+\cdots+\varepsilon_n}{n}
}_{\text{平均后的随机误差}}
$$

因为 $E[\varepsilon_i]=0$，所以：

$$
E[\bar X]=\mu
$$

也就是说，样本平均仍然以真实值 $\mu$ 为中心。

下面来看它的波动大小。假设各次测量独立，并且：

$$
\operatorname{Var}(X_i)=\sigma^2
$$

那么：

$$
\operatorname{Var}(\bar X)
=
\operatorname{Var}
\left(
\frac1n
\sum_{i=1}^nX_i
\right)
$$

利用方差的缩放性质，常数 $1/n$ 提出后需要平方：

$$
=
\frac1{n^2}
\operatorname{Var}
\left(
\sum_{i=1}^nX_i
\right)
$$

又因为各次测量相互独立，所以方差可以相加：

$$
=
\frac1{n^2}
n\sigma^2
$$

所以：

$$
\boxed{
\operatorname{Var}(\bar X)
=
\frac{\sigma^2}{n}
}
$$

因此样本平均的标准差为：

$$
\boxed{
\sigma_{\bar X}
=
\frac{\sigma}{\sqrt n}
}
$$

这说明：

```text
样本数 n 增大
      ↓
正负随机误差得到更多机会相互抵消
      ↓
平均值的标准差按 1/√n 缩小
```

例如，单次测量的标准差为 $\sigma=1$：

| 测量次数 $n$ | 总和的标准差 $\sqrt n\,\sigma$ | 平均值的标准差 $\sigma/\sqrt n$ |
|---:|---:|---:|
| $1$ | $1$ | $1$ |
| $4$ | $2$ | $0.5$ |
| $100$ | $10$ | $0.1$ |

可以看到：总和的波动虽然在增大，但除以测量次数以后，平均值的波动却在减小。

因此：

> 多测几次再取平均，通常比只测一次更可靠。

而且需要注意，样本数增加得很快，标准差下降得相对较慢：

- 测量次数变为原来的 $4$ 倍，标准差才缩小为原来的 $1/2$；
- 测量次数变为原来的 $100$ 倍，标准差才缩小为原来的 $1/10$。

最后，这个结论依赖于“误差相互独立”这个重要条件。

如果仪器总是固定地多测 $2$ 克，那么这属于系统误差。无论测量多少次再取平均，这个 $2$ 克的偏差都不会被消除。

所以更准确地说：

$$
\boxed{
\text{多次取平均可以减小独立随机误差，但不能自动消除系统误差}
}
$$

---

# 三十二、伯努利分布的方差

设：

$$
X\sim\operatorname{Bernoulli}(p)
$$

我们已经知道：

$$
E[X]=p
$$

利用：

$$
\operatorname{Var}(X)
=
E[X^2]-(E[X])^2
$$

由于伯努利：

$$
X\in\{0,1\}
$$

所以一个非常特别的性质是：

$$
X^2=X
$$

因为：

$$
0^2=0
$$

$$
1^2=1
$$

因此：

$$
E[X^2]
=
E[X]
=
p
$$

所以：

$$
\operatorname{Var}(X)
=
p-p^2
$$

提取：

$$
p
$$

得到：

$$
\boxed{
\operatorname{Var}(X)
=
p(1-p)
}
$$

---

# 三十三、为什么 Bernoulli 在 $p=0.5$ 时最不稳定？

看：

$$
\operatorname{Var}(X)=p(1-p)
$$

如果：

$$
p\approx0
$$

那么几乎总是：

$$
X=0
$$

结果很稳定。

如果：

$$
p\approx1
$$

那么几乎总是：

$$
X=1
$$

也很稳定。

而：

$$
p=0.5
$$

时：

```text
0和1各一半
```

最难预测。

此时：

$$
\operatorname{Var}(X)
=
0.5\times0.5
=
0.25
$$

达到最大。

所以：

$$
\boxed{
\text{概率最接近50/50时，伯努利随机性最大}
}
$$

---

# 三十四、二项分布的方差为什么是 $np(1-p)$？

设：

$$
X\sim\operatorname{Binomial}(n,p)
$$

第十四课已经知道：

$$
X=X_1+\cdots+X_n
$$

其中各：

$$
X_i\sim\operatorname{Bernoulli}(p)
$$

并且相互独立。

每一个：

$$
\operatorname{Var}(X_i)
=
p(1-p)
$$

独立时方差相加：

$$
\operatorname{Var}(X)
=
\sum_{i=1}^n
\operatorname{Var}(X_i)
$$

所以：

$$
=
np(1-p)
$$

因此：

$$
\boxed{
\operatorname{Var}(X)
=
np(1-p)
}
$$

于是标准差：

$$
\boxed{
\sigma_X
=
\sqrt{np(1-p)}
}
$$

---

# 三十五、泊松分布的方差非常特别

若：

$$
X\sim\operatorname{Poisson}(\lambda)
$$

那么：

$$
E[X]=\lambda
$$

而且：

$$
\boxed{
\operatorname{Var}(X)=\lambda
}
$$

所以：

$$
\boxed{
\text{Poisson 的均值和方差相等}
}
$$

这是泊松分布一个非常重要的结构特征。

以后做计数数据建模时，如果实际数据：

$$
\text{方差远大于均值}
$$

可能意味着简单 Poisson 模型并不合适。

---

# 三十六、指数分布的方差

若：

$$
T\sim\operatorname{Exp}(\lambda)
$$

我们已经知道：

$$
E[T]=\frac1\lambda
$$

其方差是：

$$
\boxed{
\operatorname{Var}(T)
=
\frac1{\lambda^2}
}
$$

所以标准差：

$$
\boxed{
\sigma_T=\frac1\lambda
}
$$

非常特别：

$$
\boxed{
\text{指数分布的标准差 = 期望}
}
$$

---

# 三十七、正态分布终于完整了

如果：

$$
X\sim\mathcal N(\mu,\sigma^2)
$$

现在我们可以正式理解两个参数：

$$
\boxed{
E[X]=\mu
}
$$

以及：

$$
\boxed{
\operatorname{Var}(X)=\sigma^2
}
$$

所以：

```text
μ
= 中心位置

σ²
= 平方波动尺度

σ
= 原单位下的典型波动尺度
```

这也是为什么正态分布记作：

$$
\mathcal N(\mu,\sigma^2)
$$

而不是随便写两个参数。

---

# 三十八、一个特别重要的反例：方差相同，分布仍然可以完全不同

和“期望相同不代表分布相同”一样，

即使两个随机变量：

$$
E[X]=E[Y]
$$

而且：

$$
\operatorname{Var}(X)=\operatorname{Var}(Y)
$$

它们的分布仍然可能完全不同。

例如一个可能：

> 只有两个离散点。

另一个可能：

> 连续钟形分布。

另一个还可能：

> 大量质量集中在中心，但偶尔有极端值。

所以：

$$
\boxed{
\text{期望 + 方差}
}
$$

仍然只是：

> 分布的两个摘要。

不是完整分布。

---

# 三十九、方差为什么对异常值特别敏感？

因为它使用：

$$
(X-\mu)^2
$$

例如偏差：

```text
1 → 1
2 → 4
10 → 100
100 → 10000
```

所以极端点很容易强烈拉高方差。

这既是优点，也是局限。

### 优点

能敏感识别大偏离。

### 局限

少量极端异常值可能显著改变方差。

所以数据分析中有时也会使用：

* 中位数；
* MAD；
* 分位数；
* IQR；

等更稳健的统计量。

但这些以后再学。

---

# 四十、机器学习中方差出现在哪里？

## 1. 数据标准化

常见：

$$
z=\frac{x-\mu}{\sigma}
$$

其中：

$$
\sigma=\sqrt{\operatorname{Var}(X)}
$$

所以标准化本质是：

```text
减掉中心
+
除以波动尺度
```

---

## 2. 高斯噪声

例如：

$$
\epsilon\sim\mathcal N(0,\sigma^2)
$$

其中：

$$
\sigma^2
$$

直接控制噪声强度。

---

## 3. 初始化

神经网络初始化中的 variance scaling，本质就在控制：

> 信号经过网络传播时，不要让波动越来越大或越来越小。

---

## 4. Bias–Variance

未来统计学习中会遇到：

> bias–variance tradeoff。

其中 variance 描述：

> 模型对不同训练数据有多敏感。

虽然和今天随机变量方差的具体对象不同，但数学思想仍然是：

> 围绕某个中心有多大波动。

---

# 四十一、把期望和方差放在一张图里

假设概率分布：

```text
                  /\
                /    \
              /        \
_____________/__________\____________→ x
                  μ
```

### 期望

问：

> 重心在哪里？

$$
E[X]=\mu
$$

---

### 方差

问：

> 概率质量距离这个重心通常有多远？

但用的是：

> 平方距离的平均。

$$
\operatorname{Var}(X)
=
E[(X-\mu)^2]
$$

---

### 标准差

问：

> 把平方距离重新恢复成原始单位以后，波动尺度是多少？

$$
\sigma
=
\sqrt{\operatorname{Var}(X)}
$$

所以：

```text
分布
 ↓
中心
 E[X]

 ↓

围绕中心的平方分散
 Var(X)

 ↓

原单位下的波动尺度
 σ
```

---

# 理解检查

请先凭直觉思考。

## 1

为什么不能定义：

$$
\operatorname{Var}(X)
=
E[X-E[X]]
$$

？

因为它永远等于什么？

---

## 2

为什么：

$$
(X-E[X])^2
$$

能解决正负偏差抵消的问题？

---

## 3

如果一个随机变量：

$$
X=7
$$

永远成立，那么：

$$
\operatorname{Var}(X)=?
$$

为什么？

---

## 4

如果：

$$
Y=X+1000
$$

为什么：

$$
\operatorname{Var}(Y)
=
\operatorname{Var}(X)
$$

？

从图像上，它只是发生了什么？

---

## 5

如果：

$$
Y=10X
$$

为什么方差不是乘10，而是乘：

$$
100
$$

？

提示：

> 方差研究的是平方距离。

---

## 6

为什么：

$$
E[X^2]
$$

和：

$$
(E[X])^2
$$

不是同一回事？

---

## 7 —— 最重要

考虑：

$$
A=
\begin{cases}
0,&0.5\\
10,&0.5
\end{cases}
$$

和：

$$
B=5
$$

它们：

$$
E[A]=E[B]=5
$$

为什么：

$$
\operatorname{Var}(A)=25
$$

而：

$$
\operatorname{Var}(B)=0
$$

？

如果你能不用公式，直接用“距离中心有多远”的语言解释出来，就真正掌握了方差的核心。

---

# 这一节真正需要记住的核心

只需要牢牢记住下面 **5 句话**：

1. **期望只告诉我们分布中心在哪里；方差告诉我们随机变量围绕这个中心散得有多开。**

2. 直接平均偏差：

$$
E[X-E[X]]
$$

永远是0，因为左右偏差会抵消，因此我们改为研究平方偏差：

$$
\boxed{
\operatorname{Var}(X)
=
E[(X-E[X])^2]
}
$$

3. 方差还可以写成：

$$
\boxed{
\operatorname{Var}(X)
=
E[X^2]-(E[X])^2
}
$$

但这条公式必须能从定义一步一步展开出来，而不是死记。

4. 方差的单位是原单位平方，因此定义标准差：

$$
\boxed{
\sigma=\sqrt{\operatorname{Var}(X)}
}
$$

它重新回到原始单位，可以理解成随机变量的典型波动尺度。

5. **期望和方差都只是概率分布的摘要。即使两个随机变量的期望和方差都相同，它们的完整概率分布仍然可能不同。**

---

下一步为什么自然进入**协方差**？

因为到现在为止，我们只研究一个随机变量：

> $X$ 自己怎么波动。

但现实中经常同时有两个随机变量：

$$
X=\text{身高}
$$

$$
Y=\text{体重}
$$

真正的问题会变成：

> **当 $X$ 比自己的平均值偏大时，$Y$ 是否也倾向于比自己的平均值偏大？**

这就是：

$$
\boxed{\text{协方差 Cov(X,Y)}}
$$

它可以看成方差从“一个随机变量如何偏离自己”推广到：

> **两个随机变量是否一起偏离各自中心。**
