# 第二十七课：边缘分布——只关心一个变量时，其他变量的概率怎么办？

上一课，我们用联合分布描述了：

> **同一次试验中，两个随机变量怎样共同取值，以及每种组合有多大概率。**

现在，我们改变研究问题：

> **已经知道 $X,Y$ 的联合分布，但现在只关心 $X$，不再关心 $Y$。怎样得到 $X$ 自己的分布？**

这就是本课的核心：**边缘分布**。

我们现在的位置是：

```text
多维随机变量
把多个读数作为整体
        ↓
联合分布
这些读数怎样共同取值？
        ↓
边缘分布 ← 第二十七课
只保留其中一些读数，概率怎样汇总？
        ↓
条件分布
已知某些读数以后，其他读数的分布怎样改变？
```

今天始终围绕一个动作展开：

> **不再区分某个变量的取值，但要把这些取值对应的概率保留下来、汇总起来。**

---

## 一、问题背景：不看右边数字以后，左边的分布是什么？

### 1. 继续使用上一课的十张卡片

袋子里事先放好十张卡片。每张卡片上有两个数字，一个在左边，一个在右边。

具体配置不变：

| 卡片编号   | 左边数字 $X$ | 右边数字 $Y$ | 这一类的卡片数量 |
| ------ | ---------: | ---------: | -------: |
| 1      |          0 |          0 |        1 |
| 2、3    |          0 |          1 |        2 |
| 4、5    |          1 |          0 |        2 |
| 6      |          1 |          1 |        1 |
| 7      |          2 |          0 |        1 |
| 8、9、10 |          2 |          1 |        3 |

**一次完整试验：从这个袋子里等可能地随机抽出一张卡片，读取它的左、右两个数字，然后结束。**

所以，每张具体卡片被抽到的概率都是：

$$
\frac1{10}.
$$

这里仍然是：

> **一次抽取、一张卡片、两个读数。**

上一课得到的联合概率表是：

| $X\backslash Y$ | $Y=0$ | $Y=1$ |
| ----------------- | ------: | ------: |
| $X=0$           | $0.1$ | $0.2$ |
| $X=1$           | $0.2$ | $0.1$ |
| $X=2$           | $0.1$ | $0.3$ |

例如：

$$
P(X=2,Y=1)=0.3
$$

表示抽到的同一张卡片，左边为 2，并且右边为 1。

### 2. 现在改变观察目的

假设我们不再关心右边写着什么，只问：

> **抽出的卡片，左边数字等于 2 的概率是多少？**

注意，这不是换了袋子，也不是重新设计抽取规则。

**抽取机制完全没变，只是我们的问题变了。**

原来问：

> 左边是 2，**并且右边是 1**吗？

现在问：

> 左边是 2 吗？**右边是什么都可以。**

于是，下面两种卡片都符合要求：

```text
(2,0) → 符合
(2,1) → 也符合
```

对应编号为：

$$
7、8、9、10.
$$

十张卡片中有四张符合要求，所以：

$$
P(X=2)=\frac4{10}=0.4.
$$

也可以直接从联合概率表得到：

$$
\boxed{
P(X=2)
=
P(X=2,Y=0)+P(X=2,Y=1)
=
0.1+0.3
=
0.4.
}
$$

这一次加法，就是边缘分布最核心的动作。

---

## 二、直觉：遮住一个读数，不等于丢掉那些卡片

想象你把每张卡片右边的数字遮住。

原来我们区分：

```text
(2,0)
(2,1)
```

遮住右边后，它们都只显示：

```text
2
```

于是：

```text
完整组合                         只保留左边

(2,0)，概率0.1 ─────┐
                    ├────────→ 2，概率0.4
(2,1)，概率0.3 ─────┘
```

为什么概率要相加？

因为我们现在不再区分：

> 这个 2 是和 0 配对出现的，还是和 1 配对出现的。

但两类结果都必须保留。

所以：

$$
\boxed{
\text{不再区分取值组合}
\quad\Longrightarrow\quad
\text{把对应的概率合并}
}
$$

这和第九课的思想完全一致：多个结果被映射到同一个数值时，它们携带的概率也一起汇总过去。

只不过这一次，我们压缩的是：

$$
(x,y)\longmapsto x.
$$

也就是：

> **从一对数字中，只保留第一个数字。**

这里要立刻分清：

**不关心 $Y$，不是令 $Y=0$，也不是删掉所有 $Y=1$ 的卡片。**

我们只是忽略右边数字的区别，没有排除任何一类右边数字。

---

## 三、数学定义：什么是边缘分布？

对于随机向量：

$$
(X,Y),
$$

单独考虑 $X$ 时，$X$ 自己的概率分布，称为这个联合分布关于 $X$ 的**边缘分布**。

同理，单独考虑 $Y$ 时，得到关于 $Y$ 的边缘分布。

因此：

> **边缘分布不是一种全新的单变量分布，而是单个变量的分布在“联合分布背景下”的名称。**

我们以前一直写的：

$$
P(X=x)
$$

并没有变成另一种概率。

现在只是明确了：

> 它可以从更完整的共同取值规律中提取出来。

### 1. 离散情况下的记号

联合概率质量函数是：

$$
p_{X,Y}(x,y)=P(X=x,Y=y).
$$

$X$ 的边缘概率质量函数是：

$$
p_X(x)=P(X=x).
$$

这里：

| 记号               | 描述什么                       |
| ---------------- | -------------------------- |
| $X,Y$          | 两个随机变量                     |
| $x,y$          | 两个具体候选数值                   |
| $p_{X,Y}(x,y)$ | 组合 $(x,y)$ 的概率           |
| $p_X(x)$       | 只要求 $X=x$、不限制 $Y$ 时的概率 |

现在要解决的，就是：

$$
p_{X,Y}(x,y)
\quad\longrightarrow\quad
p_X(x).
$$

### 2. 公式怎样从直觉中长出来？

假设 $Y$ 的所有可能取值可以列成：

$$
y_1,y_2,y_3,\ldots
$$

其中 $j$ 只是取值列表的编号，$y_j$ 表示第 $j$ 个候选值。

要使 $X=x$，就可能是：

```text
X=x，并且Y=y₁

或者

X=x，并且Y=y₂

或者

X=x，并且Y=y₃

……
```

这些情况有两个特点。

**第一，没有遗漏。**只要 $X=x$，实际出现的 $Y$ 总会落在它的某个可能取值上。

**第二，彼此互斥。**同一次试验中，$Y$ 不可能同时等于两个不同的数值。

因此，可以把这些事件的概率直接相加：

$$
P(X=x)
=
P(X=x,Y=y_1)
+
P(X=x,Y=y_2)
+\cdots
$$

用求和符号压缩：

$$
\boxed{
p_X(x)=\sum_j p_{X,Y}(x,y_j).
}
$$

整条公式读作：

> **固定我们关心的 $x$，遍历 $Y$ 的所有可能取值，把相应的联合概率加起来。**

同理：

$$
\boxed{
p_Y(y)=\sum_i p_{X,Y}(x_i,y).
}
$$

这里的 $x_i$ 遍历 $X$ 的可能取值。

所以最实用的记忆方式不是“横着加还是竖着加”，而是：

$$
\boxed{
\text{保留谁，就固定谁；不再区分谁，就对谁求和。}
}
$$

因为表格可以转置，但这个原则不会变。

---

## 四、把整张联合表变成两个边缘分布

### 1. 求 $X$ 的边缘分布

固定 $X$，把这一行中不同 $Y$ 的概率相加。

$$
P(X=0)=0.1+0.2=0.3,
$$

$$
P(X=1)=0.2+0.1=0.3,
$$

$$
P(X=2)=0.1+0.3=0.4.
$$

因此：

| $x$      |       0 |       1 |       2 |
| ---------- | ------: | ------: | ------: |
| $p_X(x)$ | $0.3$ | $0.3$ | $0.4$ |

其他数值的概率为 0。

### 2. 求 $Y$ 的边缘分布

固定 $Y$，把对应一列中不同 $X$ 的概率相加。

$$
P(Y=0)=0.1+0.2+0.1=0.4,
$$

$$
P(Y=1)=0.2+0.1+0.3=0.6.
$$

因此：

| $y$      |       0 |       1 |
| ---------- | ------: | ------: |
| $p_Y(y)$ | $0.4$ | $0.6$ |

### 3. 把结果放回原表

| $X\backslash Y$ |     $Y=0$ |     $Y=1$ | **$X$ 的边缘概率** |
| ----------------- | ----------: | ----------: | --------------: |
| $X=0$           |     $0.1$ |     $0.2$ |     **$0.3$** |
| $X=1$           |     $0.2$ |     $0.1$ |     **$0.3$** |
| $X=2$           |     $0.1$ |     $0.3$ |     **$0.4$** |
| **$Y$ 的边缘概率**   | **$0.4$** | **$0.6$** |       **$1$** |

这样就很容易理解“边缘”这个名字：单变量概率出现在联合表的右侧、下侧边缘。

但不要把它理解成：

> 只研究数值范围边界上的那些结果。

例如 $P(X=1)=0.3$ 也属于边缘分布，虽然 1 是 $X$ 取值范围中间的数。

---

## 五、为什么这里是相加，不是平均，也不需要独立？

这部分把几个容易出错的操作一起分清。

### 1. 为什么不除以列数？

刚才：

$$
P(X=2)=0.1+0.3=0.4.
$$

为什么不是：

$$
\frac{0.1+0.3}{2}=0.2?
$$

因为我们不是在问：

> 这一行的两个概率，平均是多少？

而是在问：

> 这一行对应的两类结果，合起来拥有多少概率？

回到原始卡片看：

```text
(2,0)：1张
(2,1)：3张

左边为2：总共4张
```

应该计算的是：

$$
\frac{1+3}{10},
$$

不是再把它除以 2。

**边缘化是在合并概率，不是在计算格子概率的算术平均。**

### 2. 为什么不再乘一次 $P(Y=y)$？

因为：

$$
p_{X,Y}(x,y)
$$

已经是完整事件“$X=x$ 并且 $Y=y$”的概率。

例如：

$$
p_{X,Y}(2,1)=0.3
$$

已经把右边为 1 这一要求考虑进去了。

所以边缘化应当直接相加：

$$
p_X(2)=p_{X,Y}(2,0)+p_{X,Y}(2,1).
$$

**不能再额外给每个联合概率乘一次 $Y$ 的概率。**

### 3. 为什么不要求独立？

回顾推导，我们只用了：

```text
不同的Y取值互斥
+
所有Y取值没有遗漏
+
互斥事件概率相加
```

没有用到独立性。

所以：

$$
\boxed{
\text{无论 }X,Y\text{ 是否独立，都可以从联合分布求边缘分布。}
}
$$

“不再观察 $Y$”是我们研究视角的变化。

“$X,Y$ 独立”是概率模型的结构性质。

**不看一个变量，不会让两个变量自动变得独立。**

### 4. 汇总之后，为什么总概率仍然为 1？

$X$ 的边缘概率满足：

$$
0.3+0.3+0.4=1.
$$

因为我们只是把原来的六个格子重新分成三组：

$$
\begin{aligned}
\sum_i p_X(x_i)
&=\sum_i\left[\sum_j p_{X,Y}(x_i,y_j)\right]\\
&=1.
\end{aligned}
$$

每个原始格子恰好被算了一次，没有遗漏，也没有重复。

因此：

> **边缘化会丢掉配对细节，但不会丢掉总概率。**

---

## 六、几何直觉：把平面上的概率质量汇总到一条轴上

上一课，联合分布的取值放在平面上：

```text
Y
↑
1    ● 0.2          ● 0.1          ● 0.3

0    ● 0.1          ● 0.2          ● 0.1
     └──────────────────────────────────→ X
     0              1              2
```

这里两条坐标轴分别是 $X,Y$ 的取值，点旁边才是概率。

现在只保留 $X$。

相同横坐标的点会合并：

```text
横坐标为0：0.1 + 0.2 → 0.3
横坐标为1：0.2 + 0.1 → 0.3
横坐标为2：0.1 + 0.3 → 0.4
```

最后在 $X$ 轴上得到：

```text
X 的位置：    0          1          2
对应概率：   0.3        0.3        0.4
```

这可以理解成一种**带着概率重量的投影**。

注意，不是只把点的轮廓投到轴上，也不是只保留每组中最大的概率。

而是：

> **所有被投到同一个位置的概率质量，都要合并。**

如果之后把 $p_X(x)$ 画成概率质量图，坐标轴又变成：

```text
横轴：X的取值
纵轴：p_X(x)，即对应概率
```

因此，“联合取值图中的纵轴 $Y$”与“边缘 PMF 图中的纵轴概率”，不是同一个含义。

---

## 七、最重要的区别：边缘分布不是条件分布

我们用同一套卡片，对比三个问题。

### 问题 A：左边是 2，并且右边是 1

这是联合概率：

$$
P(X=2,Y=1)=\frac3{10}=0.3.
$$

要求两个条件同时满足，分母是全部十张卡片。

### 问题 B：只要求左边是 2，右边不限

这是边缘概率：

$$
P(X=2)=\frac4{10}=0.4.
$$

所有左边为 2 的卡片都保留，分母仍是全部十张卡片。

### 问题 C：已经知道右边是 1，再问左边是否是 2

现在，信息告诉我们：

$$
Y=1.
$$

因此只需要考虑右边为 1 的六张卡片：

$$
2、3、6、8、9、10.
$$

其中，左边为 2 的是：

$$
8、9、10.
$$

所以：

$$
P(X=2\mid Y=1)=\frac36=0.5.
$$

这正是第六课建立的条件概率思想：先限制到已知条件对应的结果，再在这个范围内重新计算比例。

用已有公式表示：

$$
\begin{aligned}
P(X=2\mid Y=1)
&=\frac{P(X=2,Y=1)}{P(Y=1)}\\
&=\frac{0.3}{0.6}\\
&=0.5.
\end{aligned}
$$

三个结果放在一起：

| 问题                    | 概率表达式              |    本例结果 |
| --------------------- | ------------------ | ------: |
| 两个条件同时满足              | $P(X=2,Y=1)$     | $0.3$ |
| 只要求 $X=2$，不限制 $Y$ | $P(X=2)$         | $0.4$ |
| 已知 $Y=1$，再问 $X=2$ | $P(X=2\mid Y=1)$ | $0.5$ |

核心区别是：

$$
\boxed{
\begin{aligned}
\text{边缘化：}&\quad\text{不区分另一变量，合并所有情况。}\\
\text{条件化：}&\quad\text{知道另一变量的信息，筛选并重新归一化。}
\end{aligned}
}
$$

**“不知道右边是多少”与“已经知道右边是 1”，是不同的信息状态。**

本课只用这个对比建立边界，下一课再系统研究条件分布。

---

## 八、从联合 CDF，怎样得到边缘 CDF？

我们已经知道，单变量的 CDF 是：

$$
F_X(x)=P(X\le x),
$$

其中 $x$ 是人为指定的阈值。

上一课的联合 CDF 是：

$$
F_{X,Y}(x,y)=P(X\le x,Y\le y).
$$

现在想只保留：

$$
X\le x,
$$

不再限制 $Y$，应该怎样处理第二个阈值？

### 1. 先在卡片例子里理解

本例中 $Y$ 只会取 0 或 1。

因此，只要把第二个阈值设为 1：

$$
Y\le1
$$

就已经包含了 $Y$ 的全部可能取值。

所以，对任意 $x$：

$$
F_{X,Y}(x,1)=P(X\le x)=F_X(x).
$$

例如：

$$
\begin{aligned}
F_X(1)
&=F_{X,Y}(1,1)\\
&=P(X=0)+P(X=1)\\
&=0.3+0.3\\
&=0.6.
\end{aligned}
$$

但如果把第二个阈值设为 0：

$$
F_{X,Y}(1,0)=0.1+0.2=0.3,
$$

就还排除了 $Y=1$ 的情况，不能得到 $F_X(1)$。

### 2. 推广到没有有限上界的变量

对于一般的实值随机变量，$Y$ 可能没有一个有限的最大值。

这时，就让第二个阈值不断增大：

$$
\boxed{
F_X(x)=\lim_{b\to+\infty}F_{X,Y}(x,b).
}
$$

其中 $b$ 是施加在 $Y$ 上的阈值。

它不断增大，意味着：

> 在保持 $X\le x$ 的同时，逐步取消对 $Y$ 的上界限制。

同理：

$$
\boxed{
F_Y(y)=\lim_{a\to+\infty}F_{X,Y}(a,y).
}
$$

这里的“趋于正无穷”不是说：

$$
Y=+\infty,
$$

而是说：

> **用于筛选 $Y$ 的阈值越来越宽松，最终包含全部概率。**

这个方法不要求随机变量是离散型，也不要求存在联合密度。

---

## 九、连续情况下：为什么对另一变量积分？

离散情况下，我们已经得到：

$$
p_X(x)=\sum_j p_{X,Y}(x,y_j).
$$

连续情况下，思想不变，只是不能再把不可数多个单点概率逐个相加。

我们要沿着不再关心的方向，**通过积分累积密度**。

### 1. 先说清楚适用条件

设随机向量：

$$
(U,V)
$$

存在二维联合概率密度：

$$
f_{U,V}(u,v).
$$

这里换用 $U,V$，与前面的卡片变量区分。

$u,v$ 是取值平面中的两个坐标。

我们现在想得到：

$$
f_U(u),
$$

也就是只观察第一个坐标时的概率密度。

### 2. 先问区间概率，不直接盯着一个点

我们想求：

$$
P(a<U\le b).
$$

其中 $a<b$，是给 $U$ 设置的两个区间端点。

由于没有限制 $V$，对应的平面区域就是：

> 横坐标在 $a,b$ 之间，纵坐标可以取全部实数的一条竖直带。

按照联合密度的区域积分规则：

$$
P(a<U\le b)
=
\int_a^b
\left[
\int_{-\infty}^{+\infty}
f_{U,V}(u,v)\,dv
\right]du.
$$

逐层解释：

**内层积分**固定 $u$，沿着 $v$ 的方向累计。

**外层积分**再把 $u$ 从 $a$ 到 $b$ 这一段累积起来。

另一方面，第十二课的一维密度规则告诉我们：

$$
P(a<U\le b)=\int_a^b f_U(u)\,du.
$$

一维密度通过区间积分给出概率，正是之前已经建立的定义。

对照这两个表达式，可以把内层积分作为 $U$ 的边缘密度：

$$
\boxed{
f_U(u)
=
\int_{-\infty}^{+\infty}f_{U,V}(u,v)\,dv.
}
$$

同理：

$$
\boxed{
f_V(v)
=
\int_{-\infty}^{+\infty}f_{U,V}(u,v)\,du.
}
$$

仍然是同一个原则：

> **保留 $U$，就把 $V$ 方向积分掉；保留 $V$，就把 $U$ 方向积分掉。**

### 3. 为什么积分一次以后，得到的还是密度，不是概率？

因为我们还没有给保留下来的 $U$ 一个区间宽度。

得到：

$$
f_U(u)
$$

以后，如果在 $u$ 附近取很小的宽度 $\Delta u$，并且密度在这一小段内变化不大，才有：

$$
P(u<U\le u+\Delta u)
\approx f_U(u)\Delta u.
$$

这延续了第十二课“很小区间的概率约等于密度乘以宽度”的直觉。

因此：

$$
\boxed{
\int f_{U,V}(u,v)\,dv
\text{ 得到的是 }f_U(u)，
\text{不是 }P(U=u).
}
$$

我们把二维密度变成了一维密度；再沿剩下的维度对一个区间积分，才得到概率。

---

## 十、连续例子：二维分布均匀，边缘分布也一定均匀吗？

这一节换一个明确的连续试验，**不再抽卡片**。

### 1. 一次试验是什么？

在下面这个三角形区域中，按面积均匀地随机选取**一个点**：

$$
D=\{(u,v):0\le v\le u\le1\}.
$$

然后记录：

$$
U=\text{这个点的横坐标},
\qquad
V=\text{这个点的纵坐标}.
$$

因此仍然是：

> **一次试验、一个点、两个坐标。**

条件：

$$
0\le v\le u\le1
$$

表示点位于单位正方形内、对角线 $v=u$ 的下方：

```text
V
↑
1                 ●
                / │
              /   │
            /     │
          /       │
        /         │
0     ●───────────● → U
      0           1

随机点在三角形内部按面积均匀分布。
```

“按面积均匀”意味着相同面积的区域具有相同概率。

三角形面积为：

$$
\frac{1\times1}{2}=\frac12.
$$

若区域内联合密度为常数 $k$，总概率必须为 1：

$$
k\times\frac12=1.
$$

因此：

$$
k=2.
$$

联合密度可以写成：

$$
f_{U,V}(u,v)=
\begin{cases}
2,&0\le v\le u\le1,\\
0,&\text{其他位置}.
\end{cases}
$$

### 2. 求横坐标 $U$ 的边缘密度

我们要保留 $u$，对 $v$ 积分。

但积分范围不能凭记忆写，必须先问：

> **固定这个横坐标 $u$ 后，纵坐标 $v$ 能从哪里到哪里？**

由三角形条件：

$$
0\le v\le u.
$$

例如：

```text
固定u=0.2：v只能在0到0.2之间。

固定u=0.8：v可以在0到0.8之间。
```

所以，当 $0\le u\le1$ 时：

$$
\begin{aligned}
f_U(u)
&=\int_0^u 2\,dv\\
&=2\times(u-0)\\
&=2u.
\end{aligned}
$$

完整写成：

$$
\boxed{
f_U(u)=
\begin{cases}
2u,&0\le u\le1,\\
0,&\text{其他}.
\end{cases}
}
$$

它不是一条水平线，而是随着 $u$ 增大而上升。

为什么？

不是右边每个小面积块的联合密度更高——区域内的联合密度始终是 2。

而是：

> **越靠右，相同横向宽度对应的竖直带越长，能够汇集的概率越多。**

所以：

$$
\boxed{
\text{二维区域内均匀}
\not\Rightarrow
\text{每个坐标的边缘分布均匀}
}
$$

### 3. 求纵坐标 $V$ 的边缘密度

这次固定 $v$，对 $u$ 积分。

从：

$$
0\le v\le u\le1
$$

可知，当 $0\le v\le1$ 时：

$$
v\le u\le1.
$$

因此：

$$
\begin{aligned}
f_V(v)
&=\int_v^1 2\,du\\
&=2(1-v).
\end{aligned}
$$

完整写成：

$$
\boxed{
f_V(v)=
\begin{cases}
2(1-v),&0\le v\le1,\\
0,&\text{其他}.
\end{cases}
}
$$

它随着 $v$ 增大而下降。

因为越靠上，相同纵向宽度对应的水平带越短，能汇集的概率越少。

### 4. 用边缘密度计算一个概率

例如：

> 随机点的横坐标不超过 $0.5$，概率是多少？

只关心横坐标，因此直接使用 $U$ 的边缘密度：

$$
\begin{aligned}
P(U\le0.5)
&=\int_0^{0.5}2u\,du\\
&=\left[u^2\right]_0^{0.5}\\
&=0.25.
\end{aligned}
$$

这里 $u^2$ 的导数是 $2u$，所以它是积分计算中使用的原函数。

从平面面积也能验证：

横坐标不超过 $0.5$ 的部分，是一个底、高均为 $0.5$ 的小三角形，面积为：

$$
\frac{0.5\times0.5}{2}=0.125.
$$

乘以联合密度 2：

$$
0.125\times2=0.25.
$$

两种方法完全一致。

### 5. 积分边界为什么如此重要？

本例不能把：

$$
f_U(u)
$$

直接算成：

$$
\int_0^1 2\,dv=2.
$$

因为固定 $u<1$ 时，$v>u$ 的那部分已经位于三角形之外，密度是 0，不是 2。

**一般公式可以对整个实数轴积分，是因为区域外的密度自动为 0；简化积分范围时，必须遵守联合取值区域的约束。**

---

## 十一、知道两个边缘分布，能不能反推出联合分布？

通常不能。

这正好把第二十五课的问题反过来再看一次。

### 1. 回到两个简单袋子的反例

这是另一套卡片设置，与本课的十张卡片袋子区分。

甲袋有两张卡片：

$$
(0,0),\qquad(1,1).
$$

乙袋有两张卡片：

$$
(0,1),\qquad(1,0).
$$

每次从**事先指定的一个袋子**中等可能地抽一张，两张卡片被抽到的概率各为 $\frac12$。

这里把左、右读数分别记为 $L,R$。

两个袋子都满足：

$$
P(L=0)=P(L=1)=\frac12,
$$

$$
P(R=0)=P(R=1)=\frac12.
$$

所以两个边缘分布完全相同。

但甲袋：

$$
P(L=R)=1.
$$

乙袋：

$$
P(L=R)=0.
$$

**联合规律完全不同。**

### 2. 为什么反推会失败？

边缘化时：

```text
共同取值及其概率
        ↓
把另一个坐标的区别合并
        ↓
只剩各自的概率
```

我们已经丢掉了：

> 哪个左边数字和哪个右边数字配对。

因此：

$$
\boxed{
\text{联合分布可以确定边缘分布，}
\quad
\text{边缘分布一般不能确定联合分布。}
}
$$

如果另外知道变量独立，那么在离散情况下可以使用：

$$
p_{X,Y}(x,y)=p_X(x)p_Y(y).
$$

但这里新增的“独立”是额外信息，**不是边缘分布自动提供的结论**。

### 3. 边缘分布到底保留了多少信息？

它并不是只留下一个均值或一个方差。

它保留了：

> **所保留变量自己的完整分布。**

丢掉的是：

> **它与其他变量怎样共同取值的信息。**

这两个判断可以同时成立。

---

## 十二、与期望、方差连接：边缘分布提取出来以后有什么用？

回到十张卡片的主例：

$$
P(X=0)=0.3,\qquad
P(X=1)=0.3,\qquad
P(X=2)=0.4.
$$

现在只想知道左边数字的平均位置，就不需要反复使用整张联合表。

第二十一课已经建立：期望是以取值概率为权重的加权平均。

所以：

$$
\begin{aligned}
E[X]
&=0\times0.3+1\times0.3+2\times0.4\\
&=1.1.
\end{aligned}
$$

再按照“到均值的平方距离取平均”计算方差：

$$
\begin{aligned}
\operatorname{Var}(X)
&=(0-1.1)^2\times0.3\\
&\quad +(1-1.1)^2\times0.3\\
&\quad +(2-1.1)^2\times0.4\\
&=0.363+0.003+0.324\\
&=0.69.
\end{aligned}
$$

整条主线现在是：

```text
一次随机抽取
        ↓
样本点：具体抽到哪张卡片
        ↓
随机向量：(X,Y)，记录两个数字
        ↓
联合分布：每种数字组合的概率
        ↓
边缘分布：只看左边数字的概率规律
        ↓
E[X]：左边数字的平均位置
Var(X)：左边数字围绕均值的分散程度
```

但如果想研究协方差，就涉及同一次试验中的：

$$
(X-E[X])(Y-E[Y]).
$$

这时配对关系重新变得重要，通常不能只依靠两个边缘分布完成计算。

**单个变量的特征，可以从它的边缘分布计算；涉及共同变化的特征，一般还需要联合信息。**

---

## 十三、实际应用：从多项记录中提取我们真正关心的分布

### 1. 机器学习、深度学习与计算机视觉：总体错误率

假设一个固定测试集有 100 张图像，并且已经用一个固定模型得到预测结果。

下面是人为设定的测试集统计：

| 图像条件 | 预测正确 | 预测错误 |  合计 |
| ---- | ---: | ---: | --: |
| 正常光照 |   56 |   14 |  70 |
| 弱光   |   18 |   12 |  30 |
| 合计   |   74 |   26 | 100 |

现在定义一次试验：

> 从这 100 张图像中等可能地抽取一张，记录它的光照类型和预测是否错误。

如果只关心“是否错误”，不再区分光照，那么：

$$
P(\text{错误})
=
P(\text{正常光照且错误})
+
P(\text{弱光且错误}).
$$

所以：

$$
P(\text{错误})
=
\frac{14}{100}+\frac{12}{100}
=
0.26.
$$

这就是把光照类型边缘化。

但如果已经知道抽到的是弱光图像，再问错误概率，就变成：

$$
P(\text{错误}\mid\text{弱光})
=
\frac{12}{30}
=
0.4.
$$

你再次看到了：

> **总体汇总，与已知条件后的重新判断，不是同一种操作。**

这里的 $0.26$ 是“从这个固定测试集均匀抽一张”这一模型下的准确概率；它不能自动等同于未来部署数据上的真实错误率。

### 2. 强化学习：只关注下一状态

先把当前状态和动作固定。

一次环境交互可能同时给出：

```text
下一状态
奖励
```

如果只关心下一状态，不关心这次具体奖励是多少，就把对应于各种奖励的联合概率汇总起来。

我们没有假设下一状态与奖励独立，只是不再区分奖励取值。

### 3. 数据分析与工程：只关注任务耗时

一次任务可能同时记录：

```text
耗时
能耗
峰值内存
```

如果只想研究耗时分布，就汇总其他指标的各种情况，保留耗时这一维。

而如果想研究“耗时达标并且内存不超限”，就仍然需要这些指标的共同取值信息。

另外，**边缘分布不一定只能是一维**：从三个变量中只汇总掉一个，保留另外两个，得到的也是相对于原三维分布的边缘分布。

---

## 十四、理解检查

先思考，再看下面的解释。

**第一个问题：求 $X$ 的边缘分布时，是对 $X$ 求和，还是对 $Y$ 求和？**

要对 $Y$ 求和。因为我们想保留不同 $X$ 取值之间的区别，而合并不同 $Y$ 的情况。

**第二个问题：只取联合表中 $Y=1$ 的一列，就得到 $X$ 的边缘分布了吗？**

没有。那一列只保留了右边为 1 的情况，漏掉了所有右边为 0 的卡片。

本例这一列的总概率为：

$$
0.2+0.1+0.3=0.6,
$$

不是 1。

如果把这一列再除以 $0.6$，得到的将是**已知 $Y=1$ 时 $X$ 的条件分布**，不是原来的边缘分布。

**第三个问题：边缘分布丢失信息，是否意味着它不能完整描述 $X$？**

不是。它仍然完整描述 $X$ 自己的分布，只是一般不能恢复 $X$ 与 $Y$ 的配对关系。

---

## 这一节真正需要记住的核心

**边缘分布是在联合分布中，只保留某些变量时得到的概率分布；被保留变量的含义和原来的随机机制没有改变。**

**离散情况下，对不再区分的变量求和；存在联合密度时，对不再区分的变量积分。**

**边缘化是合并所有相关情况，不是把另一个变量设为 0，不是取平均，也不要求独立。**

**边缘化保留总概率以及所保留变量自己的完整分布，但一般会丢掉变量之间的共同取值信息。**

**边缘分布“不限制另一个变量”，条件分布“利用另一个变量的已知信息”；这是两者最根本的区别。**

下一课进入**第二十八课：条件分布**。我们已经知道“不看右边时，左边怎样分布”，接下来就自然要问：**如果已经看到了右边数字，左边的整张概率分布应该怎样更新？**
