# 第二十六课：联合分布——怎样完整描述两个随机变量的共同取值规律？

上一课，我们已经用抽卡片的例子建立了一个认识：

> **从袋子里只抽一张卡片，却可以同时记录两个数字：左边的 $X$ 和右边的 $Y$。**

把这两个读数组成：

$$
(X,Y),
$$

就得到了一个二维随机变量。

但“把两个数字放在一起”还没有完成全部工作。接下来必须回答：

> **每一种数字组合，究竟有多大的概率出现？知道这些概率以后，又怎样计算我们关心的事件？**

这就是本课的核心：**联合分布**。

我们现在的位置是：

```text
第二十五课：多维随机变量
把同一次试验中的多个数值作为一个整体
                ↓
第二十六课：联合分布
这些数值怎样共同取值，概率怎样分配？
                ↓
边缘分布
只看其中一个变量，怎样得到它自己的分布？
                ↓
条件分布
知道一个变量以后，另一个变量的分布怎样改变？
```

今天先把“共同取值的概率如何组织、如何使用”讲清楚，不展开边缘分布和条件分布的系统推导。

---

## 一、问题背景：有了两个读数，怎样回答更复杂的问题？

### 1. 先明确这次的试验设置

我们继续使用卡片，但把设置改得稍微丰富一点。

**今天只有一个袋子，里面事先放好十张卡片。**

每张卡片都有编号，而且正面写着两个数字。袋子里的具体配置如下：

| 卡片编号   | 左边数字 | 右边数字 | 这一类共有几张 |
| ------ | ---: | ---: | ------: |
| 1      |    0 |    0 |       1 |
| 2、3    |    0 |    1 |       2 |
| 4、5    |    1 |    0 |       2 |
| 6      |    1 |    1 |       1 |
| 7      |    2 |    0 |       1 |
| 8、9、10 |    2 |    1 |       3 |

总张数是：

$$
1+2+2+1+1+3=10.
$$

注意：**这张表描述的是试验开始之前，袋子里放了哪些卡片；不是抽取十次以后得到的观测记录。**

我们规定一次完整试验为：

> **从袋子里等可能地随机抽出一张卡片，读取它的左、右两个数字，然后结束。**

因此，每一张具体卡片被抽到的概率都是：

$$
\frac1{10}.
$$

如果重复试验，就在每次读取后将卡片放回并混匀，恢复同样的抽取设置。

### 2. 定义两个随机变量

规定：

$$
X=\text{抽到的卡片上，左边的数字},
$$

$$
Y=\text{抽到的卡片上，右边的数字}.
$$

例如，抽到编号 8 的卡片，本次就得到：

$$
(X,Y)=(2,1).
$$

这里依然是：

> **一次抽取、一张卡片、两个读数。**

现在我们不只关心抽到哪张卡片，还可能问：

“左边是 2，右边是 1”的概率是多少？

“左右数字相同”的概率是多少？

“左边数字大于右边数字”的概率是多少？

如果每次都回到十张原始卡片中重新查找，当然能算；但当卡片很多、组合很多时，会越来越不方便。

我们希望先把信息整理成一种形式：

> **以后不必反复查看原始卡片，只根据数字组合及其概率，就能回答这些问题。**

联合分布就是这种整理。

---

## 二、直觉：把概率按“数字组合”重新汇总

第九课已经建立过一个思想：

> 随机变量把样本点映射到数字上，分布则把原来的概率按照这些数字重新汇总。

今天没有改变这个思想，只是把汇总位置从“一个数字”换成了“一对数字”。

### 1. 原始样本点是什么？

本例中，可以用卡片编号区分原始结果：

$$
\Omega=\{1,2,\ldots,10\}.
$$

这里的编号只是**样本点的标签**，不是卡片上的左边数字或右边数字。

例如：

$$
\omega=8
$$

表示抽到了编号 8 的卡片。

根据卡片上的内容：

$$
X(8)=2,\qquad Y(8)=1.
$$

所以：

$$
\omega=8
\quad\longmapsto\quad
(X(\omega),Y(\omega))=(2,1).
$$

### 2. 哪些卡片会产生同一个组合？

编号 8、9、10 的卡片都写着：

$$
(2,1).
$$

因此：

$$
\{X=2,Y=1\}=\{8,9,10\}.
$$

左边是用数值条件描述事件，右边是把这个事件包含的样本点列出来。

由于三张卡片各自被抽到的概率都是 $\frac1{10}$，所以：

$$
\begin{aligned}
P(X=2,Y=1)
&=P(\{8,9,10\})\\
&=\frac1{10}+\frac1{10}+\frac1{10}\\
&=\frac3{10}.
\end{aligned}
$$

可以把它想成：

```text
原始样本点                     数字组合

卡片8，概率1/10 ───┐
卡片9，概率1/10 ───┼──────→ (2,1)，概率3/10
卡片10，概率1/10 ──┘
```

同样，编号 2、3 的卡片都产生 $(0,1)$，因此：

$$
P(X=0,Y=1)=\frac2{10}.
$$

于是我们得到一个关键认识：

$$
\boxed{
\text{原始卡片等可能}
\;\not\Rightarrow\;
\text{数字组合等可能}
}
$$

不同的组合可能汇集了不同数量的卡片，因此获得不同的概率。

---

## 三、数学定义：什么是联合分布与联合概率质量函数？

### 1. 联合分布描述什么？

对于定义在同一个概率空间上的随机变量 $X,Y$，它们组成随机向量：

$$
(X,Y).
$$

**这个随机向量的概率分布，就称为 $X,Y$ 的联合分布。**

它描述的是：

> 这对数值落在什么位置、什么范围，以及相应的概率是多少。

所以“联合分布”不是把两张单变量分布表摆在一起，而是研究：

$$
\boxed{\text{同一次试验中的共同取值规律。}}
$$

### 2. 离散情况下，直接记录每个组合的概率

本例中的数字组合可以逐个列出来，因此可以定义：

$$
\boxed{
p_{X,Y}(x,y)=P(X=x,Y=y)
}
$$

它叫作**联合概率质量函数**，简称**联合 PMF**。

逐个解释：

| 符号               | 含义                   |
| ---------------- | -------------------- |
| $X,Y$          | 两个随机变量，即读取左边、右边数字的规则 |
| $x,y$          | 我们指定的两个具体数值          |
| 下标 $X,Y$       | 表明这个函数描述的是哪一对随机变量    |
| $p_{X,Y}$      | 整个联合概率质量函数           |
| $p_{X,Y}(x,y)$ | 指定组合 $(x,y)$ 获得的概率 |

例如：

$$
p_{X,Y}(2,1)=\frac3{10}.
$$

意思是：

> 这一次抽到的卡片，左边为 2，并且右边为 1，概率是十分之三。

### 3. 逗号表示“并且”，不是相乘

这里：

$$
P(X=x,Y=y)
$$

是下面这个表达式的简写：

$$
\boxed{
P\bigl(\{X=x\}\cap\{Y=y\}\bigr).
}
$$

其中，$\cap$ 表示两个事件的交集，也就是两个条件同时成立。

因此：

```text
P(X=x,Y=y)

不是：
先抽一次得到x，再抽一次得到y。

而是：
同一次试验得到的一对读数，同时满足X=x、Y=y。
```

---

## 四、从卡片配置表，得到联合概率分布表

现在把六种组合各自对应的卡片数量除以十：

| $X\backslash Y$ | $Y=0$ | $Y=1$ |
| ----------------- | ------: | ------: |
| $X=0$           | $0.1$ | $0.2$ |
| $X=1$           | $0.2$ | $0.1$ |
| $X=2$           | $0.1$ | $0.3$ |

这就是本例的**联合概率分布表**。

例如，第二行、第一列的：

$$
0.2
$$

表示：

$$
P(X=1,Y=0)=0.2.
$$

每一格都在回答：

> 对应的那一对数字，在同一次试验中出现的概率是多少？

表格之外的组合，例如：

$$
(3,1),\qquad (1,2),
$$

在本例中没有对应卡片，因此概率为 0。

### 一个概率与一个分布，仍然不是同一个东西

$$
p_{X,Y}(2,1)=0.3
$$

只是某一个组合的概率。

**整张表加上“其他组合概率为 0”的说明，才给出了完整的联合分布。**

这与一维时完全一致：

```text
一个概率：
回答一个事件有多可能。

一个分布：
描述整个随机对象的概率怎样分配。
```

---

## 五、联合概率表为什么必须“非负、总和为 1”？

### 1. 每一格不能为负

因为每个格子本质上都是一个事件的概率，所以：

$$
p_{X,Y}(x,y)\ge0.
$$

### 2. 整张表的概率必须加起来等于 1

一次抽取结束后，必然得到某一种数字组合，而且不可能同时得到两种不同的精确组合。

例如，这一次不可能既是：

$$
(X,Y)=(0,0),
$$

又是：

$$
(X,Y)=(2,1).
$$

因此，不同组合对应的事件彼此互斥，所有组合又覆盖全部可能结果，所以：

$$
(0.1+0.2)+(0.2+0.1)+(0.1+0.3)=1.
$$

一般地，如果 $X$ 的候选取值记为 $x_i$，$Y$ 的候选取值记为 $y_j$，就写成：

$$
\boxed{
\sum_i\sum_j p_{X,Y}(x_i,y_j)=1.
}
$$

这里，$i,j$ 只是两个取值列表的编号。

两层求和的意思是：

> 固定一行，把这一行的各列加起来；再把所有行的结果加起来。

**不是把每个格子计算两次，而是用两个编号遍历每个格子一次。**

还要注意：

> **总和为 1 的是整张联合概率表，不是每一行、每一列都必须等于 1。**

---

## 六、几何直觉：联合分布是平面上的概率配置

以前，一个随机变量的取值放在数轴上。

现在，一对随机变量的取值放在平面上。

本例可以表示成：

```text
Y 的取值
↑
1   ● 0.2          ● 0.1          ● 0.3

0   ● 0.1          ● 0.2          ● 0.1
    └──────────────────────────────────→ X 的取值
    0              1              2
```

每个点的位置由 $(x,y)$ 决定，点旁边标注的是这个组合的概率。

这里需要特别区分：

> **横轴是 $X$ 的取值，纵轴是 $Y$ 的取值；两条坐标轴都不是概率。**

例如右上方的点：

$$
(2,1)
$$

表示“左边为 2，右边为 1”。

它旁边的：

$$
0.3
$$

才表示这个点获得的概率。

所以：

$$
\boxed{
\text{坐标回答“是什么组合”，概率回答“这个组合有多容易出现”。}
}
$$

---

## 七、怎样用联合分布计算事件概率？

核心方法没有变化：

> **先找出满足条件的数值组合，再把这些组合的概率加起来。**

### 例一：左右数字相同

我们要求：

$$
P(X=Y).
$$

先不要计算，先找组合。

本例中满足条件的只有：

$$
(0,0),\qquad(1,1).
$$

虽然 $X$ 可以取 2，但 $Y$ 不会取 2，所以不存在有正概率的 $(2,2)$。

于是：

$$
\begin{aligned}
P(X=Y)
&=P(X=0,Y=0)+P(X=1,Y=1)\\
&=0.1+0.1\\
&=0.2.
\end{aligned}
$$

### 例二：左边数字大于右边数字

现在求：

$$
P(X>Y).
$$

逐个筛选：

| 数字组合      | 是否满足 $X>Y$？ |      概率 |
| --------- | ------------- | ------: |
| $(0,0)$ | 不满足           | $0.1$ |
| $(0,1)$ | 不满足           | $0.2$ |
| $(1,0)$ | 满足            | $0.2$ |
| $(1,1)$ | 不满足           | $0.1$ |
| $(2,0)$ | 满足            | $0.1$ |
| $(2,1)$ | 满足            | $0.3$ |

所以：

$$
\begin{aligned}
P(X>Y)
&=p_{X,Y}(1,0)+p_{X,Y}(2,0)+p_{X,Y}(2,1)\\
&=0.2+0.1+0.3\\
&=0.6.
\end{aligned}
$$

**不能因为六种组合中有三种满足条件，就直接算成 $\frac36$。**

因为六种组合并不等可能。

### 把方法写成一个统一表达式

设 $D$ 是取值平面中我们关心的一个区域，例如：

$$
D=\{(x,y):x>y\}.
$$

这里的 $D$ 表示所有“第一个坐标大于第二个坐标”的数值对。

那么：

$$
\boxed{
P\bigl((X,Y)\in D\bigr)
=
\sum_{(x,y)\in D}p_{X,Y}(x,y)
}
$$

其中，求和遍历落在 $D$ 中的离散取值组合。

整条公式只是在说：

> **把目标区域内各个点的概率质量收集起来。**

---

## 八、一个重要反例：共同取值的概率，为什么不能直接相乘？

回到本例，考虑：

$$
P(X=2,Y=1).
$$

从联合表直接得到：

$$
P(X=2,Y=1)=0.3.
$$

再分别看两个事件。

“左边为 2”包含 $(2,0)$ 和 $(2,1)$，所以：

$$
P(X=2)=0.1+0.3=0.4.
$$

“右边为 1”包含 $(0,1),(1,1),(2,1)$，所以：

$$
P(Y=1)=0.2+0.1+0.3=0.6.
$$

直接相乘却得到：

$$
P(X=2)P(Y=1)=0.4\times0.6=0.24.
$$

显然：

$$
0.3\ne0.24.
$$

原因是：**本例中的 $X,Y$ 不独立。**

第七课已经强调过，单独知道两个事件的概率，不足以确定它们共同发生的概率。

当 $X,Y$ 独立时，才有：

$$
\boxed{
p_{X,Y}(x,y)=p_X(x)p_Y(y)
}
$$

对所有取值组合成立。其中：

$$
p_X(x)=P(X=x),\qquad p_Y(y)=P(Y=y)
$$

分别是两个变量自己的概率质量函数。

因此：

> **独立性是联合分布可能具有的一种特殊结构，不是定义联合分布时自动附带的条件。**

---

## 九、联合 CDF：从“一个组合的概率”，转向“一个范围的累计概率”

前面使用联合 PMF，回答的是：

$$
P(X=x,Y=y).
$$

也就是：

> 恰好得到某一个组合的概率是多少？

但我们还会关心：

> 左边数字不超过某个阈值，同时右边数字也不超过某个阈值，概率是多少？

这会自然连接到第十课的 CDF。

### 1. 回忆一维 CDF

一维分布函数定义为：

$$
F_X(x)=P(X\le x).
$$

它把数轴上不超过阈值 $x$ 的概率全部累积起来。

现在有两个坐标，就分别设置两个阈值。

于是定义：

$$
\boxed{
F_{X,Y}(x,y)=P(X\le x,\ Y\le y).
}
$$

这叫作**联合分布函数**，也叫**联合 CDF**。

这里：

$X,Y$ 是两个随机变量。

$x,y$ 是人为指定的两个阈值。

$F_{X,Y}(x,y)$ 是同时满足两个阈值条件的累计概率。

### 2. 它在平面上累计哪里？

条件：

$$
X\le x
$$

要求落在竖直边界 $x$ 的左边。

条件：

$$
Y\le y
$$

要求落在水平边界 $y$ 的下边。

两个条件同时成立，就是：

> **点 $(x,y)$ 左下方的全部区域，包含边界。**

数学上写成：

$$
(-\infty,x]\times(-\infty,y].
$$

这里的“$\times$”表示把两个坐标范围组合成一个区域，不是把两个概率相乘。

所以：

```text
一维 CDF：
累计一个阈值左边的概率。

二维 CDF：
累计两个阈值共同圈定的左下方区域的概率。
```

### 3. 用卡片例子计算一次

求：

$$
F_{X,Y}(1,0).
$$

按照定义：

$$
F_{X,Y}(1,0)=P(X\le1,Y\le0).
$$

本例中 $Y$ 只能取 0 或 1，所以 $Y\le0$ 就是 $Y=0$。

同时 $X\le1$，允许 $X=0$ 或 $X=1$。

因此符合条件的是：

$$
(0,0),\qquad(1,0).
$$

所以：

$$
\boxed{
F_{X,Y}(1,0)=0.1+0.2=0.3.
}
$$

但是：

$$
p_{X,Y}(1,0)=0.2.
$$

两者并不相同：

$$
\boxed{
p_{X,Y}(1,0)
\text{ 只看一个点，}
\quad
F_{X,Y}(1,0)
\text{ 累计一个区域。}
}
$$

### 4. 阈值不必是随机变量的可能取值

例如：

$$
F_{X,Y}(1.5,0.5)
=
P(X\le1.5,Y\le0.5).
$$

符合条件的仍然只有：

$$
(0,0),\qquad(1,0).
$$

所以：

$$
F_{X,Y}(1.5,0.5)=0.3.
$$

虽然本例中的 $X$ 不会取 1.5，$Y$ 不会取 0.5，但它们完全可以作为**筛选阈值**。

---

## 十、联合 CDF 的公式与基本直觉

### 1. 离散情况下，CDF 就是联合 PMF 的累计

因此：

$$
\boxed{
F_{X,Y}(x,y)
=
\sum_{\substack{u\le x\\v\le y}}
p_{X,Y}(u,v).
}
$$

这里：

$x,y$ 是固定的两个阈值。

$u,v$ 是求和时依次检查的候选取值。

求和只包含同时满足：

$$
u\le x,\qquad v\le y
$$

的离散组合。

为什么换成 $u,v$？

因为需要区分：

> **我们把边界设在哪里，以及正在检查哪个点。**

### 2. 放宽一个阈值，累计概率不会减少

固定 $y$，把 $x$ 增大，意味着向右扩大累计区域。

原来计入的点不会被删除，只可能增加新的点，所以 $F_{X,Y}(x,y)$ 不会减小。

固定 $x$，增大 $y$，道理相同。

### 3. 只放宽一个方向，不一定累计到 1

例如：

$$
F_{X,Y}(2,0)=0.1+0.2+0.1=0.4.
$$

虽然 $X\le2$ 已经包含本例中 $X$ 的所有取值，但 $Y\le0$ 仍然排除了整列 $Y=1$ 的结果。

只有两个条件都覆盖全部可能取值时，本例才有：

$$
F_{X,Y}(2,1)=1.
$$

这提醒我们：

> **联合 CDF 同时受两个阈值限制，不能只盯着其中一个。**

---

## 十一、怎样用联合 CDF 计算一个矩形区域的概率？

这是本课最重要的一段推导。

第十课的一维区间公式是：

$$
P(a<X\le b)=F_X(b)-F_X(a).
$$

它来自“截至右端的全部概率，减去截至左端的全部概率”。

现在我们想计算：

$$
P(a<X\le b,\ c<Y\le d),
$$

其中：

$$
a<b,\qquad c<d.
$$

$a,b$ 限定第一个坐标的范围，$c,d$ 限定第二个坐标的范围。

我们仍然只使用“累计量相减”。

### 第一步：先把 $X$ 限制在目标范围内

$$
F_{X,Y}(b,d)
$$

包含所有：

$$
X\le b,\qquad Y\le d
$$

的结果。

减去：

$$
F_{X,Y}(a,d),
$$

就删掉其中 $X\le a$ 的部分。

所以：

$$
F_{X,Y}(b,d)-F_{X,Y}(a,d)
=
P(a<X\le b,Y\le d).
$$

这时，左右边界已经正确，但下面仍然包含了我们不要的 $Y\le c$。

### 第二步：算出需要删掉的下方部分

同理：

$$
F_{X,Y}(b,c)-F_{X,Y}(a,c)
=
P(a<X\le b,Y\le c).
$$

它正好是刚才那块区域里，位于下方、需要删除的部分。

### 第三步：再相减

因此：

$$
\begin{aligned}
&P(a<X\le b,\ c<Y\le d)\\
&=\bigl[F_{X,Y}(b,d)-F_{X,Y}(a,d)\bigr]\\
&\quad-\bigl[F_{X,Y}(b,c)-F_{X,Y}(a,c)\bigr].
\end{aligned}
$$

展开括号：

$$
\boxed{
\begin{aligned}
&P(a<X\le b,\ c<Y\le d)\\
&=F_{X,Y}(b,d)
-F_{X,Y}(a,d)
-F_{X,Y}(b,c)
+F_{X,Y}(a,c).
\end{aligned}
}
$$

最后一项为什么是加号？

因为外面减去的是整个括号：

$$
-\bigl[F_{X,Y}(b,c)-F_{X,Y}(a,c)\bigr]
=
-F_{X,Y}(b,c)+F_{X,Y}(a,c).
$$

几何上也可以理解为：

> 左下角的重叠区域被两次减法重复删掉了，需要补回来一次。

### 用卡片例子验证

求：

$$
P(0<X\le2,\ 0<Y\le1).
$$

直接看联合表，满足条件的只有：

$$
(1,1),\qquad(2,1).
$$

所以：

$$
P(0<X\le2,\ 0<Y\le1)=0.1+0.3=0.4.
$$

再用 CDF：

$$
F_{X,Y}(2,1)=1,
$$

$$
F_{X,Y}(0,1)=0.1+0.2=0.3,
$$

$$
F_{X,Y}(2,0)=0.1+0.2+0.1=0.4,
$$

$$
F_{X,Y}(0,0)=0.1.
$$

因此：

$$
\begin{aligned}
&P(0<X\le2,\ 0<Y\le1)\\
&=1-0.3-0.4+0.1\\
&=0.4.
\end{aligned}
$$

两种方法完全一致。

**这里的 $<$ 和 $\le$ 不能随意交换。**本例是离散分布，边界上的点可能拥有正概率。

---

## 十二、连续情形：联合概率密度在做什么？

前面主要研究卡片这样的离散模型。现在只在这一节换一个连续模型，看看同一个思想怎样延伸。

### 1. 从“小区间”变成“小矩形”

第十二课已经建立：

> 在密度变化不明显的很小区间内，概率近似等于密度乘以区间宽度。

二维情况下，如果联合分布能够用一个二维密度函数表示，我们记作：

$$
f_{U,V}(u,v).
$$

这里换用 $U,V$，避免与前面的卡片变量混淆。

取一个很小的矩形，其两个方向的宽度分别为：

$$
\Delta u,\qquad\Delta v.
$$

小矩形的面积是：

$$
\Delta u\,\Delta v.
$$

如果密度在这块小矩形内变化不大，那么：

$$
\boxed{
\begin{aligned}
&P(u<U\le u+\Delta u,\ v<V\le v+\Delta v)\\
&\approx f_{U,V}(u,v)\,\Delta u\,\Delta v.
\end{aligned}
}
$$

也就是说：

> **小区域的概率，近似等于联合密度乘以小区域的面积。**

### 2. 把小矩形的概率加起来，就得到二重积分

如果想计算一个较大区域 $D$ 内的概率，就把它分成很多小块，累积各块概率。

数学上写成：

$$
\boxed{
P\bigl((U,V)\in D\bigr)
=
\iint_D f_{U,V}(u,v)\,du\,dv.
}
$$

其中：

$\iint_D$ 表示在整个二维区域 $D$ 上累积。

$du\,dv$ 对应两个方向上的微小面积元素。

$f_{U,V}(u,v)$ 表示该位置附近单位坐标面积上的概率密度。

所以二重积分不是另一种概率规则，只是：

> **把“在一条线上累积”，推广成“在一个平面区域内累积”。**

密度必须非负，整个平面上的密度积分必须为 1。

### 3. 一个只需要矩形面积的例子

假设：

> **每次在一个宽为 2、高为 1 的矩形内，按面积均匀地随机选取一个点，记录它的横坐标 $U$ 和纵坐标 $V$。**

每次只选一个点，得到一对坐标。

矩形范围为：

$$
0\le U\le2,\qquad0\le V\le1.
$$

“按面积均匀”表示：

> 等面积的区域具有相同的概率。

因此联合密度在矩形内是常数，设为 $k$。

整个矩形面积为：

$$
2\times1=2.
$$

总概率必须为 1，所以：

$$
2k=1
\quad\Longrightarrow\quad
k=\frac12.
$$

于是：

$$
f_{U,V}(u,v)=
\begin{cases}
\frac12,&0\le u\le2,\ 0\le v\le1,\\
0,&\text{其他位置}.
\end{cases}
$$

现在求：

$$
P(0.5<U\le1.5,\ 0<V\le0.5).
$$

目标矩形的面积是：

$$
(1.5-0.5)\times(0.5-0)=0.5.
$$

乘以密度：

$$
\boxed{
P(0.5<U\le1.5,\ 0<V\le0.5)
=
0.5\times\frac12
=
0.25.
}
$$

这就是二重积分在常数密度下最简单的计算。

### 4. 二维密度图中的“高度”仍然不是概率

如果把 $f_{U,V}(u,v)$ 画成三维曲面：

```text
底面的第一个方向：U 的取值
底面的第二个方向：V 的取值
向上的高度：联合概率密度
```

那么：

> **区域概率对应的是该底面区域上方、密度曲面下方的体积。**

不是密度高度本身，也不是一般情况下的底面面积本身。

还有一个边界：**两个变量各自连续，并不自动保证存在普通的二维联合密度。**例如 $V=U$ 时，取值全在一条对角线上，而不是铺在二维区域里。

联合 CDF 则不要求存在密度，仍然可以定义为：

$$
F_{U,V}(u,v)=P(U\le u,V\le v).
$$

---

## 十三、把三个名称放回同一个框架

现在不要把“联合分布”“联合 PMF”“联合 PDF”“联合 CDF”当作四种互不相关的东西。

**联合分布是整体概率规律；另外三个是描述它的方式。**

| 名称                      | 回答的问题                 | 适用范围      |
| ----------------------- | --------------------- | --------- |
| 联合分布                    | 数值组合及其区域的概率怎样分配？      | 总概念       |
| 联合 PMF：$p_{X,Y}(x,y)$ | 某个精确组合获得多少概率？         | 离散型       |
| 联合 PDF：$f_{X,Y}(x,y)$ | 某个位置附近的概率有多密集？        | 存在二维联合密度时 |
| 联合 CDF：$F_{X,Y}(x,y)$ | 两个坐标都不超过指定阈值的累计概率是多少？ | 任意二维随机变量  |

本课的知识连接可以概括成：

$$
\boxed{
\text{联合分布}
=
\text{一维分布的思想}
+
\text{保留多个坐标的共同取值关系}
}
$$

原来的主线没有改变：

```text
随机试验
抽一张卡片
    ↓
样本点
抽到哪张具体卡片？
    ↓
随机向量
读取这张卡片上的两个数字
    ↓
联合分布
各个数字组合获得多少概率？
    ↓
计算事件概率，或提取期望、方差、协方差等特征
```

---

## 十四、实际应用：联合分布为什么与算法工作直接相关？

### 1. 机器学习、深度学习与计算机视觉：真实类别和预测类别必须配对

假设从一个固定测试集中等可能地抽取一张图像。

记录这张图像的真实类别，以及固定模型对它的预测类别。

这就是同一张图像对应的两个数值标签。

对于二分类任务，每张图像会落到四种组合之一：

```text
真实0，预测0
真实0，预测1
真实1，预测0
真实1，预测1
```

各组合的图像数量除以测试集总数，就给出了**这个有限抽样模型下的联合分布**，也就是按总样本数归一化的混淆矩阵。

只知道真实类别比例，再知道预测类别比例，还不能确定错误率，因为错误率取决于：

> **真实标签和预测标签怎样配对。**

这套测试集上的分布，也不能自动等同于未来全部图像的真实分布。

### 2. 工程与数据分析：多个约束能否同时满足？

假设一次任务同时记录耗时和能耗。

我们可能要求：

> 耗时不超过指定上限，并且能耗也不超过指定上限。

这对应联合分布中的一个矩形区域。

分别知道“耗时达标的概率”和“能耗达标的概率”，通常还不足以直接得到“同时达标的概率”。

### 3. 强化学习：一次交互中的下一状态和奖励

把当前状态与动作先固定。

执行一次动作以后，环境给出下一状态和奖励。这两个结果来自同一次交互，需要保留配对关系。

联合分布描述的就是：

> 哪一种下一状态，会和哪一种奖励一起出现，以及相应的概率是多少？

---

## 十五、理解检查

回到本课的卡片联合分布表：

| $X\backslash Y$ | $Y=0$ | $Y=1$ |
| ----------------- | ------: | ------: |
| $X=0$           | $0.1$ | $0.2$ |
| $X=1$           | $0.2$ | $0.1$ |
| $X=2$           | $0.1$ | $0.3$ |

先思考两个问题：

**“左边数字加上右边数字至少为 2”，应该选哪些组合？**

**$p_{X,Y}(0,1)$ 和 $F_{X,Y}(0,1)$ 是否相同？**

对照解释：

第一个事件包含：

$$
(1,1),\quad(2,0),\quad(2,1),
$$

所以：

$$
P(X+Y\ge2)=0.1+0.1+0.3=0.5.
$$

第二个问题中：

$$
p_{X,Y}(0,1)=P(X=0,Y=1)=0.2,
$$

而：

$$
\begin{aligned}
F_{X,Y}(0,1)
&=P(X\le0,Y\le1)\\
&=p_{X,Y}(0,0)+p_{X,Y}(0,1)\\
&=0.1+0.2\\
&=0.3.
\end{aligned}
$$

差别仍然是：

> **PMF 查询一个点，CDF 累计一个区域。**

---

## 这一节真正需要记住的核心

**联合分布描述同一次试验中，多个随机变量怎样共同取值，以及概率怎样分配。**

**离散情况下，用联合 PMF 给每个数值组合分配概率；计算一个事件时，先找出满足条件的组合，再把概率相加。**

**联合 CDF 的定义是 $F_{X,Y}(x,y)=P(X\le x,Y\le y)$，它累计的是两个阈值左下方的全部概率，不是某一个点的概率。**

**存在联合密度时，区域概率通过对密度作二重积分得到；密度高度本身不是概率。**

**联合概率不能未经独立性判断就拆成两个单变量概率的乘积。**

下一课自然进入**第二十七课：边缘分布**。我们已经知道整张联合概率表，接下来要解决的是：**当只关心 $X$、不再关心 $Y$ 时，怎样从共同概率规律中恢复 $X$ 自己的分布？**
