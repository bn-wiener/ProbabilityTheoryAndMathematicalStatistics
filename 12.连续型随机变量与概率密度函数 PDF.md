# 第十二课：连续型随机变量与概率密度函数 PDF——概率为什么从“点”变成了“面积”？

今天只研究一个核心问题：

> **当随机变量可能取一整段连续数值时，为什么不能再给每一个具体数值直接分配概率？我们应该怎样描述它的概率分布？**

我们现在走到了这里：

```text
现实随机现象
    ↓
样本空间
    ↓
事件与概率
    ↓
条件概率、独立性
    ↓
随机变量 X
    ↓
概率分布
    ↓
CDF：F_X(x)=P(X≤x)
    ↓
离散型 / 连续型随机变量
          ↓
连续型随机变量 + PDF  ← 今天
          ↓
常见连续分布
          ↓
期望、方差
```

今天最重要的一次观念转换是：

> **离散型随机变量：概率可以“放在点上”。**
> **连续型随机变量：概率不能放在单个点上，而是分布在区间的面积里。**

---

## 1. 问题背景：为什么我们需要 PDF？

先考虑一个现实问题：

> 随机测量一个零件的长度 $X$。

假设长度大约在：

$$
10\text{ cm}\sim 12\text{ cm}
$$

之间。

它可能是：

```text
10.0
10.1
10.01
10.001
10.0001
...
```

甚至理论模型中可以是这一区间中的任意实数。

这和掷骰子完全不同。

骰子只有：

$$
1,2,3,4,5,6
$$

六个取值。

所以对于骰子，我们完全可以写：

$$
P(X=1),P(X=2),\ldots,P(X=6)
$$

每个点都分到一块概率。

但长度怎么办？

难道要写：

$$
P(X=10)
$$

$$
P(X=10.1)
$$

$$
P(X=10.01)
$$

然后一直写下去？

根本写不完。

更严重的问题是：

> 对连续型随机变量来说，**单独一个精确数值的概率通常等于0。**

例如：

$$
P(X=10.537281934\ldots)=0
$$

可是长度明明最终会得到某个具体数值。

这看起来很奇怪。

所以我们需要一种全新的描述方法。

它就是：

$$
\boxed{\text{概率密度函数 PDF}}
$$

---

# 2. 先建立一个画面：离散概率像“硬币”，连续概率像“沙子”

这是今天最重要的直觉。

## 离散型：概率是一堆一堆放在点上的

假设：

$$
X\in\{1,2,3,4\}
$$

可以想象概率质量像四堆硬币：

```text
概率
 ↑
 │          ●
 │    ●     │
 │    │     │     ●
 │ ●  │     │     │
 └──────────────────→ X
   1  2     3     4
```

某个点的柱子有多高，就对应：

$$
P(X=x)
$$

例如：

$$
P(X=2)=0.3
$$

这里“2这个点”本身真的拥有概率质量。

---

## 连续型：概率像沙子铺在数轴上

现在假设长度可以在一整段区间内取值。

概率不再是一堆一堆放在点上，而像一层沙子：

```text
密度
 ↑
 │            ______
 │          /        \
 │        /            \
 │_______/______________\_______→ X
```

这层曲线的高度不是概率。

它表示：

> **这一小片区域附近，概率有多“密集”。**

真正的概率，要看：

$$
\boxed{\text{曲线下面积}}
$$

例如我们问：

> $X$ 落在10.5到11之间的概率是多少？

对应：

```text
                 PDF
                  ↑
                  │
              ____│____
            /░░░░░░░░░░\
          /  ░░░░░░░░░░  \
─────────┼───┼────────┼──────→ X
        10  10.5      11

              ↑
          这一块面积
           = 概率
```

所以今天先记一句：

$$
\boxed{\text{连续型随机变量：概率 = 密度曲线下面的面积}}
$$

---

# 3. 为什么“一个点的概率”会变成0？

这是连续型随机变量最反直觉的地方。

我们慢慢来看。

假设 $X$ 表示一个人的身高。

你问：

> 一个人身高恰好等于170 cm的概率是多少？

注意“恰好”意味着数学意义上的：

$$
X=170.000000000\ldots
$$

不是：

```text
测量仪器显示170 cm
```

现实中的仪器会四舍五入。

比如显示170 cm，实际可能是：

$$
169.5\leq X<170.5
$$

这其实是一个区间，所以当然可以有正概率。

但如果要求精确到无限位：

$$
X=170.000000000\ldots
$$

这是数轴上的一个点。

---

## 用面积理解

概率来自面积：

$$
P(a\le X\le b)
$$

对应区间从 $a$ 到 $b$。

如果现在：

$$
a=b=170
$$

区间宽度变成：

$$
170-170=0
$$

一个点没有宽度：

```text
正常区间：

      |────────────|
      a            b
      有宽度


单独一个点：

             |
            170
          宽度 = 0
```

所以曲线下面的面积也为0。

因此：

$$
\boxed{P(X=170)=0}
$$

---

# 4. 概率为0，为什么这个值还可能真的出现？

这里一定要把两个概念分开：

```text
概率为0
≠
这个点不属于可能结果
```

这是连续世界和有限离散世界最大的区别之一。

假设随机从：

$$
[0,1]
$$

之间选择一个实数。

最终一定会选到某个具体数，比如：

$$
0.372918\ldots
$$

但是在选择之前：

$$
P(X=0.372918\ldots)=0
$$

任何一个提前指定的精确点，概率都是0。

为什么不矛盾？

因为：

> 最终结果必须是一个点，但在无限连续的候选点中，没有哪个预先指定的单点拥有正的概率面积。

可以这样想：

```text
最终：
一定落在数轴上的某一个位置

但：

事先指定一个“无限精确”的点
它本身没有长度
所以没有概率面积
```

因此：

> **“概率为0”与“逻辑上不可能”在连续模型中不是一回事。**

这和前面学习概率时埋下的伏笔正好对应。

---

# 5. PDF 到底是什么？

现在正式定义。

对于我们当前课程中常见的连续型随机变量 $X$，如果存在一个函数：

$$
f_X(x)
$$

使得任意区间 $[a,b]$ 的概率可以写成：

$$
\boxed{
P(a\le X\le b)
=
\int_a^b f_X(x)\,dx
}
$$

那么：

$$
f_X(x)
$$

称为随机变量 $X$ 的**概率密度函数**，英文：

> Probability Density Function

简称：

$$
\boxed{\text{PDF}}
$$

---

# 6. 公式里的每个符号是什么意思？

不要急着计算。

我们逐个拆开。

## $X$

随机变量。

例如：

$$
X=\text{某个灯泡的寿命}
$$

它是：

> 从随机实验结果映射出来的一个数字。

---

## $x$

一个普通数值。

注意：

$$
X
$$

和

$$
x
$$

不是一个东西。

通常：

```text
X = 随机变量
x = X可能取得的某个具体数值
```

例如：

$$
X=\text{灯泡寿命}
$$

而：

$$
x=1000
$$

表示1000小时这个具体数字。

---

## $f_X(x)$

表示：

> 随机变量 $X$ 在 $x$ 附近的概率密集程度。

注意关键词：

$$
\boxed{\text{密度}}
$$

不是：

$$
\boxed{\text{概率}}
$$

所以一般：

$$
\boxed{f_X(x)\ne P(X=x)}
$$

对于连续型随机变量：

$$
P(X=x)=0
$$

但：

$$
f_X(x)
$$

完全可以大于0。

---

## $a,b$

我们关心的一个区间的两个端点。

例如：

$$
a=10,\qquad b=11
$$

---

## $\int_a^b$

这是积分。

现在不必把积分想得太抽象。

你可以先把它理解成：

> 把从 $a$ 到 $b$ 之间无数个极小面积片全部加起来。

也就是：

```text
小宽度 × 对应高度
+
小宽度 × 对应高度
+
小宽度 × 对应高度
+
……
```

最后得到整个面积。

---

所以整个公式：

$$
P(a\le X\le b)
=
\int_a^b f_X(x)\,dx
$$

用普通话就是：

> **把 $a$ 到 $b$ 之间的概率密度全部累积起来，得到随机变量落入这一区间的概率。**

---

# 7. 这个公式为什么会“长出来”？

我们不要死记积分。

先回到一个非常小的区间：

$$
[x,x+\Delta x]
$$

其中：

$$
\Delta x
$$

表示一个很小的宽度。

想象 PDF 曲线在这一小段上变化不明显：

```text
密度
 ↑
 │      ┌────────
 │      │ f_X(x)
 │      │
 │______│___________→ x
        x      x+Δx
        ← Δx →
```

那么这一小块概率大约就是：

$$
\text{面积}
\approx
\text{高度}\times\text{宽度}
$$

即：

$$
P(x\le X\le x+\Delta x)
\approx
f_X(x)\Delta x
$$

这非常重要。

它告诉你：

$$
\boxed{
\text{概率}
\approx
\text{概率密度}\times\text{区间宽度}
}
$$

如果把整个 $[a,b]$ 切成无数个非常非常小的区间：

```text
a
│
├─小片1─┤
        ├─小片2─┤
                ├─小片3─┤
                        ...
                              b
```

每一块的概率：

$$
\approx \text{密度}\times\text{小宽度}
$$

把所有小块加起来。

当小块无限变窄时，这个求和就变成积分：

$$
\boxed{
P(a\le X\le b)
=
\int_a^b f_X(x)\,dx
}
$$

所以积分不是突然出现的。

逻辑是：

```text
我们想计算一个区间中的概率
        ↓
概率分布在整个区间中
        ↓
把区间切成许多极小片
        ↓
每片概率 ≈ 密度 × 宽度
        ↓
把所有小片概率加起来
        ↓
极限意义下就是积分
```

---

# 8. PDF 必须满足什么条件？

一个合法的概率密度函数至少需要满足两个核心条件。

## 条件一：密度不能是负数

$$
\boxed{f_X(x)\geq0}
$$

因为如果某一块密度是负的：

```text
高度 < 0
↓
面积 < 0
↓
概率 < 0
```

而概率不能为负。

---

## 条件二：整个曲线下的总面积必须为1

因为随机变量最终一定会取得整个可能范围中的某个值。

所以：

$$
\boxed{
\int_{-\infty}^{+\infty} f_X(x)\,dx=1
}
$$

注意：

这不是说随机变量一定可以取所有实数。

如果某些区域不可能出现，可以在那里规定：

$$
f_X(x)=0
$$

例如：

```text
          密度
           ↑
           │      ┌────────┐
           │      │        │
───────────┼──────┼────────┼────→ x
                 0        1

外面密度 = 0
```

整个实数轴下的总面积仍然是1。

---

# 9. 最简单例子：在 0 到 2 之间均匀随机

现在第一次真正计算。

假设：

$$
X
$$

可以在：

$$
[0,2]
$$

之间均匀出现。

“均匀”的意思是：

> 同样长度的区间拥有同样大小的概率。

所以它的 PDF 应该是一条水平线：

```text
f_X(x)
 ↑
 │        ┌──────────────┐
 │        │              │
 │        │              │
 └────────┼──────────────┼────→ x
          0              2
```

假设水平高度为：

$$
c
$$

整个面积必须等于1。

矩形面积：

$$
\text{宽}\times\text{高}
$$

因此：

$$
2\times c=1
$$

所以：

$$
c=\frac12
$$

于是：

$$
f_X(x)=
\begin{cases}
\frac12,&0\le x\le2\\
0,&\text{其他}
\end{cases}
$$

---

## 求 $X$ 落在0.5到1之间的概率

我们想求：

$$
P(0.5\le X\le1)
$$

对应矩形：

```text
密度
 ↑
0.5 ────────┌────────────────┐
             │ ░░░░░          │
             │ ░░░░░          │
 ────────────┼─┼─────┼────────┼──→ x
             0 0.5    1        2
```

宽度：

$$
1-0.5=0.5
$$

高度：

$$
\frac12
$$

所以概率：

$$
P(0.5\le X\le1)
=
0.5\times\frac12
=
\frac14
$$

也就是：

$$
0.25
$$

你现在其实已经完成了一个积分：

$$
\int_{0.5}^{1}\frac12\,dx
=
\frac14
$$

但这里真正重要的不是积分技巧。

而是：

$$
\boxed{\text{概率就是那块面积}}
$$

---

# 10. PDF 的高度为什么可以大于1？

这是初学者非常容易困惑的问题。

我们知道：

$$
0\leq P(A)\leq1
$$

于是很容易误以为：

$$
0\leq f_X(x)\leq1
$$

这是错误的。

因为：

> **PDF 的高度不是概率。**

---

假设：

$$
X
$$

均匀分布在：

$$
[0,0.5]
$$

上。

画面：

```text
密度
 ↑
2│        ┌─────────────┐
 │        │             │
 │        │             │
 │        │             │
 └────────┼─────────────┼────→ x
          0            0.5
```

整个概率必须为1。

矩形宽度：

$$
0.5
$$

所以高度必须是：

$$
2
$$

因为：

$$
0.5\times2=1
$$

因此：

$$
f_X(x)=2
$$

完全合法。

虽然：

$$
2>1
$$

但概率仍然没有超过1。

因为：

$$
\boxed{\text{概率看面积，不看高度}}
$$

---

## 一个生活类比：人口密度

假设某地区人口密度为：

$$
10000\text{ 人/km}^2
$$

你不能说：

> “概率怎么超过1了？”

因为“人口密度”和“人口数”不是一个单位。

类似地：

```text
PDF 高度 = 概率密度
面积     = 概率
```

密度可以很高。

但所有区域的概率面积加起来必须等于1。

---

# 11. 为什么单点概率为0，现在可以严格看出来了

假设：

$$
P(X=a)
$$

如果使用面积公式：

$$
P(X=a)
=
\int_a^a f_X(x)\,dx
$$

积分区间从 $a$ 到 $a$。

区间宽度为：

$$
0
$$

所以：

$$
\boxed{P(X=a)=0}
$$

于是连续型随机变量还有一个非常实用的性质：

$$
P(a<X<b)
$$

$$
P(a\le X<b)
$$

$$
P(a<X\le b)
$$

$$
P(a\le X\le b)
$$

对连续型随机变量来说都一样。

为什么？

例如：

$$
P(a\le X\le b)
$$

和：

$$
P(a<X\le b)
$$

只差一个点：

$$
X=a
$$

但：

$$
P(X=a)=0
$$

所以删掉这个点不会改变概率。

---

# 12. PDF 和上一课 CDF 到底是什么关系？

这是今天最关键的知识连接。

我们已经有：

$$
F_X(x)=P(X\le x)
$$

CDF 回答：

> 从最左边一直累计到 $x$，累计了多少概率？

现在有 PDF：

$$
f_X(x)
$$

表示：

> $x$ 附近的概率有多密集。

把两者画在脑子里：

```text
PDF：

密度
 ↑
 │          /\
 │         /  \
 │_______ /____\________→ x


CDF：

累计概率
 ↑
1│                  ______
 │              ___/
 │           __/
 │       ___/
0│______/
 └──────────────────────→ x
```

---

## 从 PDF 得到 CDF

CDF 是从最左边累积到 $x$ 的总概率。

因此：

$$
\boxed{
F_X(x)
=
\int_{-\infty}^{x}f_X(t)\,dt
}
$$

这里为什么换成了 $t$？

只是为了避免和积分上限 $x$ 混淆。

$t$ 没有什么特殊含义，它只是积分过程中的临时变量。

这条公式用普通话说：

> **CDF 在 $x$ 处的高度，就是 PDF 从最左边到 $x$ 之间的总面积。**

---

# 13. 为什么 $f_X(x)=F_X'(x)$？

如果你已经学过导数，我们可以建立一个非常漂亮的关系。

刚才：

$$
F_X(x)
=
\int_{-\infty}^{x}f_X(t)\,dt
$$

也就是说：

> $F_X(x)$ 是累计面积。

现在让 $x$ 向右移动一小点：

$$
x\rightarrow x+\Delta x
$$

CDF 增加了多少？

大约增加：

$$
f_X(x)\Delta x
$$

所以：

$$
F_X(x+\Delta x)-F_X(x)
\approx
f_X(x)\Delta x
$$

两边除以：

$$
\Delta x
$$

得到：

$$
\frac{
F_X(x+\Delta x)-F_X(x)
}{
\Delta x
}
\approx
f_X(x)
$$

当：

$$
\Delta x\to0
$$

左边就是导数。

因此，在可以求导的位置：

$$
\boxed{
f_X(x)=F_X'(x)
}
$$

于是：

```text
PDF
↓ 积累面积
CDF

CDF
↓ 看瞬时增长速度
PDF
```

这是非常重要的一对关系。

---

# 14. PDF vs CDF：到底分别描述什么？

把它们放在一起。

| 概念      | PDF            | CDF                |
| ------- | -------------- | ------------------ |
| 记号      | $f_X(x)$     | $F_X(x)$         |
| 描述      | $x$ 附近概率有多密集 | $X\le x$ 已累计多少概率 |
| 自身是不是概率 | 通常不是           | 是                  |
| 取值一定≤1吗 | 不一定            | 一定在0到1之间           |
| 单调递增吗   | 不要求            | 一定不减               |
| 区间概率    | 对PDF积分         | CDF作差              |
| 关系      | $f=F'$       | $F=\int f$       |

特别记住：

$$
\boxed{f_X(x)\ne P(X=x)}
$$

而：

$$
\boxed{F_X(x)=P(X\le x)}
$$

---

# 15. 用 CDF 计算区间概率

因为：

$$
F_X(b)=P(X\le b)
$$

而：

$$
F_X(a)=P(X\le a)
$$

那么直觉上：

```text
≤ b 的全部概率
-
≤ a 的全部概率
=
a 到 b 之间的概率
```

对于连续型随机变量：

$$
\boxed{
P(a<X\le b)
=
F_X(b)-F_X(a)
}
$$

由于单点概率为0，所以也可以写成：

$$
P(a\le X\le b)
=
F_X(b)-F_X(a)
$$

于是区间概率有两种完全等价的视角：

$$
\boxed{
P(a\le X\le b)
=
\int_a^b f_X(x)\,dx
}
$$

以及：

$$
\boxed{
P(a\le X\le b)
=
F_X(b)-F_X(a)
}
$$

一个看面积，一个看累计概率之差。

---

# 16. 离散型 vs 连续型：最关键的区别

现在把两个世界放在一起。

## 离散型

例如骰子：

$$
X\in\{1,2,3,4,5,6\}
$$

概率真正落在一个个点上：

```text
概率质量
 ↑
 │    ●
 │    │
 │ ●  │  ●
 └────────────→ X
   1  2  3
```

可以直接问：

$$
P(X=2)
$$

而且完全可能：

$$
P(X=2)>0
$$

---

## 连续型

例如温度：

```text
密度
 ↑
 │        ____
 │      /      \
 │_____/________\______→ X
```

单个点：

$$
P(X=x)=0
$$

概率必须通过区间得到：

$$
P(a\le X\le b)
$$

---

最核心的对照：

```text
离散型：
点本身有概率质量
P(X=x) 可以 > 0

连续型：
点本身没有概率面积
P(X=x) = 0

但某段区间：
P(a≤X≤b) 可以 > 0
```

---

# 17. PMF vs PDF：不要混淆

以后看到下面两个符号，一定要知道它们在做完全不同的事。

## PMF

离散型随机变量使用。

它告诉你：

$$
p_X(x)=P(X=x)
$$

即：

> **这个点本身有多少概率。**

---

## PDF

连续型随机变量使用。

$$
f_X(x)
$$

告诉你：

> **这个点附近的概率有多密集。**

它不是：

$$
P(X=x)
$$

概率必须通过：

$$
\int_a^b f_X(x)\,dx
$$

得到。

因此：

$$
\boxed{
\text{PMF 的高度本身就是概率}
}
$$

而：

$$
\boxed{
\text{PDF 的高度不是概率，面积才是概率}
}
$$

这是整节课最容易混淆、也最需要内化的一句话。

---

# 18. 一个反例：两个点 PDF 高度一样，意味着什么？

假设：

$$
f_X(1)=f_X(3)
$$

能不能说：

$$
P(X=1)=P(X=3)
$$

？

对于连续型随机变量：

$$
P(X=1)=0
$$

而：

$$
P(X=3)=0
$$

所以形式上它们当然相等，但这并不是 PDF 相等真正想告诉我们的东西。

真正含义是：

> **在1附近和3附近，如果取同样小的区间宽度，两边得到的概率大约相同。**

比如很小的 $\Delta x$：

$$
P(1\le X\le1+\Delta x)
\approx
f_X(1)\Delta x
$$

而：

$$
P(3\le X\le3+\Delta x)
\approx
f_X(3)\Delta x
$$

如果密度相同，那么这两个小区间的概率近似相同。

所以 PDF 描述的是：

$$
\boxed{\text{局部概率浓度}}
$$

---

# 19. 当前知识 = 哪些旧知识 + 什么新思想？

今天其实没有推翻前面的任何内容。

而是在原来的框架上增加一种新的分布表达方式。

```text
旧知识1：
随机变量 X
把现实随机结果映射成数字

旧知识2：
概率分布
描述 X 的各个可能值有多可能

旧知识3：
CDF
F_X(x)=P(X≤x)

            +

今天的新思想：
连续数轴上无法把概率质量逐点分配
所以改用“概率密度”

            ↓

PDF f_X(x)
            ↓
区间面积 = 概率
```

所以概率论的主线依旧没有变化：

```text
现实随机现象
    ↓
样本结果
    ↓
随机变量 X
    ↓
X 在数轴上的概率规律
```

只是：

```text
离散世界 → 用“点质量”

连续世界 → 用“密度 + 面积”
```

---

# 20. 现实和机器学习中的例子

## 测量误差

假设：

$$
X=\text{传感器测量误差}
$$

误差可能是：

```text
-0.031
-0.0309
-0.03081
...
```

因此通常用连续分布建模。

PDF 可以告诉我们误差在哪些区域最集中。

---

## 深度学习中的噪声

例如人为加入：

$$
\epsilon\sim\text{Gaussian}
$$

这里的噪声 $\epsilon$ 通常作为连续型随机变量。

以后看到正态分布：

$$
\mathcal N(\mu,\sigma^2)
$$

它对应的那条经典钟形曲线，就是一个 PDF。

---

## 回归问题

图像分类的标签可能是离散的：

```text
猫
狗
鸟
```

但回归任务中的输出可能是：

```text
房价
深度
温度
位置
速度
年龄
```

这些量经常使用连续型随机变量描述。

---

## 计算机视觉

例如一个目标框的位置：

$$
X=\text{目标中心横坐标}
$$

理论上可能连续变化。

模型不一定只预测一个确定坐标，还可能建模：

> 目标位置在哪些区域更可能出现。

这时就会自然涉及连续概率分布。

---

# 21. 今天最需要形成的“脑内动画”

以后只要看到连续型随机变量，你脑子里应该自动出现这样一幅图：

```text
随机变量 X
     ↓
数轴上一整段连续可能取值
     ↓
上方盖着一条 PDF 曲线
     ↓

       f_X(x)
          ↑
          │          /\
          │        /    \
          │      /░░░░░░░\
          │_____/░░░░░░░░░\____
          └─────┼─────────┼────→ x
                a         b

                  ↓

阴影面积
=
P(a≤X≤b)
```

而不是：

```text
曲线高度
=
概率
```

---

# 22. 理解检查

先不要看答案，自己在脑中判断。

### 问题1

一个连续型随机变量满足：

$$
f_X(2)=0.8
$$

能否说：

$$
P(X=2)=0.8
$$

？

**不能。**

$0.8$ 是密度，不是单点概率。

连续型随机变量：

$$
P(X=2)=0
$$

---

### 问题2

PDF 某处：

$$
f_X(x)=2
$$

合法吗？

**完全可能。**

只要：

$$
f_X(x)\ge0
$$

并且总面积：

$$
\int_{-\infty}^{\infty}f_X(x)\,dx=1
$$

即可。

---

### 问题3

如果：

$$
P(X=3)=0
$$

那么 $X=3$ 是不是不可能发生？

对于连续型随机变量：

**不能这样判断。**

概率为0的单点仍然可以属于样本空间。

---

### 问题4

连续型随机变量真正有意义的是：

$$
P(X=x)
$$

还是：

$$
P(a\le X\le b)
$$

？

通常真正有概率信息的是后者：

$$
P(a\le X\le b)
$$

---

### 问题5

PDF 和 CDF 的关系是什么？

PDF 是局部密度，CDF 是累计概率：

$$
F_X(x)=\int_{-\infty}^{x}f_X(t)\,dt
$$

在可导处：

$$
f_X(x)=F_X'(x)
$$

---

# 这一节真正需要记住的核心

只压缩成5句话：

1. **连续型随机变量的概率不是放在单个点上的；通常 $P(X=x)=0$。**
2. **PDF $f_X(x)$ 描述的是概率密度，而不是 $X=x$ 的概率。**
3. **区间概率等于 PDF 曲线下面的面积：**

   $$
   P(a\le X\le b)=\int_a^b f_X(x)\,dx
   $$
4. **PDF 可以大于1，但总面积必须等于1；概率受0到1限制，密度高度不受1限制。**
5. **CDF 是 PDF 的累计面积：**

   $$
   F_X(x)=\int_{-\infty}^{x}f_X(t)\,dt
   $$

## 下一步为什么会自然进入下一课？

现在我们已经有了两种描述随机变量的基本语言：

```text
离散型
→ PMF
→ 概率质量放在一个个点上

连续型
→ PDF
→ 概率通过曲线下面积得到
```

但这仍然只是“通用语言”。

现实问题中，我们不会每次都从零设计一条概率分布。大量随机现象会反复出现相似的结构，于是人们把这些结构总结成了一批**常见概率分布模型**。

下一步最自然的是从最简单的分布开始：

$$
\boxed{\text{伯努利分布 Bernoulli}}
$$

它只回答一个极其基础的问题：

> **一次试验只有“成功/失败”两种结果时，如何用一个随机变量完整描述它？**

这个模型看起来简单，但它会一路长成后面的**二项分布**，并直接连接分类问题、点击/不点击、合格/不合格、正确/错误等大量机器学习场景。
