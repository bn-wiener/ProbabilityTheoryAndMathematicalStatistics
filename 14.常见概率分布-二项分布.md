# 第十四课：二项分布——重复 $n$ 次伯努利试验，一共成功多少次？

上一课我们只研究了一次“成功 / 失败”试验：

$$
X\sim \operatorname{Bernoulli}(p)
$$

也就是：

$$
X=
\begin{cases}
1,&\text{目标事件发生}\\
0,&\text{目标事件不发生}
\end{cases}
$$

今天只增加一个新思想：

> **如果这样的试验重复很多次，我们不再问某一次是否成功，而问“一共成功了多少次”，这个总次数服从什么分布？**

这就是**二项分布（Binomial Distribution）**。

---

# 一、先确定它在整个知识地图中的位置

目前我们走到了：

```text
随机现象
  ↓
样本空间
  ↓
事件
  ↓
概率
  ↓
条件概率
  ↓
独立性
  ↓
随机变量
  ↓
概率分布
  ↓
CDF
  ↓
离散型 / 连续型随机变量
  ↓
常见概率分布
  ↓
伯努利分布
  ↓
二项分布 ← 今天
  ↓
其他常见分布
  ↓
数学期望
  ↓
方差
```

所以今天还**没有进入数学期望**。

我们继续沿着“常见概率分布”这条线学习。

---

# 二、问题背景：为什么有伯努利分布还不够？

假设一件产品的不合格概率是：

$$
p=0.1
$$

抽查一件产品。

定义：

$$
X=
\begin{cases}
1,&\text{不合格}\\
0,&\text{合格}
\end{cases}
$$

那么：

$$
X\sim\operatorname{Bernoulli}(0.1)
$$

这很好。

但是工厂真正关心的问题通常不会只是：

> “这一件是否不合格？”

而是：

> **抽查100件产品，一共有多少件不合格？**

这时最终结果可能是：

$$
0,1,2,\ldots,100
$$

已经不再只有0和1。

因此伯努利分布不够用了。

---

# 三、最核心的直觉：二项分布就是“数 1”

想象我们连续检查5个产品。

每个产品都用伯努利变量表示：

```text
第1件：合格     → 0
第2件：不合格   → 1
第3件：合格     → 0
第4件：不合格   → 1
第5件：不合格   → 1
```

于是得到：

```text
0  1  0  1  1
```

如果我们想知道：

> 一共有几件不合格？

直接相加：

$$
0+1+0+1+1=3
$$

所以：

$$
X_1+X_2+\cdots+X_5
$$

实际上就是在：

> **数“成功事件”一共出现了多少次。**

这就是二项分布最底层的来源。

---

# 四、从伯努利分布一步一步长出二项分布

假设我们重复进行 $n$ 次试验。

第 $i$ 次定义一个伯努利随机变量：

$$
X_i=
\begin{cases}
1,&\text{第 }i\text{ 次成功}\\
0,&\text{第 }i\text{ 次失败}
\end{cases}
$$

其中：

$$
i=1,2,\ldots,n
$$

如果每次成功概率都是：

$$
p
$$

那么：

$$
X_i\sim\operatorname{Bernoulli}(p)
$$

现在定义一个新的随机变量：

$$
X=X_1+X_2+\cdots+X_n
$$

这个 $X$ 是什么？

因为每次成功贡献1，失败贡献0，所以：

$$
\boxed{X=\text{$n$ 次试验中的成功总次数}}
$$

这就是二项随机变量。

---

# 五、一个具体画面

假设抛5次硬币。

定义：

```text
正面 → 1
反面 → 0
```

某一次实际结果：

```text
正  反  正  正  反
```

对应：

```text
1   0   1   1   0
```

所以：

$$
X=1+0+1+1+0=3
$$

这里要特别注意三个层次：

```text
现实结果：
正反正正反

        ↓ 0/1编码

5个伯努利变量：
1,0,1,1,0

        ↓ 求和

二项随机变量：
X=3
```

因此：

> 伯努利分布研究一次试验。

而：

> 二项分布研究多次伯努利试验中成功次数的总和。

---

# 六、什么时候才能使用二项分布？

这是今天非常重要的一部分。

不能只要看到“做了很多次实验”就使用二项分布。

经典二项模型通常需要满足四个条件。

---

## 条件1：试验次数固定

事先明确重复：

$$
n
$$

次。

例如：

> 抛10次硬币。

这里：

$$
n=10
$$

---

## 条件2：每次试验只有两种我们关心的结果

例如：

```text
成功 / 失败
是 / 否
点击 / 不点击
合格 / 不合格
正面 / 反面
```

注意：

这里不是说现实世界必须只有两种物理结果。

比如掷骰子有6种基本结果，但是我们完全可以定义：

```text
掷出6 → 成功
没掷出6 → 失败
```

于是每次试验就变成伯努利试验。

---

## 条件3：每次成功概率相同

每次都有：

$$
P(\text{成功})=p
$$

不能第一次是0.1、第二次0.5、第三次0.9，然后还直接使用同一个标准二项分布。

---

## 条件4：各次试验相互独立

也就是说：

> 前一次结果不会改变后一次成功概率。

例如理想情况下重复抛同一枚硬币：

```text
第1次正面
```

不会让：

```text
第2次正面概率
```

自动变大或变小。

---

# 七、把四个条件压缩成一句话

二项分布描述的是：

> **固定进行 $n$ 次相互独立、成功概率均为 $p$ 的伯努利试验，成功总次数的分布。**

如果：

$$
X=\text{成功次数}
$$

那么记作：

$$
\boxed{X\sim\operatorname{Binomial}(n,p)}
$$

也常简写为：

$$
\boxed{X\sim B(n,p)}
$$

---

# 八、每个符号是什么意思？

$$
X\sim\operatorname{Binomial}(n,p)
$$

逐个解释。

### $X$

随机变量。

它表示：

> $n$ 次试验中成功的总次数。

---

### $n$

试验总次数。

所以 $n$ 通常是正整数。

例如抛10次硬币：

$$
n=10
$$

---

### $p$

每一次试验的成功概率。

满足：

$$
0\le p\le1
$$

---

### $X$ 可以取什么值？

既然一共试验 $n$ 次：

最少一次都不成功：

$$
X=0
$$

最多全部成功：

$$
X=n
$$

所以：

$$
\boxed{X\in\{0,1,2,\ldots,n\}}
$$

---

# 九、真正的问题来了：怎样计算“恰好成功 $k$ 次”的概率？

现在进入今天最重要的公式。

假设：

$$
X\sim\operatorname{Binomial}(n,p)
$$

我们想求：

$$
P(X=k)
$$

也就是：

> $n$ 次试验中恰好成功 $k$ 次的概率是多少？

不要背公式。

我们一步一步让它长出来。

---

# 十、先从最简单的具体例子推

假设抛3次公平硬币。

定义：

```text
正面 = 成功
反面 = 失败
```

每次：

$$
p=\frac12
$$

我们问：

> 恰好出现2次正面的概率是多少？

即：

$$
P(X=2)
$$

---

# 十一、第一步：有哪些方式可以出现2次成功？

3次中恰好2次正面，可以是：

```text
正 正 反
正 反 正
反 正 正
```

一共有：

$$
3
$$

种排列。

---

# 十二、第二步：其中某一种具体排列的概率是多少？

例如：

```text
正 正 反
```

由于各次独立：

$$
P(\text{正正反})
=
P(\text{正})P(\text{正})P(\text{反})
$$

所以：

$$
=
\frac12\times\frac12\times\frac12
$$

即：

$$
=\frac18
$$

另外两种：

```text
正反正
反正正
```

也各自是：

$$
\frac18
$$

于是：

$$
P(X=2)
=
3\times\frac18
=
\frac38
$$

关键结构已经出现了：

```text
恰好2次成功的排列数
×
某一个具体排列的概率
```

---

# 十三、推广到一般情况

现在考虑 $n$ 次试验。

希望恰好成功 $k$ 次。

那么同时意味着：

$$
n-k
$$

次失败。

---

## 第一步：某一种固定排列的概率

比如：

```text
成功 成功 …… 成功 失败 …… 失败
```

其中：

* $k$ 个成功；
* $n-k$ 个失败。

一个成功的概率是：

$$
p
$$

所以 $k$ 个成功共同贡献：

$$
p^k
$$

一个失败的概率是：

$$
1-p
$$

所以 $n-k$ 个失败共同贡献：

$$
(1-p)^{n-k}
$$

因为各次独立，所以相乘：

$$
\boxed{
p^k(1-p)^{n-k}
}
$$

但这只是：

> **某一种具体成功/失败排列。**

还没有结束。

---

# 十四、为什么还需要组合数？

例如5次试验，恰好2次成功。

不仅可以：

```text
成功 成功 失败 失败 失败
```

还可以：

```text
成功 失败 成功 失败 失败
失败 成功 成功 失败 失败
……
```

所以问题变成：

> 从 $n$ 个位置中，选择哪 $k$ 个位置放“成功”。

这有：

$$
\binom nk
$$

种方法。

读作：

> $n$ 选 $k$。

其计算公式为：

$$
\binom nk=\frac{n!}{k!(n-k)!}
$$

---

# 十五、为什么组合数会这样出现？

假设5次实验恰好成功2次。

我们只需要选择：

> 哪两个位置成功？

例如：

```text
位置： 1  2  3  4  5
       ↑        ↑
      成功     成功
```

只要选定成功位置，其他位置自然都是失败。

所以：

$$
\binom52=10
$$

意味着：

> 有10种不同的“2次成功、3次失败”的位置安排。

而每一种安排概率相同：

$$
p^2(1-p)^3
$$

因此总概率为：

$$
10p^2(1-p)^3
$$

---

# 十六、二项分布 PMF 就这样长出来了

最终：

$$
\boxed{
P(X=k)
=
\binom nk
p^k(1-p)^{n-k}
}
$$

其中：

$$
k=0,1,\ldots,n
$$

这就是二项分布的概率质量函数。

---

# 十七、不要背公式，记住它的三个部件

整个公式：

$$
P(X=k)
=
\underbrace{\binom nk}_{\text{成功位置有多少种安排}}
\underbrace{p^k}_{\text{$k$ 次成功}}
\underbrace{(1-p)^{n-k}}_{\text{$n-k$ 次失败}}
$$

所以它真正说的是：

```text
恰好成功 k 次的概率
=
这种成功/失败排列有多少种
×
每一种排列的概率
```

这比死记：

$$
\binom nkp^k(1-p)^{n-k}
$$

重要得多。

---

# 十八、完整例子：抛5次公平硬币，恰好3次正面

设：

$$
X=\text{5次抛掷中正面出现次数}
$$

因为：

* 固定试验5次；
* 每次只有正/反；
* 每次正面概率都是 $1/2$；
* 各次独立；

因此：

$$
X\sim\operatorname{Binomial}\left(5,\frac12\right)
$$

现在求：

$$
P(X=3)
$$

---

## 第一步：选择哪3次出现正面

$$
\binom53=10
$$

---

## 第二步：某一种排列的概率

3次正面：

$$
\left(\frac12\right)^3
$$

2次反面：

$$
\left(\frac12\right)^2
$$

所以：

$$
\left(\frac12\right)^3
\left(\frac12\right)^2
=
\left(\frac12\right)^5
$$

---

## 第三步：乘以10种排列

$$
P(X=3)
=
10\left(\frac12\right)^5
$$

因此：

$$
P(X=3)
=
\frac{10}{32}
=
\frac5{16}
$$

---

# 十九、二项分布长什么样？

假设：

$$
X\sim\operatorname{Binomial}(10,0.5)
$$

$X$ 可以取：

$$
0,1,2,\ldots,10
$$

概率大致会呈现：

```text
概率
 ^
 |
 |                 █
 |              █  █  █
 |           █  █  █  █  █
 |        █  █  █  █  █  █  █
 |____█___█__█__█__█__█__█__█___█____> X
     0   1  2  3  4  5  6  7  8 ...
```

当：

$$
p=0.5
$$

分布通常比较对称。

为什么中间概率大？

因为：

> “一半左右成功”的排列方式非常多。

而：

```text
全部失败
```

只有一种排列。

```text
FFFFFFFFFF
```

全部成功也只有一种：

```text
SSSSSSSSSS
```

所以两端概率比较低。

这给我们一个很好的组合直觉：

> 二项分布的形状既受到单次概率 $p$ 控制，也受到“有多少种排列能产生这个成功次数”控制。

---

# 二十、如果 $p\neq0.5$ 呢？

例如：

$$
X\sim\operatorname{Binomial}(10,0.1)
$$

每次成功本身就很难。

那么大量成功就更罕见。

概率质量会更多集中在：

$$
0,1,2
$$

附近。

直觉：

```text
p 小
 ↓
每次成功困难
 ↓
总成功次数通常较小
```

反过来，如果：

$$
p=0.9
$$

总成功次数通常会靠近10。

所以：

> $p$ 决定了二项分布大致偏向哪里。

以后学习**数学期望**时，我们会严格计算这个“中心位置”。

今天暂时不提前进入。

---

# 二十一、伯努利分布和二项分布到底是什么关系？

这是本节必须真正理解的地方。

## 伯努利分布

研究：

> 一次试验是否成功。

$$
X_i\in\{0,1\}
$$

---

## 二项分布

研究：

> $n$ 次独立伯努利试验一共成功多少次。

$$
X=X_1+\cdots+X_n
$$

所以：

$$
\boxed{
\text{二项随机变量}
=
\text{多个独立同参数伯努利随机变量之和}
}
$$

也就是：

```text
Bernoulli
Bernoulli
Bernoulli
Bernoulli
   ↓
全部相加
   ↓
Binomial
```

---

# 二十二、伯努利分布其实是二项分布的特殊情况

如果：

$$
n=1
$$

二项分布就只做一次试验：

$$
X\sim\operatorname{Binomial}(1,p)
$$

此时：

$$
X\in\{0,1\}
$$

于是它恰好就是：

$$
\operatorname{Bernoulli}(p)
$$

所以：

$$
\boxed{
\operatorname{Bernoulli}(p)
=
\operatorname{Binomial}(1,p)
}
$$

但学习顺序上，最好先从伯努利建立0/1直觉，再推广到二项分布。

---

# 二十三、反例一：没有独立性，不能直接使用二项分布

假设从一个袋子中：

> 不放回抽球。

第一次抽走一个红球后，袋子内部比例发生改变。

于是第二次抽到红球的概率可能改变。

也就是说：

$$
P(\text{第二次红}\mid\text{第一次红})
$$

可能不等于原来的概率。

这时各次试验不独立。

因此：

> 不能机械地套标准二项分布。

这正是我们之前学习“独立性”的意义。

---

# 二十四、反例二：每次成功概率不同，也不一定是二项分布

假设连续投篮三次：

$$
p_1=0.9,\qquad
p_2=0.5,\qquad
p_3=0.2
$$

即使三次互相独立，因为成功概率不同，也不是标准：

$$
\operatorname{Binomial}(3,p)
$$

模型。

“成功次数”仍然是一个随机变量，但它的分布不再是普通二项分布。

所以：

```text
独立
+
相同成功概率
```

两个条件都很重要。

---

# 二十五、反例三：“至少一次成功”不是二项随机变量的取值定义

假设：

$$
X=\text{10次试验的成功次数}
$$

那么 $X$ 是二项随机变量。

但是：

> “至少一次成功”

其实是事件：

$$
\{X\ge1\}
$$

它不是新的二项随机变量定义。

要区分：

```text
X
→ 一个随机变量

X=3
→ 一个具体取值条件

X≥1
→ 一个关于 X 的事件

P(X≥1)
→ 这个事件的概率
```

这个层次以后会非常重要。

---

# 二十六、“至少一次成功”为什么经常用补事件？

假设：

$$
X\sim\operatorname{Binomial}(n,p)
$$

我们想计算：

$$
P(X\ge1)
$$

直接算：

$$
P(X=1)+P(X=2)+\cdots+P(X=n)
$$

当然可以。

但非常麻烦。

注意：

```text
至少成功1次
```

它的反面就是：

```text
一次都没成功
```

即：

$$
X=0
$$

所以：

$$
P(X\ge1)
=
1-P(X=0)
$$

现在：

$$
P(X=0)
=
\binom n0p^0(1-p)^n
$$

因为：

$$
\binom n0=1
$$

以及：

$$
p^0=1
$$

所以：

$$
P(X=0)=(1-p)^n
$$

最终：

$$
\boxed{
P(X\ge1)
=
1-(1-p)^n
}
$$

但重点仍然不是背公式。

核心逻辑是：

```text
至少一次成功
+
一次都没成功
=
全部可能情况
```

所以利用补事件更简单。

---

# 二十七、一个机器学习例子

假设某分类模型对于某类独立样本的错误概率近似为：

$$
p=0.02
$$

现在测试100张这样的图片。

定义：

$$
X=\text{100张图片中的错误数量}
$$

在“各样本错误事件近似独立、错误概率近似相同”的模型假设下：

$$
X\sim\operatorname{Binomial}(100,0.02)
$$

这时就可以研究：

* 正好错误0张；
* 正好错误2张；
* 错误不超过5张；
* 至少错误1张。

例如至少出现一次错误：

$$
P(X\ge1)
=
1-(1-0.02)^{100}
$$

即：

$$
1-0.98^{100}
$$

这里有一个很重要的工程直觉：

> 单次错误概率即使只有2%，重复很多次以后，“至少出现一次错误”的概率可能已经相当大。

这就是概率模型能够帮助我们避免只盯着单次风险的原因。

---

# 二十八、为什么叫“二项”分布？

这个名字其实和：

$$
(p+(1-p))^n
$$

的二项式展开有关。

根据二项式定理：

$$
(a+b)^n
=
\sum_{k=0}^{n}
\binom nk a^k b^{n-k}
$$

令：

$$
a=p
$$

$$
b=1-p
$$

得到：

$$
(p+(1-p))^n
=
\sum_{k=0}^{n}
\binom nkp^k(1-p)^{n-k}
$$

左边：

$$
p+(1-p)=1
$$

所以：

$$
1^n=1
$$

因此：

$$
\boxed{
\sum_{k=0}^{n}
\binom nkp^k(1-p)^{n-k}=1
}
$$

右边恰好就是二项分布所有可能取值概率之和。

这说明：

> 二项分布的 PMF 确实把总概率完整地分配成了1。

现在不要求记住二项式定理的推导，只需要知道：

> “二项分布”这个名字不是凭空来的，它的概率公式和二项式展开具有完全相同的结构。

---

# 二十九、把今天的知识重新装回主线

整个过程是：

```text
现实问题
“100个人中有多少人点击？”
          ↓

一次点击事件
点击 / 不点击
          ↓

一个伯努利变量
Xi ∈ {0,1}
          ↓

重复 n 次
X1,X2,...,Xn
          ↓

把0和1加起来
X = X1+...+Xn
          ↓

得到成功总次数
0,1,...,n
          ↓

Binomial(n,p)
```

所以今天真正增加的唯一新思想其实只有：

$$
\boxed{\text{把多个伯努利变量加起来计数}}
$$

---

# 三十、理解检查

建议先不用往上看答案，凭直觉判断。

### 1

抛10次公平硬币，定义：

$$
X=\text{正面总次数}
$$

那么 $X$ 服从什么分布？

它的两个参数分别是什么？

---

### 2

为什么：

$$
P(X=k)
$$

中会出现：

$$
\binom nk
$$

？

这个组合数到底在“数”什么？

---

### 3

为什么：

$$
p^k(1-p)^{n-k}
$$

还不是完整的 $P(X=k)$？

---

### 4

从袋子中不放回连续抽10个球，统计红球数量。

能否直接说它服从二项分布？

关键应该检查哪个条件？

---

### 5

如果：

$$
X\sim\operatorname{Binomial}(20,0.1)
$$

那么 $X=0.1$ 有没有可能？

为什么？

注意区分：

$$
p
$$

和：

$$
X
$$

---

### 6

下面这句话你能不能自己解释出来：

$$
\boxed{
\text{Binomial}
=
\text{sum of Bernoulli}
}
$$

如果这一句真正理解了，今天的核心就已经抓住了。

---

# 这一节真正需要记住的核心

1. **伯努利分布研究一次成功/失败；二项分布研究 $n$ 次伯努利试验中的成功总次数。**
2. 若 $n$ 次试验相互独立、每次成功概率都是 $p$，且 $X$ 表示成功次数，则

   $$
   X\sim\operatorname{Binomial}(n,p).
   $$
3. 二项分布的核心公式

   $$
   P(X=k)=\binom nkp^k(1-p)^{n-k}
   $$

   来自“成功位置的排列数 × 某一种排列的概率”。
4. 二项分布的关键假设不是“有很多次试验”，而是**固定次数、二分类、相互独立、成功概率相同**。
5. 最本质的关系是：

   $$
   \boxed{X=X_1+\cdots+X_n}
   $$

   即**二项随机变量就是多个独立同参数伯努利随机变量之和**。

下一步，我们可以继续沿着“常见离散分布”进入**几何分布**：二项分布问的是“固定做 $n$ 次，一共成功几次”，而几何分布会反过来问：

> **为了等到第一次成功，我到底需要试多少次？**

这两个问题只改变了一个视角，却会产生完全不同的随机变量和概率分布。
