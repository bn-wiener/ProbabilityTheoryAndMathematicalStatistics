# 第十九课：Gamma / Erlang 分布——等到第 $r$ 次事件，一共需要多久？

第十八课我们研究了：

> **从现在开始，到下一次事件发生，需要等待多久？**

在恒定速率泊松到达模型下，这个等待时间服从指数分布。第十八课最后也已经自然留下了下一步：如果不是等第 1 次，而是**等到第 $r$ 次事件**，要把若干段等待时间累积起来。

今天就只解决这个问题：

$$
\boxed{\text{第 }r\text{ 次事件发生之前，总共要等待多久？}}
$$

它会带出两个紧密相关的名字：

$$
\boxed{\text{Erlang 分布}}
$$

以及它更一般的推广：

$$
\boxed{\text{Gamma 分布}}
$$

---

## 一、先看它在整个知识地图中的位置

我们目前学过的分布，可以先整理成两条非常漂亮的主线。

### 离散试验世界

```text
一次是否成功
    ↓
Bernoulli

固定做 n 次，成功多少次
    ↓
Binomial

等到第一次成功，需要试几次
    ↓
Geometric

等到第 r 次成功，需要试几次
    ↓
Negative Binomial
```

第十六课中的负二项分布正是研究：

> 为了获得第 $r$ 次成功，总共需要进行多少次离散试验。

---

### 连续时间世界

```text
固定观察 t 时间，发生多少个事件
    ↓
Poisson

等到第 1 次事件，需要多久
    ↓
Exponential

等到第 r 次事件，需要多久
    ↓
Erlang / Gamma  ← 今天
```

因此你可以先看到一个非常重要的对应：

$$
\boxed{
\text{Geometric : Negative Binomial}
=
\text{Exponential : Erlang}
}
$$

左边是**离散次数的等待**，右边是**连续时间的等待**。

---

# 二、问题背景：为什么指数分布已经不够？

假设服务器的请求按照恒定速率随机到达：

$$
\lambda=2\text{ 次/小时}
$$

第十八课可以定义：

$$
T_1=\text{等到第1个请求所需时间}
$$

于是：

$$
T_1\sim \operatorname{Exp}(2)
$$

但是现在业务需求变了。

系统必须收集到 **3 个请求**以后才能开始处理。

于是我们关心：

$$
T_3=\text{从现在开始到第3个请求到来所需的总时间}
$$

一次实际结果可能是：

```text
现在
0
│────●─────────●──────●────────────────→ 时间
    0.2h      0.8h   1.1h
                   ↑
               第3次事件

T₃ = 1.1 小时
```

这里显然已经不是：

> “下一次事件多久到？”

而是：

> **“连续等到第3次事件，一共用了多久？”**

所以普通指数分布不再直接描述 $T_3$。

---

# 三、最重要的直觉：把等待过程切成一段一段

假设我们要等到第 3 个请求。

整个过程其实可以拆成三段：

```text
开始
 │
 │<── W₁ ──>│ 第1次事件
             │
             │<── W₂ ──>│ 第2次事件
                         │
                         │<── W₃ ──>│ 第3次事件
```

定义：

$$
W_1=\text{等第1次事件的时间}
$$

$$
W_2=\text{第1次事件以后，再等第2次事件的时间}
$$

$$
W_3=\text{第2次事件以后，再等第3次事件的时间}
$$

在理想泊松到达模型下，每一段都有：

$$
W_i\sim\operatorname{Exp}(\lambda)
$$

而我们真正关心的总等待时间：

$$
\boxed{
T_3=W_1+W_2+W_3
}
$$

一般地：

$$
\boxed{
T_r=W_1+W_2+\cdots+W_r
}
$$

这就是今天最根本的来源：

> **Erlang 分布 = 多个独立同参数指数等待时间之和。**

这和第十六课的负二项分布极其相似：

```text
负二项：
多个几何等待次数相加

Erlang：
多个指数等待时间相加
```

---

# 四、先不要急着看 PDF：$T_r\le t$ 到底是什么意思？

定义：

$$
T_r=\text{第 }r\text{ 次事件出现的时间}
$$

现在问：

$$
T_r\le t
$$

这句话翻译成自然语言：

> 到时间 $t$ 为止，第 $r$ 次事件已经发生。

那么到 $t$ 为止，一共至少发生了多少次？

当然是：

$$
r
$$

次。

而第十七课已经定义过：

$$
N(t)=\text{前 }t\text{ 时间内发生的事件个数}
$$

在恒定速率泊松模型下：

$$
N(t)\sim\operatorname{Poisson}(\lambda t)
$$

这是泊松分布的计数视角。

因此：

$$
\boxed{
\{T_r\le t\}
=
\{N(t)\ge r\}
}
$$

这条关系极其重要。

它说的是同一件事的两种语言：

```text
等待语言：

第 r 次事件已经在 t 以前发生

                 ⇕

计数语言：

前 t 时间里至少已经发生 r 次
```

---

# 五、于是 CDF 自然长出来了

CDF 定义：

$$
F_{T_r}(t)=P(T_r\le t)
$$

刚才已经知道：

$$
P(T_r\le t)=P(N(t)\ge r)
$$

直接把：

$$
r,r+1,r+2,\ldots
$$

全部加起来当然可以，但很麻烦。

所以用补事件。

“至少发生 $r$ 次”的反面是：

> 只发生了 $0,1,\ldots,r-1$ 次。

因此：

$$
P(T_r\le t)
=
1-P(N(t)\le r-1)
$$

于是：

$$
F_{T_r}(t)
=
1-\sum_{k=0}^{r-1}P(N(t)=k)
$$

而：

$$
N(t)\sim\operatorname{Poisson}(\lambda t)
$$

所以：

$$
P(N(t)=k)
=
e^{-\lambda t}
\frac{(\lambda t)^k}{k!}
$$

最终：

$$
\boxed{
F_{T_r}(t)
=
1-
\sum_{k=0}^{r-1}
e^{-\lambda t}
\frac{(\lambda t)^k}{k!},
\qquad t\ge0
}
$$

公式看起来比较长，但它的思想只有一句：

> **到 $t$ 时还没有等到第 $r$ 次事件，就是截至 $t$ 最多只出现了 $r-1$ 次。**

所以对应的尾概率更直观：

$$
\boxed{
P(T_r>t)
=
\sum_{k=0}^{r-1}
e^{-\lambda t}
\frac{(\lambda t)^k}{k!}
}
$$

---

# 六、一个具体例子：等第 3 个服务器请求

假设：

$$
\lambda=2\text{ 次/小时}
$$

定义：

$$
T_3=\text{等到第3个请求所需时间}
$$

现在问：

> 1小时以内，第3个请求已经到达的概率是多少？

我们求：

$$
P(T_3\le1)
$$

先不要套 Gamma 公式。

翻译成计数语言：

> 前1小时至少已经来了3个请求。

所以：

$$
P(T_3\le1)
=
P(N(1)\ge3)
$$

而：

$$
N(1)\sim\operatorname{Poisson}(2)
$$

于是：

$$
P(T_3\le1)
=
1-P(N(1)\le2)
$$

即：

$$
=
1-
\left[
P(N=0)+P(N=1)+P(N=2)
\right]
$$

逐项：

$$
P(N=0)=e^{-2}
$$

$$
P(N=1)=2e^{-2}
$$

$$
P(N=2)=
e^{-2}\frac{2^2}{2!}
=
2e^{-2}
$$

所以：

$$
P(T_3\le1)
=
1-5e^{-2}
$$

近似：

$$
\boxed{0.3233}
$$

也就是大约：

$$
32.3\%
$$

注意我们还没学任何 Gamma 新公式。

仅仅依靠：

$$
\boxed{
T_3\le1
\iff
N(1)\ge3
}
$$

就已经算出来了。

---

# 七、现在才推 PDF：第 $r$ 次事件为什么恰好落在 $t$ 附近？

这是今天最值得理解的推导。

我们想研究：

> 第 $r$ 次事件落在很短的时间区间

$$
(t,t+h]
$$

里的概率。

其中 $h$ 非常小。

要让第 $r$ 次事件在这里发生，需要什么？

## 第一步：截至 $t$，必须恰好已经发生 $r-1$ 次

```text
0 ───────────────────────── t
      前面恰好 r-1 个事件
```

如果只有 $r-2$ 个，那么接下来一个事件只是第 $r-1$ 次。

如果已经有 $r$ 个，那么第 $r$ 次早就发生了。

所以必须：

$$
N(t)=r-1
$$

它的概率为：

$$
P(N(t)=r-1)
=
e^{-\lambda t}
\frac{(\lambda t)^{r-1}}{(r-1)!}
$$

---

## 第二步：接下来极短的 $h$ 时间里发生一次事件

当 $h$ 很小时：

$$
P(\text{接下来 }h\text{ 时间发生事件})
\approx
\lambda h
$$

这是泊松到达模型中“速率为 $\lambda$”的含义之一。

---

## 第三步：两个条件同时满足

因此：

$$
P(t<T_r\le t+h)
$$

大约等于：

$$
e^{-\lambda t}
\frac{(\lambda t)^{r-1}}{(r-1)!}
\cdot\lambda h
$$

重新整理：

$$
\approx
\frac{
\lambda^r t^{r-1}e^{-\lambda t}
}{
(r-1)!
}
h
$$

而连续型随机变量的局部概率满足：

$$
P(t<T_r\le t+h)
\approx f_{T_r}(t)h
$$

对照两边，得到：

$$
\boxed{
f_{T_r}(t)
=
\frac{
\lambda^r t^{r-1}e^{-\lambda t}
}{
(r-1)!
},
\qquad t\ge0
}
$$

这就是 **Erlang 分布**的 PDF。

---

# 八、不要背公式：看懂它的三个组成部分

公式：

$$
f_{T_r}(t)
=
\frac{\lambda^r t^{r-1}e^{-\lambda t}}{(r-1)!}
$$

可以重新写成：

$$
f_{T_r}(t)
=
\underbrace{
e^{-\lambda t}
\frac{(\lambda t)^{r-1}}{(r-1)!}
}_{\text{截至 }t\text{ 恰好已经发生 }r-1\text{ 次}}
\cdot
\underbrace{\lambda}_{\text{此刻附近继续发生的速率}}
$$

所以整条 PDF 实际上在说：

```text
第 r 次事件在 t 附近出现

=

前面恰好已经来了 r-1 个
×
现在附近再来一个
```

这和几何分布：

$$
(1-p)^{k-1}p
$$

以及负二项分布的构造思想其实高度统一。

---

# 九、为什么当 $r=1$ 时，会退化成指数分布？

这是必须亲手验证的。

Erlang PDF：

$$
f_{T_r}(t)
=
\frac{\lambda^r t^{r-1}e^{-\lambda t}}{(r-1)!}
$$

令：

$$
r=1
$$

得到：

$$
f_{T_1}(t)
=
\frac{
\lambda^1t^0e^{-\lambda t}
}{
0!
}
$$

因为：

$$
t^0=1,\qquad0!=1
$$

所以：

$$
f_{T_1}(t)
=
\lambda e^{-\lambda t}
$$

这就是指数分布：

$$
\boxed{
T_1\sim\operatorname{Exp}(\lambda)
}
$$

因此：

$$
\boxed{
\operatorname{Exponential}(\lambda)
=
\operatorname{Erlang}(1,\lambda)
}
$$

就像：

$$
\boxed{
\operatorname{Geometric}
=
\operatorname{NegativeBinomial}(r=1)
}
$$

一样。

---

# 十、Erlang 分布到底是什么？

现在可以正式定义。

假设：

$$
W_1,W_2,\ldots,W_r
$$

是相互独立、参数相同的指数随机变量：

$$
W_i\sim\operatorname{Exp}(\lambda)
$$

定义：

$$
T_r=W_1+\cdots+W_r
$$

那么：

$$
\boxed{
T_r\sim\operatorname{Erlang}(r,\lambda)
}
$$

其中：

* $r=1,2,3,\ldots$：要等多少次事件；
* $\lambda>0$：事件发生速率；
* $T_r$：等到第 $r$ 次事件的总时间。

PDF：

$$
\boxed{
f_{T_r}(t)
=
\frac{\lambda^r}{(r-1)!}
t^{r-1}e^{-\lambda t},
\qquad t\ge0
}
$$

---

# 十一、图像为什么不再像指数分布一样从最高处一路下降？

指数分布：

$$
r=1
$$

PDF：

```text
密度
 ^
 |\
 | \
 |  \
 |   \____
 +----------------→ t
```

刚开始密度最高，然后不断下降。

为什么？

因为你只等第一次事件：

> 马上发生当然完全可能。

---

现在考虑：

$$
r=3
$$

要等第3次事件。

刚开始：

$$
t\approx0
$$

时，第3次事件几乎不可能已经发生。

因为必须在极短时间里连续积累3次事件。

所以 PDF 在0附近反而很低：

```text
密度
 ^
 |          /\
 |        /    \
 |      /        \
 |_____/           \______
 +------------------------→ t
 0
```

为什么先上升？

因为随着时间过去：

> 已经有足够时间积累前面的事件。

为什么后来下降？

因为：

> 如果拖得特别特别久第 $r$ 次还没到，也越来越罕见。

因此当 $r>1$ 时，你会看到：

```text
太早
→ 来不及积累 r 个事件
→ 密度低

中间
→ 最典型
→ 密度高

太晚
→ 早就应该积累够了
→ 密度又低
```

这就是 Gamma/Erlang 形状产生的直觉。

---

# 十二、$r$ 增大，会发生什么？

假设 $\lambda$ 不变。

比较：

$$
T_1,\ T_2,\ T_5,\ T_{10}
$$

它们分别表示：

```text
等第1次
等第2次
等第5次
等第10次
```

显然：

```text
要等的事件越多
       ↓
通常需要累计更长时间
       ↓
概率质量向右移动
```

所以大致：

```text
T₁       T₂          T₅                T₁₀
 /\        /\           /\                 /\
/  \      /  \         /  \               /  \
------------------------------------------------→ t
```

这里只建立位置直觉。

后面正式学习数学期望后，我们会证明：

$$
E[T_r]=\frac r\lambda
$$

因为今天还没有进入期望，所以现在不要把它当成需要背的公式。

你只需知道：

> 等的事件数越多，总等待通常越长。

---

# 十三、$\lambda$ 增大又会发生什么？

现在固定：

$$
r
$$

例如始终等第3次事件。

比较：

$$
\lambda=1,\quad2,\quad10
$$

$\lambda$ 表示单位时间事件发生得有多频繁。

因此：

```text
λ 大
↓
事件来得快
↓
第 r 次事件更早出现
↓
T_r 整体向左缩
```

相反：

```text
λ 小
↓
事件稀疏
↓
需要更久才积累 r 个事件
↓
T_r 向右移动
```

注意：

$$
r
$$

控制的是：

> **需要累计几个事件。**

而：

$$
\lambda
$$

控制的是：

> **事件到来的速度。**

这两个参数不能混。

---

# 十四、那 Gamma 分布又是什么？为什么还要另起一个名字？

到这里我们有：

$$
r=1,2,3,\ldots
$$

也就是形状参数只能取正整数。

这非常适合解释：

> 等到第 $r$ 次泊松事件。

但数学上可以把：

$$
r
$$

从正整数推广为任意正实数：

$$
\alpha>0
$$

于是得到：

$$
\boxed{\text{Gamma 分布}}
$$

它的 PDF 写成：

$$
\boxed{
f_X(x)
=
\frac{\lambda^\alpha}{\Gamma(\alpha)}
x^{\alpha-1}e^{-\lambda x},
\qquad x\ge0
}
$$

记作：

$$
\boxed{
X\sim\operatorname{Gamma}(\alpha,\lambda)
}
$$

这里我们采用：

* $\alpha$：shape，形状参数；
* $\lambda$：rate，速率参数。

---

# 十五、这个大写 $\Gamma(\alpha)$ 是什么？

不要把：

$$
\Gamma
$$

和“Gamma 分布的名称”神秘化。

它首先是一个数学函数：

$$
\boxed{
\Gamma(\alpha)
=
\int_0^\infty
u^{\alpha-1}e^{-u}\,du
}
$$

它叫：

> Gamma 函数。

它最关键的性质是：

$$
\boxed{
\Gamma(n)=(n-1)!
}
$$

当：

$$
n=1,2,3,\ldots
$$

时成立。

例如：

$$
\Gamma(1)=1=0!
$$

$$
\Gamma(2)=1=1!
$$

$$
\Gamma(3)=2=2!
$$

$$
\Gamma(4)=6=3!
$$

所以当 Gamma 分布中的：

$$
\alpha=r
$$

恰好是正整数时：

$$
\Gamma(r)=(r-1)!
$$

于是 Gamma PDF：

$$
\frac{\lambda^\alpha}{\Gamma(\alpha)}
x^{\alpha-1}e^{-\lambda x}
$$

就变成：

$$
\frac{\lambda^r}{(r-1)!}
x^{r-1}e^{-\lambda x}
$$

也就是我们刚才得到的 Erlang PDF。

因此：

$$
\boxed{
\text{Erlang 分布是 Gamma 分布中 shape 为正整数的特殊情况}
}
$$

---

# 十六、为什么 Gamma 函数会出现在分母？

这里不能只说“定义就是这样”。

回想 PDF 必须满足：

$$
\int_0^\infty f_X(x)\,dx=1
$$

假设我们先猜测 Gamma 分布的形状主体应该是：

$$
x^{\alpha-1}e^{-\lambda x}
$$

但这个函数下面的总面积一般不是1。

所以必须乘一个归一化常数：

$$
C
$$

即：

$$
f_X(x)
=
C x^{\alpha-1}e^{-\lambda x}
$$

要求：

$$
C\int_0^\infty
x^{\alpha-1}e^{-\lambda x}\,dx
=1
$$

令：

$$
u=\lambda x
$$

则：

$$
x=\frac u\lambda
$$

并且：

$$
dx=\frac1\lambda du
$$

代进去：

$$
\int_0^\infty
x^{\alpha-1}e^{-\lambda x}\,dx
$$

变成：

$$
\int_0^\infty
\left(\frac u\lambda\right)^{\alpha-1}
e^{-u}
\frac1\lambda du
$$

把 $\lambda$ 提出去：

$$
=
\frac1{\lambda^\alpha}
\int_0^\infty
u^{\alpha-1}e^{-u}\,du
$$

而后面的积分按照 Gamma 函数定义就是：

$$
\Gamma(\alpha)
$$

所以：

$$
\int_0^\infty
x^{\alpha-1}e^{-\lambda x}\,dx
=
\frac{\Gamma(\alpha)}{\lambda^\alpha}
$$

因此为了让总面积变成1，必须选择：

$$
C=
\frac{\lambda^\alpha}{\Gamma(\alpha)}
$$

所以 Gamma PDF 的前面那一坨：

$$
\boxed{
\frac{\lambda^\alpha}{\Gamma(\alpha)}
}
$$

本质上只是：

> **归一化系数。**

它保证曲线下面总面积正好为1。

---

# 十七、为什么 $\Gamma(n)=(n-1)!$？

我们不完整展开特殊函数理论，只看它为什么和阶乘接上。

Gamma 函数满足：

$$
\boxed{
\Gamma(\alpha+1)
=
\alpha\Gamma(\alpha)
}
$$

这是通过分部积分得到的。

并且：

$$
\Gamma(1)
=
\int_0^\infty e^{-u}du
=
1
$$

所以：

$$
\Gamma(2)
=
1\Gamma(1)
=
1
$$

然后：

$$
\Gamma(3)
=
2\Gamma(2)
=
2
$$

继续：

$$
\Gamma(4)
=
3\Gamma(3)
=
3\times2
=
6
$$

因此：

$$
\Gamma(n)
=
(n-1)(n-2)\cdots1
$$

即：

$$
\boxed{\Gamma(n)=(n-1)!}
$$

你可以暂时把 Gamma 函数理解成：

> **把阶乘从整数推广到了正实数。**

---

# 十八、Gamma、Erlang、Exponential 三者到底什么关系？

这是本节必须彻底分清楚的。

```text
Gamma(α, λ)
α 可以是任意正数
        │
        │ α 恰好是正整数 r
        ↓
Erlang(r, λ)
        │
        │ r = 1
        ↓
Exponential(λ)
```

所以：

$$
\boxed{
\text{Exponential}
\subset
\text{Erlang}
\subset
\text{Gamma}
}
$$

具体：

$$
\operatorname{Exp}(\lambda)
=
\operatorname{Gamma}(1,\lambda)
$$

而：

$$
\operatorname{Erlang}(r,\lambda)
=
\operatorname{Gamma}(r,\lambda),
\qquad r\in\mathbb N^+
$$

---

# 十九、一个非常重要的区别：Gamma 一般不再无记忆

指数分布最特别的性质之一是：

$$
P(T>s+t\mid T>s)=P(T>t)
$$

即无记忆性。第十八课已经详细建立了这个概念。

那么：

$$
T_r,\quad r>1
$$

还无记忆吗？

**一般不无记忆。**

为什么？

假设你在等第3个请求。

已经过去1小时，仍然没等到第3个。

仅仅知道：

$$
T_3>1
$$

还不能告诉我们目前到底已经来了几个请求。

可能：

```text
0个
```

也可能：

```text
1个
```

还可能：

```text
2个
```

如果已经来了2个：

> 只差最后1个。

如果一个都没来：

> 还差3个。

所以过去发生了什么会影响剩余等待的状态。

这和“只等第1次”很不一样。

因此：

$$
\boxed{
\text{指数分布有无记忆性，但一般的 Gamma/Erlang 不具有无记忆性。}
}
$$

这也是指数分布非常特殊的地方。

---

# 二十、一个反例：看到“正的连续量”不能就套 Gamma

Gamma 分布的取值范围确实是：

$$
X\ge0
$$

但：

> 正的连续随机变量 $\neq$ Gamma 分布。

比如：

* 身高；
* 0～1之间的比例；
* 固定10分钟内均匀到达的时间；
* 对数正态的耗时；
* 截断后的测量值；

都可能非负，却不一定具有 Gamma 结构。

Gamma 最自然的来源之一是：

> **多个独立指数等待时间的累积。**

因此还是那条原则：

$$
\boxed{\text{先看随机机制，再选概率分布}}
$$

不能只看数据长得像“非负”。

---

# 二十一、Poisson、Exponential、Gamma 其实是同一条时间轴上的三种视角

这是今天最重要的整体统一。

假设事件以恒定速率：

$$
\lambda
$$

随机到达。

时间轴：

```text
0 ─────●────────●──────●────────────●────→
       1        2      3            4
```

我们可以定义三个完全不同的随机变量。

### 第一台相机：固定时间，数几个

$$
N(t)=\text{前 }t\text{ 时间的事件数}
$$

得到：

$$
\boxed{
N(t)\sim\operatorname{Poisson}(\lambda t)
}
$$

---

### 第二台相机：等第一个点

$$
T_1=\text{第1次事件的等待时间}
$$

得到：

$$
\boxed{
T_1\sim\operatorname{Exp}(\lambda)
}
$$

---

### 第三台相机：等第 $r$ 个点

$$
T_r=\text{第 }r\text{ 次事件的等待时间}
$$

得到：

$$
\boxed{
T_r\sim\operatorname{Erlang}(r,\lambda)
}
$$

而且最关键的桥梁：

$$
\boxed{
T_r\le t
\iff
N(t)\ge r
}
$$

左边从“时间”看。

右边从“计数”看。

它们研究的是**同一个随机过程**。

---

# 二十二、再与离散世界对照一次

现在你已经能够看到一组非常漂亮的平行结构：

| 问题           | 离散试验              | 连续时间           |
| ------------ | ----------------- | -------------- |
| 等第一次成功       | Geometric         | Exponential    |
| 等第 $r$ 次成功 | Negative Binomial | Erlang / Gamma |
| 随机变量是什么      | 试验次数              | 等待时间           |
| 时间尺度         | $1,2,3,\ldots$  | $t\ge0$      |

特别是：

```text
几何：
F F F S

指数：
──────●
```

以及：

```text
负二项：
不断试，直到第 r 个 S

Erlang：
时间不断走，直到第 r 个 ●
```

它们不是孤立的四个公式。

它们都是：

> **“等待目标事件累计到某个数量”**

在离散世界和连续世界中的两个版本。

---

# 二十三、机器学习和工程中为什么会出现 Gamma 分布？

先在概率论本身理解清楚后，再看应用。

### 1. 多阶段任务耗时

假设一个系统要依次完成若干随机阶段：

```text
阶段1
 ↓
阶段2
 ↓
阶段3
```

每阶段耗时近似独立指数分布。

那么总耗时：

$$
T=W_1+W_2+W_3
$$

具有 Erlang 结构。

---

### 2. 事件驱动系统

一个服务必须收到第 $r$ 条请求以后才能启动批处理。

那么：

$$
T_r=\text{等到第 }r\text{ 条请求的时间}
$$

在泊松到达假设下可以用 Erlang 建模。

---

### 3. 网络与排队

很多排队模型里：

* 泊松描述到达计数；
* 指数描述相邻到达间隔；
* Erlang/Gamma 描述多个到达间隔累计的时间。

所以这几个分布经常成套出现。

---

### 4. Bayesian 建模

Gamma 分布以后还会以另一个极重要身份出现：

> **给泊松分布的发生率 $\lambda$ 建模。**

也就是把：

$$
\lambda
$$

本身也看成随机变量。

这会连接到 Gamma–Poisson 共轭结构。

不过这属于后面的统计推断/Bayesian 内容，现在先不要展开。

---

# 二十四、一个实际中非常容易踩的坑：rate 和 scale

不同教材、软件对 Gamma 分布的第二个参数有两种常见写法。

我们今天使用：

$$
\boxed{
\operatorname{Gamma}(\alpha,\lambda)
}
$$

其中：

$$
\lambda=\text{rate}
$$

PDF：

$$
f(x)=
\frac{\lambda^\alpha}{\Gamma(\alpha)}
x^{\alpha-1}e^{-\lambda x}
$$

但有些资料使用：

$$
\theta=\text{scale}
$$

并写：

$$
f(x)
=
\frac{1}{\Gamma(\alpha)\theta^\alpha}
x^{\alpha-1}e^{-x/\theta}
$$

两者关系：

$$
\boxed{
\theta=\frac1\lambda
}
$$

所以以后看到 Gamma 参数时，**不要只看第二个数字**。

第一件事应该检查：

> 它写的是 rate 还是 scale？

这和我们之前学习几何分布、负二项分布时强调的“先检查随机变量定义和参数化方式”是同一个原则。

---

# 二十五、本课最容易混淆的概念

| 概念          | 研究什么                             |
| ----------- | -------------------------------- |
| Poisson     | 固定 $t$，里面一共发生多少次               |
| Exponential | 第1次事件什么时候到                       |
| Erlang      | 第 $r$ 次事件什么时候到，$r$ 为正整数      |
| Gamma       | Erlang 的连续 shape 推广，$\alpha>0$ |

最核心的三条关系：

$$
\boxed{
T_r\le t
\iff
N(t)\ge r
}
$$

$$
\boxed{
T_r=W_1+\cdots+W_r
}
$$

$$
\boxed{
\operatorname{Exp}(\lambda)
=
\operatorname{Gamma}(1,\lambda)
}
$$

---

# 二十六、理解检查

先不要急着计算，重点看你能不能把随机变量认出来。

### 问题 1

某网站请求按照速率 $\lambda$ 的泊松过程到达。

问：

> 接下来一小时会来多少个请求？

应该想到什么分布？

---

### 问题 2

问：

> 到下一条请求，要等待多久？

是什么分布？

---

### 问题 3

问：

> 到第5条请求，要等待多久？

应该想到：

$$
\boxed{\text{Erlang / Gamma}}
$$

为什么？

---

### 问题 4

若：

$$
T_4\le 10
$$

它用计数随机变量 $N(10)$ 应该怎样表示？

请先自己翻译：

> 到10这个时刻，第4次事件已经发生。

因此应该是：

$$
\boxed{N(10)\ge4}
$$

---

### 问题 5

为什么：

$$
T_r
$$

可以写成：

$$
W_1+\cdots+W_r
$$

？

每一个 $W_i$ 分别表示什么？

---

### 问题 6——最重要

为什么 Erlang PDF：

$$
f_{T_r}(t)
=
\frac{\lambda^rt^{r-1}e^{-\lambda t}}{(r-1)!}
$$

里面会出现：

$$
t^{r-1}
$$

而指数分布没有？

核心不是代数。

而是因为：

> **第 $r$ 次事件在 $t$ 附近出现之前，前 $t$ 时间必须先积累恰好 $r-1$ 次事件。**

这一部分的泊松概率中自然产生：

$$
(\lambda t)^{r-1}
$$

所以 PDF 中自然出现：

$$
t^{r-1}
$$

---

# 这一节真正需要记住的核心

只需要牢牢记住下面 **5 句话**：

1. **指数分布研究等到第1次事件；Erlang 分布研究等到第 $r$ 次事件的总等待时间。**

2. 在速率为 $\lambda$ 的泊松到达模型中，

$$
\boxed{
T_r=W_1+\cdots+W_r
}
$$

其中每一个 $W_i$ 都是独立的 $\operatorname{Exp}(\lambda)$ 等待时间。

3. 等待视角和计数视角满足：

$$
\boxed{
T_r\le t
\iff
N(t)\ge r
}
$$

这是 Poisson 与 Erlang 之间最重要的桥梁。

4. Erlang 是 Gamma 的整数 shape 特例：

$$
\boxed{
\operatorname{Erlang}(r,\lambda)
=
\operatorname{Gamma}(r,\lambda)
}
$$

而指数分布进一步是：

$$
\boxed{
\operatorname{Exp}(\lambda)
=
\operatorname{Gamma}(1,\lambda)
}
$$

5. Gamma 分布一般写作：

$$
\boxed{
f_X(x)
=
\frac{\lambda^\alpha}{\Gamma(\alpha)}
x^{\alpha-1}e^{-\lambda x},
\qquad x\ge0
}
$$

但现阶段最重要的不是背它，而是理解：**它从“多个连续等待阶段累积”这件事长出来。**

---

下一步自然会出现一个重要问题：

> 到目前为止，我们学了很多分布，有的偏左、有的偏右、有的只取非负数。那么现实中极其常见的那条“钟形曲线”到底为什么如此重要？

这会自然把我们带向下一类核心连续分布：

$$
\boxed{\text{正态分布 Normal Distribution}}
$$

但在进入它之前，你现在最值得先确认的一点是：

$$
\boxed{
T_r\le t
\iff
N(t)\ge r
}
$$

如果这条关系你能不靠背诵、自己从时间轴上解释出来，那么第十九课的核心逻辑已经真正建立起来了。
