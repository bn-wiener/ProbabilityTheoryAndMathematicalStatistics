# 第十五课：几何分布——为了等到第一次成功，需要试多少次？

上一课的二项分布问的是：

> **固定做 $n$ 次试验，一共成功多少次？**

今天我们只改变一个视角：

> **不再提前固定试验次数，而是一直做，直到第一次成功。我们一共需要做多少次？**

这就产生了今天的核心概念：

$$
\boxed{\text{几何分布 Geometric Distribution}}
$$

---

# 一、先看它在整条主线中的位置

目前我们走到了：

```text
随机现象
  ↓
样本空间
  ↓
事件
  ↓
概率
  ↓
条件概率
  ↓
独立性
  ↓
随机变量
  ↓
概率分布
  ↓
CDF
  ↓
离散型 / 连续型随机变量
  ↓
常见概率分布
  ├─ 伯努利分布：一次是否成功
  ├─ 二项分布：n次中成功多少次
  └─ 几何分布：第一次成功要等多少次 ← 今天
```

你会发现，前三个分布其实都来自同一个底层实验：

> **重复进行成功概率为 $p$ 的伯努利试验。**

区别只是我们在观察不同的东西。

```text
伯努利：
这一次成功了吗？

二项：
固定做 n 次，一共成功多少次？

几何：
一直做下去，第一次成功出现在第几次？
```

所以今天不是凭空学习一个新公式，而是在**改变随机变量的定义**。

---

# 二、问题背景：为什么二项分布解决不了这个问题？

假设你不断投篮。

每一次命中的概率都是：

$$
p=0.2
$$

而且每次投篮结果相互独立。

现在我问：

> 投10次，一共命中几次？

这是二项分布。

但是换一个问题：

> **我要一直投，直到第一次命中。第一次命中出现在第几次？**

可能是：

```text
第一次就中
第二次才中
第三次才中
……
第20次才中
……
```

这时候试验次数事先没有固定。

所以不能写：

$$
X\sim\operatorname{Binomial}(n,p)
$$

因为二项分布里的 $n$ 必须事先确定。

我们需要一个新的随机变量：

$$
X=\text{第一次成功出现时的试验次数}
$$

这就是几何随机变量。

---

# 三、先建立脑中的画面

假设：

```text
成功 = S
失败 = F
```

不断进行试验。

第一次成功可能出现在：

```text
第1次：
S

第2次：
F S

第3次：
F F S

第4次：
F F F S

第5次：
F F F F S
...
```

观察它们的共同结构。

如果：

$$
X=4
$$

意味着什么？

不是“4次都成功”。

而是：

```text
前三次必须失败
第4次必须成功

F F F S
```

所以几何分布最重要的直觉是：

> **第一次成功出现在第 $k$ 次
> = 前 $k-1$ 次全部失败，并且第 $k$ 次成功。**

先牢牢记住这一句话。

后面的公式几乎会自己出现。

---

# 四、随机变量到底是什么？

定义：

$$
X=\text{直到第一次成功所需要的试验次数}
$$

那么：

$$
X\in\{1,2,3,\ldots\}
$$

为什么从1开始？

因为哪怕运气再好，也至少要做一次试验，才能第一次成功。

所以：

$$
X=0
$$

是不可能的。

---

# 五、从最简单的例子开始

假设抛一枚公平硬币。

定义：

```text
正面 = 成功
反面 = 失败
```

那么：

$$
p=P(\text{正面})=\frac12
$$

定义：

$$
X=\text{第一次出现正面所需要的抛掷次数}
$$

---

## 情况1：$X=1$

表示第一次就成功：

```text
正
```

所以：

$$
P(X=1)=\frac12
$$

---

## 情况2：$X=2$

第一次必须反面，第二次正面：

```text
反 正
```

由于两次独立：

$$
P(X=2)
=
\frac12\times\frac12
=
\frac14
$$

---

## 情况3：$X=3$

必须：

```text
反 反 正
```

所以：

$$
P(X=3)
=
\frac12\times\frac12\times\frac12
=
\frac18
$$

---

## 情况4：$X=4$

必须：

```text
反 反 反 正
```

所以：

$$
P(X=4)=\frac1{16}
$$

于是得到：

| $X$ | 所需结果 |       概率 |
| ----: | ---- | -------: |
|     1 | 正    |  $1/2$ |
|     2 | 反正   |  $1/4$ |
|     3 | 反反正  |  $1/8$ |
|     4 | 反反反正 | $1/16$ |
|     … | …    |        … |

可以明显看到：

> 第一次成功拖得越晚，就需要前面连续失败越多次，因此概率不断下降。

---

# 六、一般公式是怎么长出来的？

现在假设：

$$
P(\text{成功})=p
$$

那么：

$$
P(\text{失败})=1-p
$$

我们想求：

$$
P(X=k)
$$

也就是：

> 第一次成功恰好出现在第 $k$ 次。

按照刚才建立的直觉，这件事必须满足：

```text
前 k-1 次失败
+
第 k 次成功
```

---

## 第一步：前 $k-1$ 次全部失败

一次失败概率为：

$$
1-p
$$

因为各次独立，所以连续 $k-1$ 次失败：

$$
(1-p)^{k-1}
$$

---

## 第二步：第 $k$ 次成功

概率为：

$$
p
$$

---

## 第三步：两部分同时发生

所以相乘：

$$
\boxed{
P(X=k)
=
(1-p)^{k-1}p
}
$$

其中：

$$
k=1,2,3,\ldots
$$

这就是几何分布的 PMF。

---

# 七、为什么这里没有组合数？

这是一个非常重要的对比。

二项分布：

$$
P(X=k)
=
\binom nkp^k(1-p)^{n-k}
$$

里面有：

$$
\binom nk
$$

但是几何分布：

$$
P(X=k)
=
(1-p)^{k-1}p
$$

却没有组合数。

为什么？

---

## 二项分布

如果问：

> 5次中恰好成功2次。

成功可以出现在很多不同位置：

```text
S S F F F
S F S F F
F S S F F
...
```

所以必须数：

> 有多少种位置安排？

这就是组合数。

---

## 几何分布

如果问：

> 第5次才第一次成功。

结果形式只有一种：

```text
F F F F S
```

前4次必须失败，第5次必须成功。

成功的位置已经被“第一次成功在第5次”完全确定了。

没有别的排列。

所以：

$$
\boxed{\text{几何分布不需要组合数}}
$$

这是理解公式结构的关键。

---

# 八、严格数学定义

如果一系列伯努利试验满足：

1. 每次只有成功和失败两种状态；
2. 各次试验相互独立；
3. 每次成功概率保持为 $p$；
4. $X$ 表示第一次成功所需的试验次数；

那么称：

$$
X\sim\operatorname{Geometric}(p)
$$

并且：

$$
\boxed{
P(X=k)
=
(1-p)^{k-1}p,
\qquad
k=1,2,\ldots
}
$$

---

# 九、这里的参数 $p$ 到底控制什么？

假设：

$$
p=0.9
$$

每次成功都非常容易。

所以第一次成功大概率很快出现：

```text
X=1 很常见
X=2 少一些
X=10 极少
```

反过来：

$$
p=0.01
$$

每次成功都很困难。

那么可能需要等很久。

所以直觉上：

```text
p 大
↓
单次容易成功
↓
第一次成功来得早
↓
X 通常比较小
```

而：

```text
p 小
↓
单次难成功
↓
需要更多尝试
↓
X 通常比较大
```

以后学习期望时，我们会严格回答：

> 平均需要等待多少次？

今天先不提前进入。

---

# 十、概率质量图的直觉

例如：

$$
X\sim\operatorname{Geometric}(0.3)
$$

那么：

$$
P(X=1)=0.3
$$

$$
P(X=2)=0.7\times0.3=0.21
$$

$$
P(X=3)=0.7^2\times0.3=0.147
$$

……

画出来大概是：

```text
概率
 ^
 |
0.30 ┤ █
     │ █
0.21 ┤ █   █
     │ █   █
0.15 ┤ █   █   █
     │ █   █   █
     │ █   █   █   █
     └────────────────────→ X
       1   2   3   4   5 ...
```

概率会逐渐下降。

原因非常直接：

> 要拖到更晚才第一次成功，就必须连续经历更多失败。

---

# 十一、CDF：第一次成功“在第 $k$ 次以内”发生

我们已经学过：

$$
F_X(k)=P(X\le k)
$$

在几何分布里：

$$
X\le k
$$

是什么意思？

就是：

> **前 $k$ 次试验中至少成功过一次。**

直接计算可以写：

$$
P(X=1)+P(X=2)+\cdots+P(X=k)
$$

但更简单的方法是补事件。

---

## $X\le k$ 的反面是什么？

如果第一次成功还没有出现在前 $k$ 次：

$$
X>k
$$

就意味着：

> 前 $k$ 次全部失败。

每次失败概率：

$$
1-p
$$

连续 $k$ 次失败：

$$
P(X>k)=(1-p)^k
$$

于是：

$$
P(X\le k)
=
1-P(X>k)
$$

所以：

$$
\boxed{
F_X(k)
=
1-(1-p)^k
}
$$

对于整数 $k\ge1$。

---

# 十二、一个极其重要的尾概率公式

刚才实际上得到：

$$
\boxed{
P(X>k)=(1-p)^k
}
$$

它非常有直觉。

例如：

$$
X>5
$$

表示：

> 做完5次以后，第一次成功仍然没有出现。

那就意味着：

```text
F F F F F
```

所以：

$$
P(X>5)=(1-p)^5
$$

这个公式实际应用非常频繁。

---

# 十三、几何分布最特殊的性质：无记忆性

现在来到今天最值得理解的性质。

假设你已经连续失败了10次。

你可能直觉觉得：

> “都失败10次了，第11次是不是更该成功了？”

如果每次试验确实独立，而且成功概率始终是 $p$，答案是：

> **不会。**

第11次成功概率依然是：

$$
p
$$

前面的10次失败不会让下一次“补偿性”地更容易成功。

这正是独立性的含义。

---

# 十四、“无记忆”到底是什么意思？

假设：

$$
X=\text{第一次成功需要的次数}
$$

现在已知：

$$
X>10
$$

也就是：

> 前10次全部失败了。

现在再问：

> 从此刻开始，还要再等超过5次的概率是多少？

数学上：

$$
P(X>15\mid X>10)
$$

几何分布告诉我们：

$$
\boxed{
P(X>15\mid X>10)
=
P(X>5)
}
$$

意思是：

> 已经等了多久，不改变未来还需要等待多久的概率结构。

这就是**无记忆性**。

---

# 十五、为什么会无记忆？

不要背性质，我们推一下。

根据条件概率：

$$
P(X>15\mid X>10)
=
\frac{P(X>15\cap X>10)}
{P(X>10)}
$$

因为：

$$
X>15
$$

一定意味着：

$$
X>10
$$

所以：

$$
\{X>15\}\subseteq\{X>10\}
$$

因此交集就是：

$$
X>15
$$

所以：

$$
=
\frac{P(X>15)}{P(X>10)}
$$

利用：

$$
P(X>k)=(1-p)^k
$$

得到：

$$
=
\frac{(1-p)^{15}}{(1-p)^{10}}
$$

指数相除：

$$
=(1-p)^5
$$

而：

$$
P(X>5)=(1-p)^5
$$

所以：

$$
\boxed{
P(X>15\mid X>10)=P(X>5)
}
$$

更一般地：

$$
\boxed{
P(X>m+n\mid X>m)=P(X>n)
}
$$

这就是几何分布的无记忆性。

---

# 十六、用一个生活画面理解无记忆性

想象你不断抛硬币，等第一次正面。

已经连续：

```text
反 反 反 反 反 反 反 反
```

8次反面。

第9次抛之前，硬币并不知道：

> “我已经欠他一个正面了。”

如果硬币公平：

$$
P(\text{第9次正面})=\frac12
$$

依然如此。

所以：

```text
过去已经失败多少次
不会改变
未来单次试验的成功概率
```

前提当然是：

> **试验独立且成功概率没有改变。**

---

# 十七、反例：什么时候不能用“无记忆”？

假设你投篮。

连续投10次都没进以后：

* 体力下降；
* 心态变化；
* 手臂疲劳。

那么第11次命中率可能已经不是原来的 $p$。

这意味着：

$$
P(\text{第11次成功}\mid\text{前10次失败})
\neq p
$$

于是试验不再符合同参数独立伯努利假设。

此时：

> 不能机械套几何分布。

这也是为什么概率模型永远带有假设。

---

# 十八、二项分布 vs 几何分布

这是必须主动比较的一对概念。

假设每次成功概率都是 $p$。

## 二项分布

先固定：

$$
n
$$

次。

然后问：

$$
X=\text{这 $n$ 次中一共成功多少次？}
$$

随机的是：

> 成功次数。

试验总数固定。

---

## 几何分布

不固定总次数。

一直进行，直到第一次成功。

问：

$$
X=\text{第一次成功出现在第几次？}
$$

随机的是：

> 所需试验次数。

---

可以压缩成：

|       | 二项分布            | 几何分布            |
| ----- | --------------- | --------------- |
| 固定什么？ | 试验次数 $n$      | 成功目标：第一次成功      |
| 随机什么？ | 成功总次数           | 等待次数            |
| 可能取值  | $0,\dots,n$   | $1,2,3,\dots$ |
| 核心问题  | 做 $n$ 次，成功几次？ | 做几次才第一次成功？      |

一句话：

$$
\boxed{
\text{Binomial：固定次数，数成功}
}
$$

$$
\boxed{
\text{Geometric：固定成功目标，数次数}
}
$$

---

# 十九、几何分布 vs 伯努利分布

伯努利：

$$
X\in\{0,1\}
$$

描述：

> 单次是否成功。

几何：

$$
X\in\{1,2,3,\ldots\}
$$

描述：

> 要进行多少个伯努利试验才第一次成功。

所以又可以看到：

```text
Bernoulli
是底层积木

Binomial
把 Bernoulli 加起来

Geometric
不断做 Bernoulli，直到第一次出现 1
```

这三种分布已经形成一个非常完整的小体系。

---

# 二十、注意：几何分布有两种定义习惯

这是以后看教材、论文、Python库时非常容易踩坑的地方。

今天我们采用：

$$
X=\text{第一次成功所需的试验次数}
$$

所以：

$$
X\in\{1,2,3,\ldots\}
$$

PMF：

$$
P(X=k)=(1-p)^{k-1}p
$$

---

但有些资料定义：

$$
Y=\text{第一次成功之前的失败次数}
$$

那么：

$$
Y\in\{0,1,2,\ldots\}
$$

此时：

$$
P(Y=k)=(1-p)^kp
$$

它们其实只差1：

$$
\boxed{X=Y+1}
$$

所以如果以后发现几何分布的公式指数是：

$$
k
$$

而不是：

$$
k-1
$$

先不要认为谁错了。

先检查：

> 随机变量到底定义的是“试验次数”，还是“成功前的失败次数”。

这是概率论里很典型的：

> **先看随机变量定义，再看公式。**

---

# 二十一、机器学习与工程中的应用

## 1. 网络重试

一个网络请求每次成功概率为 $p$。

不断重试直到成功。

那么：

$$
X=\text{成功前需要尝试多少次}
$$

可以在理想独立同概率假设下建模为几何分布。

---

## 2. 强化学习

智能体反复尝试某种动作，直到第一次获得目标奖励。

如果每次独立且成功概率稳定：

$$
X=\text{首次得到奖励所需尝试次数}
$$

具有几何分布结构。

---

## 3. 计算机视觉中的采样

假设独立抽取图片，每一张满足某个稀有条件的概率为 $p$。

不停抽样直到第一次抽到目标类别。

那么等待样本数可以用几何模型描述。

---

## 4. 故障检测

每次检测某个缺陷，发现缺陷的概率为 $p$。

如果各次检测独立：

> 第一次检测出缺陷所需检测次数

也具有几何分布结构。

---

# 二十二、反例一：成功概率改变

假设第 $i$ 次尝试成功概率是：

$$
p_i
$$

例如：

$$
p_1=0.1,\quad
p_2=0.2,\quad
p_3=0.3
$$

那么：

$$
P(X=3)
$$

应该是：

$$
(1-p_1)(1-p_2)p_3
$$

而不是：

$$
(1-p)^2p
$$

因为根本不存在统一的一个 $p$。

所以不是标准几何分布。

---

# 二十三、反例二：试验不独立

假设抽球不放回。

第一次没抽到红球以后，袋子中红球比例发生变化。

第二次成功概率随之改变。

这不满足几何分布所要求的独立同概率结构。

---

# 二十四、反例三：把 $X=k$ 理解错

如果：

$$
X=5
$$

表示：

> 第5次才第一次成功。

必须对应：

```text
F F F F S
```

不能是：

```text
S F F F S
```

因为这种情况下第一次成功其实在：

$$
X=1
$$

所以几何分布关注的关键词是：

$$
\boxed{\text{第一次}}
$$

---

# 二十五、一个完整例子

假设每次发送数据包成功的概率为：

$$
p=0.8
$$

各次发送相互独立。

如果失败，则继续重发，直到成功。

定义：

$$
X=\text{第一次发送成功所需发送次数}
$$

那么：

$$
X\sim\operatorname{Geometric}(0.8)
$$

---

## 第一次就成功

$$
P(X=1)=0.8
$$

---

## 第2次才成功

必须：

```text
失败 成功
```

所以：

$$
P(X=2)
=
0.2\times0.8
=
0.16
$$

---

## 第3次才成功

必须：

```text
失败 失败 成功
```

所以：

$$
P(X=3)
=
0.2^2\times0.8
=
0.032
$$

---

## 超过3次仍然没有成功

这表示前三次全部失败：

$$
P(X>3)
=
0.2^3
=
0.008
$$

注意最后这个问题里，第4次以后发生什么并不重要。

只要前三次全部失败，就已经满足：

$$
X>3
$$

---

# 二十六、把三种分布放在同一个实验里

假设每次用户点击广告的概率为：

$$
p=0.1
$$

---

### 问题A

随机观察一个用户：

> 他点击了吗？

定义：

$$
X\in\{0,1\}
$$

对应：

$$
\boxed{\operatorname{Bernoulli}(0.1)}
$$

---

### 问题B

观察100个独立用户：

> 有多少人点击？

定义：

$$
X\in\{0,\ldots,100\}
$$

对应：

$$
\boxed{\operatorname{Binomial}(100,0.1)}
$$

---

### 问题C

不断观察独立用户：

> 第几个用户才第一次点击？

定义：

$$
X\in\{1,2,\ldots\}
$$

对应：

$$
\boxed{\operatorname{Geometric}(0.1)}
$$

请注意：

> **概率机制完全一样，只因为“我们定义的随机变量不同”，最终概率分布就不同。**

这一点非常重要。

概率分布不是只由现实世界决定的，还取决于：

> **你究竟选择研究现实中的哪个数值特征。**

---

# 二十七、这一课真正连接了什么旧知识？

今天的新知识可以写成：

$$
\boxed{
\text{几何分布}
=
\text{伯努利试验}
+
\text{独立性}
+
\text{重复试验}
+
\text{第一次成功的等待时间}
}
$$

尤其是独立性现在终于开始发挥实际作用。

之前学：

$$
P(A\cap B)=P(A)P(B)
$$

可能感觉只是一个概率公式。

今天你可以看到：

$$
P(X=k)
=
(1-p)^{k-1}p
$$

之所以能够这样乘起来，本质上正是依赖各次试验独立。

---

# 理解检查

先自己思考，不急着往上翻。

### 1

假设每次试验成功概率：

$$
p=0.2
$$

定义 $X$ 为第一次成功需要的试验次数。

那么：

$$
P(X=3)
$$

为什么是：

$$
0.8^2\times0.2
$$

而不是：

$$
0.2^3
$$

？

---

### 2

为什么几何分布的 PMF：

$$
P(X=k)=(1-p)^{k-1}p
$$

没有二项分布中的：

$$
\binom nk
$$

？

---

### 3

如果：

$$
X\sim\operatorname{Geometric}(0.1)
$$

$X$ 能不能取：

$$
0
$$

？

为什么？

---

### 4

连续抛10次硬币都是反面。

第11次出现正面的概率是不是大于：

$$
\frac12
$$

？

如果硬币公平且各次独立，为什么？

---

### 5

下面两个问题分别对应什么分布？

> 抛10次硬币，出现多少次正面？

和：

> 一直抛硬币，直到第一次正面，需要多少次？

---

### 6 —— 最重要

请尝试自己解释：

> 为什么同样是重复伯努利试验，只因为随机变量定义不同，就会得到二项分布和几何分布两个不同的概率分布？

如果这一点真正理解了，你对“随机变量决定我们观察随机现象的哪个数字特征”这一核心思想已经进一步内化了。

---

# 这一节真正需要记住的核心

1. **几何分布研究的是：重复独立同概率伯努利试验，第一次成功出现在第几次。**
2. 如果每次成功概率是 $p$，则

   $$
   X\sim\operatorname{Geometric}(p),
   \qquad
   P(X=k)=(1-p)^{k-1}p.
   $$
3. 公式来自一句话：**前 $k-1$ 次全部失败，第 $k$ 次成功。**
4. 几何分布没有组合数，因为“第一次成功在第 $k$ 次”只有一种成功/失败位置结构。
5. 几何分布具有无记忆性：在模型假设成立时，已经失败多久不会改变未来等待的概率结构。

下一步自然会出现一个问题：

> 几何分布只等“**第一次成功**”，如果我想一直等到**第2次、第3次，甚至第 $r$ 次成功**，需要多少次试验呢？

这会自然产生下一种分布：

$$
\boxed{\text{负二项分布 / Pascal 分布}}
$$

它可以看成几何分布从“等待第一次成功”向“等待第 $r$ 次成功”的自然推广。
