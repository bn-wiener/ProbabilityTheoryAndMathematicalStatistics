# 第五课：概率——给事件的可能性分配数字

今天进入概率论最核心的概念：

> 怎样用一个数字表示事件发生的可能性，并保证这些数字彼此不矛盾？

当前位置：

```text
随机现象
   ↓
样本空间
   ↓
事件
   ↓
概率  ← 今天
   ↓
条件概率
   ↓
独立性
```

---

# 1. 问题背景：为什么需要概率？

考虑两个事件：

- $A$：明天下雨；
- $B$：明天地震。

我们可以说：

> 下雨比地震更可能发生。

但“更可能”仍然比较模糊。为了进行计算和决策，我们还需要知道：

- 高多少？
- 是否值得带伞？
- 是否值得暂停户外活动？
- 多个风险合在一起有多大？
- 得到新信息后，判断应该怎样改变？

因此，我们希望用一个统一的数字尺度表示事件发生的可能性。

```text
不可能                            必然
  0 ├─────────────────────────────┤ 1
          可能性越来越大 →
```

这就是概率的作用：

> 概率把“事件有多可能发生”转换成0到1之间的数字。

---

# 2. 直觉解释：概率是“可能性重量”

把样本空间想象成一块总重量为1的物体：

```text
整个样本空间 Ω
总概率重量 = 1
┌─────────────────────────────┐
│                             │
│     事件 A 占据其中一部分     │
│       ┌─────────────┐       │
│       │             │       │
│       └─────────────┘       │
│                             │
└─────────────────────────────┘
```

事件 $A$ 的概率，就是分配给区域 $A$ 的“概率重量”。

- 区域没有任何可能结果：概率为0；
- 区域是整个样本空间：概率为1；
- 区域得到的概率重量越大：事件越容易发生。

不过，这里的“区域大小”不是简单指数学图形的面积，也不一定等于样本点个数。

如果不同结果的发生倾向不同，那么即使两个事件包含相同数量的样本点，它们的概率也可能不同。

---

# 3. 概率究竟来自哪里？

在不同场景中，概率可能来自不同依据。

## 3.1 对称性与理论模型

对于一枚理想的公平硬币，正面和反面在模型中地位对称，因此给它们分配相同概率。

由于两个结果的总概率必须为1，所以各自得到一半。

这种概率来自：

```text
模型假设 + 对称性
```

---

## 3.2 大量重复实验

假设抛一枚硬币很多次：

```text
抛掷次数       正面次数       正面频率
   10              7             0.70
  100             54             0.54
 1000            503             0.503
10000           4991             0.4991
```

短期频率可能明显波动，但在适当条件下，大量重复后的频率常会逐渐稳定在某个数值附近。

这个稳定值可以帮助我们估计概率。

这种概率来自：

```text
重复观察 + 数据
```

---

## 3.3 历史数据与统计模型

例如保险公司利用历史数据估计：

- 不同年龄段的事故风险；
- 某种疾病的发生率；
- 不同机器的故障率。

这种概率不是简单依靠对称性，而是通过数据和统计模型估计出来的。

---

## 3.4 当前信息下的可信程度

对于“明天下雨”这种不能在完全相同条件下无限重复的问题，概率也可以表达：

> 在当前气象信息和模型下，我们对事件发生程度的合理判断。

所以概率既可以描述随机机制中的长期倾向，也可以表达给定信息下的不确定程度。

但无论概率从哪里得到，它都必须遵守一致的数学规则。

---

# 4. 概率的数学表达

设：

- $\Omega$ 是样本空间；
- $A$ 是样本空间中的一个事件。

事件 $A$ 的概率记作：

$
P(A)
$

其中：

- $P$ 是一个概率规则；
- $A$ 是输入给这个规则的事件；
- $P(A)$ 是这个规则分配给事件 $A$ 的数字。

可以把它理解成一个“事件称重器”：

```text
事件 A
  ↓
概率规则 P
  ↓
数字 P(A)
```

注意：

$
P(A)
$

不是说“$P$乘以$A$”。

括号表示：

> 把事件 $A$ 交给概率规则 $P$，得到它的概率。

例如：

$
P(A)=0.8
$

表示事件 $A$ 的概率为0.8，也可以写成80%。

它并不表示 $A$ 一定发生，而是表示它具有较高的发生可能性。

---

# 5. 概率必须遵守的三条基本规则

现代概率论并不先规定“每种概率必须怎样计算”，而是要求所有合法概率都必须遵守三条公理。

公理可以理解为：

> 为了让整个概率体系自洽，我们接受的最基本规则。

---

## 规则一：概率不能为负

对于任意事件 $A$：

$
P(A)\geq 0
$

符号解释：

- $P(A)$：事件 $A$ 的概率；
- $\geq$：大于或等于；
- 整个式子表示概率不能是负数。

为什么？

因为概率表示可能性的重量。负的可能性没有合理含义。

---

## 规则二：整个样本空间的概率为1

$
P(\Omega)=1
$

因为 $\Omega$ 包含当前模型中的全部可能结果。

一次试验结束后，实际结果一定落在 $\Omega$ 中，所以“发生样本空间中的某个结果”是必然事件。

因此：

```text
全部可能性加在一起 = 1
```

数字1表示必然，而不是“样本空间中只有一个结果”。

---

## 规则三：互斥事件的概率可以直接相加

如果事件 $A$ 和 $B$ 互斥，即：

$
A\cap B=\varnothing
$

那么：

$
P(A\cup B)=P(A)+P(B)
$

先解释符号：

- $A\cap B$：$A$ 与 $B$ 同时发生；
- $\varnothing$：空集；
- $A\cap B=\varnothing$：两个事件不能同时发生；
- $A\cup B$：$A$ 或 $B$ 至少一个发生；
- $P(A\cup B)$：至少一个发生的概率。

为什么可以相加？

因为两个事件没有重叠区域。

```text
A          B
┌────┐    ┌────┐
│    │    │    │
└────┘    └────┘
```

把两个互不重叠区域的概率重量相加，就得到总区域的概率重量。

---

# 6. 为什么概率一定在0到1之间？

第一条公理已经告诉我们：

$
P(A)\geq 0
$

现在还需要说明为什么：

$
P(A)\leq 1
$

事件 $A$ 与它的补事件 $A^c$ 合在一起，就是整个样本空间：

$
A\cup A^c=\Omega
$

而且 $A$ 与 $A^c$ 不可能同时发生：

$
A\cap A^c=\varnothing
$

因此根据互斥事件的加法规则：

$
P(A\cup A^c)=P(A)+P(A^c)
$

因为 $A\cup A^c=\Omega$，所以左边可以换成：

$
P(\Omega)=P(A)+P(A^c)
$

又因为：

$
P(\Omega)=1
$

所以：

$
1=P(A)+P(A^c)
$

而 $P(A^c)\geq0$，所以 $P(A)$ 不可能超过1。

最终得到：

$
0\leq P(A)\leq1
$

这不是需要孤立背诵的规则，而是从概率公理自然得到的结果。

---

# 7. 补事件公式是怎么得到的？

上一段已经得到：

$
1=P(A)+P(A^c)
$

为了求 $P(A^c)$，在等式两边同时减去 $P(A)$：

$
P(A^c)=1-P(A)
$

这就是补事件公式。

它表达的直觉非常简单：

```text
A发生的可能性
+
A不发生的可能性
=
全部可能性
```

例如某零件合格的概率为0.98，那么不合格的概率为：

$
1-0.98=0.02
$

补事件方法特别适合处理：

- 至少发生一次；
- 并非全部成功；
- 没有出现某结果；
- 不发生故障。

很多时候，计算“至少一次发生”比较复杂，而计算“一次都没有发生”更简单。

---

# 8. 不可能事件为什么概率为0？

样本空间可以写成：

$
\Omega=\Omega\cup\varnothing
$

因为给一个集合并上空集，不会增加任何结果。

同时，$\Omega$ 与空集互斥，所以：

$
P(\Omega)=P(\Omega)+P(\varnothing)
$

在等式两边减去 $P(\Omega)$，得到：

$
P(\varnothing)=0
$

因此不可能事件的概率为0。

但要特别注意：

> 概率为0的事件，在连续模型中不一定逻辑上绝对不可能。

例如从一条连续线段上随机选择一个位置，恰好选中某一个指定点的概率可以是0，但那个点仍然属于样本空间。

目前先记住边界：

```text
不可能事件 → 概率一定为0

概率为0 → 在有限样本空间中通常是不可能
         在连续模型中不一定
```

以后学习连续型随机变量时会详细解释这个现象。

---

# 9. 最简单的概率计算：等可能模型

假设一个有限样本空间中的所有基本结果等可能。

如果：

- 样本空间共有 $n$ 个基本结果；
- 事件 $A$ 包含其中 $m$ 个基本结果；

那么：

$
P(A)=\frac{m}{n}
$

也可以写成：

$P(A)=\frac{\text{事件 }A\text{ 包含的等可能基本结果数}}{\text{样本空间中的等可能基本结果总数}}$

这个公式是怎样长出来的？

## 第一步：所有基本结果等可能

如果有 $n$ 个结果，而且它们地位相同，那么每个结果应得到相同的概率重量。

## 第二步：全部概率重量为1

所以每个基本结果的概率为：

$
\frac{1}{n}
$

## 第三步：事件 $A$ 包含 $m$ 个互斥基本结果

这些基本结果不能在一次试验中同时发生，因此概率可以相加：

$P(A)=\underbrace{\frac1n+\frac1n+\cdots+\frac1n}_{m\text{ 个}}$

一共有 $m$ 个相同的 $\frac1n$，所以：

$
P(A)=\frac{m}{n}
$

因此，这个公式不是凭空规定的，而是来自：

```text
基本结果等可能
+
总概率为1
+
互斥概率相加
```

---

# 10. 骰子例子：完整展示概念链条

随机试验：

> 掷一次公平的六面骰子，记录点数。

## 第一步：样本空间

$
\Omega=\{1,2,3,4,5,6\}
$

## 第二步：定义事件

设事件 $A$ 为“掷出偶数”：

$
A=\{2,4,6\}
$

## 第三步：确认等可能条件

“公平骰子”是关键假设，它表示六个基本结果在模型中等可能。

## 第四步：计算总结果数

样本空间包含6个等可能结果：

$
n=6
$

## 第五步：计算事件包含的结果数

事件 $A$ 包含3个结果：

$
m=3
$

## 第六步：计算概率

$
P(A)=\frac{3}{6}=\frac12
$

这个结果表示：

> 在公平骰子模型下，偶数事件获得了全部概率重量的一半。

它不是说每两次一定出现一次偶数，也不是说下一次骰子会有“半个偶数结果”。

---

# 11. 为什么“结果个数之比”不能总是使用？

考虑一个不均匀骰子。

样本空间仍然是：

$
\Omega=\{1,2,3,4,5,6\}
$

事件“掷出偶数”仍然是：

$
A=\{2,4,6\}
$

虽然事件包含3个结果，总共包含6个结果，但如果骰子的不同面不等可能，就不能直接写：

$
P(A)=\frac36
$

原因是：

> 结果个数只表示集合包含多少成员，不表示每个成员拥有多少概率重量。

假设2、4、6三个结果都特别容易出现，那么偶数事件的概率可能明显大于一半。

因此，经典的“有利结果数除以总结果数”必须满足：

1. 样本空间有限；
2. 基本结果等可能；
3. 结果划分完整且互斥。

“数结果”不是概率的普遍定义，只是等可能模型下的计算方法。

---

# 12. 一般加法公式为什么要减去重叠部分？

设：

$
A=\{2,4,6\}
$

表示“掷出偶数”。

$
B=\{4,5,6\}
$

表示“点数大于3”。

我们想计算“$A$ 或 $B$ 发生”的概率。

如果直接相加：

$
P(A)+P(B)
$

会发生什么？

结果4和6同时属于 $A$ 与 $B$，所以它们被计算了两次：

```text
计算 A：2、4、6
计算 B：      4、5、6
                 ↑   ↑
                重复计算
```

因此，需要把重复计算的交集减掉一次：

$
P(A\cup B)=P(A)+P(B)-P(A\cap B)
$

公式的生长过程是：

```text
先加上 A
+
再加上 B
-
减去被重复计算一次的共同部分
```

如果 $A$ 与 $B$ 互斥，那么：

$
A\cap B=\varnothing
$

交集概率为0，公式就变回：

$
P(A\cup B)=P(A)+P(B)
$

所以互斥事件加法公式，是一般加法公式的特殊情况。

---

# 13. 概率 vs 频率

这是必须主动区分的一对概念。

## 频率

假设重复试验 $n$ 次，事件 $A$ 发生了 $k$ 次。

事件 $A$ 的频率是：

$
f_n(A)=\frac{k}{n}
$

其中：

- $n$：实际完成的试验总次数；
- $k$：事件 $A$ 实际发生的次数；
- $f_n(A)$：这批实验中 $A$ 出现的比例。

频率来自已经观察到的数据。

---

## 概率

$
P(A)
$

描述模型中事件 $A$ 的可能性大小。

概率是模型属性，不会因为某一小批实验偶然多出现几次就立刻改变。

---

## 二者的关系

```text
概率：随机机制的理论倾向
频率：有限次实验中实际观察到的比例
```

例如公平硬币正面的理论概率是二分之一，但抛10次可能出现：

```text
7次正面
```

此时正面频率是：

$
\frac7{10}=0.7
$

这不表示理论概率自动变成0.7。

随着试验次数增加，在合适条件下，频率通常会逐渐靠近概率。这将在“大数定律”中严格解释。

---

# 14. 概率不是对单次结果的比例切割

假设明天下雨的概率是70%。

它不表示：

- 明天70%的时间一定下雨；
- 这个城市70%的地区一定下雨；
- 明天会发生0.7次下雨；
- 明天下雨已经发生了70%。

它表示：

> 在明确的事件定义、时间范围、地点范围和当前信息下，模型给“下雨”事件分配了0.7的概率。

一次实际结果仍然只有两类：

```text
下雨
或
不下雨
```

概率描述的是实验发生之前的不确定性，不是把一次结果切成若干比例。

---

# 15. 概率相同，不代表事件相同

掷一次公平骰子，定义：

$
A=\{1,2\}
$

$
B=\{5,6\}
$

这两个事件包含的结果完全不同，但都有两个等可能样本点，因此：

$
P(A)=P(B)=\frac26=\frac13
$

所以：

```text
两个事件概率相同
≠
两个事件是同一个事件
```

概率只是事件的一个数值特征。它无法保存事件包含哪些具体结果的全部信息。

这和未来要学习的“期望相同不代表分布相同”具有相似逻辑：

> 一个摘要数字不能代替完整结构。

---

# 16. 实际应用

## 机器学习

模型可能输出：

```text
猫：0.80
狗：0.15
其他：0.05
```

这些数字表达模型在当前输入和训练信息下，对不同类别的概率判断。

它们必须非负，并且所有互斥类别的总概率应为1。

---

## 计算机视觉

目标检测中可以研究：

- 图像中存在行人的概率；
- 在存在行人的条件下成功检测的概率；
- 出现误报或漏报的概率。

这些概念最终会连接到条件概率和统计评价指标。

---

## 工程可靠性

如果一个设备在一年内故障的概率为0.02，那么它不故障的概率为：

$
1-0.02=0.98
$

工程师可以结合故障损失决定是否增加冗余设备或提前维护。

---

## 强化学习

环境对同一个动作可能产生多个后续状态，每个状态具有不同概率。

智能体不仅要考虑“可能到哪里”，还要考虑：

> 各个结果有多可能，以及它们分别带来多少回报。

---

# 17. 本节概念全链条

以公平骰子为例：

```text
现实中的随机现象
骰子落地的点数不确定
        ↓
随机试验
掷一次公平骰子
        ↓
样本空间
Ω = {1,2,3,4,5,6}
        ↓
事件
A = {2,4,6}，表示掷出偶数
        ↓
概率
P(A) = 3/6 = 1/2
```

现在我们已经完成了从现实到概率数字的第一次完整转换。

---

# 理解检查

请先自己思考：

1. 一枚公平硬币连续三次都出现正面，能否据此断定第四次更应该出现反面？
2. 一个事件的概率为0.9，但最后没有发生，是否与概率模型矛盾？
3. 为什么“不均匀骰子的偶数概率”不能只用偶数结果个数除以总结果个数？
4. 已知 $P(A)=0.3$，那么 $P(A^c)$ 是多少？为什么？
5. 如果 $P(A)=0.5$、$P(B)=0.4$，能否直接断定 $P(A\cup B)=0.9$？还缺少什么信息？
6. 概率与频率分别描述什么？

# 这一节真正需要记住的核心

1. 概率 $P(A)$ 是分配给事件 $A$ 的可能性重量，取值在0和1之间。  
2. 全部可能结果的总概率为1；互斥事件合并时，概率可以直接相加。  
3. “事件包含的结果数除以总结果数”只适用于有限且基本结果等可能的模型。  
4. 概率是随机机制或信息状态下的理论判断，频率是有限次实验中实际观察到的比例。  
5. 高概率不等于必然，低概率不等于不可能；概率描述的是不确定性，而不是保证单次结果。

下一课将进入“条件概率”：普通概率基于当前信息衡量事件；一旦获得新信息，样本空间中的有效范围会缩小，事件的概率也可能随之改变。