# 第十八课：指数分布——下一次事件还要等多久？

上一课的泊松分布研究：

> **固定一段时间内，某类事件一共发生多少次？**

今天我们不再固定终点后“数次数”，而是站在起点向前看：

> **从现在开始，到下一次事件发生，需要等待多长时间？**

这就产生了今天的核心概念：

$$
\boxed{\text{指数分布 Exponential Distribution}}
$$

第十五课的几何分布已经研究过“为了等到第一次成功，需要进行多少次离散试验”；今天的指数分布则把这种“等待”从离散次数推广到连续时间。

同时，第十二课已经建立了连续型随机变量的基本规则：单点概率通常为零，区间概率通过概率密度曲线下面积计算。指数分布正是我们正式学习的一个重要连续型分布。

---

# 一、先看它在知识地图中的位置

目前常见分布可以按“我们到底在观察什么”整理成：

```text
一次试验是否成功
        ↓
Bernoulli 伯努利分布

固定做 n 次，成功多少次
        ↓
Binomial 二项分布

离散试验中，第一次成功要等几次
        ↓
Geometric 几何分布

离散试验中，第 r 次成功要等几次
        ↓
Negative Binomial 负二项分布

固定时间内，事件发生多少次
        ↓
Poisson 泊松分布

连续时间中，下一次事件要等多久
        ↓
Exponential 指数分布  ← 今天
```

其中最重要的一对是：

$$
\boxed{\text{泊松分布：数事件}}
$$

$$
\boxed{\text{指数分布：等事件}}
$$

它们通常描述的是**同一个随机到达机制的两个不同侧面**。

---

# 二、问题背景：泊松分布为什么还不够？

假设某服务器平均每小时收到 3 个异常请求。

上一课可以定义：

$$
N(1)=\text{一小时内收到的异常请求数}
$$

并使用泊松分布描述：

$$
N(1)\sim \operatorname{Poisson}(3)
$$

它可以回答：

* 一小时恰好收到 2 个异常请求的概率是多少？
* 一小时没有异常请求的概率是多少？
* 一小时至少收到一次异常请求的概率是多少？

但工程师还可能关心另一个问题：

> 从现在开始，到下一个异常请求到来，要等待多少分钟？

这个结果可能是：

```text
3.2 分钟
10.7 分钟
20.01 分钟
……
```

它不是：

$$
0,1,2,3,\ldots
$$

这样的离散次数，而是一个连续时间。

所以我们定义一个新的随机变量：

$$
T=\text{从现在到下一次事件发生的等待时间}
$$

那么：

$$
T\in[0,+\infty)
$$

我们需要一个连续型概率分布来描述 $T$。

这就是指数分布。

---

# 三、最核心的画面：不再“数点”，而是“量距离”

想象事件发生在一条时间轴上：

```text
现在
0
│────────────●────────●───────●────────→ 时间
             ↑
          第一次事件
```

如果第一个点出现在 12 分钟处，那么：

$$
T=12\text{ 分钟}
$$

这里：

* 泊松随机变量关心某段时间里有几个点；
* 指数随机变量关心从起点到第一个点有多远。

同一条时间轴，可以定义两种不同随机变量：

```text
0 ───────●────●──────────●────────→ 时间
         ↑

T：起点到第一个点的距离

N(t)：0 到 t 之间一共有几个点
```

这再次体现了随机变量的本质：

> 同一个随机现象，因为研究问题不同，可以定义不同的数值特征。

---

# 四、先校准参数 $\lambda$ 的含义

为了描述事件到来的快慢，我们引入：

$$
\lambda>0
$$

今天统一把 $\lambda$ 理解为：

> **单位时间内事件的平均发生强度，也叫发生率。**

例如：

$$
\lambda=3\text{ 次/小时}
$$

表示：

> 在长期平均意义下，每小时大约发生 3 次事件。

注意它绝不表示：

> 每小时一定恰好发生 3 次。

实际一小时中的事件数仍然可能是：

$$
0,1,2,3,4,\ldots
$$

---

## 观察时间为 $t$ 时，泊松参数是多少？

如果单位时间发生率是：

$$
\lambda
$$

观察时间长度为：

$$
t
$$

那么这段时间对应的泊松参数为：

$$
\lambda t
$$

所以定义：

$$
N(t)=\text{从现在到时间 }t\text{ 内的事件数}
$$

在理想泊松到达模型下：

$$
\boxed{
N(t)\sim\operatorname{Poisson}(\lambda t)
}
$$

例如：

$$
\lambda=3\text{ 次/小时}
$$

观察：

$$
t=2\text{ 小时}
$$

那么：

$$
\lambda t=3\times2=6
$$

所以：

$$
N(2)\sim\operatorname{Poisson}(6)
$$

---

## 单位必须一致

假设：

$$
\lambda=3\text{ 次/小时}
$$

而我们研究 20 分钟。

不能直接计算：

$$
3\times20
$$

因为一个使用“小时”，另一个使用“分钟”。

应先把 20 分钟换成小时：

$$
20\text{ 分钟}=\frac13\text{ 小时}
$$

于是：

$$
\lambda t
=
3\times\frac13
=
1
$$

这里：

$$
\lambda t
$$

没有单位。

这是合理的，因为它最终会出现在指数函数：

$$
e^{-\lambda t}
$$

的指数位置上。

---

# 五、指数分布公式从哪里来？

我们不直接背 PDF，而是先从一个最简单的问题开始：

> 等待时间超过 $t$ 的概率是多少？

数学上写作：

$$
P(T>t)
$$

现在观察这两个事件：

### 事件一

$$
T>t
$$

表示：

> 到时间 $t$ 为止，第一个事件仍然没有出现。

### 事件二

$$
N(t)=0
$$

表示：

> 从现在到时间 $t$ 内，事件发生了 0 次。

它们其实是同一个事件：

$$
\boxed{
\{T>t\}=\{N(t)=0\}
}
$$

用时间轴理解：

```text
0 ───────────────── t ───────●────→
  前 t 时间没有任何点

⇕

等待第一个点的时间超过 t
```

因此：

$$
P(T>t)=P(N(t)=0)
$$

---

# 六、利用泊松分布计算“一个事件都没有”

因为：

$$
N(t)\sim\operatorname{Poisson}(\lambda t)
$$

泊松分布的 PMF 为：

$$
P(N(t)=k)
=
e^{-\lambda t}
\frac{(\lambda t)^k}{k!}
$$

现在我们需要的是：

$$
k=0
$$

代入：

$$
P(N(t)=0)
=
e^{-\lambda t}
\frac{(\lambda t)^0}{0!}
$$

其中：

$$
(\lambda t)^0=1
$$

并且：

$$
0!=1
$$

所以：

$$
P(N(t)=0)=e^{-\lambda t}
$$

因此：

$$
\boxed{
P(T>t)=e^{-\lambda t},
\qquad t\geq0
}
$$

这叫指数分布的**生存函数**或**尾概率函数**。

可以记作：

$$
S_T(t)=P(T>t)=e^{-\lambda t}
$$

但名字不是重点。

重点是：

> 等待超过 $t$ 的概率，等于前 $t$ 时间内一次事件都没发生的概率。

---

# 七、从尾概率得到 CDF

我们已经学过分布函数：

$$
F_T(t)=P(T\leq t)
$$

它表示：

> 等待时间不超过 $t$，也就是事件在 $t$ 时间以内已经到达。

事件：

$$
\{T\leq t\}
$$

与：

$$
\{T>t\}
$$

互为补事件。

因此：

$$
P(T\leq t)
=
1-P(T>t)
$$

代入：

$$
P(T>t)=e^{-\lambda t}
$$

得到：

$$
\boxed{
F_T(t)=1-e^{-\lambda t},
\qquad t\geq0
}
$$

由于等待时间不可能为负数，所以完整写法是：

$$
\boxed{
F_T(t)=
\begin{cases}
0,&t<0\\[4pt]
1-e^{-\lambda t},&t\geq0
\end{cases}
}
$$

第十课已经建立了 CDF 的核心思想：在数轴上移动阈值 $t$，累计左侧全部概率。这里 $F_T(t)$ 累计的是“截至时间 $t$，事件已经到达”的概率。

---

# 八、从 CDF 得到 PDF

对于连续型随机变量，PDF 是 CDF 的导数。

直觉上：

> CDF 表示截至当前位置累计了多少概率；PDF 表示概率在当前位置附近累积得有多快。

我们已经得到：

$$
F_T(t)=1-e^{-\lambda t}
$$

对 $t$ 求导：

$$
f_T(t)
=
\frac{d}{dt}
\left(1-e^{-\lambda t}\right)
$$

常数 1 的导数为 0。

而：

$$
\frac{d}{dt}e^{-\lambda t}
=
-\lambda e^{-\lambda t}
$$

前面还有一个负号，因此：

$$
\frac{d}{dt}\left(-e^{-\lambda t}\right)
=
\lambda e^{-\lambda t}
$$

最终：

$$
\boxed{
f_T(t)=\lambda e^{-\lambda t},
\qquad t\geq0
}
$$

完整写法为：

$$
\boxed{
f_T(t)=
\begin{cases}
\lambda e^{-\lambda t},&t\geq0\\[4pt]
0,&t<0
\end{cases}
}
$$

如果随机变量 $T$ 具有这个 PDF，我们写作：

$$
\boxed{
T\sim\operatorname{Exponential}(\lambda)
}
$$

也常简写为：

$$
T\sim\operatorname{Exp}(\lambda)
$$

---

# 九、公式里的每一部分表示什么？

$$
f_T(t)=\lambda e^{-\lambda t}
$$

逐个解释。

## $T$

随机变量：

> 到下一次事件发生所需的等待时间。

---

## $t$

数轴上的一个普通时间值。

例如：

$$
t=10\text{ 分钟}
$$

注意：

$$
T\neq t
$$

* $T$：整个随机变量；
* $t$：我们代入函数的一个具体时间。

---

## $\lambda$

单位时间内事件的发生率。

例如：

$$
\lambda=3\text{ 次/小时}
$$

$\lambda$ 越大，事件越频繁，等待时间通常越短。

---

## $e^{-\lambda t}$

表示：

> 到时间 $t$ 为止，仍然没有任何事件发生的概率。

它会随着 $t$ 增大而减小。

---

## $f_T(t)$

不是：

$$
P(T=t)
$$

而是：

> 等待时间在 $t$ 附近的概率密度。

对于指数分布这样的连续型随机变量：

$$
\boxed{P(T=t)=0}
$$

真正的概率必须通过一个有宽度的区间计算。

---

# 十、PDF 为什么是合法的？

合法 PDF 需要满足两个条件。

## 条件一：不能为负

因为：

$$
\lambda>0
$$

并且：

$$
e^{-\lambda t}>0
$$

所以：

$$
f_T(t)=\lambda e^{-\lambda t}\geq0
$$

---

## 条件二：整个曲线下面积等于 1

计算：

$$
\int_0^{+\infty}\lambda e^{-\lambda t}\,dt
$$

因为：

$$
\int \lambda e^{-\lambda t}\,dt
=
-e^{-\lambda t}
$$

所以：

$$
\int_0^{+\infty}\lambda e^{-\lambda t}\,dt
=
\left[-e^{-\lambda t}\right]_0^{+\infty}
$$

当：

$$
t\to+\infty
$$

时：

$$
e^{-\lambda t}\to0
$$

而在：

$$
t=0
$$

时：

$$
e^{-\lambda\cdot0}=1
$$

因此：

$$
0-(-1)=1
$$

所以：

$$
\boxed{
\int_0^{+\infty}\lambda e^{-\lambda t}\,dt=1
}
$$

全部概率面积刚好为 1。

---

# 十一、PDF 和 CDF 的图像直觉

## PDF

$$
f_T(t)=\lambda e^{-\lambda t}
$$

在：

$$
t=0
$$

时：

$$
f_T(0)=\lambda
$$

之后不断下降，并逐渐接近 0：

```text
fT(t)
  ^
λ |\
  | \
  |  \
  |   \
  |     \____
0 +------------------------→ t
  0
```

它表示：

> 第一次事件比较容易较早出现；拖得越久才第一次出现，其无条件概率密度越低。

---

## CDF

$$
F_T(t)=1-e^{-\lambda t}
$$

从 0 开始，逐渐上升并接近 1：

```text
FT(t)
1 |                     ______
  |                 ___/
  |              __/
  |           __/
  |       ___/
0 +______/--------------------→ t
  0
```

它表示：

> 随着等待时间上限不断放宽，“事件已经出现”的累计概率越来越大。

---

## 生存函数

$$
S_T(t)=P(T>t)=e^{-\lambda t}
$$

从 1 开始，逐渐下降到 0：

```text
ST(t)
1 |\
  | \
  |  \
  |    \____
  |         \____
0 +------------------------→ t
  0
```

它表示：

> 到时间 $t$ 为止，仍然没等到事件的概率。

---

# 十二、最简单的完整例子

假设某客服中心平均每小时接到 3 个电话。

设：

$$
T=\text{从现在开始到下一通电话的等待时间}
$$

在理想的恒定速率泊松到达模型下：

$$
T\sim\operatorname{Exp}(3)
$$

这里时间单位是：

> 小时。

---

## 问题一：等待超过 20 分钟的概率是多少？

20 分钟等于：

$$
\frac13\text{ 小时}
$$

我们求：

$$
P\left(T>\frac13\right)
$$

利用尾概率：

$$
P(T>t)=e^{-\lambda t}
$$

代入：

$$
\lambda=3,\qquad t=\frac13
$$

得到：

$$
P\left(T>\frac13\right)
=
e^{-3\times\frac13}
$$

所以：

$$
P\left(T>\frac13\right)
=
e^{-1}
$$

近似为：

$$
\boxed{0.3679}
$$

也就是说：

> 等待超过 20 分钟仍未接到电话的概率约为 36.79%。

---

## 问题二：20 分钟以内接到电话的概率是多少？

我们求：

$$
P\left(T\leq\frac13\right)
$$

利用 CDF：

$$
F_T(t)=1-e^{-\lambda t}
$$

所以：

$$
P\left(T\leq\frac13\right)
=
1-e^{-1}
$$

近似为：

$$
\boxed{0.6321}
$$

也就是大约 63.21%。

注意两个事件互为补事件：

```text
20分钟内接到电话
+
等待超过20分钟
=
全部可能情况
```

所以：

$$
0.6321+0.3679=1
$$

---

# 十三、等待时间落在一个区间内怎么计算？

假设：

$$
0\leq a<b
$$

我们想求：

$$
P(a<T\leq b)
$$

通过 CDF：

$$
P(a<T\leq b)
=
F_T(b)-F_T(a)
$$

代入指数分布的 CDF：

$$
=
\left(1-e^{-\lambda b}\right)
-
\left(1-e^{-\lambda a}\right)
$$

展开括号：

$$
=
1-e^{-\lambda b}-1+e^{-\lambda a}
$$

所以：

$$
\boxed{
P(a<T\leq b)
=
e^{-\lambda a}-e^{-\lambda b}
}
$$

---

## 例子：等待时间在 10 到 30 分钟之间

仍然设：

$$
\lambda=3\text{ 次/小时}
$$

10 分钟等于：

$$
\frac16\text{ 小时}
$$

30 分钟等于：

$$
\frac12\text{ 小时}
$$

所以：

$$
P\left(\frac16<T\leq\frac12\right)
=
e^{-3\times\frac16}
-
e^{-3\times\frac12}
$$

即：

$$
=
e^{-1/2}-e^{-3/2}
$$

近似为：

$$
0.6065-0.2231
$$

因此：

$$
\boxed{
P(10<T\leq30\text{ 分钟})
\approx0.3834
}
$$

---

# 十四、指数分布最重要的性质：无记忆性

几何分布已经出现过无记忆性：

> 已经连续失败了多少次，不改变从现在开始还要等待多少次的概率结构。

指数分布也具有同样的性质，只不过等待量从离散次数变成了连续时间。

假设：

$$
T\sim\operatorname{Exp}(\lambda)
$$

已经知道：

$$
T>s
$$

也就是：

> 已经等了 $s$ 时间，事件仍然没有出现。

现在问：

> 还需要再等待超过 $t$ 时间的概率是多少？

这意味着总等待时间超过：

$$
s+t
$$

所以要计算：

$$
P(T>s+t\mid T>s)
$$

---

# 十五、无记忆性一步一步推导

根据条件概率：

$$
P(T>s+t\mid T>s)
=
\frac{
P(\{T>s+t\}\cap\{T>s\})
}{
P(T>s)
}
$$

如果：

$$
T>s+t
$$

那么一定有：

$$
T>s
$$

所以：

$$
\{T>s+t\}\subseteq\{T>s\}
$$

因此交集就是：

$$
\{T>s+t\}
$$

于是：

$$
P(T>s+t\mid T>s)
=
\frac{P(T>s+t)}{P(T>s)}
$$

利用尾概率：

$$
P(T>x)=e^{-\lambda x}
$$

得到：

$$
=
\frac{e^{-\lambda(s+t)}}{e^{-\lambda s}}
$$

指数相除时指数相减：

$$
=
e^{-\lambda(s+t)+\lambda s}
$$

括号展开：

$$
-\lambda(s+t)+\lambda s
=
-\lambda s-\lambda t+\lambda s
=
-\lambda t
$$

因此：

$$
P(T>s+t\mid T>s)
=
e^{-\lambda t}
$$

而：

$$
e^{-\lambda t}=P(T>t)
$$

所以：

$$
\boxed{
P(T>s+t\mid T>s)=P(T>t)
}
$$

这就是指数分布的无记忆性。

---

# 十六、无记忆性到底在说什么？

假设电话到达服从指数等待模型。

你已经等了 30 分钟，电话还没来。

现在问：

> 从此刻开始，还要再等超过 10 分钟的概率是多少？

指数分布告诉你：

> 它等于刚开始时等待超过 10 分钟的概率。

过去等了多久，不会改变剩余等待时间的分布。

可以画成：

```text
开始──────────────现在──────────未来
       已等30分钟       再等10分钟？

已经等过的30分钟
不会改变未来10分钟的概率结构
```

---

## 这不表示“等待时间不会变长”

这里很容易产生误解。

从一开始看：

$$
P(T>60\text{ 分钟})
<
P(T>10\text{ 分钟})
$$

当然成立。

因为要等待超过 60 分钟，比等待超过 10 分钟更罕见。

无记忆性讨论的不是这个。

它讨论的是：

> 已知已经成功等过一段时间且事件没来，未来剩余等待的概率结构是否改变？

所以：

```text
无条件看：
等得越久越罕见

条件在“已经等到现在仍没发生”之下：
未来重新开始，概率结构不变
```

两者并不矛盾。

---

# 十七、为什么 PDF 在下降，却又说发生率不变？

指数分布的 PDF：

$$
f_T(t)=\lambda e^{-\lambda t}
$$

随着 $t$ 增大而下降。

这是否表示：

> 等得越久，事件反而越不容易发生？

不是。

$f_T(t)$ 描述的是：

> 从最初起点看，第一个事件恰好落在时间 $t$ 附近的无条件概率密度。

时间越晚，仍然没有事件发生的样本路径已经越来越少，所以无条件密度会下降。

但是，条件在“已经等到 $t$ 仍然没有事件”之下，未来短时间内的发生概率保持不变。

严格地说：

$$
P(T\leq t+h\mid T>t)
$$

这里真正想表达的是：

> 已经等到 $t$ 还没发生，那么接下来 $h$ 时间内会不会发生？

更准确写成：

$$
P(t<T\leq t+h\mid T>t)
$$

它等于：

$$
1-P(T>t+h\mid T>t)
$$

利用无记忆性：

$$
=
1-P(T>h)
$$

所以：

$$
\boxed{
P(t<T\leq t+h\mid T>t)
=
1-e^{-\lambda h}
}
$$

右边完全不含 $t$。

这说明：

> 无论之前已经等了多久，未来长度为 $h$ 的时间窗口中，事件发生的条件概率都一样。

---

# 十八、指数分布是几何分布的连续版本

几何分布研究：

$$
G=\text{第一次成功所需的试验次数}
$$

其尾概率为：

$$
P(G>k)=(1-p)^k
$$

意思是：

> 前 $k$ 次全部失败。

指数分布研究：

$$
T=\text{第一次事件出现前的连续等待时间}
$$

其尾概率为：

$$
P(T>t)=e^{-\lambda t}
$$

可以对照：

|      | 几何分布             | 指数分布               |
| ---- | ---------------- | ------------------ |
| 时间形式 | 离散试验次数           | 连续时间               |
| 随机变量 | 第一次成功需要几次        | 下一次事件需要等多久         |
| 取值   | $1,2,3,\ldots$ | $t\geq0$         |
| 参数   | 成功概率 $p$       | 发生率 $\lambda$    |
| 尾概率  | $(1-p)^k$      | $e^{-\lambda t}$ |
| 无记忆性 | 有                | 有                  |

---

## 连续极限的直觉

把连续时间切成非常短的小片，每片长度为：

$$
\Delta t
$$

如果事件发生率为 $\lambda$，那么很短一片中发生事件的概率近似为：

$$
p\approx\lambda\Delta t
$$

不发生的概率近似为：

$$
1-\lambda\Delta t
$$

要等待超过时间 $t$，意味着前面大约：

$$
\frac{t}{\Delta t}
$$

个小片全部没有事件。

因此：

$$
P(T>t)
\approx
(1-\lambda\Delta t)^{t/\Delta t}
$$

当：

$$
\Delta t\to0
$$

也就是时间片切得越来越细时，这个表达式趋向：

$$
e^{-\lambda t}
$$

于是连续时间中的几何等待，最终形成指数分布。

所以可以理解为：

$$
\boxed{
\text{指数分布}
=
\text{几何分布的连续时间极限}
}
$$

---

# 十九、泊松分布与指数分布必须主动区分

假设事件以速率 $\lambda$ 随机到达。

## 泊松分布

定义：

$$
N(t)=\text{时间 }t\text{ 内的事件个数}
$$

那么：

$$
N(t)\sim\operatorname{Poisson}(\lambda t)
$$

它是离散随机变量：

$$
N(t)\in\{0,1,2,\ldots\}
$$

---

## 指数分布

定义：

$$
T=\text{到下一次事件的等待时间}
$$

那么：

$$
T\sim\operatorname{Exp}(\lambda)
$$

它是连续随机变量：

$$
T\in[0,+\infty)
$$

---

## 二者的核心桥梁

$$
\boxed{
T>t
\iff
N(t)=0
}
$$

等待超过 $t$：

> 等价于前 $t$ 时间里一个事件都没有。

另外：

$$
\boxed{
T\leq t
\iff
N(t)\geq1
}
$$

等待不超过 $t$：

> 等价于前 $t$ 时间里至少已经出现一次事件。

可以把它们理解成同一机制的两台相机：

```text
相机一：
拍下固定时间窗口
数里面有几个点
→ Poisson

相机二：
从起点向前看
量到第一个点的距离
→ Exponential
```

---

# 二十、$\lambda$ 增大时会发生什么？

思考一下：

> 如果单位时间内事件发生得更频繁，等待下一次事件通常应该变长还是变短？

应该变短。

因为：

```text
λ 大
↓
单位时间事件多
↓
事件来得频繁
↓
下一次事件通常来得更快
↓
等待时间 T 通常较小
```

反过来：

```text
λ 小
↓
事件稀少
↓
需要等待更久
↓
T 通常较大
```

以后学习数学期望时会严格推导：

$$
\boxed{
E[T]=\frac1\lambda
}
$$

目前先建立量纲直觉。

如果：

$$
\lambda=3\text{ 次/小时}
$$

那么平均等待尺度应当约为：

$$
\frac13\text{ 小时/次}
$$

也就是：

$$
20\text{ 分钟}
$$

注意：

$$
\lambda
$$

不是平均等待时间。

它是发生率。

平均等待时间与发生率互为倒数。

---

# 二十一、最容易犯的错误一：把密度当成概率

假设：

$$
T\sim\operatorname{Exp}(4)
$$

那么：

$$
f_T(0)=4
$$

你可能会觉得：

> 概率怎么能等于 4？

问题在于：

$$
f_T(0)
$$

不是概率。

它是概率密度。

真正的单点概率仍然是：

$$
P(T=0)=0
$$

概率必须通过面积计算：

$$
P(a<T\leq b)
=
\int_a^b f_T(t)\,dt
$$

这正是连续型随机变量中“PDF 可以大于 1，但概率不能大于 1”的典型例子。

另外，若时间单位是小时，则：

$$
f_T(t)
$$

的单位是：

$$
1/\text{小时}
$$

所以它本来就不是一个没有单位的概率数值。

---

# 二十二、最容易犯的错误二：等待了很久，就“该发生了”

假设已经很久没有请求到达。

一种常见直觉是：

> 都等这么久了，下一秒应该更容易到来。

在指数模型下，这种想法不成立。

过去长时间没有发生，不会让未来自动获得“补偿”。

这与抛硬币中的错误直觉相似：

> 已经连续出现很多次反面，下一次正面就更应该出现。

只要模型假设各阶段独立、速率不变，那么历史不会改变未来的发生率。

---

# 二十三、指数分布并不适合所有等待时间

指数分布有一个非常强的假设：

> 事件的条件发生率始终保持不变。

因此它没有“年龄效应”。

也就是说，一台已经运行 1000 小时的机器，在指数模型中，其未来短时间内的故障概率结构与一台刚启动的机器相同。

这在某些电子元件的随机故障阶段可能是合理近似，但对明显磨损老化的设备就可能不合理。

---

## 反例一：机器会老化

假设零件运行时间越长，故障率越高：

```text
刚投入使用
故障率较低

使用多年
故障率明显升高
```

这不满足无记忆性。

因此寿命未必适合简单指数分布。

---

## 反例二：请求存在高峰期

网站请求强度可能是：

```text
凌晨：很低
白天：较高
活动期间：突然暴增
```

此时发生率：

$$
\lambda
$$

并不是常数。

使用一个固定 $\lambda$ 的指数分布可能过于粗糙。

---

## 反例三：事件会成簇爆发

某次网络故障可能诱发大量重试请求：

```text
一个请求失败
↓
大量客户端同时重试
↓
后续事件集中出现
```

这时事件之间存在依赖，也不符合最简单的泊松—指数模型。

所以：

$$
\boxed{
\text{等待时间是正数}
\not\Rightarrow
\text{等待时间一定服从指数分布}
}
$$

必须检查随机机制。

---

# 二十四、把现实、样本和随机变量重新连起来

以服务器请求为例。

## 现实中的随机现象

异常请求在时间轴上随机到达。

---

## 一次完整结果

从严格角度看，一次结果可能是一整串到达时刻：

```text
2.1分钟，7.4分钟，8.0分钟，15.6分钟……
```

---

## 定义随机变量

我们只提取第一个到达时间：

$$
T=\text{第一个异常请求的到达等待时间}
$$

---

## 随机变量的取值

某次观测可能得到：

$$
T=2.1\text{ 分钟}
$$

另一次可能得到：

$$
T=15.6\text{ 分钟}
$$

---

## 概率分布

在恒定速率泊松到达模型下：

$$
T\sim\operatorname{Exp}(\lambda)
$$

并具有：

$$
P(T>t)=e^{-\lambda t}
$$

$$
F_T(t)=1-e^{-\lambda t}
$$

$$
f_T(t)=\lambda e^{-\lambda t}
$$

完整链条是：

```text
现实中的随机到达
        ↓
时间轴上的随机事件点
        ↓
随机变量 T 提取“到第一个点的距离”
        ↓
T 的取值是连续时间
        ↓
T 服从指数分布
```

---

# 二十五、实际应用

## 1. 网络系统与排队系统

可以描述：

* 两次请求之间的时间；
* 下一位客户的到达时间；
* 下一次网络包到达前的等待时间。

指数分布与泊松到达共同构成排队论的基础模型。

---

## 2. 可靠性工程

在“故障率近似恒定”的工作阶段，可以用指数分布描述：

* 元件寿命；
* 下一次随机故障的等待时间；
* 系统无故障运行时间。

但必须明确：

> 指数寿命模型不描述逐渐老化的机制。

---

## 3. 数据分析与生存分析

指数分布可以作为最简单的生存时间模型。

它的生存函数：

$$
S(t)=e^{-\lambda t}
$$

描述：

> 个体或设备“存活超过 $t$”的概率。

---

## 4. 机器学习

在事件时间建模中，指数分布可以作为连续时间概率模型，例如：

* 预测下一次事件的时间；
* 建模用户下一次行为等待时间；
* 构造生存时间似然函数；
* 作为点过程模型的基础组件。

它也帮助理解一个重要建模思想：

> 对“发生次数”建模和对“发生时间”建模，虽然观察对象不同，但可能来自同一个底层随机机制。

---

# 二十六、检查是否真正理解

## 问题一

某系统平均每小时出现 6 次事件。

定义：

$$
T=\text{到下一次事件的等待时间}
$$

那么指数分布参数是：

$$
\lambda=6\text{ 次/小时}
$$

等待超过 10 分钟的概率是多少？

10 分钟等于：

$$
\frac16\text{ 小时}
$$

所以：

$$
P\left(T>\frac16\right)
=
e^{-6\times\frac16}
=
e^{-1}
$$

即：

$$
\boxed{P(T>10\text{ 分钟})\approx0.3679}
$$

---

## 问题二

已经等待了 30 分钟，事件仍未出现。

从现在开始，再等待超过 10 分钟的概率是多少？

由于无记忆性：

$$
P(T>40\mid T>30)
=
P(T>10)
$$

所以仍然是：

$$
\boxed{e^{-1}\approx0.3679}
$$

这里所有时间都使用分钟表达也可以，只要把发生率换成：

$$
6\text{ 次/小时}
=
0.1\text{ 次/分钟}
$$

于是：

$$
e^{-0.1\times10}=e^{-1}
$$

---

## 问题三

参数从：

$$
\lambda=2
$$

增大到：

$$
\lambda=8
$$

等待时间通常会怎样变化？

答案：

> 事件发生得更频繁，因此等待时间通常变短。

不是因为公式规定如此，而是因为 $\lambda$ 本身代表发生率。

---

## 问题四

如果：

$$
f_T(0)=5
$$

能否说“等待时间等于 0 的概率是 5”？

不能。

因为：

$$
f_T(0)
$$

是密度，不是单点概率。

对于连续型指数分布：

$$
P(T=0)=0
$$

---

# 这一节真正需要记住的核心

1. **指数分布描述连续时间中，到下一次随机事件发生所需的等待时间。**

2. 在恒定速率为 $\lambda$ 的泊松到达模型中：

$$
\boxed{
T\sim\operatorname{Exp}(\lambda)
}
$$

并且：

$$
\boxed{
P(T>t)=e^{-\lambda t}
}
$$

3. 由补事件和求导分别得到：

$$
\boxed{
F_T(t)=1-e^{-\lambda t}
}
$$

$$
\boxed{
f_T(t)=\lambda e^{-\lambda t}
}
$$

4. 泊松分布数固定时间内的事件个数，指数分布量到下一次事件的等待时间；二者通过：

$$
\boxed{
T>t\iff N(t)=0
}
$$

连接起来。

5. 指数分布具有无记忆性：

$$
\boxed{
P(T>s+t\mid T>s)=P(T>t)
}
$$

但这依赖“发生率恒定”的模型假设，并不适用于所有现实等待时间。

下一步会非常自然：

> 指数分布研究等到**第一次事件**需要多久；如果要一直等到**第 $r$ 次事件**，总等待时间服从什么分布？

这正如几何分布推广到负二项分布一样，指数分布将自然推广到 **Gamma 分布；当 $r$ 为正整数时，也称 Erlang 分布**。第十六课已经建立了“负二项变量是多个几何等待时间之和”的离散结构；下一课将建立它在连续时间中的对应结构。
