# 第十一课：离散型随机变量 vs 连续型随机变量——“概率到底落在点上，还是铺在区间里？”

按照我们前面的主线，第九课进入“概率分布”，第十课学习了 CDF（分布函数）。现在自然来到一个非常关键的分岔点：

```text
随机现象
   ↓
样本空间
   ↓
事件
   ↓
概率
   ↓
条件概率
   ↓
独立性
   ↓
随机变量
   ↓
概率分布
   ↓
CDF
   ↓
┌────────────────────┐
│ 离散型 / 连续型随机变量 │ ← 今天
└────────────────────┘
   ↓
PMF / PDF
   ↓
常见概率分布
   ↓
期望、方差
```

这也正是我们最开始知识地图中安排的下一层：在理解随机变量、概率分布和分布函数之后，再区分离散型与连续型随机变量。

今天先解决一个核心问题：

> **为什么有些随机变量可以直接问“X 恰好等于某个数的概率是多少”，而另一些随机变量这么问几乎没有意义？**

---

# 一、问题背景：两个看起来都“取数字”的随机变量

先看两个随机变量。

### 情形 A：掷骰子

掷一次骰子，定义：

$$
X=\text{骰子的点数}
$$

那么：

$$
X\in\{1,2,3,4,5,6\}
$$

我们完全可以问：

> $X=4$ 的概率是多少？

公平骰子下：

$$
P(X=4)=\frac16
$$

这里，“4”这个**单独的点**拥有真实的概率重量。

---

再看另一个问题。

### 情形 B：随机测量一个人的身高

定义：

$$
Y=\text{这个人的身高}
$$

理论模型中，身高可能是：

```text
170 cm
170.1 cm
170.01 cm
170.001 cm
170.0001 cm
……
```

现在问：

> 身高**恰好等于**170.000000000000000…… cm 的概率是多少？

这时就出现了一个奇怪的问题。

现实中当然有人测出来“170 cm”。

但那通常意味着：

```text
169.5 ≤ 真正身高 < 170.5
```

经过四舍五入以后显示成了 170。

如果我们无限精确地问：

$$
P(Y=170)
$$

对于典型的连续随机变量，答案反而是：

$$
\boxed{P(Y=170)=0}
$$

但是：

> $Y=170$ 并不是“不可能事件”。

这就是今天必须解决的核心矛盾。

---

# 二、先建立最重要的直觉：概率是“颗粒”还是“铺开的质量”

把概率想象成总质量为 1 的东西。

这是我们之前建立过的概率直觉：概率相当于分配给事件的“可能性重量”。

现在这个概率重量有两种完全不同的分配方式。

---

## 1. 离散型：概率像一颗颗珠子

例如骰子：

```text
概率
 ↑
 │   ●   ●   ●   ●   ●   ●
 │
 └──────────────────────────→ X
     1   2   3   4   5   6
```

每一个允许的数值：

```text
1
2
3
4
5
6
```

都可以单独拿到一块概率。

例如：

$$
P(X=1)=\frac16
$$

$$
P(X=2)=\frac16
$$

……

概率是“一颗一颗”放在数轴上的。

这就是**离散型随机变量**最核心的画面。

---

## 2. 连续型：概率像把水铺在整条区间上

想象往一根很长的水槽里倒了一桶水。

```text
概率质量
        ╭────────╮
      ╭─╯        ╰─╮
──────╯              ╰──────
────────────────────────────→ x
```

水不是集中成若干颗珠子，而是铺在整个区间上。

现在你用一根“没有宽度的针”去问：

> 针尖对应的这个**单独位置**占了多少水？

答案是：

```text
没有宽度
→ 没有面积
→ 概率为 0
```

但如果你问：

> 170 到 171 之间有多少概率？

这时你取的是一个有宽度的区间：

```text
170 ├──────────────┤ 171
           ↑
        有一段宽度
```

于是可以拥有正的概率。

所以连续型随机变量真正有意义的问题通常是：

$$
P(170<Y<171)
$$

而不是单独盯着：

$$
P(Y=170)
$$

---

# 三、今天最需要区分的三个层次

这里非常容易混乱。

## 层次 1：随机变量

$$
X
$$

它是一条映射规则。

我们之前已经沿着这条主线走过：

```text
现实中的随机结果
      ↓
样本点 ω
      ↓
随机变量 X
      ↓
数值 X(ω)
```

---

## 层次 2：随机变量的可能取值

例如骰子随机变量：

$$
X\in\{1,2,3,4,5,6\}
$$

这里的：

$$
4
$$

只是一个数值。

---

## 层次 3：某个取值对应的概率

$$
P(X=4)
$$

这是一个概率。

因此一定要区分：

```text
X       ：随机变量
4       ：随机变量可能取到的一个数
P(X=4)  ：X 恰好取 4 的概率
```

离散型和连续型的差别，主要就在第三层：

> **单个数值能不能承载正的概率。**

---

# 四、什么叫“离散”？

先不急着给严格定义。

“离散”的直觉就是：

> 一个值和另一个值之间存在“空隙”，或者至少这些值可以一个一个编号出来。

例如：

$$
\{1,2,3,4,5,6\}
$$

明显是一颗一颗的。

再例如：

$$
\{0,1,2,3,\ldots\}
$$

虽然有无限多个，但仍然可以：

```text
第1个：0
第2个：1
第3个：2
……
```

所以它依然是离散的。

---

## 一个重要误区

很多人会认为：

> 离散型 = 取值个数有限。

这是错的。

例如：

> 不断抛硬币，直到第一次出现正面。

定义：

$$
X=\text{第一次出现正面时的抛掷次数}
$$

那么：

$$
X\in\{1,2,3,4,\ldots\}
$$

它可以取无限多个值。

但这些值仍然可以一个一个列出来，因此它仍然是离散型随机变量。

---

# 五、数学定义：离散型随机变量

现在再给严格定义。

如果随机变量 $X$ 的所有可能取值能够排列成：

$$
x_1,x_2,x_3,\ldots
$$

即它的取值至多是**可数个**，那么我们称 $X$ 为离散型随机变量。

这里的“可数”直觉上就是：

> 可以一个一个编号。

例如：

$$
X\in\{0,1,2,3,\ldots\}
$$

即使无限多个，也可以编号。

---

# 六、离散型随机变量为什么可以直接给每个点分配概率？

假设：

$$
X\in\{x_1,x_2,x_3,\ldots\}
$$

我们可以分别定义：

$$
P(X=x_1)
$$

$$
P(X=x_2)
$$

$$
P(X=x_3)
$$

……

因为这些事件：

$$
\{X=x_1\},\{X=x_2\},\ldots
$$

彼此互斥。

一次实验中，$X$ 不可能既等于 $x_1$，又等于 $x_2$。

所以：

```text
一个取值
→ 一块概率
→ 所有概率块加起来 = 1
```

即：

$$
\sum_i P(X=x_i)=1
$$

先不要把这个 $\sum$ 当成新公式背。

它只是表达：

> 把所有可能取值对应的概率全部加起来，就覆盖了所有可能情况，因此总和必须为 1。

这和概率公理中：

$$
P(\Omega)=1
$$

是完全同一个思想。

---

# 七、离散型随机变量的 PMF

现在我们终于能正式引出一个新名字：

> **概率质量函数，Probability Mass Function，简称 PMF。**

定义：

$$
p_X(x)=P(X=x)
$$

一个符号一个符号看。

### $X$

我们研究的随机变量。

### $x$

一个普通数值，是我们拿来询问的候选值。

### $p_X$

表示“随机变量 $X$ 的概率质量函数”。

### $p_X(x)$

表示：

> $X$ 恰好取值 $x$ 时，这个点拥有多少概率质量。

所以：

$$
\boxed{p_X(x)=P(X=x)}
$$

不是一个神秘公式。

它只是给：

```text
“X 恰好等于 x 的概率”
```

起了一个函数名字。

---

# 八、最简单例子：公平骰子

定义：

$$
X=\text{骰子的点数}
$$

那么：

$$
X\in\{1,2,3,4,5,6\}
$$

公平意味着：

$$
P(X=1)=\cdots=P(X=6)=\frac16
$$

于是 PMF 可以写成：

$$
p_X(x)=
\begin{cases}
\frac16,&x=1,2,3,4,5,6\\
0,&\text{其他}
\end{cases}
$$

不要被这个分段函数吓到。

它只是说：

```text
如果问 1、2、3、4、5、6：
概率 = 1/6

如果问其他数字：
概率 = 0
```

例如：

$$
p_X(4)=\frac16
$$

而：

$$
p_X(7)=0
$$

因为骰子不可能掷出 7 点。

---

# 九、PMF 的图像为什么是一根根柱子？

公平骰子的 PMF 可以想象成：

```text
pX(x)

1/6 │   ●   ●   ●   ●   ●   ●
    │   │   │   │   │   │   │
    │   │   │   │   │   │   │
  0 └───┼───┼───┼───┼───┼───┼────→ x
        1   2   3   4   5   6
```

注意：

> 真正有意义的是这些“点”对应的概率。

图上连不连线并不重要。

甚至严格来说，不应该把它想成一条连续曲线，因为：

$$
X=2.3
$$

根本不是骰子的合法取值。

---

# 十、PMF 怎么计算一个事件的概率？

假设骰子变量 $X$，我们想算：

$$
P(X\leq3)
$$

事件：

$$
X\leq3
$$

意味着：

$$
X=1,\quad X=2,\quad X=3
$$

三个事件互斥，所以：

$$
P(X\leq3)
=
P(X=1)+P(X=2)+P(X=3)
$$

代入：

$$
=\frac16+\frac16+\frac16
$$

所以：

$$
P(X\leq3)=\frac12
$$

这揭示了离散型随机变量的计算方式：

> **区间概率 = 区间内所有点的概率质量相加。**

可以想象成捡珠子：

```text
1     2     3     4     5     6
●     ●     ●     ●     ●     ●
└───────────┘
  把前三颗概率珠子的重量加起来
```

---

# 十一、那么什么叫“连续型”？

现在进入今天真正容易产生认知冲突的部分。

假设随机变量：

$$
Y=\text{某个零件的长度}
$$

在数学模型中：

$$
Y\in[0,10]
$$

这意味着 0 到 10 之间所有实数都有可能作为结果。

例如：

$$
2
$$

$$
2.1
$$

$$
2.01
$$

$$
2.001
$$

甚至两个不同的数之间永远还能继续插入更多数字。

例如：

$$
2<2.5<3
$$

2 和 2.5 之间还有：

$$
2.25
$$

然后还有：

$$
2.125
$$

如此无限继续。

它已经不是“一颗颗可以编号的点”了。

---

# 十二、为什么连续情况下不能给每个点分配正概率？

这是今天最关键的推理。

假设连续区间里的每个点都拥有相同的一个正概率：

$$
c>0
$$

但区间里有无限多得不可数的点。

那么所有点的概率加起来会爆掉，不可能保持：

$$
P(\Omega)=1
$$

所以在典型连续模型里，单独一个点必须满足：

$$
\boxed{P(Y=y)=0}
$$

对每一个具体 $y$ 都成立。

---

这里非常容易产生误解：

> “概率为 0，岂不是说明这个值永远不会出现？”

不是。

这正是连续模型和有限离散模型非常不一样的地方。

---

# 十三、为什么“概率为 0”仍然可能发生？

用数轴理解。

假设随机变量均匀地落在：

$$
[0,1]
$$

之间。

最终它一定要落在某一个具体数上，例如：

$$
0.372819\ldots
$$

但是在实验发生之前：

```text
[0────────────────────────────1]
```

有无穷无尽的候选点。

没有任何一个单独的点能够占据一段“宽度”。

因此每个单点：

$$
P(Y=y)=0
$$

但实验最终仍然会得到某个具体值。

这里的：

```text
概率 0
```

不能理解成：

```text
逻辑上绝对不可能
```

我们第五课其实已经提前埋下了这个伏笔：在连续模型中，概率为 0 的事件未必是不可能事件。

现在终于解释它为什么会发生。

---

# 十四、连续随机变量的概率到底在哪里？

答案：

> **不在单独的点上，而在区间上。**

例如：

$$
P(0.2<Y<0.4)
$$

可以大于 0。

直觉上：

```text
0────────0.2████████0.4────────1
              ↑
         这一段有宽度
```

因此可以承载概率。

这就产生一个新对象：

> 概率密度函数 PDF。

---

# 十五、先用“人口密度”理解 PDF

暂时不要把 PDF 当成概率。

假设两个城市：

### 城市 A

面积：

$$
1\text{ km}^2
$$

人口密度：

$$
1000\text{ 人/km}^2
$$

总人口：

$$
1000\times1=1000
$$

### 城市 B

面积：

$$
10\text{ km}^2
$$

人口密度同样：

$$
1000\text{ 人/km}^2
$$

总人口：

$$
1000\times10=10000
$$

所以：

> 密度不是总量。

而是：

> 单位长度、单位面积或单位空间中有多少“量”。

概率密度也是一样。

---

# 十六、PDF 是“概率在某个位置附近有多拥挤”

连续随机变量 $X$ 的概率密度记作：

$$
f_X(x)
$$

它不是：

$$
P(X=x)
$$

这一点必须非常明确。

对于连续随机变量：

$$
\boxed{P(X=x)=0}
$$

但：

$$
f_X(x)
$$

可能：

* 等于 0.2；
* 等于 1；
* 等于 3；
* 甚至大于 1。

所以：

$$
\boxed{f_X(x)\neq P(X=x)}
$$

一般不能把 PDF 的高度直接理解为概率。

---

# 十七、PDF 为什么可以大于 1？

这是概率论中极经典的反直觉点。

假设：

$$
X
$$

均匀分布在区间：

$$
[0,0.5]
$$

整个区间宽度只有：

$$
0.5
$$

但总概率必须是：

$$
1
$$

如果密度处处相同，设高度为 $h$。

那么：

```text
概率 = 宽度 × 高度
```

即：

$$
0.5\times h=1
$$

所以：

$$
h=2
$$

也就是说：

$$
f_X(x)=2
$$

完全合法。

因为 2 是**密度**，不是概率。

真正的概率是面积：

$$
\text{宽度}\times\text{密度}
$$

例如：

$$
P(0<X<0.1)
$$

对应面积：

$$
0.1\times2=0.2
$$

概率还是在：

$$
[0,1]
$$

之间。

---

# 十八、连续型随机变量的核心计算：概率 = 面积

如果 $X$ 的概率密度是：

$$
f_X(x)
$$

那么：

$$
P(a<X\leq b)
=
\int_a^b f_X(x)\,dx
$$

如果你现在对积分还没有建立完整直觉，不需要慌。

今天只理解它的几何意思：

```text
fX(x)
 ↑
 │            ╭───────╮
 │          ╭─╯       ╰─╮
 │        ╭─╯           ╰─╮
 │───────╯████████████████╰────→ x
          a              b

          阴影面积
              =
        P(a < X ≤ b)
```

积分在这里的作用就是：

> 把从 $a$ 到 $b$ 之间无数极小条的概率质量全部累积起来。

---

# 十九、为什么离散是“求和”，连续是“积分”？

现在把两种情况并排看。

### 离散型

概率是一颗颗的：

```text
●   ●   ●   ●
```

所以要求某个范围内的总概率：

> 把这些点的概率加起来。

因此：

$$
P(X\in A)
=
\sum_{x\in A}p_X(x)
$$

---

### 连续型

概率铺成了一片：

```text
████████████████
```

不能逐个点相加。

我们只能把无数极窄的小条累积起来：

$$
P(X\in A)
=
\int_A f_X(x)\,dx
$$

因此：

```text
离散：求和 Σ
连续：积分 ∫
```

这两件事的底层思想其实完全一致：

> **都是累积概率质量。**

只是概率质量的存放方式不同。

---

# 二十、现在把第十课 CDF 接回来

上一课学习的 CDF：

$$
F_X(x)=P(X\leq x)
$$

它其实有一个巨大的优势：

> **CDF 对离散型和连续型都适用。**

这是为什么我们先学 CDF，再来区分离散和连续。

---

## 离散型的 CDF

假设：

$$
X\in\{1,2,3\}
$$

并且：

$$
P(X=1)=0.2
$$

$$
P(X=2)=0.5
$$

$$
P(X=3)=0.3
$$

那么：

$$
F_X(1)=P(X\leq1)=0.2
$$

而：

$$
F_X(2)=P(X\leq2)
$$

所以：

$$
F_X(2)=0.2+0.5=0.7
$$

再到：

$$
F_X(3)=1
$$

图像像楼梯：

```text
F(x)
1.0 │                  ┌────────
    │                  │
0.7 │         ┌────────┘
    │         │
0.2 │ ┌───────┘
    │ │
0.0 └─┴────────┴────────┴──────→ x
      1        2        3
```

为什么有跳跃？

因为：

> 某一个点突然加入了一整块正概率。

---

# 二十一、连续型的 CDF

连续型随机变量中：

$$
F_X(x)=P(X\leq x)
$$

仍然成立。

但因为一个单点没有概率质量：

$$
P(X=x)=0
$$

所以 CDF 不会因为经过一个单独的 $x$ 而突然跳一大块。

典型情况下图像是连续变化的：

```text
F(x)
1 │                  ______
  │               __/
  │            __/
  │         __/
  │      __/
0 │_____/
  └──────────────────────→ x
```

因此一个非常重要的直觉是：

```text
离散型：
概率堆在点上
→ CDF 一阶一阶跳

连续型：
概率铺在区间上
→ CDF 连续地长
```

---

# 二十二、CDF、PMF、PDF 三者到底什么关系？

这是以后必须牢牢区分的三个东西。

| 对象        | PMF      | PDF       | CDF           |
| --------- | -------- | --------- | ------------- |
| 全称        | 概率质量函数   | 概率密度函数    | 累积分布函数        |
| 常用于       | 离散型      | 连续型       | 两者都可以         |
| 表示什么      | 单个点的概率质量 | 某处附近概率有多密 | 左边累计了多少概率     |
| 单点是否直接是概率 | 是        | 不是        | 不是单点概率，而是累计概率 |
| 典型计算      | 求和       | 积分        | $P(X\le x)$ |

最关键的是：

$$
\boxed{p_X(x)=P(X=x)}
$$

但：

$$
\boxed{f_X(x)\neq P(X=x)}
$$

而：

$$
\boxed{F_X(x)=P(X\leq x)}
$$

---

# 二十三、用“钱”再建立一次直觉

假设总共有 100 元。

## 离散型

钱被分成几个信封：

```text
x=1：10元
x=2：30元
x=3：60元
```

问：

> $X=2$ 有多少钱？

直接回答：

```text
30元
```

这就是 PMF 的感觉。

---

## 连续型

现在把 100 元想成均匀撒在一条长带上的“金粉”。

一个没有宽度的位置：

```text
|
```

拿不到有限的一撮金粉。

但一段区间：

```text
|────────|
```

可以获得一部分。

PDF 描述的是：

> 这个位置附近金粉撒得有多密。

而区间概率：

> 是这段区间内总共收集到多少金粉。

---

# 二十四、一个非常重要的反例：取值是整数，不一定自动说明本质上是离散？

从随机变量的数学模型看，如果它只可能取：

$$
0,1,2,\ldots
$$

那么它当然是离散型。

但现实量的**记录方式**可能让你误判。

例如真实身高是连续量：

$$
172.384\ldots
$$

但数据库只记录整数厘米：

```text
172
```

那么有两个不同的随机变量：

### 真正身高

$$
X=\text{真实身高}
$$

可以建模为连续型。

### 数据库记录身高

$$
Y=\text{四舍五入后的整数身高}
$$

则：

$$
Y\in\{\ldots,170,171,172,173,\ldots\}
$$

是离散型。

所以：

> 离散还是连续，讨论的是**我们定义的随机变量**，不是某个现实对象“天生”只能属于一类。

---

# 二十五、另一个重要边界：现实测量永远有限精度，为什么还用连续模型？

温度计可能只显示：

```text
23.7℃
```

身高尺可能只精确到：

```text
1 mm
```

计算机中的浮点数也是有限的。

那为什么还要使用连续型随机变量？

因为连续模型是一种非常有用的数学近似。

如果：

* 潜在值可以在一个范围内细微变化；
* 测量精度远小于我们关心的尺度；
* 用连续模型计算更自然；

那么把它建模为连续变量通常非常合理。

例如：

* 身高；
* 时间；
* 温度；
* 电压；
* 传感器噪声；
* 模型参数误差。

所以：

```text
现实记录有限精度
≠
必须采用离散概率模型
```

---

# 二十六、离散 vs 连续：从“系统描述什么”理解

这是今天最重要的对照。

| 问题    | 离散型        | 连续型      |
| ----- | ---------- | -------- |
| 典型含义  | 数“有几个”     | 测“有多少”   |
| 典型例子  | 人数、次数、类别编号 | 时间、长度、温度 |
| 取值    | 可逐个列出      | 连续范围     |
| 单点概率  | 可以 $>0$  | 通常为 0    |
| 概率载体  | 点          | 区间       |
| 核心函数  | PMF        | PDF      |
| 求范围概率 | 求和         | 求面积/积分   |
| CDF   | 阶梯状常见      | 连续曲线常见   |

可以记一个非常有用的经验：

> **计数问题往往离散，测量问题往往连续。**

但这是经验，不是定义。

---

# 二十七、一个完整例子：抛三次硬币

把我们之前所有层次重新连起来。

## 现实随机试验

连续抛三次硬币。

---

## 样本空间

例如：

$$
\Omega=
\{
HHH,HHT,HTH,HTT,THH,THT,TTH,TTT
\}
$$

这里为了简写：

* $H$：正面；
* $T$：反面。

样本空间负责列出所有可能基本结果，这正是我们第三课建立的作用。

---

## 定义随机变量

令：

$$
X=\text{三次抛掷中正面出现次数}
$$

于是：

```text
TTT → 0
HTT → 1
THT → 1
TTH → 1
HHT → 2
HTH → 2
THH → 2
HHH → 3
```

随机变量把原来的样本点映射成数字。

---

## 可能取值

$$
X\in\{0,1,2,3\}
$$

所以 $X$ 是离散型随机变量。

---

## PMF

如果硬币公平且相互独立，那么：

$$
P(X=0)=\frac18
$$

$$
P(X=1)=\frac38
$$

$$
P(X=2)=\frac38
$$

$$
P(X=3)=\frac18
$$

所以概率质量可以画成：

```text
概率

3/8 │       ●       ●
    │       │       │
2/8 │       │       │
    │       │       │
1/8 │ ●     │       │     ●
    │ │     │       │     │
  0 └─┼─────┼───────┼─────┼──→ X
      0     1       2     3
```

这里再次看到：

> 每一个点可以拥有正概率。

---

# 二十八、机器学习里这两类变量在哪里出现？

现在概率论本身已经讲清楚，再连接到机器学习。

---

## 1. 分类标签：离散型

例如猫狗分类：

$$
Y\in\{\text{cat},\text{dog}\}
$$

经过数字编码：

$$
Y\in\{0,1\}
$$

这是离散随机变量。

模型可以输出：

$$
P(Y=0\mid X=x)
$$

和：

$$
P(Y=1\mid X=x)
$$

这些都是具体类别的概率质量。

---

## 2. 目标检测中的类别：离散型

例如：

```text
人
车
自行车
狗
```

类别本身是离散的。

---

## 3. 边界框坐标：连续型

例如：

$$
x,y,w,h
$$

坐标和宽高通常按连续值建模。

因此回归任务常会引入连续概率分布。

---

## 4. 神经网络噪声：连续型

例如经常假设：

$$
\epsilon\sim N(0,\sigma^2)
$$

这里的高斯噪声是典型连续随机变量。

稍后学习正态分布时会正式展开。

---

## 5. 强化学习动作

动作空间可能两种都有。

### 离散动作

```text
向左
向右
跳跃
停止
```

### 连续动作

例如机器人电机扭矩：

$$
a\in[-1,1]
$$

这就是连续动作空间。

---

# 二十九、你以后看到随机变量，先问这三个问题

这是非常实用的分析习惯。

### 第一问：它在描述什么？

是在：

```text
计数？
类别？
还是测量值？
```

---

### 第二问：可能取值能否一个个列出来？

如果可以：

```text
大概率考虑离散型
```

如果是完整连续范围：

```text
考虑连续型
```

---

### 第三问：单独一个点是否应该拥有概率质量？

如果：

$$
P(X=x)
$$

可以直接是正数，通常是离散型。

如果：

$$
P(X=x)=0
$$

而概率必须通过区间计算，通常是连续型。

---

# 三十、今天最需要防止的 5 个错误

### 错误 1

> 连续型随机变量 $X$ 取到 $x$ 的概率就是 $f_X(x)$。

错。

正确关系是：

$$
P(X=x)=0
$$

而 $f_X(x)$ 是密度。

---

### 错误 2

> PDF 必须在 0 到 1 之间。

错。

PDF 可以大于 1。

受限制的是**面积对应的概率**：

$$
0\leq P(A)\leq1
$$

---

### 错误 3

> 离散随机变量必须只有有限个值。

错。

例如：

$$
X\in\{1,2,3,\ldots\}
$$

仍然可以是离散型。

---

### 错误 4

> 连续随机变量中，概率为 0 就是不可能。

错。

单点：

$$
P(X=x)=0
$$

但 $x$ 仍然可能是最终实现的取值。

---

### 错误 5

> PMF 和 PDF 只是同一个东西在两种情况下的不同名称。

不完全对。

它们解决的目标相似，但数学含义不同：

$$
p_X(x)=P(X=x)
$$

而：

$$
f_X(x)
$$

只是密度，概率需要通过面积得到。

---

# 三十一、把今天的知识装回整个概率论框架

我们最开始的主线是：

```text
现实中的随机现象
      ↓
样本空间
      ↓
事件
      ↓
概率
      ↓
随机变量
      ↓
概率分布
```

现在继续细化：

```text
随机变量 X
     ↓
它可能取哪些数？
     ↓
 ┌───────────────┬────────────────┐
 │               │                │
可数个值        连续范围
 │               │
离散型           连续型
 │               │
PMF              PDF
 │               │
点的概率          区间面积
 │               │
 └──────→ CDF ←──┘
        P(X≤x)
```

这里 CDF 是一座非常重要的桥：

> **不论离散还是连续，CDF 都是在回答“左边累计了多少概率”。**

---

# 理解检查

这次我建议你先不要算，先凭直觉回答。

设：

$$
X=\text{一天进入某商店的顾客人数}
$$

以及：

$$
Y=\text{一位顾客在店内停留的时间}
$$

思考下面几个问题：

1. $X$ 更适合建模成离散型还是连续型？为什么？
2. $Y$ 更适合建模成离散型还是连续型？为什么？
3. 对 $X$ 来说，$P(X=10)$ 能不能是 $0.2$？
4. 对典型连续模型的 $Y$ 来说，$P(Y=10)=0$，是否表示顾客绝对不可能恰好停留 10 分钟？
5. 如果 $f_Y(10)=2$，能不能说“顾客恰好停留10分钟的概率是2”？
6. 离散型为什么用“求和”，连续型为什么用“面积/积分”？
7. CDF 为什么可以同时描述离散型和连续型随机变量？

---

# 这一节真正需要记住的核心

只压缩成 5 句话：

1. **离散型随机变量的概率质量集中在一个个可数的取值点上，因此单点 $P(X=x)$ 可以大于0。**
2. **连续型随机变量的概率铺在区间上，典型情况下每个单点都有 $P(X=x)=0$，真正有概率的是区间。**
3. **离散型用 PMF：$p_X(x)=P(X=x)$；连续型用 PDF，$f_X(x)$ 是密度，不是单点概率。**
4. **离散概率通过求和累积，连续概率通过面积/积分累积；两者底层都是在累积概率质量。**
5. **CDF $F_X(x)=P(X\le x)$ 同时适用于离散型和连续型，因此它是统一两种随机变量的重要工具。**

下一步最自然的不是马上背很多分布，而是把今天最容易混淆的地方彻底攻克：

> **PDF 到底是什么？为什么 PDF 可以大于 1，而概率绝不能大于 1？以及为什么“一个点的概率为 0”却仍然可能取到这个点？**

这会是理解连续型随机变量真正的门槛。
