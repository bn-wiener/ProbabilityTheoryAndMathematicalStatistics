# 第二十一课：数学期望——如果随机结果会不停变化，我们怎样定义它“长期平均会落在哪里”？

第二十课我们学习了正态分布，也反复碰到了一个词：

$$
\mu
$$

我们把它叫作：

> 分布的中心位置。

但到目前为止，这个“中心”更多还是直觉。

现在我们必须正式回答一个更底层的问题：

> **一个随机变量会取很多不同的值，而且每个值出现的概率不同，那么怎样用一个数字描述它长期而言“平均处在什么位置”？**

这就是今天的核心：

$$
\boxed{\text{数学期望 Expectation}}
$$

今天最重要的目标不是记住：

$$
E[X]=\sum x_ip_i
$$

而是彻底理解：

> **为什么一定要“取值 × 概率”再相加？**

---

# 一、先看它在整个概率论主线中的位置

我们已经走过：

```text
随机现象
   ↓
样本空间
   ↓
事件
   ↓
概率
   ↓
条件概率
   ↓
独立性
   ↓
随机变量
   ↓
概率分布
   ↓
CDF
   ↓
离散 / 连续
   ↓
常见概率分布
   ├─ Bernoulli
   ├─ Binomial
   ├─ Geometric
   ├─ Negative Binomial
   ├─ Poisson
   ├─ Exponential
   ├─ Gamma
   └─ Normal
   ↓
数学期望 ← 今天
   ↓
方差
   ↓
协方差与相关系数
```

到目前为止，我们主要是在研究：

> **整个分布长什么样。**

从今天开始，我们第一次尝试：

> **用少量数字概括一个完整分布。**

---

# 二、为什么已经知道“概率分布”，还需要期望？

假设有一个随机变量：

$$
X=
\text{一天的收入}
$$

可能取：

$$
0,\ 100,\ 1000
$$

概率分别是：

$$
0.5,\ 0.4,\ 0.1
$$

完整分布是：

| 收入 $x$ |  概率 |
| -------: | --: |
|        0 | 0.5 |
|      100 | 0.4 |
|     1000 | 0.1 |

这张表当然已经完整描述了随机变量。

但如果老板问：

> 这个业务一天“大概能赚多少”？

你总不能每次都回答：

> “50%的概率0元，40%的概率100元，10%的概率1000元……”

有时候我们希望把整个分布压缩成一个数字，用来描述：

> **它长期重复以后平均会落在哪里。**

这就是期望的作用。

---

# 三、最重要的直觉：期望不是“下一次最可能出现的值”

这一点必须一开始就切开。

假设：

$$
X=
\begin{cases}
0,&P=0.5\\
100,&P=0.4\\
1000,&P=0.1
\end{cases}
$$

最可能出现的是：

$$
0
$$

因为概率最大：

$$
0.5
$$

但期望并不会因此就是0。

所以：

$$
\boxed{
\text{期望}\neq\text{最可能值}
}
$$

期望研究的是：

> **如果这个随机过程重复很多很多次，所有结果加起来再取平均，大约会靠近哪里。**

---

# 四、从最普通的“平均数”开始

先不要碰概率。

假设你有5个数：

$$
2,\ 2,\ 2,\ 8,\ 8
$$

普通平均数：

$$
\frac{2+2+2+8+8}{5}
$$

整理一下：

$$
=
\frac{3\times2+2\times8}{5}
$$

再拆开：

$$
=
2\frac35+8\frac25
$$

注意这里出现了什么：

```text
数值2
×
它出现的频率3/5

+

数值8
×
它出现的频率2/5
```

所以平均数其实可以写成：

$$
\boxed{
\text{平均}
=
\sum
\text{数值}\times\text{出现频率}
}
$$

这就是期望公式的前身。

---

# 五、从频率走向概率

现在假设随机变量：

$$
X=
\begin{cases}
2,&P(X=2)=0.6\\
8,&P(X=8)=0.4
\end{cases}
$$

如果我们重复实验很多很多次，比如：

$$
10000
$$

次。

那么根据长期频率直觉：

* 大约60%会得到2；
* 大约40%会得到8。

因此大约会有：

$$
6000
$$

次得到2，

以及：

$$
4000
$$

次得到8。

总和大约：

$$
6000\times2+4000\times8
$$

平均：

$$
\frac{
6000\times2+4000\times8
}{
10000
}
$$

拆开：

$$
=
2\frac{6000}{10000}
+
8\frac{4000}{10000}
$$

当重复次数非常大时：

$$
\frac{6000}{10000}
\approx0.6
$$

$$
\frac{4000}{10000}
\approx0.4
$$

于是长期平均就接近：

$$
2\times0.6+8\times0.4
$$

所以：

$$
\boxed{
E[X]
=
2\times0.6+8\times0.4
}
$$

这就是为什么期望必须是：

$$
\boxed{
\text{取值}\times\text{概率}
}
$$

概率本质上承担的是：

> **长期出现频率的权重。**

---

# 六、数学定义是怎样自然长出来的？

假设离散随机变量 $X$ 可能取：

$$
x_1,x_2,x_3,\ldots
$$

对应概率：

$$
p_1,p_2,p_3,\ldots
$$

其中：

$$
p_i=P(X=x_i)
$$

长期重复以后：

$$
x_i
$$

大约会占全部实验的：

$$
p_i
$$

比例。

所以它对最终平均值的贡献大约就是：

$$
x_ip_i
$$

把所有取值的贡献加起来：

$$
\boxed{
E[X]
=
\sum_i x_iP(X=x_i)
}
$$

也可以写成：

$$
\boxed{
E[X]
=
\sum_i x_ip_i
}
$$

---

# 七、公式里每一个符号是什么意思？

$$
E[X]
=
\sum_i x_iP(X=x_i)
$$

我们逐个解释。

## $X$

随机变量。

例如：

$$
X=\text{掷骰子的点数}
$$

---

## $E$

Expectation 的首字母。

$$
E[X]
$$

表示：

> 随机变量 $X$ 的数学期望。

---

## $x_i$

$X$ 的第 $i$ 个可能取值。

例如骰子：

$$
x_i\in\{1,2,3,4,5,6\}
$$

---

## $P(X=x_i)$

随机变量恰好取得：

$$
x_i
$$

的概率。

---

## $\sum_i$

表示：

> 把所有可能取值对应的贡献全部加起来。

---

所以整个公式的普通话是：

> **把每一个可能取值乘以它出现的概率，再把所有结果加起来。**

---

# 八、为什么叫“加权平均”？

普通平均：

$$
\frac{x_1+x_2+\cdots+x_n}{n}
$$

本质上每一个数字权重一样：

$$
\frac1n
$$

例如：

$$
\frac{2+4+6}{3}
=
2\times\frac13
+
4\times\frac13
+
6\times\frac13
$$

所以普通平均其实也是一种加权平均，只不过：

> 所有权重都相同。

而期望中，不同取值的重要程度由：

$$
P(X=x_i)
$$

决定。

例如：

```text
值           概率
0            0.9
100          0.1
```

显然不能简单算：

$$
\frac{0+100}{2}=50
$$

因为0出现得远比100频繁。

正确应该是：

$$
0\times0.9
+
100\times0.1
=
10
$$

所以：

$$
\boxed{
\text{期望就是以概率作为权重的平均值}
}
$$

---

# 九、最经典例子：公平骰子的期望

掷一次公平六面骰子。

定义：

$$
X=\text{骰子点数}
$$

则：

$$
X\in\{1,2,3,4,5,6\}
$$

而每个值：

$$
P(X=k)=\frac16
$$

所以：

$$
E[X]
=
1\times\frac16
+
2\times\frac16
+
3\times\frac16
+
4\times\frac16
+
5\times\frac16
+
6\times\frac16
$$

提取：

$$
\frac16
$$

得到：

$$
E[X]
=
\frac{1+2+3+4+5+6}{6}
$$

因为：

$$
1+2+3+4+5+6=21
$$

所以：

$$
\boxed{
E[X]=3.5
}
$$

---

# 十、但骰子根本掷不出3.5，为什么期望可以等于3.5？

这是期望学习中第一个非常重要的认知关口。

骰子可能取：

$$
1,2,3,4,5,6
$$

确实不可能：

$$
X=3.5
$$

但期望并不是：

> 下一次实验会取得的结果。

它描述的是：

> **大量重复以后，平均值靠近哪里。**

例如掷：

$$
10000
$$

次骰子。

把所有点数相加，再除以10000。

这个平均数完全可能接近：

$$
3.5
$$

所以：

$$
\boxed{
E[X]\text{ 不要求是 }X\text{ 的可能取值}
}
$$

这是一定要内化的。

---

# 十一、用“重心”理解期望

这是我最推荐的几何直觉。

假设：

$$
X
$$

有三个可能值：

$$
0,\ 1,\ 2
$$

概率：

$$
0.2,\ 0.5,\ 0.3
$$

把概率想成重量：

```text
数轴

0                 1                 2
|-----------------|-----------------|
重量0.2           重量0.5          重量0.3
```

现在把这些概率重量放在对应位置。

期望就是：

> **整套概率质量在数轴上的重心位置。**

公式：

$$
E[X]
=
0\times0.2
+
1\times0.5
+
2\times0.3
$$

得到：

$$
E[X]=1.1
$$

虽然：

$$
1.1
$$

可能不是一个实际可能取值，

但它完全可以作为：

> 整体概率质量的平衡点。

---

# 十二、为什么“位置 × 重量”能找到重心？

这和物理里的力矩非常像。

假设数轴上：

```text
0                     10
●---------------------●
重量9                 重量1
```

虽然两个位置是：

$$
0,\ 10
$$

但左边重得多。

所以重心一定不会在中点5，而应该靠近0。

计算：

$$
\frac{
0\times9+10\times1
}{
9+1
}
=
1
$$

概率的总重量本来就是：

$$
1
$$

因此分母不需要再写：

$$
E[X]
=
\sum x_ip_i
$$

这就是期望为什么可以理解成概率重心。

---

# 十三、概率总和等于1为什么很重要？

普通加权平均一般形式是：

$$
\frac{
w_1x_1+\cdots+w_nx_n
}{
w_1+\cdots+w_n
}
$$

但概率满足：

$$
p_1+p_2+\cdots=1
$$

所以分母已经是：

$$
1
$$

于是：

$$
\frac{
p_1x_1+\cdots+p_nx_n
}{
1
}
$$

直接就是：

$$
\boxed{
E[X]=\sum_i x_ip_i
}
$$

所以期望公式其实就是一个标准的加权平均。

---

# 十四、伯努利分布的期望为什么是 $p$？

现在把前面的分布重新接回来。

假设：

$$
X\sim\operatorname{Bernoulli}(p)
$$

那么：

$$
P(X=1)=p
$$

$$
P(X=0)=1-p
$$

根据期望定义：

$$
E[X]
=
0\cdot(1-p)
+
1\cdot p
$$

第一项：

$$
0
$$

第二项：

$$
p
$$

所以：

$$
\boxed{
E[X]=p
}
$$

这非常漂亮。

因为 Bernoulli 随机变量：

```text
发生 → 1
不发生 → 0
```

所以它的长期平均就是：

> 事件发生的长期比例。

也就是：

$$
p
$$

---

# 十五、这其实把“概率”和“期望”连接起来了

对任意事件 $A$，定义示性随机变量：

$$
I_A=
\begin{cases}
1,&A\text{发生}\\
0,&A\text{不发生}
\end{cases}
$$

那么：

$$
P(I_A=1)=P(A)
$$

因此：

$$
E[I_A]
=
0\cdot P(A^c)+1\cdot P(A)
$$

所以：

$$
\boxed{
E[I_A]=P(A)
}
$$

这条关系以后非常非常重要。

它表示：

$$
\boxed{
\text{一个事件的概率}
=
\text{它的0/1指示变量的期望}
}
$$

---

# 十六、二项分布的期望先用直觉看

假设：

$$
X\sim\operatorname{Binomial}(n,p)
$$

其中：

$$
X=\text{成功总次数}
$$

如果：

$$
n=100
$$

每次成功概率：

$$
p=0.2
$$

那么长期来看：

> 100次中平均大约20次成功。

所以我们自然预期：

$$
E[X]=20
$$

一般就是：

$$
\boxed{
E[X]=np
}
$$

但现在我要强调：

> 不要先背这个公式。

下一部分我们会从随机变量求和把它推出来。

---

# 十七、期望最重要的性质：线性性

假设有两个随机变量：

$$
X,\ Y
$$

定义：

$$
Z=X+Y
$$

那么：

$$
\boxed{
E[X+Y]=E[X]+E[Y]
}
$$

这叫：

> **期望的线性性。**

更一般：

$$
\boxed{
E[aX+bY]
=
aE[X]+bE[Y]
}
$$

这条性质极其重要。

---

# 十八、为什么会有 $E[X+Y]=E[X]+E[Y]$？

先从一次具体实验理解。

如果一次实验中：

$$
X=3
$$

$$
Y=7
$$

那么：

$$
X+Y=10
$$

如果重复很多次：

```text
第1次：
X₁ + Y₁

第2次：
X₂ + Y₂

第3次：
X₃ + Y₃
...
```

那么总平均：

$$
\frac{
(X_1+Y_1)+\cdots+(X_n+Y_n)
}{n}
$$

把括号重新整理：

$$
=
\frac{X_1+\cdots+X_n}{n}
+
\frac{Y_1+\cdots+Y_n}{n}
$$

当实验很多时：

第一项接近：

$$
E[X]
$$

第二项接近：

$$
E[Y]
$$

因此：

$$
E[X+Y]
=
E[X]+E[Y]
$$

这就是线性性的长期平均直觉。

---

# 十九、非常重要：线性性不要求独立！

这一点以后很容易混淆。

不管：

$$
X,Y
$$

独不独立，都有：

$$
\boxed{
E[X+Y]=E[X]+E[Y]
}
$$

独立性在这里不是必要条件。

这是期望非常强大的地方。

后面学习方差时情况就不同了：

$$
Var(X+Y)
$$

通常会涉及：

$$
Cov(X,Y)
$$

那时候独立性才会变得非常重要。

---

# 二十、现在推导二项分布的期望

第十四课已经知道：

$$
X\sim\operatorname{Binomial}(n,p)
$$

可以写成：

$$
X=X_1+\cdots+X_n
$$

其中每一个：

$$
X_i\sim\operatorname{Bernoulli}(p)
$$

而：

$$
E[X_i]=p
$$

利用线性性：

$$
E[X]
=
E[X_1+\cdots+X_n]
$$

所以：

$$
=
E[X_1]+\cdots+E[X_n]
$$

每一项都是：

$$
p
$$

一共有：

$$
n
$$

项。

因此：

$$
\boxed{
E[X]=np
}
$$

所以二项分布的期望不是一条凭空背诵的公式。

它来自：

$$
\boxed{
\text{Binomial}
=
\text{Bernoulli 之和}
}
$$

再加：

$$
\boxed{
E[\text{和}]
=
\text{期望之和}
}
$$

---

# 二十一、这也是“为什么100个人点击率10%，平均约10人点击”

假设：

$$
p=0.1
$$

观察：

$$
n=100
$$

个用户。

每个用户定义：

$$
X_i=
\begin{cases}
1,&\text{点击}\\
0,&\text{不点击}
\end{cases}
$$

总点击人数：

$$
X=X_1+\cdots+X_{100}
$$

于是：

$$
E[X]
=
100\times0.1
=
10
$$

所以：

> 平均意义上，100人里有10人点击。

但注意：

这不表示每次观察100个人都恰好10人点击。

一次可能：

$$
7
$$

也可能：

$$
13
$$

甚至：

$$
3
$$

期望只描述长期平均中心。

---

# 二十二、常数的期望是什么？

假设一个随机变量其实恒等于：

$$
5
$$

即：

$$
X=5
$$

概率：

$$
1
$$

那么：

$$
E[X]
=
5\times1
=
5
$$

所以：

$$
\boxed{
E[c]=c
}
$$

常数没有随机性，长期平均当然就是它自己。

---

# 二十三、为什么 $E[aX+b]=aE[X]+b$？

假设：

$$
Y=aX+b
$$

意思是把每一次结果：

$$
X
$$

都变成：

$$
aX+b
$$

长期平均：

```text
先把 X 的所有结果乘 a
→ 平均也乘 a

然后每次都加 b
→ 平均也加 b
```

所以：

$$
E[aX+b]
=
aE[X]+b
$$

例如：

$$
Y=2X+10
$$

如果：

$$
E[X]=3
$$

那么：

$$
E[Y]
=
2\times3+10
=
16
$$

---

# 二十四、连续随机变量怎么办？

前面离散型：

$$
E[X]
=
\sum_x xP(X=x)
$$

是：

> 每一个点 × 这个点的概率质量。

但连续随机变量有一个问题：

$$
P(X=x)=0
$$

所以不能写：

$$
xP(X=x)
$$

然后一个点一个点求和。

怎么办？

回到第十二课的思想：

> 连续世界里的概率不是放在点上，而是通过 PDF 的面积累积。

所以对于连续型随机变量：

$$
\boxed{
E[X]
=
\int_{-\infty}^{+\infty}
x f_X(x)\,dx
}
$$

---

# 二十五、为什么连续期望是这个积分？

把数轴切成很多很窄的小区间：

$$
[x,x+\Delta x]
$$

这一小段概率大约：

$$
f_X(x)\Delta x
$$

那么这一小片对平均位置的贡献大约就是：

$$
x
\cdot
f_X(x)\Delta x
$$

把所有小片加起来：

$$
\sum
x f_X(x)\Delta x
$$

当：

$$
\Delta x\to0
$$

就得到积分：

$$
\boxed{
E[X]
=
\int x f_X(x)\,dx
}
$$

所以连续期望依然是：

$$
\boxed{
\text{位置}\times\text{概率重量}
}
$$

只是：

```text
离散：
一个点一个点加
Σ

连续：
无数极小概率片累积
∫
```

底层思想完全一样。

---

# 二十六、最简单的连续例子：$Uniform[0,2]$

假设：

$$
X\sim U(0,2)
$$

PDF：

$$
f_X(x)=\frac12
$$

当：

$$
0\le x\le2
$$

我们从图像上就能看出来：

```text
密度
 ^
 |       ┌───────────────┐
 |       │               │
 |_______│_______________│______> x
         0               2
                 ↑
                 1
```

概率均匀地铺在0到2之间。

显然重心应该在中间：

$$
1
$$

所以：

$$
E[X]=1
$$

积分也验证：

$$
E[X]
=
\int_0^2x\frac12dx
$$

$$
=
\frac12
\left[
\frac{x^2}{2}
\right]_0^2
$$

$$
=
\frac12\times2
$$

所以：

$$
\boxed{E[X]=1}
$$

---

# 二十七、正态分布的 $\mu$ 为什么就是期望？

上一课：

$$
X\sim\mathcal N(\mu,\sigma^2)
$$

钟形曲线关于：

$$
\mu
$$

左右完全对称。

把概率看作重量。

左边和右边以完全对称的方式分布。

所以它的重心必然正好落在：

$$
\mu
$$

于是：

$$
\boxed{
E[X]=\mu
}
$$

因此第二十课中的：

$$
\mu
$$

现在终于有了正式身份：

> **它就是正态随机变量的数学期望。**

这也把第二十课和今天正式接起来了。

---

# 二十八、指数分布的期望为什么和 $\lambda$ 互为倒数？

第十八课学习：

$$
T\sim\operatorname{Exp}(\lambda)
$$

其中：

$$
\lambda
$$

表示：

> 每单位时间平均发生多快。

我们当时就建立了一个直觉：

如果：

$$
\lambda=2\text{ 次/小时}
$$

那么典型等待尺度应该约：

$$
\frac12\text{ 小时}
$$

正式结果是：

$$
\boxed{
E[T]=\frac1\lambda
}
$$

例如：

$$
\lambda=3\text{ 次/小时}
$$

则：

$$
E[T]=\frac13\text{ 小时}=20\text{ 分钟}
$$

这与前面的量纲直觉完全一致。

积分推导以后可以单独展开，现在先抓住：

```text
发生率高
↓
等得短

发生率低
↓
等得长
```

---

# 二十九、Gamma / Erlang 的期望也可以直接从求和得到

上一课：

$$
T_r
=
W_1+\cdots+W_r
$$

每一个：

$$
W_i\sim\operatorname{Exp}(\lambda)
$$

所以：

$$
E[W_i]=\frac1\lambda
$$

于是：

$$
E[T_r]
=
E[W_1+\cdots+W_r]
$$

利用线性性：

$$
=
E[W_1]+\cdots+E[W_r]
$$

所以：

$$
=
\frac1\lambda+\cdots+\frac1\lambda
$$

一共：

$$
r
$$

项。

因此：

$$
\boxed{
E[T_r]=\frac r\lambda
}
$$

这就是上一课我们只建立了直觉、没有正式证明的结果。

现在终于可以解释：

```text
等1次
平均约 1/λ

等r次
平均约 r/λ
```

---

# 三十、泊松分布为什么 $E[X]=\lambda$？

泊松分布：

$$
X\sim\operatorname{Poisson}(\lambda)
$$

其中：

$$
\lambda
$$

本来就是：

> 一个固定区间中的平均事件强度尺度。

所以直觉上：

$$
E[X]=\lambda
$$

是非常自然的。

正式推导可以由 PMF 得到。

$$
E[X]
=
\sum_{k=0}^\infty
k
e^{-\lambda}
\frac{\lambda^k}{k!}
$$

当：

$$
k=0
$$

那项是0，所以从1开始：

$$
=
\sum_{k=1}^\infty
k
e^{-\lambda}
\frac{\lambda^k}{k!}
$$

因为：

$$
k!
=
k(k-1)!
$$

所以：

$$
\frac{k}{k!}
=
\frac1{(k-1)!}
$$

于是：

$$
E[X]
=
e^{-\lambda}
\sum_{k=1}^\infty
\frac{\lambda^k}{(k-1)!}
$$

提一个：

$$
\lambda
$$

出来：

$$
=
\lambda e^{-\lambda}
\sum_{k=1}^\infty
\frac{\lambda^{k-1}}{(k-1)!}
$$

令：

$$
j=k-1
$$

则：

$$
j=0,1,2,\ldots
$$

所以：

$$
E[X]
=
\lambda e^{-\lambda}
\sum_{j=0}^\infty
\frac{\lambda^j}{j!}
$$

而指数函数展开：

$$
e^\lambda
=
\sum_{j=0}^\infty
\frac{\lambda^j}{j!}
$$

因此：

$$
E[X]
=
\lambda e^{-\lambda}e^\lambda
$$

所以：

$$
\boxed{
E[X]=\lambda
}
$$

---

# 三十一、期望是不是一定存在？

不是。

这是一个非常重要的边界。

公式：

$$
E[X]
=
\sum x_ip_i
$$

或者：

$$
E[X]
=
\int xf_X(x)dx
$$

只有在这个加权结果有合理意义时，期望才存在。

某些分布的尾部非常重，可能导致：

$$
E[X]
$$

发散。

也就是说：

> 随机变量有合法概率分布，不代表一定有有限期望。

现在只建立这个边界。

以后遇到重尾分布时再深入。

---

# 三十二、期望相同，分布可以完全不同

这是本课最重要的反例之一。

### 随机变量 A

$$
P(A=0)=0.5
$$

$$
P(A=10)=0.5
$$

于是：

$$
E[A]=5
$$

---

### 随机变量 B

$$
B=5
$$

概率1。

于是：

$$
E[B]=5
$$

所以：

$$
E[A]=E[B]=5
$$

但：

```text
A：
一半0，一半10
波动非常大

B：
永远都是5
完全不波动
```

因此：

$$
\boxed{
\text{期望相同}
\not\Rightarrow
\text{分布相同}
}
$$

甚至：

$$
\boxed{
\text{期望相同}
\not\Rightarrow
\text{随机行为相似}
}
$$

这正是为什么下一课还需要：

$$
\boxed{\text{方差}}
$$

---

# 三十三、一个赌博例子：期望不是保证收益

有一个游戏。

投入：

$$
10\text{ 元}
$$

结果：

```text
90%：损失10元
10%：获得100元
```

如果定义净收益：

$$
X=
\begin{cases}
-10,&0.9\\
100,&0.1
\end{cases}
$$

那么：

$$
E[X]
=
-10\times0.9
+
100\times0.1
$$

$$
=
-9+10
$$

所以：

$$
\boxed{
E[X]=1
}
$$

意味着：

> 长期大量重复，这个随机收益平均每局是正1元。

但一次实际游戏绝大多数时候：

$$
X=-10
$$

因为概率：

$$
0.9
$$

所以再次强调：

$$
\boxed{
\text{正期望}
\neq
\text{下一次一定赚钱}
}
$$

---

# 三十四、期望和“典型结果”甚至可能差得很远

考虑：

$$
X=
\begin{cases}
0,&0.99\\
1000,&0.01
\end{cases}
$$

那么：

$$
E[X]
=
0\times0.99
+
1000\times0.01
=
10
$$

所以：

$$
E[X]=10
$$

但99%的时候：

$$
X=0
$$

而：

$$
X=10
$$

甚至从来不会发生。

所以期望是：

> **概率重心 / 长期平均**

而不一定是：

> “最典型的一次观测”。

---

# 三十五、期望 vs 平均值

这是必须主动区分的一组概念。

## 数学期望

$$
E[X]
$$

是：

> 概率分布本身的理论属性。

---

## 样本平均

如果实际观测：

$$
X_1,X_2,\ldots,X_n
$$

那么样本平均：

$$
\bar X
=
\frac1n
\sum_{i=1}^nX_i
$$

是：

> 这批实际数据算出来的平均值。

二者关系是：

```text
理论世界：
E[X]

实际数据：
X₁,X₂,...,Xₙ
      ↓
样本平均 X̄
```

以后大数定律会告诉我们：

> 在合适条件下，样本数越来越大时，样本平均会靠近期望。

所以：

$$
\boxed{
\text{期望}
\neq
\text{某一次样本平均}
}
$$

但二者存在深刻联系。

---

# 三十六、机器学习里为什么“期望”到处都是？

机器学习中很多目标本质上都是：

> 在数据分布下平均表现如何。

例如损失函数：

$$
L(\theta;X,Y)
$$

模型真正关心的理想目标往往是：

$$
\boxed{
E_{(X,Y)}[L(\theta;X,Y)]
}
$$

意思是：

> 如果数据不断从真实分布产生，模型的平均损失是多少？

训练集中常用：

$$
\frac1n
\sum_{i=1}^n
L(\theta;x_i,y_i)
$$

近似这个期望。

于是你可以第一次看到：

```text
理论目标：
真实数据分布上的期望损失

        ↓ 用有限样本近似

训练目标：
样本平均损失
```

这就是概率论直接进入机器学习的一个核心入口。

---

# 三十七、强化学习里的期望回报

强化学习常讨论：

$$
E[\text{return}]
$$

因为环境具有随机性。

执行同一个策略：

> 每一次轨迹获得的奖励可能不同。

所以不能只看一条轨迹。

真正想优化的是：

$$
\boxed{
\text{长期平均意义上的回报}
}
$$

也就是期望回报。

---

# 三十八、为什么损失函数训练经常取平均？

例如一个 batch：

$$
L_1,L_2,\ldots,L_n
$$

训练时经常写：

$$
\frac1n\sum_iL_i
$$

为什么？

因为这些样本可以被看成来自某个数据分布。

样本平均损失实际上是在估计：

$$
E[L]
$$

所以：

> 深度学习里每天都在算的 batch mean，本质上与概率论中的数学期望直接相连。

---

# 三十九、把目前几个重要分布的期望放在一起

现在不是为了背表，而是看结构。

| 分布                       | 随机变量含义          |            期望 |
| ------------------------ | --------------- | ------------: |
| $Bernoulli(p)$         | 一次成功/失败         |         $p$ |
| $Binomial(n,p)$        | $n$ 次成功总数     |        $np$ |
| $Poisson(\lambda)$     | 固定区间事件数         |   $\lambda$ |
| $Exponential(\lambda)$ | 下一次事件等待时间       | $1/\lambda$ |
| $Erlang(r,\lambda)$    | 第 $r$ 次事件等待时间 | $r/\lambda$ |
| $Normal(\mu,\sigma^2)$ | 中心波动型连续量        |       $\mu$ |

注意这些结果其实大量符合我们的直觉。

---

# 四十、这一课真正新增的思想是什么？

今天不是多背了一个公式。

而是第一次学会：

> **把完整概率分布压缩成一个“中心位置”数字。**

整个逻辑是：

```text
随机变量
   ↓
有很多可能取值
   ↓
每个值有不同概率
   ↓
长期重复时
不同值以不同频率出现
   ↓
用概率作为权重
   ↓
求加权平均
   ↓
数学期望
```

因此：

$$
\boxed{
E[X]
=
\text{概率分布在数轴上的重心}
}
$$

---

# 理解检查

先不要急着往后看答案。

## 1

一个公平骰子的期望：

$$
3.5
$$

为什么不能理解为：

> 下一次最可能掷出3.5？

---

## 2

如果：

$$
X=
\begin{cases}
0,&0.99\\
1000,&0.01
\end{cases}
$$

为什么：

$$
E[X]=10
$$

但10却可能一次都不会出现？

---

## 3

为什么期望要写成：

$$
x_iP(X=x_i)
$$

而不是简单把所有可能值取算术平均？

核心应该回答：

> 因为……

---

## 4

如果：

$$
X\sim\operatorname{Bernoulli}(p)
$$

为什么：

$$
E[X]=p
$$

？

不要背，直接利用：

```text
0出现概率1-p
1出现概率p
```

解释。

---

## 5

如果：

$$
X=X_1+\cdots+X_n
$$

为什么：

$$
E[X]
=
E[X_1]+\cdots+E[X_n]
$$

可以理解成“各部分长期平均相加”？

---

## 6 —— 最重要

现在有两个随机变量：

$$
A=
\begin{cases}
0,&0.5\\
10,&0.5
\end{cases}
$$

和：

$$
B=5
$$

为什么二者：

$$
E[A]=E[B]=5
$$

却不能说它们的随机行为一样？

如果你已经意识到：

> A 围绕5波动很大，而B根本不波动，

那么下一课为什么需要“方差”已经自然出现了。

---

# 这一节真正需要记住的核心

只需要牢牢记住下面 **5 句话**：

1. **数学期望描述随机变量长期重复后的平均位置，也可以理解成概率分布在数轴上的重心。**

2. 对离散随机变量：

$$
\boxed{
E[X]
=
\sum_i x_iP(X=x_i)
}
$$

之所以“取值乘概率”，是因为概率承担长期出现频率的权重。

3. 对连续随机变量：

$$
\boxed{
E[X]
=
\int_{-\infty}^{+\infty}
x f_X(x)\,dx
}
$$

本质仍然是“位置 × 概率重量”，只是求和变成积分。

4. 期望具有极其重要的线性性：

$$
\boxed{
E[aX+bY]
=
aE[X]+bE[Y]
}
$$

而且**不要求 $X,Y$ 独立**。

5. **期望只是分布的中心摘要，不是最可能值，也不是下一次结果，更不能完整描述随机变量。**

---

下一步为什么自然进入**方差**？

因为现在马上出现一个无法回避的问题：

$$
A=
\begin{cases}
0,&0.5\\
10,&0.5
\end{cases}
$$

和：

$$
B=5
$$

明明都有：

$$
E=5
$$

但一个非常不稳定，一个完全稳定。

所以：

> **只知道“中心在哪里”还不够，我们还必须知道结果围绕这个中心散得有多开。**

这就是下一课：

$$
\boxed{\text{方差 Var(X)}}
$$

而我们会从最底层解释：

> **为什么方差偏偏要计算 $(X-E[X])^2$，为什么不能直接平均 $X-E[X]$，又为什么标准差要再开平方？**
