# 第七课：独立性——“知道 A 发生了，会不会改变我对 B 的判断？”

按照我们已经建立的主线，前面从事件和概率走到了条件概率；下一步自然就是**独立性**。第一课的知识地图本来就是：

```text
事件
 ↓
概率
 ↓
条件概率
 ↓
独立性   ← 第七课
 ↓
随机变量
```

也就是说，独立性不是突然冒出来的新概念，它本质上是对条件概率提出的一个特殊问题。

今天只解决一个核心问题：

> **两个事件之间到底有没有“概率上的关系”？**

暂时不要把“独立”理解成“两个事情看起来没关系”。
我们要建立一个精确得多的判断标准。

---

# 一、问题背景：为什么有了条件概率，还需要“独立性”？

先看两个场景。

## 场景 A：抽牌

一副牌中随机抽一张。

定义：

$$
A=\{\text{抽到红桃}\}
$$

$$
B=\{\text{抽到红色牌}\}
$$

假如我告诉你：

> 已经知道抽到的是红桃。

那么你对“它是不是红色牌”的判断会不会改变？

当然会。

因为红桃一定是红色。

原来你只知道：

```text
可能红
也可能黑
```

知道“红桃”以后：

```text
一定是红色
```

说明：

> 关于 \(A\) 的信息，改变了我们对 \(B\) 的判断。

所以 \(A\) 和 \(B\) 显然存在关系。

---

再看另一个例子。

## 场景 B：连续抛两次公平硬币

定义：

$$
A=\{\text{第一次是正面}\}
$$

$$
B=\{\text{第二次是正面}\}
$$

如果我告诉你：

> 第一次已经出现正面。

这会不会让第二次更容易出现正面？

在通常的独立抛掷模型里：

> 不会。

知道第一次结果之后，我们对于第二次正面的概率判断完全不变。

这就是我们想捕捉的现象：

```text
知道 A
    ↓
对 B 的概率判断不变
    ↓
A 与 B 独立
```

所以，**独立性的存在目的就是判断：**

> 一个事件提供的信息，对另一个事件有没有概率上的影响。

---

# 二、它在整个概率论中的位置

到目前为止，我们已经有：

```text
样本空间 Ω
    ↓
事件 A、B
    ↓
概率 P(A)、P(B)
    ↓
条件概率 P(B|A)
```

普通概率：

$$
P(B)
$$

回答：

> 还没有额外知道 \(A\) 时，我认为 \(B\) 有多可能？

条件概率：

$$
P(B\mid A)
$$

回答：

> 已经知道 \(A\) 发生之后，我认为 \(B\) 有多可能？

于是最自然的问题就是：

> 这两个数字一样吗？

如果：

$$
P(B\mid A)=P(B)
$$

那么知道 \(A\) 以后，对 \(B\) 的判断完全没有改变。

这就是独立性的核心。

所以可以先记住一句非常重要的话：

> **独立性 = 条件信息没有改变概率判断。**

---

# 三、先建立最重要的直觉

想象你脑中有一个“概率仪表盘”。

原来：

```text
B 的可能性
████████░░
```

现在有人告诉你：

```text
A 已经发生
```

你重新更新判断。

### 如果仪表盘变了

例如：

```text
原来：
B  ████░░░░░░

知道 A 后：
B  █████████░
```

说明：

> A 给了你关于 B 的信息。

所以不独立。

---

### 如果仪表盘完全没变

```text
原来：
B  █████░░░░░

知道 A 后：
B  █████░░░░░
```

说明：

> A 对预测 B 没有提供额外信息。

所以独立。

这比“两个事件有没有因果关系”更准确。

概率论里的独立性首先讨论的是：

> **信息是否改变概率。**

而不是：

> 现实中是否存在物理因果关系。

---

# 四、数学定义是怎样自然长出来的？

我们不是先背公式，而是一步一步推。

假设事件 \(A\) 已经发生。

我们重新判断事件 \(B\)。

条件概率写作：

$$
P(B\mid A)
$$

如果知道 \(A\) 后，\(B\) 的概率完全不变，那么：

$$
P(B\mid A)=P(B)
$$

这就是独立性的最直接表达。

---

## 数学定义

如果：

$$
P(A)>0
$$

并且：

$$
P(B\mid A)=P(B)
$$

那么可以理解为：

> \(A\) 的发生没有改变 \(B\) 的概率。

于是我们称：

> \(A\) 和 \(B\) 相互独立。

这里每一个符号都要理解：

* \(A\)：一个事件；
* \(B\)：另一个事件；
* \(P(B)\)：没有额外条件时 \(B\) 的概率；
* \(P(B\mid A)\)：已知 \(A\) 已经发生时 \(B\) 的条件概率；
* 两者相等：说明知道 \(A\) 并没有带来关于 \(B\) 的新概率信息。

---

# 五、为什么课本更常写成 \(P(A\cap B)=P(A)P(B)\)？

这是独立性最经典的公式：

$$
\boxed{P(A\cap B)=P(A)P(B)}
$$

但不要背。

我们从条件概率推出来。

条件概率定义告诉我们：

$$
P(B\mid A)
=
\frac{P(A\cap B)}{P(A)}
$$

前提是：

$$
P(A)>0
$$

如果 \(A\) 与 \(B\) 独立，那么：

$$
P(B\mid A)=P(B)
$$

所以把左边换成条件概率公式：

$$
\frac{P(A\cap B)}{P(A)}
=
P(B)
$$

现在等式两边同时乘 \(P(A)\)：

$$
P(A\cap B)
=
P(A)P(B)
$$

于是独立性公式自然出现：

$$
\boxed{
P(A\cap B)=P(A)P(B)
}
$$

它不是一个孤立的新公式。

它其实就是：

```text
P(B|A) = P(B)
        ↓
把条件概率展开
        ↓
P(A∩B)/P(A) = P(B)
        ↓
两边乘 P(A)
        ↓
P(A∩B) = P(A)P(B)
```

---

# 六、这个乘法公式到底在说什么？

$$
P(A\cap B)=P(A)P(B)
$$

左边：

$$
P(A\cap B)
$$

表示：

> A 和 B 同时发生的真实概率。

右边：

$$
P(A)P(B)
$$

表示：

> 如果 A 和 B 彼此完全不提供信息，那么按照各自概率相乘，应该得到的同时发生概率。

所以独立性实际上是在比较：

```text
真实的共同发生概率

         vs

“如果互不影响”时应有的共同发生概率
```

如果二者恰好相等：

> 它们符合独立结构。

---

# 七、最简单例子：连续抛两次硬币

随机试验：

> 连续独立抛两次公平硬币，记录顺序。

样本空间：

$$
\Omega=
\{\text{正正},\text{正反},\text{反正},\text{反反}\}
$$

这延续了我们前面使用过的样本空间模型。样本空间负责描述所有可能的基本结果，事件则是其中我们关心的一组结果。 

假设四个基本结果等可能。

定义：

$$
A=\{\text{第一次是正面}\}
$$

所以：

$$
A=
\{\text{正正},\text{正反}\}
$$

因此：

$$
P(A)=\frac24=\frac12
$$

再定义：

$$
B=\{\text{第二次是正面}\}
$$

所以：

$$
B=
\{\text{正正},\text{反正}\}
$$

于是：

$$
P(B)=\frac24=\frac12
$$

---

## A 和 B 同时发生意味着什么？

“第一次正面”并且“第二次正面”：

$$
A\cap B=\{\text{正正}\}
$$

所以：

$$
P(A\cap B)=\frac14
$$

现在计算：

$$
P(A)P(B)
=
\frac12\times\frac12
=
\frac14
$$

发现：

$$
P(A\cap B)=P(A)P(B)
$$

所以：

$$
A,B\text{ 独立}
$$

---

# 八、从条件概率角度再看一次

原来第二次出现正面的概率：

$$
P(B)=\frac12
$$

现在告诉你：

> 第一次已经是正面。

那么样本空间实际上被条件 \(A\) 限制成：

```text
正正
正反
```

在这两个剩余的等可能结果里：

* 正正：第二次是正面；
* 正反：第二次是反面。

所以：

$$
P(B\mid A)
=
\frac12
$$

于是：

$$
P(B\mid A)=P(B)=\frac12
$$

这才是独立性的真正画面：

```text
原来：
第二次正面的概率 = 1/2

知道第一次是正面：
第二次正面的概率 = 1/2

完全没变
        ↓
独立
```

---

# 九、一个“不独立”的例子

仍然掷一次公平骰子：

$$
\Omega=\{1,2,3,4,5,6\}
$$

定义：

$$
A=\{2,4,6\}
$$

表示“偶数”。

所以：

$$
P(A)=\frac36=\frac12
$$

再定义：

$$
B=\{4,5,6\}
$$

表示“大于3”。

所以：

$$
P(B)=\frac36=\frac12
$$

现在问：

> 已知 \(A\) 发生，也就是已经知道结果是偶数，那么 \(B\) 发生的概率是多少？

知道偶数以后，只剩：

$$
\{2,4,6\}
$$

其中大于3的是：

$$
\{4,6\}
$$

所以：

$$
P(B\mid A)=\frac23
$$

而原来：

$$
P(B)=\frac12
$$

显然：

$$
\frac23\neq\frac12
$$

所以：

$$
A,B\text{ 不独立}
$$

直觉上：

```text
原来：
“大于3”的概率 = 1/2

知道“结果是偶数”以后：
“大于3”的概率 = 2/3

判断发生变化
        ↓
不独立
```

---

# 十、独立最容易和“互斥”混淆

这是本课最重要的概念辨析。

我们前面已经学习过：

> 互斥表示两个事件不能同时发生。

数学上：

$$
A\cap B=\varnothing
$$

于是：

$$
P(A\cap B)=0
$$

而独立要求：

$$
P(A\cap B)=P(A)P(B)
$$

这两件事讨论的根本不是一个维度。

---

## 互斥问的是

> 能不能一起发生？

```text
A发生
 ↓
B绝对不能发生
```

---

## 独立问的是

> 知道一个发生以后，会不会改变另一个的概率？

```text
知道A发生
 ↓
B的概率仍和原来一样
```

---

# 十一、为什么“互斥”通常恰恰意味着“不独立”？

假设：

$$
P(A)>0,\qquad P(B)>0
$$

并且 \(A,B\) 互斥。

因为它们不能同时发生：

$$
P(A\cap B)=0
$$

可是：

$$
P(A)P(B)>0
$$

所以：

$$
P(A\cap B)\neq P(A)P(B)
$$

因此不独立。

---

还有一个更直觉的解释。

假设：

$$
A=\{\text{骰子出现奇数}\}
$$

$$
B=\{\text{骰子出现偶数}\}
$$

它们互斥。

原本：

$$
P(B)=\frac12
$$

但一旦告诉你：

> \(A\) 已经发生，即骰子是奇数。

那么：

$$
P(B\mid A)=0
$$

从：

$$
\frac12
$$

瞬间变成：

$$
0
$$

这哪里是“互不影响”？

恰恰是极强的信息：

> 一旦知道 A，就完全排除了 B。

所以：

$$
\boxed{\text{互斥通常意味着强依赖，而不是独立}}
$$

---

# 十二、一个特别容易掉进去的坑

很多初学者会想：

> “两件事情彼此独立，所以应该不会同时发生吧？”

这正好反了。

例如连续抛两次硬币：

* \(A\)：第一次正面；
* \(B\)：第二次正面。

它们独立。

但当然可以同时发生：

```text
正正
```

并且：

$$
P(A\cap B)=\frac14
$$

不是0。

所以：

```text
独立
≠
互不相容

独立事件完全可以同时发生
```

---

# 十三、另一个重要误区：独立不是“完全没关系”

概率论的独立是一个**非常具体的数学概念**：

$$
P(B\mid A)=P(B)
$$

它不等于自然语言中的：

> “这两件事在世界上完全没有任何联系。”

例如两个事件可能：

* 有共同背景；
* 有共同原因；
* 从同一个实验中产生；

但它们在某个概率模型中仍然可能独立。

反过来，看起来“完全不同”的两个事情，也未必概率独立。

---

## 例子

设：

* \(A\)：今天机器温度过高；
* \(B\)：今天机器发生故障。

它们名字是两个不同事件。

但如果高温明显提高故障率：

$$
P(B\mid A)>P(B)
$$

那么它们不独立。

所以不能靠事件名称判断。

必须看：

$$
P(B\mid A)
$$

是否等于：

$$
P(B)
$$

---

# 十四、独立性是对称的吗？

我们刚才用：

$$
P(B\mid A)=P(B)
$$

看起来像是在说：

> A 不影响 B。

那会不会存在：

> A 不影响 B，但 B 会影响 A？

对于概率独立而言，在正常定义下不会。

如果：

$$
P(A)>0,\qquad P(B)>0
$$

并且：

$$
P(B\mid A)=P(B)
$$

那么：

$$
P(A\cap B)=P(A)P(B)
$$

于是：

$$
P(A\mid B)
=
\frac{P(A\cap B)}{P(B)}
$$

把独立关系代进去：

$$
P(A\mid B)
=
\frac{P(A)P(B)}{P(B)}
$$

约掉 \(P(B)\)：

$$
P(A\mid B)=P(A)
$$

所以：

```text
A不改变B
⇔
B不改变A
```

因此独立是一种对称关系。

---

# 十五、用一张图统一理解

假设整个样本空间的概率重量是1：

```text
Ω
┌────────────────────────────────┐
│                                │
│      ┌──────────────┐          │
│      │      A       │          │
│      │       ┌──────┼─────┐    │
│      │       │ A∩B  │ B   │    │
│      └───────┼──────┘     │    │
│              └────────────┘    │
│                                │
└────────────────────────────────┘
```

独立不是说：

```text
A 和 B 不重叠
```

而是说：

> B 在 A 内部所占的“概率比例”，与 B 在整个空间中的概率比例相同。

因为：

$$
P(B\mid A)
=
\frac{P(A\cap B)}{P(A)}
$$

如果独立：

$$
P(B\mid A)=P(B)
$$

也就是：

$$
\frac{P(A\cap B)}{P(A)}=P(B)
$$

所以可以把独立性想象成：

> **把视野缩进 A 以后，B 所占的概率比例没有发生变化。**

这是一个非常有用的几何直觉。

---

# 十六、为什么独立时概率要“相乘”？

这是以后非常常用的思想。

假设：

$$
P(A)=0.2
$$

$$
P(B)=0.3
$$

并且两者独立。

什么意思？

第一次筛选：

> A 只占所有可能性的20%。

然后在这20%里面，因为知道 A 并不会改变 B 的比例，所以 B 仍然占30%。

于是同时满足 A 和 B 的部分就是：

$$
20\%\times30\%
$$

即：

$$
0.2\times0.3=0.06
$$

所以：

$$
P(A\cap B)=0.06
$$

可以把它想象成连续筛选：

```text
全部概率重量 = 1

先满足 A：
1 × 0.2 = 0.2

再满足 B：
0.2 × 0.3 = 0.06
```

独立使得第二步仍然可以使用原来的 \(P(B)\)。

如果不独立，就必须用：

$$
P(B\mid A)
$$

于是一般情况其实是：

$$
P(A\cap B)
=
P(A)P(B\mid A)
$$

独立时：

$$
P(B\mid A)=P(B)
$$

所以才简化为：

$$
P(A\cap B)
=
P(A)P(B)
$$

这两个公式之间的关系非常重要：

$$
\boxed{
\text{一般乘法公式}
\quad
P(A\cap B)=P(A)P(B\mid A)
}
$$

独立只是它的特殊情况：

$$
\boxed{
P(A\cap B)=P(A)P(B)
}
$$

---

# 十七、别把“连续抛硬币”天然当成独立

这是一个建模边界。

我们刚才说：

> 两次公平硬币抛掷独立。

严格来说，这不是单纯由“抛硬币”三个字自动推出的。

我们其实加入了一个模型假设：

> 每次抛掷不会影响下一次的结果机制。

如果存在某个装置：

```text
第一次正面
      ↓
机械装置自动改变第二次硬币
      ↓
第二次更容易出现反面
```

那么两次抛掷就可能不独立。

因此：

> **独立性不是看到两个不同试验就自动成立，而是概率模型的一种结构假设，或者需要从模型机制中推出。**

这和之前学习概率时的一个重要原则完全一致：

> 不能仅仅看到几个结果，就擅自认为它们等可能。

同样地：

> 不能仅仅看到两次实验，就擅自认为它们独立。

---

# 十八、反例：两次抽球为什么可能不独立？

袋子里有：

```text
2 个红球
1 个蓝球
```

随机抽一个球。

定义：

$$
A=\{\text{第一次抽到红球}\}
$$

$$
B=\{\text{第二次抽到红球}\}
$$

关键问题是：

> 抽完第一个球放不放回去？

---

## 情况一：不放回

第一次之前：

$$
P(B)=\frac23
$$

从对称性看，第二个位置是红球的概率也是 \(\frac23\)。

现在如果已知第一次抽到了红球，那么袋子里剩：

```text
1红
1蓝
```

所以：

$$
P(B\mid A)=\frac12
$$

而：

$$
\frac12\neq\frac23
$$

所以不独立。

直觉：

```text
第一次抽走一个红球
        ↓
改变了袋子的组成
        ↓
改变第二次红球概率
```

---

## 情况二：抽完放回并充分混合

第一次即使抽到红球，放回以后袋中仍然是：

```text
2红
1蓝
```

于是：

$$
P(B\mid A)=\frac23
$$

而：

$$
P(B)=\frac23
$$

所以：

$$
P(B\mid A)=P(B)
$$

两次抽球可以在这个理想模型中视为独立。

---

这组例子非常重要，因为它说明：

> **独立与否不是由事件名称决定，而是由随机机制决定。**

---

# 十九、“放回”和“不放回”到底改变了什么？

用信息语言看：

### 不放回

知道第一次结果，可以帮助预测第二次。

```text
第一次的信息
     ↓
改变剩余组成
     ↓
第二次概率改变
```

因此存在依赖。

---

### 放回

知道第一次结果，没有改变第二次抽取前的袋子组成。

```text
第一次的信息
     ↓
没有改变下一次概率机制
     ↓
第二次概率不变
```

因此独立。

这已经开始体现概率论一个很深的思想：

> **依赖，本质上意味着信息有预测价值。**

---

# 二十、两个事件概率相同，和独立没有关系

假设：

$$
P(A)=P(B)=\frac12
$$

能不能推出独立？

不能。

例如掷一次公平骰子：

$$
A=\{1,2,3\}
$$

$$
B=\{1,2,3\}
$$

其实：

$$
A=B
$$

显然：

$$
P(A)=P(B)=\frac12
$$

但是：

$$
P(B\mid A)=1
$$

因为知道 A 发生，就等于知道 B 发生。

而：

$$
1\neq\frac12
$$

所以它们完全不独立。

这再次说明：

> 单看 \(P(A)\) 和 \(P(B)\) 不够。

判断独立性必须知道二者如何共同发生，例如：

$$
P(A\cap B)
$$

或者：

$$
P(B\mid A)
$$

---

# 二十一、为什么仅知道边缘概率无法判断关系？

假设：

$$
P(A)=0.5
$$

$$
P(B)=0.5
$$

可能有三种完全不同的情况。

### 情况 1：独立

$$
P(A\cap B)=0.25
$$

因为：

$$
0.5\times0.5=0.25
$$

---

### 情况 2：完全相同

如果：

$$
A=B
$$

那么：

$$
P(A\cap B)=0.5
$$

---

### 情况 3：互斥

那么：

$$
P(A\cap B)=0
$$

所以同样都是：

$$
P(A)=P(B)=0.5
$$

二者关系可以完全不同。

这告诉我们一个非常重要的思想：

```text
单个事件的概率
只能描述“各自怎样”

联合概率
才能描述“它们怎样一起”
```

这会自然通向以后非常重要的：

> 联合分布。

---

# 二十二、机器学习中的独立性

现在再把概率论本身讲清楚以后，看看它在算法中的位置。

## 1. 朴素贝叶斯

Naive Bayes 中最关键的“Naive”，就是做了很强的条件独立假设。

给定类别 \(Y\) 后，假设不同特征：

$$
X_1,X_2,\ldots,X_n
$$

近似条件独立。

于是本来很复杂的联合概率可以拆成许多乘积。

粗略形式：

$$
P(X_1,\ldots,X_n\mid Y)
$$

可以简化成：

$$
\prod_i P(X_i\mid Y)
$$

为什么能够乘？

核心就是：

> 独立结构允许联合概率分解。

---

## 2. 训练样本常假设独立同分布

机器学习中经常看到：

$$
\text{i.i.d.}
$$

它代表：

> independent and identically distributed

即：

```text
independent
= 相互独立

identically distributed
= 来自相同分布
```

这两个是完全不同的条件。

例如：

```text
独立：
一个样本的出现不提供关于另一个样本的额外概率信息

同分布：
每个样本遵循相同的概率规律
```

以后学习随机变量后，我们会正式解释 i.i.d.。

---

## 3. 时间序列往往不独立

例如：

```text
今天气温
昨天气温
前天气温
```

通常具有明显关系。

知道昨天很冷，可能会改变我们对今天温度的预测。

所以很多时间序列并不满足简单独立假设。

这也是为什么：

* 时序模型；
* 马尔可夫模型；
* RNN；
* 状态空间模型；

需要专门描述依赖结构。

---

# 二十三、这节最容易混淆的四组概念

把它们放在一起。

| 概念   | 它在问什么？            |
| ---- | ----------------- |
| 互斥   | A 和 B 能不能同时发生？    |
| 独立   | 知道 A 后，B 的概率是否改变？ |
| 条件概率 | 知道 A 后，B 的概率是多少？  |
| 因果关系 | A 的改变是否会导致 B 改变？  |

尤其要记住：

$$
\boxed{\text{独立不等于互斥}}
$$

以及：

$$
\boxed{\text{概率独立不等于没有任何现实关系}}
$$

---

# 二十四、把前七课串成一条完整链

我们已经从现实世界一路走到这里：

```text
现实中的随机现象
例如：连续抛两次硬币
        ↓
随机试验
实际进行一次
        ↓
样本空间
Ω={正正,正反,反正,反反}
        ↓
事件
A=第一次正面
B=第二次正面
        ↓
概率
P(A)、P(B)
        ↓
条件概率
P(B|A)
“知道A以后，B还有多可能？”
        ↓
独立性
P(B|A)=P(B)？
“知道A以后，B的概率变了吗？”
```

所以你可以这样理解：

> **条件概率负责“更新概率”，独立性负责判断“需不需要更新”。**

这句话非常关键。

---

# 二十五、理解检查

先不要急着看答案，凭直觉判断。

### 问题 1

连续独立抛两次公平硬币。

设：

$$
A=\{\text{第一次正面}\}
$$

$$
B=\{\text{第二次反面}\}
$$

你觉得 \(A\) 和 \(B\) 独立吗？

重点不要想“一个正一个反”。

而要问：

> 知道第一次正面，会不会改变第二次反面的概率？

---

### 问题 2

掷一次公平骰子。

$$
A=\{\text{点数是偶数}\}
$$

$$
B=\{\text{点数是奇数}\}
$$

它们：

* 互斥吗？
* 独立吗？

---

### 问题 3

袋中有2红1蓝，不放回连续抽两次。

如果第一次抽到了红球：

> 第二次抽到红球的概率是变大、变小，还是不变？

由此你能不能判断两次“抽到红球”事件是否独立？

---

### 问题 4

如果：

$$
P(A)=0.4
$$

$$
P(B)=0.5
$$

并且：

$$
P(A\cap B)=0.2
$$

那么：

$$
P(A)P(B)
=
0.4\times0.5
=
0.2
$$

你认为它们独立吗？

---

### 问题 5

如果两个事件互斥，而且：

$$
P(A)>0,\qquad P(B)>0
$$

为什么它们一定不独立？

不要背公式，试着用这句话解释：

> “一旦知道 A 发生……”

---

# 这一节真正需要记住的核心

只压缩成 5 句话：

1. **独立性研究的是信息关系：知道 \(A\) 发生后，是否改变对 \(B\) 的概率判断。**
2. 如果

   $$
   P(B\mid A)=P(B)
   $$

   就说明 \(A\) 没有给预测 \(B\) 提供额外信息。
3. 因此独立性等价于

   $$
   \boxed{P(A\cap B)=P(A)P(B)}
   $$

   这个乘法公式来自条件概率，不需要死记。
4. **互斥和独立完全不同**：互斥是“不能同时发生”，独立是“知道一个不改变另一个的概率”；两个正概率的互斥事件反而一定不独立。
5. 独立性是一种概率模型结构，不应该凭“看起来没关系”直接假设。

下一步会非常自然地进入整条主线的一个重大转折点——**随机变量**。

前面七课我们研究的核心对象一直还是：

```text
事件
```

但要真正做更强的数学计算，我们希望把：

```text
正面 / 反面
合格 / 不合格
天气状态
抽球结果
```

统一转换成**数字**。

于是下一课的问题将变成：

> **为什么仅仅研究“事件发生不发生”还不够？我们怎样把随机结果变成可以加、减、求平均、研究分布的数字？**

这就是第八课：**随机变量**。
